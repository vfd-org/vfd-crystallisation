# Existence / Life / Closure Programme — Python dependencies
# All synthetic / structural demos run on stdlib + numpy + scipy + matplotlib.
# Real-data EEG analyses additionally require mne.

numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# Optional: real-data EEG analyses (Notes B, D, E, F)
# Uncomment if running real-data scripts:
# mne>=1.5
# pandas>=2.0

# Optional: bioelectric BETSE simulations (Note A)
# BETSE is its own project; see https://github.com/betsee/betse
