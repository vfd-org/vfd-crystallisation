# Exact arithmetic certificates use only the standard scientific Python stack.
# The note's formal claims do not require sympy or scipy; numpy is used only
# for auxiliary aggregations.  Pytest is for the test suite.
numpy>=1.20      # developed on 1.24.4
sympy>=1.12      # developed on 1.13.3 (optional helper; certificates are integer/Q(sqrt 5))
pytest>=7.0
