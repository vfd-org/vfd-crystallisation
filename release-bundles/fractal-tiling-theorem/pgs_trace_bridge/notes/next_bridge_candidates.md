# Next bridge candidates — WO-VFD-PGS-TRACE-BRIDGE-001

The honest blocker is the **involution/completion**, not the observable. Any future
probe must contain a genuine functional equation, i.e. use the **completed** object:
- weight the divisor-excess Dirichlet series by the archimedean Γ-factor and test
  `Ξ_E(s) := γ(s)·Σ E(n)n^{−s}` for a real σ↔1−σ relation (this is the only way a
  σ-balance test is non-tautological);
- but per `WO-RH-ARITHMETIC-TRACE-FORM-SEARCH-001`, that completion's *positivity* needs
  a geometric substrate (Connes–Consani arithmetic site) — which is RH's open heart.

Recommendation: do **not** spin further PGS σ-probes (they will keep self-fooling
without a completion). `E(n)` is retained only as a descriptive divisor-pressure field.
