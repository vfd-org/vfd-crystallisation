# Why the simplex route failed (and this one is a scale geometry)

WO-VFD-RH-SIMPLEX-WITNESS-KERNEL-SEARCH-001 showed simplex Grams are not self-dual
(det≠1) and give the EPSTEIN zeta (involution `s↔d−s`), not ζ. `π^{−s/2}Γ(s/2)` is the
**1-D scaling-heat-kernel Mellin** (Jacobi theta of ℤ) — dimension-independent. So the
root object is the **scale/adele action at the place at infinity** (self-dual 1-D), NOT a
finite polytope. The Adelic Triskelion is therefore a *dynamic scale geometry* (helix +
`u↔−u` inversion), and the visual helix is representational, not load-bearing.
