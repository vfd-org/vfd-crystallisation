# The proof gap (explicit)

This root object reconstructs the **completed-ζ functional-equation architecture**. It
does NOT contain, and does not provide:
- a **Hilbert space** whose vectors are the relevant test functions,
- a **self-adjoint scale generator** whose spectrum is `{γ : ζ(½+iγ)=0}`,
- the **Weil positivity** form / spectral determinant proving the spectrum is real,
- the **positivity-forcing geometric substrate over Spec ℤ** (Connes–Consani arithmetic
  site) that, in char p, the curve's intersection form supplies (Hodge index ⇒ RH-curves).

RH = exactly that missing positivity. The Triskelion gives the *architecture and its
fixed axis*; the *definiteness on that axis* is the open problem. Next:
WO-VFD-RH-POSITIVE-ADELIC-WITNESS-OPERATOR-001.
