# The centre as phase-cross

W = (u₀=0, J, D, Φ, Q, K). Phase-cross identities (verified):
- `J²=I` (exact); `D=−i d/du` Hermitian (err 0); `JDJ=−D` (0).
- **Honest:** `JDJ=−D` holds for *any* reflection centre, so it does NOT pin u₀=0.
- The centre LOCATION is pinned by the **FE-axis scan**: `R(σ₀)=median|Ξ(s)−Ξ(2σ₀−s)|/(…)`
  vanishes (2e-21) **only** at σ₀=½ and fails (0.08–0.17) at 0.40/0.45/0.55/0.60.
  So the completed object is symmetric about `Re(s)=½` (⇔ `u₀=0`, `t=1`) and no other axis.
  A wrong centre `u₀≠0` FAILS this test. This is the non-tautological centre pin.
