# WO-VFD-ADELIC-TRISKELION-CENTRE-CONSTRUCTION-001

Constructs the **centre** of the Adelic Triskelion from its three verified arms —
`W = Centre(F,A∞,S)`, `Q_W = A∞ + P_F − R_S` — as a finite phase-cross / positive
witness scaffold. **STRONG PASS as a constructed-centre scaffold; NOT a proof of RH.**

- Phase-cross identities: `J²=I`, `D=−i d/du` Hermitian, `JDJ=−D` (note: holds for any
  reflection centre — does not alone pin u₀=0).
- **Centre location pinned (new):** FE residual about a variable axis σ₀ vanishes
  (2e-21) **only** at σ₀=½ (⇔ u₀=0, t=1); fails (0.08–0.17) elsewhere. The completed
  object is symmetric about Re(s)=½ and no other axis.
- **Centre from arms (new framing):** `Q_W` min eig +0.00004 (PSD, near-null);
  remove A∞/fake-Γ/random → indefinite; shuffled primes breaks the explicit-formula.
- Cutoff stability: λ_min 8.6e-5→3.9e-5 (NC 8→16) — the near-null wall.
- Regression preserved (Gaussian-Mellin, theta, FE Ξ pass).

Largely a re-framing/consolidation of the positive-witness scaffold; the new content is
the arm-decomposition of the centre and the centre-location pin. **The wall:** finite
PSD only *with* a near-null mode; infinite-limit positivity for all test functions = RH.
See `notes/`. Run `python3 src/run_centre.py`. LOCAL.
