# route_c / universal_boundary_scattering

Third route_c probe (after the envelope and funnel negatives), and the strongest:
does arithmetic RH share a *formal* boundary-scattering law with black-hole horizon
ringdown? Architecture tested: impulses → determinant → log-derivative response →
resonance poles → stability/contraction.

**Outcome (WO-RH-UNIVERSAL-BOUNDARY-SCATTERING-001): shared architecture is REAL
but KNOWN, and transfers NO proof.** Genuinely shared (not visual): Γ-function
determinant (ξ ↔ Pöschl–Teller Γ-ratio); **digamma boundary response** (the same
`½ψ(¼+iξ/2)` as the route_b capacity); log-derivative response with poles at the
resonances; resonances on line(s) (ζ-zeros ↔ PT QNMs `α(±ν−i(n+½))`, all damped);
stability = self-adjointness/contraction; **archimedean Γ ≅ greybody factor**. This
is the recognised trace-formula / Lax–Phillips scattering correspondence — not new
mathematics. **Crux:** the toy is provable because the potential is real
(self-adjoint ⟹ damped QNMs); the arithmetic instance of the same statement is
"the prime system is self-adjoint" = **Hilbert–Pólya = RH**. The architecture
relocates RH; it does not weaken it. No new invariant, no provability transfer.

- `boundary_scattering_system.py` — the abstract 7-tuple schema.
- `arithmetic_response.py` — ζ instance (`−ζ′/ζ`, archimedean `ψ`, zeros validation).
- `toy_horizon_scattering.py` — Pöschl–Teller barrier (Γ-ratio determinant, QNMs, greybody).
- `compare_scattering_laws.py` — driver + comparison + plot + JSON.
- `RH_UNIVERSAL_BOUNDARY_SCATTERING_001.md` — full report.
- `plots/scattering_comparison.png` — arithmetic response vs PT QNM plane.
- `results/rh_universal_boundary_scattering_001/` — JSON output.

**Anti-circularity:** primes + ζ/Γ + PT parameters only; zeros/QNMs validate only.
**No proof of RH; no φ; no God Prime.** LOCAL, not pushed.
