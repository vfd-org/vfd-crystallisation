# Executive summary (one page)

**Claim:** The Adelic Triskelion is a *tested normal form* for the completed-zeta
architecture — **not** a proof of the Riemann Hypothesis.

**What was eliminated (with null models):** prime-gap/divisor-excess observables
(tautological tests), naive σ-residuals (vanish at 1/2 for any weight), simplex/tetrahedral
geometry (gives the Epstein zeta, not ζ; Γ(s/2) is the 1-D scaling Mellin, not a simplex
feature), and directed-flow positivity (the scale flow is unitary — cannot create
positivity).

**What was verified (known mathematics, re-encoded and tested):**
- the three arms **F** (Euler product), **A∞** (Gaussian→½π^{−s/2}Γ(s/2)), **S** (theta
  self-duality) reconstruct the completed ζ and **each is necessary** (RootScore 6/6);
- the **completion kernel** is uniquely the archimedean Γ-factor — only it makes the
  complex-plane functional-equation residual vanish; raw/wrong/fake kernels fail;
- the **centre** is constructed as `Q_W = A∞ + P_F − R_S`, pinned to `Re(s)=1/2`
  non-tautologically (FE-axis scan: residual vanishes only at σ₀=1/2);
- positivity reduces to a **capacity contraction** `Q_W=H^{1/2}(I−K)H^{1/2}`, RH ⟺ `‖K‖≤1`.

**The shape of the wall (finite evidence):** `‖K_N‖ < 1`, approaching 1 **from below**
(0.99958→0.99981); a **stable, low-edge, non-escaping** near-null mode; capacity ratio
`R/(A∞+P)=0.9998`; nulls fail (fake-Γ pushes `‖K‖=1.67>1`). The wall is **marginal,
gapless, intrinsic** — no finite computation decides the limit.

**The remaining theorem (RH-equivalent):** construct the infinite adelic witness space and
prove `‖K‖≤1` there for all admissible test functions (six explicit conditions). Where a
geometric substrate forces this — function-field RH (Hodge index; Weil's theorem) and
H4→E8 (golden lattice) — it is proven. The number field lacks that substrate (the
Connes–Consani arithmetic site); supplying it **is** RH.

**Bottom line:** the programme did not prove RH. It eliminated false routes, verified the
completed-ζ triad as a testable root object, constructed its centre, identified positivity
as capacity contraction, and pinned the remaining theorem to the infinite adelic witness
space. That is the honest frontier.
