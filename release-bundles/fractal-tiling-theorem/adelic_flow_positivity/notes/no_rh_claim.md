# No RH claim

WO-VFD-RH-ADELIC-FLOW-POSITIVITY-001 does **not** prove RH. It tests whether directed
scale-flow induces the centre positivity, and finds it does **not**: the scale flow is
unitary (cannot create positivity; no consistent arrow). Positivity has a
contraction/semigroup form (`Q_W=H^½(I−K)H^½`, `‖K‖≤1`; `exp(−τH)` contraction) sourced
from the **PSD arithmetic capacity** `H=A+P` (archimedean Γ + pole) — remove the
archimedean arm and `H` is not PSD and no contraction exists. The full-limit `‖K‖≤1`
for all admissible test functions = RH = the deeper arithmetic-site theorem. Nothing
here is new mathematics (it is the route_b / Weil–Connes contraction) or a proof.
