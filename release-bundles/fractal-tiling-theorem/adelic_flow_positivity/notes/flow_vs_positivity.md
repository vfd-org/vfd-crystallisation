# Flow vs positivity — the decisive point

The directed scale flow is `U_τ = exp(−iτD)`, `D=−i d/du`. Since `D` is **self-adjoint**,
`U_τ` is **unitary**: `‖U_τ f‖ = ‖f‖` (verified, ratio 1.000000). A unitary evolution
preserves norms and cannot turn an indefinite form positive. The orientation/arrow
functional `Ω(f)=L(U_τ f)−L(U_−τ f)` has **no consistent sign** (signs
[1,0,0,1,0,0,0,−1] over random f). **Therefore "one-way flow ⇒ positivity" is false**:
flow orientation alone produces no positivity. (This is the WO's own §9 caution, confirmed.)
