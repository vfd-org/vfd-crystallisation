# VFD Core Frontier — index

Meta-theory / cross-domain synthesis (above the RH frontier `RH_FRONTIER.md` and
the geometry frontier `VFD_GEOMETRY_FRONTIER.md`).

| module / report | finding |
|---|---|
| `vfd_core/invariant_trace_form.py` + `run_invariant_trace_form_law.py` (+ `reports/VFD_INVARIANT_TRACE_FORM_LAW_001.md`) | WO-VFD-INVARIANT-TRACE-FORM-LAW-001: synthesise H4→E8 + RH/Weil–Connes + horizon scattering under one lens — **self-adjointness/positivity/integrality is FORM-RELATIVE.** Concrete exact anchor: **mult-by-φ on Q(√5) is non-symmetric under the naive form but self-adjoint under the trace form B=Tr(xy)** (B·T=[[1,5],[5,5]] symmetric). Grading: **H4→E8 = EXACT** (golden trace form c=1+1/√5 derived, *delivers* even-integral E8 closure); **RH = STRONG-but-OPEN** (Weil/Connes form is canonical + finite-positive, but full positivity = RH, closure NOT delivered → the standing **counterexample** to "right form ⟹ closure"); **horizon = ANALOGY** (shared Γ-determinant/log-derivative/flux architecture, no proof transfer). **Defensible law = v1/v2 (form-relativity + completion can repair failures); v3-as-guarantee REJECTED.** Right form = NECESSARY, not sufficient. Value = lens + RH search heuristic (find canonical arithmetic trace form), not a theorem. No proof of RH/physics; no E8=universe; no discovery claim (cites Wilson/Connes/Weil). |

| `vfd_core/self_adjoint_folding_engine.py` + `run_self_adjoint_folding_engine.py` (+ `reports/VFD_SELF_ADJOINT_FOLDING_ENGINE_002.md`) | WO-VFD-SELF-ADJOINT-FOLDING-ENGINE-002: given T, search a symmetric invariant form B with BT=TᵀB and decide if a PD one exists. **Engine works + RECOVERS the classical criterion: PD invariant self-adjoint form exists ⟺ T real-diagonalizable.** mult-by-φ/reflection/real-diag-3×3 → PD FOUND (recovers trace form diag(2,10) for mult-φ exactly, residual 2e-16); rotation π/5 + order-5 rot (complex spectrum) → REJECTED; Jordan (defective) → REJECTED. Useful falsifiable TOOL (symmetrizable-matrix theory), KNOWN not new. **Decisive: cannot symmetrize a complex-spectrum operator → NO RH shortcut** (RH needs spectrum real = zeros on line; engine detects, can't impose). Confirms right-form-necessary-not-sufficient concretely. |
| `route_c/arithmetic_trace_form/rh_arithmetic_trace_form_search.py` (+ `RH_ARITHMETIC_TRACE_FORM_SEARCH_001.md`) | WO-RH-ARITHMETIC-TRACE-FORM-SEARCH-001 (route_c): is there a canonical arithmetic trace form behind Weil/Connes positivity (analog of Q(√5) for H4→E8)? **YES, identified = the ADELIC EXPLICIT-FORMULA TRACE** (sum of local traces over places p≤∞, archimedean=Γ/digamma); number-field positivity of it = RH (OPEN). **Function-field PRECEDENT verified:** for 6 elliptic curves E/F_q, |α|=√q exactly (RH-for-curves holds) via Frobenius+Poincaré-duality intersection form (α·ᾱ=q>0) — Weil's THEOREM = the exact-closure analog of H4→E8. **Lesson:** the trace form delivers closure when a GEOMETRIC/cohomological substrate supplies positivity (golden lattice / curve intersection); the number field LACKS it (= Connes–Consani "arithmetic site" = the open heart of RH). Necessary-not-sufficient CONFIRMED, no number-field shortcut. RH now sharply named: construct the arithmetic-site geometry over Spec ℤ. No proof; no shortcut claim. |

| `pgs_trace_bridge/src/pgs_bridge.py` + `run_pgs_bridge.py` (+ `notes/`, `outputs/trace_summary.json`) | WO-VFD-PGS-TRACE-BRIDGE-001: can the divisor-excess observable E(n)=(τ(n)/2−1)log n (Z=exp(−E), H=log n+E) be lifted into a self-adjoint chamber-operator + σ-balance trace law? **WEAK/FAIL (outcome C).** E(n) is real + exactly 0 at primes / >0 at composite interiors (chamber picture true BY CONSTRUCTION), but does NOT beat nulls. Both flagship tests are **TAUTOLOGICAL**, shown with data to N=10⁵: (1) symmetric-tridiagonal operator → real eigenvalues for ANY diagonal (E,H,τ,log,Λ,**random** all pass, sym-error 0); (2) σ-probe R(σ)=\|Φ(σ)−Φ(1−σ)\| → argmin=0.5, R(0.5)=0 for EVERY weight incl. random (raw Dirichlet series has NO functional equation; symmetry imposed by formula). Chamber interiors not mirror-symmetric (real 0.317 vs shuffle 0.363 = weak local autocorrelation, not structure). **Missing piece = the KERNEL/INVOLUTION (genuine functional-equation / archimedean completion), NOT the observable** — exactly the archimedean-completion gap from ARITHMETIC-TRACE-FORM-SEARCH-001. E(n) retained only as descriptive divisor-pressure field. No RH claim; falsifiable bridge that falsified its own proposed transform. |

| `completion_kernel/rh_completion_kernel.py` (+ `RH_COMPLETION_KERNEL_001.md`) | WO-VFD-RH-COMPLETION-KERNEL-001: after PGS retired (Outcome C), target the missing involution layer via the **functional-equation residual on the COMPLEX plane** R_FE(s)=\|Ξ(s)−Ξ(1−s)\|/(\|Ξ(s)\|+\|Ξ(1−s)\|), Ξ=K·ζ, σ∈[0.1,0.9]×t∈{2,10,30,60}. **GENUINELY DISCRIMINATING (non-tautological — t≠0 doesn't auto-vanish, raw ζ FAILS at 0.56).** Only the **archimedean π^{−s/2}Γ(s/2)** (and full ξ) PASS: R_FE~5e-31, frac<1e-6=1.00; **all wrong/fake kernels FAIL** (wrong Γ-arg, wrong π-exponent, no-π, extra-exp → 0.44–0.74). ⇒ the completion kernel PGS lacked = the archimedean local factor (place at ∞), VERIFIED as the unique s↔1−s implementer. **Geometric DERIVATION of the Γ-factor NOT achieved = open Connes–Consani arithmetic-site problem (RH's open heart).** Confirms PGS retirement + the necessary-not-sufficient lens. Triad: primes (arithmetic) + Γ (archimedean completion) + involution (critical line = fixed axis). No RH proof; kernel verified not derived. |

| `simplex_witness_kernel/simplex_witness_kernel.py` (+ `README.md`, `results/result.json`) | WO-VFD-RH-SIMPLEX-WITNESS-KERNEL-SEARCH-001: can a simplex-derived positive form produce the archimedean completion π^{−s/2}Γ(s/2)? **FAIL (useful).** Controls regression holds (only K∞ passes, 9e-26). **Every simplex Gram is NOT self-dual** (det∈{3,4,5,…}, self-duality score 0.31–0.75); only identity/ℤ is self-dual (score 0). **FE residual K_A·ζ passes IFF det(A)=1** — all simplices fail (det^{−s/2} asymmetric under s↔1−s). Reason: **Γ(s/2) is the 1-D SCALING-heat-kernel Mellin (dimension-independent — any Gaussian gives it), NOT a simplex feature**; a d-simplex gives the EPSTEIN zeta of its lattice (s↔d−s), and the ζ FE is recovered ONLY by the self-dual 1-D lattice ℤ = arithmetic of ℚ / place at infinity, NOT a simplex. (φ-weighting lowers self-duality score 0.44→0.31 but never reaches it — real but insufficient.) ⇒ witness space = scale/adele action on ℚ; simplex only symbolic. Same missing-substrate (place at ∞ / arithmetic site) conclusion, now confirmed from the GEOMETRY side. No RH proof. |

| `adelic_triskelion/` (src + outputs + tests) | WO-VFD-ADELIC-TRISKELION-GEOMETRY-001: **a DIAGRAM, not a proof.** Rigorous visual geometry of the completed local–global architecture — three-arm helical *scale* geometry around the fixed witness axis Re(s)=1/2: Arm A finite p-adic/prime nodes (Euler), Arm B archimedean Γ completion (Gaussian exp(−πu²) ribbon = the verified COMPLETION-KERNEL Mellin π^{−s/2}Γ(s/2)), Arm C scale action/involution (u=log t, u↔−u ⇔ t↔1/t ⇔ s↔1−s). Drawn as a dynamic scale geometry (NOT a simplex — per SIMPLEX-WITNESS the completion is the adele/scale action at the place at ∞). 2D+3D PNG + interactive Plotly HTML; 5 geometry tests pass (threefold 2π/3, scale inversion u↔−u err~1e-16, prime-only arm, Gaussian profile, axis invariance). Explicitly NOT a proof of RH; the open piece (positivity-forcing substrate over Spec ℤ) is exactly what the picture does not contain. LOCAL. |

| `adelic_triskelion_root/` (src + notes + outputs) | WO-VFD-ADELIC-TRISKELION-ROOT-OBJECT-001: is the Triskelion a TESTABLE root object AT=(F,A∞,S,W,I)? **STRONG PASS as a normal-form (RootScore 6/6).** Each arm reproduces its classical operation: **F** Euler product→ζ (1e-6); **A∞** Gaussian Mellin=½π^{−s/2}Γ(s/2) (1e-17); **S** theta self-duality θ(t)=t^{−½}θ(1/t) (1e-31); completed Ξ passes FE residual (8e-31), raw ζ+fakes FAIL. **Necessity (key test): all 3 arms required** — remove A∞→raw ζ fails (0.50); remove F→Γ·random-Dirichlet (no Euler) fails (0.79); remove S→no FE to test. **Honest: this is Riemann(1859)/Tate's completed-ζ construction re-encoded as a 3-arm normal form — KNOWN verified, NOT new, NOT a proof; visual helix representational.** Proof gap explicit = positive self-adjoint witness operator (untouched). Next = WO-VFD-RH-POSITIVE-ADELIC-WITNESS-OPERATOR-001. LOCAL. |

| `positive_witness_operator/` (src + notes + outputs) | WO-VFD-RH-POSITIVE-ADELIC-WITNESS-OPERATOR-001: the RH-proof-facing scaffold at the Triskelion centre, PW=(H,D,J,Q,T,Ξ). **STRONG PASS as a scaffold; NOT a proof.** H=L²(log-scale), J(u↔−u) J²=I, **D=−i d/du Hermitian (err 0), JDJ=−D**; completed-kernel regression holds (Gaussian→Γ 4.7e-13, theta 1.4e-21, FE Ξ 2.5e-21; raw/fake FAIL); **Riemann–Weil explicit-formula balance verified, residual 3.7e-10** (σ=0.3); **Weil positivity form D=A+P−R min eig +0.00004 (PSD, near-null)**. Null discrimination: positivity rejects no-arch (−5.0)/fake-Γ(−0.11)/random-Hermitian(−4.0); explicit-formula rejects shuffled primes (2424); FE rejects raw/fake (different nulls fail different tests). **Hilbert–Pólya operator NOT exhibited** (no non-fitted A_N). **THE WALL: finite PSD only WITH a near-null mode (λ_min→0⁺); infinite-limit positivity for all test functions = RH.** KNOWN Weil/Connes criterion re-encoded, NOT new, NOT a proof; gap = positive self-adjoint witness operator / arithmetic-site geometry over Spec ℤ. LOCAL. |

| `triskelion_centre/` (src + notes + outputs) | WO-VFD-ADELIC-TRISKELION-CENTRE-CONSTRUCTION-001: construct the centre W=Centre(F,A∞,S) FROM the three arms (not a 2nd object). **STRONG PASS as constructed-centre scaffold; NOT a proof.** Phase-cross: J²=I, D Hermitian, JDJ=−D (honest: holds for ANY reflection centre, so doesn't alone pin u₀=0). **NEW decisive test — centre LOCATION pinned:** FE residual about variable axis σ₀, R(σ₀)=median\|Ξ(s)−Ξ(2σ₀−s)\|/(…), vanishes (2e-21) ONLY at σ₀=½ (⇔u₀=0,t=1); FAILS (0.08–0.17) at 0.40/0.45/0.55/0.60 → completed object symmetric about Re(s)=½ and NO other axis (non-tautological). **Centre from arms:** Q_W=A∞+P_F−R_S min eig +0.00004 (PSD, near-null); nulls rejected (no-arch −5.03, fake-Γ −0.11, random −3.91; shuffled primes breaks explicit-formula). Cutoff λ_min 8.6e-5→3.9e-5 (NC 8→16). Re-framing/consolidation of the positive-witness scaffold; new = arm-decomposition + centre-location pin. **Wall: finite PSD only WITH near-null mode; infinite-limit positivity for all test functions = RH.** KNOWN Weil/Connes, not new, not a proof. LOCAL. |

| `adelic_flow_positivity/` (src + notes + outputs) | WO-VFD-RH-ADELIC-FLOW-POSITIVITY-001: does directed scale-flow generate the centre positivity Q_W=A∞+P_F−R_S? **DECISIVE: NO — positivity is a capacity-contraction property, NOT flow orientation.** The scale flow U_τ=exp(−iτD) is **UNITARY** (‖U_τf‖/‖f‖=1.000000, D self-adjoint) → cannot create positivity; the arrow test Ω(f) has **NO consistent sign** ([1,0,0,1,0,0,0,−1]). "One-way flow ⇒ positivity" is FALSE (theorem). Positivity DOES have a contraction form: **Q_W=H^½(I−K)H^½** (residual 4.5e-15), K=H^{−½}RH^{−½}, **‖K‖=0.9998≤1**, min eig(I−K*K)=+0.00042 — = route_b. Source = **PSD capacity H=A+P** (archimedean Γ+pole): exp(−τH) contracts (0.96<1), exp(−iτD) unitary (1.0); remove archimedean → H min eig −4.99 (not PSD, no contraction). **ANSWER to the either/or: the SECOND branch — positivity requires the deeper arithmetic capacity, not flow; full-limit ‖K‖≤1 = RH = arithmetic-site theorem.** WEAK PASS (contraction holds, flow-orientation mechanism rejected). KNOWN Weil/Connes; not new, not a proof. LOCAL. |

| `capacity_operator_limit/` (src + notes + outputs) | WO-VFD-RH-CAPACITY-OPERATOR-LIMIT-001: track the finite capacity contraction K_N (Q_W=H^½(I−K)H^½, H=A∞+P_F) and isolate the infinite-limit wall. **STRONG PASS as the sharpest proof-facing formulation; NOT a proof.** Cutoff ladder NC=8..24: **‖K_N‖ 0.99958→0.99981 (<1, approaches 1 FROM BELOW)**, gap 1−‖K‖ shrinks (best 1/NC², R²=0.92) but BASIS-RESOLUTION-dependent (not a law, not extrapolable). **Near-null mode: STABLE** (overlaps →0.99999998), **low spectral edge** (com 0.21→0.06), **top-mass≈0 — does NOT escape to boundary**, archimedean-carried. **Capacity ratio R/(A∞+P)=0.9998 → reflection≈capacity** (marginal condition). Nulls fail: no-arch→H not PSD (−4.99); **fake-Γ→‖K‖=1.67>1** (wrong archimedean violates contraction); shuffled→0.42; random→indefinite. **Shape of the wall: intrinsic, spread, MARGINAL positivity (‖K‖→1, NO spectral gap) = the hardest case.** Obstruction statement (6 conditions, RH-equivalent) written. KNOWN Weil/Connes; not new, not a proof. LOCAL. |

| `adelic_triskelion_paper/` (paper + data + figures + notes) | WO-VFD-ADELIC-TRISKELION-CONSOLIDATION-PAPER-001: disciplined technical note consolidating the whole RH/Adelic-Triskelion arc. **Presents the Triskelion as a TESTED NORMAL FORM for the completed-ζ architecture, NOT a proof of RH.** `paper/main.md` (15 sections): background (Euler/Γ/theta/FE, classical); retired routes (PGS, naive-σ, simplex, flow — all with why); completion-kernel test (only π^{−s/2}Γ(s/2) passes); AT=(F,A∞,S;W) + root-object verification (RootScore 6/6, each arm necessary); centre Q_W=A∞+P_F−R_S pinned to Re(s)=½ (FE-axis scan); positive-witness scaffold (explicit-formula balance 3.7e-10); flow=unitary (no positivity); capacity operator Q_W=H^½(I−K)H^½, RH⟺‖K‖≤1; capacity-limit (‖K_N‖→1 from below, stable non-escaping near-null, marginal/gapless wall); 6-condition RH-equivalent theorem gap; what's new vs classical; claim boundary. + abstract.md, executive_summary.md, README, claim_boundary, 7 data JSONs, 4 figures. KNOWN math (Riemann/Tate/Weil/Connes); contribution = tested normal form + false-route elimination + capacity centre + precise wall isolation. No RH claim. LOCAL. |

| `horizon_capacity_bridge/` (src + notes + outputs) | WO-VFD-RH-HORIZON-CAPACITY-BRIDGE-001: map the capacity centre to positive-flux **horizon scattering**. **STRONG PASS as a theorem-PATTERN analogue; NOT a proof.** Toy Pöschl–Teller: `|R|²+|T|²=1`, `F_abs=|T|²≥0 ⟹ |R|²≤1` (positive flux ⟺ contraction). Dictionary algebraically exact: H=absorption capacity, R_S=reflection, K=reflection coefficient, Q_W=absorbed flux, Re(s)=½=horizon, ‖K‖≤1=no-superradiance. **Superradiance nulls map cleanly: fake-Γ → ‖K‖=1.67>1 (over-reflecting horizon); no-arch → no absorption (H not PSD); wrong centre → misplaced horizon (FE breaks).** Near-null = marginal NON-superradiant horizon (margin 2e-4, reflection/capacity 0.9998). "Planck" = self-dual FE-preserving cutoff (fixed point t=1), not a length. **HONEST LIMIT: the Q=H−R≥0 ⟺ ‖K‖≤1 algebra is GENERIC (random PSD H+sym R passes) → explanatory frame, NOT proof-adding; ζ-specificity is in archimedean Γ+primes, not the horizon. No proof transfer (consistent with UNIVERSAL-BOUNDARY-SCATTERING).** RH = arithmetic no-superradiance theorem ‖K‖≤1 (infinite), unchanged RH-equivalent wall. KNOWN; not new, not a proof. LOCAL. |

| `arithmetic_capacity_geometry/` (outputs + figures + notes) | WO-VFD-RH-ARITHMETIC-CAPACITY-GEOMETRY-001: identify the inner geometry of the centre (mapping/theorem WO, not a sim). **STRONG PASS on identification; NOT a proof → Outcome B (consolidate).** **LABELING CORRECTION (vs the WO itself):** verified operators give **capacity H = archimedean(Γ/digamma) + POLE** (archimedean sign-indefinite alone; pole lifts PSD) and **reflection R = the PRIMES** (Euler/prime-power trace) — the primes are the REFLECTION, not a capacity. **RH ⟺ R ≤ H ⟺ ‖K‖≤1** (capacity dominates prime reflection = arithmetic no-superradiance). **Mapping is EXACT, not loose:** H, R, Q_W, ‖K‖≤1 ARE the Weil explicit-formula objects + Connes' positivity criterion — so the capacity language is a RE-NAMING, adds no new route; the proof gap = the open **Connes–Consani arithmetic-site** problem (force Weil positivity via a geometric substrate over Spec ℤ, the char-p Hodge-index analogue). 5-condition Arithmetic Capacity Domination Theorem written (cond. 5 ‖K‖≤1 = RH-equiv heart). Boundary-leakage channels mapped. Cutaway figure. Recommend consolidation, NOT a bridge WO (would re-promise the open problem). KNOWN; not new, not a proof. LOCAL. |

| `adelic_infinity_compactification/` (src + notes + outputs) | WO-VFD-RH-ADELIC-INFINITY-COMPACTIFICATION-001: turn the infinite limit into compact boundary geometry (`u→v=tanh(u/L)`, ∞→v=±1). **WEAK PASS; NOT a proof.** **Honest core: compactification is a positivity-INVARIANT reparametrization — ‖K‖ unchanged under the similarity (Δ~1e-15, verified by conjugation).** A FINITE operator has NO genuine boundary at infinity to leak through; the near-null's boundary-vs-central character is **coordinate-dependent** (low-edge in basis index; near v=−0.9 in symmetric tanh — no invariant content; only ‖K‖ is invariant). Prime tails convergent at finite cutoff (P=500/2000/8000 all ‖K‖=0.99981). Self-dual window preserves FE (6.5e-19); non-self-dual breaks it. ⇒ compactification sharpens the PICTURE (boundary channels: scale/prime/spectral/operator-domain) but adds NO proof power; genuine boundary exists only in the infinite-dim adelic space = open Connes idele-class-compactness / arithmetic-site problem. Wall unchanged: ‖K‖≤1 on infinite compactified space = RH. KNOWN; not new, not a proof. LOCAL. **NOTE: this + the last ~3 WOs re-express the SAME wall; recommendation = consolidate/package, not more sub-WOs.** |

**Verdict:** the trace-form-closure principle is a **useful, honestly-bounded lens**: closure is form-relative; completing to the canonical invariant form *can* repair a naive failure (exactly, for H4→E8), but having the right form is necessary not sufficient (RH is the counterexample). The folding engine (#2) recovers the symmetrizability criterion (real-diagonalizable ⟺ PD self-adjoint form) and confirms no complex-spectrum/RH shortcut. The arithmetic trace-form search (#3) identifies the canonical form (adelic explicit-formula trace), verifies the function-field precedent (|α|=√q, Weil), and localises RH's open heart as the **missing geometric substrate over Spec ℤ** (Connes–Consani). One exact exemplar (H4→E8), one open exemplar (RH = absent arithmetic geometry), one analogy (horizon). Next = WO-VFD-H4-E8-FORMAL-PAPER-004 (write the exact exemplar) and WO-RH-OPERATOR-ROUTE-PUBLISH-001 (freeze the RH frontier). No proof of RH/physics; no E8=universe; no discovery/shortcut claims (cites Wilson/Connes/Weil/Connes–Consani). LOCAL, NOT pushed.

| `docs/universal-infinity-capacity/` (outputs + figures) | WO-VFD-UNIVERSAL-INFINITY-CAPACITY-GEOMETRY-001: define infinity as a completed boundary with capacity/reflection/leakage, ICG=(X,∂X,Φ,H,R,Q,K,L), stable ⟺ Q=H−R≥0 ⟺ ‖K‖≤1, across RH/BH/brain/VFD. **WEAK PASS.** RH instance concrete (corrected H=archimedean+pole, R=primes; diagnostics D1 H+0.073, D2 Q+3.9e-5, D3 ‖K‖0.9998, D5 ratio 0.9998); BH concrete (positive flux ⟺ no-superradiance); **brain/VFD CONCEPTUAL analogy only (no data).** **HONEST: the unifying law is GENERIC — holds for any PSD H+sym R (verified: random PSD passes), i.e. "stability = a positive quadratic form." A reusable organizing LANGUAGE, NOT new math, NOT a proof in any domain.** For RH re-states the same wall (infinite ‖K‖≤1 = Weil positivity = arithmetic site). Domain table + theorem template + 5 portable diagnostics + claim boundary. LOCAL. |

| `triad_closure_crossdomain/` (engine + 2 runners + Millennium map + outputs) | WO-VFD-TRIAD-CLOSURE-CROSSDOMAIN-001: domain-agnostic closure-test engine `(X,T,τ,θ,B,∂X,Q)` with 4 diagnostics — `symmetrizer` (BT=TᵀB, real spectrum), `isometry_form` (TᵀBT=B, unit-circle spectrum, Weyl-average construction), `triad_grammar`, `capacity_drift Q̄`. Run unchanged on Collatz / RH / simplex / cognition. **MEDIUM PASS.** **Collatz STRONG (family discrimination, NON-CIRCULAR):** same `Q=a·ln2−ln q` predicts 3n+1 closes (Q̄≈+0.27) but 5n+1/7n+1 escape (Q̄<0) — the classical drift heuristic reproduced; does NOT prove Collatz (avg drift ≠ no single escaping braid = the conjecture). **RH WEAK:** FE residual `R_FE(Ξ)≈1e-31` selects unique completion, but that's a known `s↔1−s` symmetry, not zeros; real wall = Weil positivity. **Simplex WEAK/structural but CORRECTS the hypothesis:** triad 3-cycles close by ISOMETRY (eigs on unit circle), NOT self-adjoint → **closure = positive invariant form (B-normality); self-adjoint = real-spectrum special case.** **Cognition TOY ONLY (no ARIA logs).** Millennium map (`MILLENNIUM_CLOSURE_MAP.md`): best fit = Navier–Stokes (dyadic-shell capacity probe reproduces ν-trichotomy; real wall = supercriticality = CRITICAL-BOUNDARY verdict); Yang–Mills (reflection-positivity = positive form); Poincaré SOLVED via Perelman monotone W-entropy = the shape decisive once; P-vs-NP & Hodge correctly DON'T fit. Fitting the shape ≠ solving; each open wall survives intact. NOT a proof/partial-proof/reduction of any problem. LOCAL, NOT pushed. |

| `invariant_closure_engine_v2/` (classifier + 7 domains + 6 reports) | WO-VFD-INVARIANT-CLOSURE-CROSSDOMAIN-002: upgrade triad-closure diagnostic → proof-pressure framework. **`closure_mode_classifier.py`** searches for a positive form B and labels the mode via spectrum+discrete-Lyapunov: DISSIPATIVE (ρ<1, Lyapunov B>0), ISOMETRIC (\|λ\|=1 diagonalizable, Weyl-average B), SELF_ADJOINT (real spectrum), CRITICAL (ρ=1 defective/Jordan), MIXED, NONE (ρ>1). **MEDIUM PASS.** **Calibration PASS** (8 known systems all classified correctly). **Markov PASS** (both chains DISSIPATIVE to stationary; reversibility = real spectrum = self-adjoint in stationary-weighted product). **Collatz family PASS** (non-circular: same Q→3n+1 closes, 5/7/9n+1 escape; 3n+1 worst non-closing window only −6.4 vs −137). **Collatz cycles PASS(known):** exact closure Diophantine `n(2^A−q^k)=Σq^{k−1−j}2^{s_j}` ELIMINATES 1-cycle class for 3n+1 (only n=1) + no small nontrivial cycles k≤6 — **reproduces Steiner 1977 via closure route, NOT new.** **Primes PASS:** real primes separate from density-matched fakes — layer1 residue mod3 (trivial coprimality) + layer2 Lemke-Oliver–Soundararajan consecutive-last-digit correlation (transition diag suppressed ~0.15<0.25; residual 7.3e-2 vs shuffled/independent ~6e-5 = **1205× separation**); KNOWN bias, does NOT touch RH positivity. **Polytope WEAK** (triad cycles close by isometry, generic). **Navier–Stokes WEAK** (capacity trend falls with viscosity but SIGN-CROSSING is integration-scheme-dependent, NOT robust — no critical-exponent claim). **RH/Weil WEAK** (finite Weil form PSD with near-null, rejects no-arch/fake-Γ completions but INSENSITIVE to shuffled primes — caught by explicit-formula balance instead; RH wall = infinite-limit positivity = Connes site, UNCHANGED). **Value-test: hits items 1 (reproduce known via new route) + 4 (correct false hypothesis: self-adjoint→positive-invariant-form); does NOT hit item 3 (new invariant) in any open problem — every wall is the original theorem.** No proof/partial-proof/reduction. LOCAL, NOT pushed. |

| `closure_certificate_theory/` (tex+PDF 240K + verify + catalogue + 3 examples + status table) | WO-VFD-CLOSURE-CERTIFICATE-THEORY-001: formalise the MISSING layer = the **closure certificate** (a *finitely checkable* object controlling *infinite* repetition at a boundary). Formal note: 7 definitions (generated system / boundary completion / positive form / closure modes / leakage L_B=λmax(TᵀBT−B) / capacity Q_B / certificate), **4 lemmas + 1 corollary + 1 proposition ALL PROVED**, every claim graded PROVEN/DIAGNOSTIC/CONJECTURAL/OPEN. **PROVEN:** (L1) dissipative cert ⟺ ρ(T)<1 [= discrete Lyapunov, recovered]; (L2) isometric cert ⟺ power-bounded invertible [invariant inner product, Weyl avg]; (L3) self-adjoint cert ⟺ real-diagonalizable [symmetrizable]; (Cor1) reversible Markov ⟹ diag(π) self-adjoint cert + ergodic ⟹ contraction on 1⊥ [detailed-balance recovered]; (L4) Collatz cycle eqn n(2^A−q^k)=Σq^{k−i}2^{s_{i−1}}, 2^A>q^k; (Prop1) **only positive-integer 1-cycle of 3n+1 is n=1 [recovers Steiner 1977 via certificate route].** `verify_certificates.py` 14/14 checks PASS. Open-problem mapping (exact missing certificate each): Collatz=no infinite non-closing braid (OPEN), RH=Weil positivity of explicit form (OPEN, Connes site; positivity form even insensitive to prime shuffle), NS=coercive high-freq norm (OPEN, supercritical), YM=reflection positivity+spectral floor (OPEN). Explicit non-overclaim section: NO proof/partial-proof/reduction of any open problem; mode is a classification, solution needs constructing the domain-native certificate. The undismissable middle ground: defs+lemmas+controls+recovered theorems stand even if conjectures are rejected. LOCAL, NOT pushed. |

| `shell_boundary_search/` (core + 4 adapters + automaton + 6 reports + tests) | WO-VFD-RUNG-GEOMETRY-CERTIFICATE-001 + WO-VFD-SHELL-BOUNDARY-SEARCH-GENERAL-001 (merged): recast "finite rule + indefinite repetition + uncontrolled boundary" as shell-boundary problem P=(X,T,h,B,Q); driver classifies CLOSED/CRITICAL/ESCAPING/WRONG_BOUNDARY/UNKNOWN + searches certificate. **MEDIUM PASS.** **CENTREPIECE (Collatz rung geometry):** rung = T-map parity bit; rung grammar = **full one-sided shift** (Terras 1976 bijection Z/2^k↔{0,1}^k, **verified k≤12**); every finite escape word (ones-frac > ln2/ln3≈0.6309) admissible (794 at len 12); all-ones len-10 word realised by **n=1023=2^10−1** (longer trajectory returns to 0.5 ones-frac). **COROLLARY (new framing): NO finite rung-word automaton can certify Collatz — no forbidden finite pattern exists; any certificate must be GLOBAL over the realised 2-adic measure = the conjecture. Excludes an entire certificate class, sharpens the wall.** Calibration (controls separate): linear contraction→CLOSED(Lyapunov)/ρ>1→ESCAPING/rotation→CLOSED(isometric)/Jordan→CRITICAL; ergodic Markov→CLOSED(spectral gap+π); NS shell capacity trend↓ with viscosity (sign-crossing flagged non-robust). Honest guardrail in framework note: reduction/search method NOT oracle; no-certificate≠no-solution (wrong shell / weak certificate class / genuinely open). Recovers Terras 1976; proves NOTHING new about Collatz/RH/NS/YM. LOCAL, NOT pushed. |

| `candidate_certificate_refuter/` (refuter + runner + test + README + json) | WO-VFD-CANDIDATE-CERTIFICATE-REFUTER-001: an honest REFUTER for proposed closure certificates at the infinity wall (RH/Collatz/NS). Skeptical by construction — tries to BREAK a candidate on finite truncations → **REFUTED (concrete witness)** or **SURVIVES-TRUNCATIONS** under a hard banner. **No code path emits "proved"** (test enforces `"PROV" not in verdict`). `refute_rh(completion)`: min-eig of finite Weil form across (NC,P) cutoffs — **fake Γ(s/3) REFUTED (−0.108), no-arch REFUTED (−4.87), correct completion SURVIVES (near-null, evidence only)**. `refute_collatz_lyapunov(V)`: scans odd n, REFUTED on first non-decrease — **every local Lyapunov (log n, n, v2+log n) REFUTED at witness n=3→5**, exactly as the full-shift theorem predicts (no finite-local certificate can exist). `refute_collatz_numeric(N)`: "all n≤N reach 1" SURVIVES = verification NOT proof. `refute_ns_coercive(floor)`: ν≥floor⇒capacity≥0; survives on caricature (banner: doesn't address 3D supercriticality). Honest ceiling = propose/finitely-test/rule-out; the surviving candidate still needs the infinite-dim proof = the open problem. 4 tests pass. Answers the "can we use this on the infinity wall" question: NO (can't certify the limit) but YES as a refuter that kills wrong guesses fast + keeps bookkeeping honest. LOCAL, NOT pushed. |
