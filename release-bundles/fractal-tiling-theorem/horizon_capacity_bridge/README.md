# WO-VFD-RH-HORIZON-CAPACITY-BRIDGE-001

Maps the Adelic Triskelion capacity centre to a positive-flux **horizon scattering** model.
**STRONG PASS as a theorem-pattern analogue; NOT a proof of RH.**

- toy Pöschl–Teller horizon: `|R|²+|T|²=1`, `F_abs=|T|²≥0 ⟹ |R|²≤1` (positive flux ⟺ contraction).
- abstract identity `Q=H−R≥0 ⟺ ‖K‖≤1` — **generic** (any PSD H + sym R), so explanatory not proof-adding.
- dictionary (algebraically exact): `H`=absorption capacity, `R_S`=reflection, `K`=reflection
  coefficient, `Q_W`=absorbed flux, `Re(s)=1/2`=horizon, `‖K‖≤1`=no-superradiance.
- superradiance nulls: **fake-Γ → ‖K‖=1.67>1 (over-reflecting horizon)**; no-arch → no absorption
  (H not PSD); wrong centre → misplaced horizon (FE breaks).
- near-null = **marginal non-superradiant** horizon (margin 2e-4, reflection/capacity 0.9998).
- "Planck scale" = self-dual FE-preserving cutoff (UV/IR fixed point t=1), not a physical length.

**RH reads as the arithmetic no-superradiance theorem** `‖K‖≤1` on the infinite adelic space —
RH-equivalent, unchanged. Consistent with the earlier scattering WO: shared architecture, no
proof transfer. Run `python3 src/run_horizon.py`. LOCAL, not pushed.
