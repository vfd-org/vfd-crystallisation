# Proof gap

Finite `‖K_N‖ ≤ 1` is **necessary but not sufficient** for RH. The remaining theorem is the
infinite-limit contraction `‖K‖ ≤ 1` on the full adelic witness space for all admissible test
functions (see operator_obstruction_statement.md, conditions 1–6). That theorem is RH-equivalent.
The wall is an **intrinsic, spread, marginal** positivity (`‖K‖→1`, no spectral gap), so no
finite-cutoff or gap argument can close it.
