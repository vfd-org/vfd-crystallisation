# The proof gap (exact)

The scaffold reaches: witness space `H=L²(log-scale)`, involution `J` (`u↔−u`),
self-adjoint scale generator `D=−i d/du` with `JDJ=−D`, the completed-kernel
regression (Gaussian→Γ, theta self-duality, FE residual), the **Riemann–Weil
explicit-formula balance** (residual 3.7e-10), and the **finite Weil positivity
form** `D=A+P−R` with `min eig = +0.00004` (PSD).

**The wall, precisely:** the finite form is PSD only **with a near-null mode**
(`λ_min → 0⁺`; the lowest eigenvalues all collapse toward 0 as the cutoff grows —
NULLMODE-FACTOR). RH ⟺ this form stays `≥ 0` in the **infinite-dimensional limit
for ALL admissible test functions** (Weil's criterion). Equivalently `‖K‖ ≤ 1` in
the limit (CONTRACTION-002). That limiting positivity is **not** delivered by any
finite computation — it is RH.

**Hilbert–Pólya:** no non-fitted self-adjoint `A_N` whose `det(zI−A_N)` reproduces
`ξ(½+iz)` is exhibited (fitting zeros would be descriptive, not explanatory). The
positive self-adjoint witness operator is **not constructed** — that is the open
problem (Connes' arithmetic site / a positivity-forcing geometric substrate over
Spec ℤ, per ARITHMETIC-TRACE-FORM-SEARCH-001).
