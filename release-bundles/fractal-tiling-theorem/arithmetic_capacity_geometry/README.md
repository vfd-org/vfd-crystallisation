# WO-VFD-RH-ARITHMETIC-CAPACITY-GEOMETRY-001

Identifies the inner geometry of the Adelic-Triskelion centre. **Geometry/theorem-mapping,
not a simulation; NOT a proof of RH. STRONG PASS on identification → Outcome B (consolidate).**

**Corrected geometry (vs the WO's own labeling):** in the verified operators,
- **capacity `H` = archimedean (Γ/digamma) + pole** (the archimedean term is sign-indefinite
  alone; the pole lifts it PSD),
- **reflection `R` = the finite primes** (Euler-product/prime-power trace) — the primes are
  the *reflection*, NOT a capacity,
- **RH ⟺ `R ≤ H` ⟺ `‖K‖≤1`** (capacity dominates reflection = arithmetic no-superradiance).

**Mapping is exact, not loose:** `H, R, Q_W, ‖K‖≤1` *are* the Weil explicit-formula objects
and Connes' positivity criterion — so the capacity language is a re-naming, and the proof
gap is identical to the open **Connes–Consani arithmetic-site** problem (force Weil
positivity via a geometric substrate over Spec ℤ, as the Hodge-index intersection form does
in char p). The 5-condition **Arithmetic Capacity Domination Theorem** is stated, with #5
(`‖K‖≤1`) identified as the RH-equivalent heart.

Docs in `outputs/` (capacity summary + A∞/prime/reflection/leakage geometry + framework
mapping + theorem + interpretation + no-rh-claim); cutaway figure in `figures/`.
**Recommendation: consolidation, not a new bridge WO** (which would re-promise the open
arithmetic-site problem). LOCAL, not pushed.
