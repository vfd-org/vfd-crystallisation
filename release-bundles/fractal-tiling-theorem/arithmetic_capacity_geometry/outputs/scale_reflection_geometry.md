# S / involution geometry — what the reflection symmetry is

The scale arm `S` is the **involution** `u↔−u` ⇔ `t↔1/t` ⇔ `s↔1−s` (theta self-duality /
functional equation). **Honest distinction:** the involution `J` is the *symmetry* of the
whole Weil form (the test functions are even; `ξ(s)=ξ(1−s)`), **not** a separate operator
`R_S`. The operator that plays the "reflection pressure" role in `Q_W=H−R` is the
**prime term `R`** (see finite_prime_capacity_geometry.md). The involution pins the
**centre/horizon** at the fixed axis `Re(s)=1/2` (`u=0`, `t=1`) — the FE-axis scan
(centre WO) showed the residual vanishes *only* there; a wrong centre breaks the FE.
> Reflection (the primes) is necessary but dangerous; the involution fixes the horizon at
> Re(s)=1/2; RH is the statement that prime reflection never exceeds capacity there.
