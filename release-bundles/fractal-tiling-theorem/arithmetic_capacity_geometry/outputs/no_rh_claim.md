# No RH claim
WO-VFD-RH-ARITHMETIC-CAPACITY-GEOMETRY-001 does NOT prove RH. It identifies the inner
geometry of the Adelic-Triskelion centre — capacity `H = A∞ + pole`, reflection `R` =
primes (correcting this WO's labeling), domination `R ≤ H` ⟺ `‖K‖≤1`. The mapping to the
Weil explicit formula / Connes positivity is **exact**, hence a re-naming, not a new route;
the proof gap is identical to Connes' open problem (force Weil positivity via the
arithmetic-site geometry over Spec ℤ). RH-equivalent; unchanged.
