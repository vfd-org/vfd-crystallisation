# Write-up set — VFD geometry & arithmetic (the closure object)

Consolidated 2026-06-08 into one geometry-first paper plus companions, per
`../SET-ARCHITECTURE.md`.

## The paper
- **`the-closure-object.tex`** — *A Closure Geometry on the 600-Cell and Its Arithmetic
  Shadow.* Geometry-first: the icosian closure object leads; its arithmetic shadow is
  the $L$-function $\zeta_K(s)\zeta_K(s-1)\,C_2(s)$ (recalled from the published
  `icosian-triad-v600`, local-2 factor honest); the certified boundary shows $\zeta$ is
  one factor only and the strong "object$\leftrightarrow\zeta$" bridge is rejected; the
  physics ($\mathcal{V}_1$ scale–phase–witness reading) is §5 interpretation, walled.
  **No RH claim.**

## Companions (clearly non-load-bearing)
- `paper-III-positivity-wall.tex` — expository: RH = one positivity, many faces; the
  $\mathbb{F}_1$ ledger; the wall. No new result.
- `note-shared-object.tex` — the cosmology↔RH firewall note.
- `../from-a-point-to-a-universe.md` — intuition (scale–phase / Riemann-sphere picture).
- `../vfd-rh-reformulation.md` — the falsifiable $\mathcal{V}_1$ / witness-operator
  reformulation (conjectural + a concrete test).
- `../vfd_math_engine/` — the verification gate, certificates, atlas (reproducibility).

## Superseded (kept for provenance in `superseded/`)
- `reach-and-limit` (RH-first draft of the paper), `paper-I-geometric-L-functions`,
  `paper-II-verification-engine` — folded into `the-closure-object`.

## Governing rule (unchanged)
Geometry-first; physics is the frame, not the proof. RH enters only as "$\zeta$ is one
factor." Cosmology and RH never touch as a claim.

## Review trail
Codex hostile-review passes in `../../../docs/reviews/`. The local-2 factor $C_2(s)$,
the demotion of the two sub-object claims to Numerical Checks, and the finite-window
framing of the counting route were the load-bearing corrections.

## Open / out-of-scope follow-ups
- The published `icosian-triad-v600` bundle's `docs/FINDINGS.md` is stale in places
  (omits $C_2(s)$ before later adding it); worth a consistency fix in that bundle, but
  it does not affect this paper.
- Optional before submission: confirm the `IcosianTriad` citation is publicly resolvable;
  add spacing-histogram and density-vs-height figures.
