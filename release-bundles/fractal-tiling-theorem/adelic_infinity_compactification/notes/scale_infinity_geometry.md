# Scale infinity as a boundary — and why it doesn't resolve the wall

`u=log t → v=tanh(u/L)` maps `u→±∞` to the boundary points `v=±1`; the involution
`u↔−u` becomes `v↔−v`, centre fixed at `v=0` (`t=1`, `Re(s)=1/2`). So scale infinity
*becomes* two self-dual boundary points.

**But this is a smooth change of variables = a similarity on the witness space.** Verified:
`‖K‖` (the spectral radius of `K`) is **invariant** under conjugation (Δ ~ 1e-15). So
compactification **relabels** where infinity sits; it does **not** change positivity.

**And a finite-dimensional operator has no genuine boundary at infinity at all** — there
is nothing to leak through. The near-null mode's "boundary vs central" character is
**coordinate-dependent** (low-edge in the basis index; near `v=−0.9` in this symmetric
tanh map) — i.e. *not an invariant*. Only `‖K‖` carries invariant content, and it is
unchanged. The genuine adelic boundary exists only in the **infinite-dimensional** space
(Connes idele-class compactness), which is the open problem.
