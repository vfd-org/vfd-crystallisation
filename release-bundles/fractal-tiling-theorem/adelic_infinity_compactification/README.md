# WO-VFD-RH-ADELIC-INFINITY-COMPACTIFICATION-001

Turns the infinite limit into compact boundary geometry. **WEAK PASS; NOT a proof of RH.**

**Honest core:** scale compactification `u→v=tanh(u/L)` (infinity → `v=±1`) is a
**positivity-invariant reparametrization** — `‖K‖` is unchanged under the similarity
(verified Δ~1e-15). A **finite** operator has **no genuine boundary at infinity** to leak
through; the near-null's boundary-vs-central character is **coordinate-dependent** (no
invariant content — only `‖K‖` is invariant). Prime/spectral "infinity" tails are
convergent at finite cutoff (the true `p→∞`, `γ→∞` boundary is not represented). Self-dual
windows preserve the FE; non-self-dual break it.

So compactification gives a coherent geometric **picture** of the boundary channels (scale,
prime, spectral, operator-domain) but adds **no proof power**. The remaining theorem —
`‖K‖≤1` on the genuine **infinite** compactified adelic witness space — is RH-equivalent and
unchanged (Connes idele-class compactness / arithmetic site). Run `python3
src/run_compactification.py`. LOCAL, not pushed.
