\setcounter{secnumdepth}{-1}
\usepackage{microtype}
\usepackage{eso-pic}
\usepackage{graphicx}
\setlength{\parindent}{1.25em}
\linespread{1.06}

% --- "behind the maths" companion boxes ---
\usepackage[most]{tcolorbox}
\definecolor{vfdaccent}{HTML}{B3541E}
\definecolor{vfdpaper}{HTML}{F7F2EC}
\newtcolorbox{codebox}{%
  enhanced, breakable,
  colback=vfdpaper, colframe=vfdaccent,
  boxrule=0pt, leftrule=2.4pt,
  sharp corners,
  top=7pt, bottom=7pt, left=11pt, right=11pt,
  before skip=12pt, after skip=12pt,
  fontupper=\small\setlength{\parindent}{0pt}%
}
