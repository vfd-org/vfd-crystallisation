Files edited:
- [verify_h_grad_1.py](/mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/cascade-derivation/scripts/verify_h_grad_1.py) — 435 lines. Replaced the old broad A1-A4 harness with a focused exact B1/B2 verifier.

Files created:
- None.

Key decisions:
- Used exact `Fraction` arithmetic throughout; no `sympy`, no floats.
- Hard-coded an explicit eight-unit icosian Z-basis and verified all 120 unit icosians decompose integrally in it.
- Did not update status flips in `cascade-h-grad-1-closure.md` or `WO-H-GRAD-1.md`, because B2 fails the requested AC counts.

Verification:
- `python3 -m py_compile papers/cascade-derivation/scripts/verify_h_grad_1.py` passes.
- `python3 papers/cascade-derivation/scripts/verify_h_grad_1.py` runs and returns exit 1 by design:
  - B1 PASS: 120/120 units decompose integrally, coefficient rank 8.
  - B2 FAIL: exact count is zero vector = 1, nonzero isotropic = 135, anisotropic = 120.
  - Sanity FAIL: 120 signed unit icosians reduce to 60 distinct classes, since `u == -u mod 2I_lat`.

Residual caveat:
- The task’s B2 target appears inconsistent with its own definition of `q` and with mod-2 reduction: antipodal units cannot remain 120 distinct classes modulo `2I_lat`.

Open sub-questions:
- Clarify whether the intended B2 convention is `Tr(N) mod 2`, the conventional `E8` root quadratic, or a sign-swapped naming convention.
- Clarify whether “120 unit icosians” should mean signed vertices or antipodal classes.
