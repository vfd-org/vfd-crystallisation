# Universal Infinity Capacity Geometry (framework note)

Defines **infinity** as a *completed boundary* with capacity/reflection/leakage, not
endlessness: `ICG=(X,∂X,Φ,H,R,Q,K,L)`, stable ⟺ `Q=H−R≥0` ⟺ `‖K‖≤1`.

**WEAK PASS.** RH and black-hole instantiations are concrete (verified Weil/Connes algebra;
scattering flux identity); **brain & generic-VFD are conceptual analogy only** (no data).
**The unification is generic** (the law holds for any PSD `H`+sym `R` — verified), i.e. the
ubiquity of "stability = a positive quadratic form." So this is a **reusable organizing
language, not new mathematics and not a proof in any domain.**

- `outputs/` — universal definition, domain table, RH/BH/brain instantiations (RH with the
  corrected `H`=archimedean+pole, `R`=primes labeling + computed diagnostics), theorem
  template, universal diagnostics, claim boundary, interpretation, `rh_diagnostics.json`.
- `figures/universal_infinity_capacity_geometry.png` — the template.

For RH it re-states the same wall (infinite `‖K‖≤1` = Weil positivity = arithmetic site).
Not a proof of RH/physics/consciousness. LOCAL, not pushed.
