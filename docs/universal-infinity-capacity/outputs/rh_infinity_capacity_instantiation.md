# RH instantiation (concrete; corrected labeling)

`ICG_RH`: `X` = completed-ζ test functions; `∂X` = adelic infinity (scale/prime/spectral/
operator-domain — see compactification WO); `Φ` = scale/Mellin flow; **`H = archimedean
Γ/digamma + ζ-pole`** (the *capacity*; archimedean sign-indefinite alone, pole lifts PSD);
**`R = finite primes / prime-power (Euler) trace`** (the *reflection*); `Q = H−R` (the
Weil positivity form); `K = H^{−1/2}RH^{−1/2}`; `L` = norm leakage at the adelic boundary.

Diagnostics (NC=16, computed): `D1` H min eig **+0.073** (`H≻0`); `D2` Q min eig **+3.9e-5**
(`Q≥0`, near-null); `D3` **`‖K‖=0.99981`** (`≤1`); `D5` near-null capacity ratio
`R/H=0.9998` (reflection ≈ capacity, marginal).

> **RH ⟺ `Q_RH ≥ 0` for all admissible test functions ⟺ `‖K‖≤1`** = prime reflection never
> exceeds archimedean+pole capacity = **no arithmetic superradiance at adelic infinity.**
> These objects **are** the Weil explicit-formula / Connes positivity objects (exact, not
> analogy); the bound in the infinite limit is open (= RH).
