# Brain instantiation (CONCEPTUAL ONLY — no data in this WO)

`ICG_BRAIN`: `X` = neural state space; `∂X` = attractor-basin / phase-transition surface;
`Φ` = neural/metabolic state flow; `H` = coherence capacity / damping / regulation; `R` =
recurrent feedback / pathological recurrence; `Q` = Lyapunov stability margin; `K` =
feedback-normalised return; `L` = seizure / decoherence leakage.

Reading: a stable attractor ⟺ feedback `R` ≤ coherence capacity `H` (Lyapunov stability);
failure (`R>H`) ⟺ runaway / loss of coherence. **This is a structural analogy only** — no
neural data is fit or tested here; it must not be read as a claim about consciousness or
clinical dynamics. (The VFD programme's actual EEG/anaesthesia work is separate; this WO
does not invoke it as evidence.)
