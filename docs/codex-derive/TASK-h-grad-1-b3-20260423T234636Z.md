Read-only consultation only. I did not edit or commit. The existing B1/B2 verifier passes, and a read-only enumeration against the repo’s current `q_I` found an important B3 correction: raw maximal totally singular 4-spaces are `270`, not `30`; `30` appears only after an extra pointing/incidence restriction, so B3 must name that restriction.

**SECTION A. Insight / External Content Relevance**

Already in cascade papers:

- B3 is the explicit map `μ_I : I/2I_lat ↠ (F_2)^3` with 5-dim kernel `K`; catalogue marks it build-required via Route K or Route Q (`cascade-h-grad-1-catalogue.md:21`).
- B1/B2 are usable inputs: the script defines the exact 8-unit basis (`verify_h_grad_1.py:190-202`), `q_mod2` (`verify_h_grad_1.py:302-314`), and verifies `1 + 135 + 120` plus polar rank 8 (`verify_h_grad_1.py:399-460`).
- The current §4 already warns that a maximal isotropic alone does not provide the Fano quotient (`cascade-h-grad-1-closure.md:193-201`) and that `deg` is ambiguous away from labelled basis classes (`cascade-h-grad-1-closure.md:213-223`).
- The stale upstream statement is in `cascade-fano-grading-lift.md:163-170`: it says `30` maximal isotropics and suggests projecting a 4-dim isotropic to a 3-dim Fano subspace. That must be tightened.

Only in `insight.md` / external:

- `insight.md:670-679` gives QMS-3 / god-prime motivation for Route Q only. It does not list the seven Fano triads, so it cannot canonicalise Route K.
- `insight.md:878-892` proposes pentagonal holonomy. This is multiplicative `Z[φ]^×` connection data, orthogonal to the additive `F_2` quotient here; catalogue already classifies it that way (`cascade-h-grad-1-catalogue.md:67-71`).
- External Route K pointers: Conway-Sloane Ch. 8 contains “The Icosians and the Leech Lattice” in the algebraic lattice chapter (TOC lines 900-910 at the Springer/book listing). Baez/Conway-Smith gives the Fano multiplication rule and octonion basis (`https://math.ucr.edu/home/baez/octonions/conway_smith/`, lines 87-90), the Kirmse mistake / Coxeter correction (lines 103-105), and Cayley integers as the `E_8` lattice (line 113). Basak gives the clean `F_8` twisted group algebra model for octonions and integral orders (`https://arxiv.org/abs/1702.05705`, lines 42-43).

**SECTION B. Priority Gaps To Close B3 Route K**

B3.1 — Maximal-Isotropic Enumeration / Count Disambiguation  
Object: `Max(q_I) = {L ≤ I/2I_lat : dim_F2 L = 4, q_I|_L = 0}`.  
Bridge: task AC2 and closure §4.  
Source route: classical `O^+(8,2)` finite-orthogonal geometry plus exact sim.  
First step: add RREF enumeration over `F_2^8`, precomputing `q_mod2`. It should print:

- `all_maximal_totally_singular_4_spaces = 270`
- `through_identity_[omega_1] = 30` as an incidence sanity check
- then separately define the Kirmse-Coxeter admissible subfamily used for `L_0`.

Do not print “30 maximal isotropics of `F_2^8`” without the qualifier.

B3.2 — Kirmse-Coxeter Admissible Frame / Canonicality  
Object: admissible frame `(L_0, η)` where `L_0` is a 4-dim KC/Hamming isotropic and `η : (F_2)^3 → I/2I_lat` labels `{1,e_1,...,e_7}`.  
Bridge: AC10 canonicality.  
Source route: classical KC/Coxeter plus new repo-local sim.  
First step: canonicalise deterministically, not by hand: fix `η(0) = [1] = omega_1 mod 2`; enumerate candidate `(L, η)` satisfying the affine Fano relations; choose lexicographically minimal `(rref(L), η(v1), η(v2), η(v4))` in current `B_I` bit ordering. If Aut/E8 generators are later available, compute orbit partitions; until B6, call this “canonical for the sim basis,” not G2-canonical.

B3.3 — Kernel, Not “Complement W0 To L0”  
Object/map: `K = ker μ_I ≤ I/2I_lat`, `dim K = 5`, and a 3-dim complement `T`.  
Bridge: fixes the current dimension mismatch: a complement to 4-dim `L_0` has 16 elements, not 8; a quotient by 5-dim `K` has 8 cosets.  
Source route: new derivation using KC frame.  
First step: require `rank(K)=5`, `I/2I_lat = K ⊕ T`, and `μ_I` is the projection to `T ≅ (F_2)^3`. The octonion basis classes are a labelled transversal/coset system, not a vector-space complement `W0` of `L0`.

B3.4 — Octonion Basis Labels  
Object: label map `η(0)=[1]`, `η(v_i)=[e_i]`, with task vectors  
`v1=100, v2=010, v3=110, v4=001, v5=101, v6=011, v7=111`.  
Bridge: Fano incidence and grading compatibility.  
Source route: Basak `F_8` twisted group algebra / Baez-Conway-Smith Fano table.  
First step: verify for every Fano line `{i,j,k}` that

`η(v_i) + η(v_j) + η(v_k) + η(0) ∈ L_0`

and therefore `μ_I(η(v_i)) + μ_I(η(v_j)) + μ_I(η(v_k)) = 0`. Signs/orientation are irrelevant for the grading, but the line list should be exactly the task’s seven lines.

B3.5 — Define And Verify `μ_I`  
Object/map: linear `μ_I : F_2^8 → F_2^3`, represented by a `3x8` matrix over `F_2`.  
Bridge: A3.  
Source route: Route K construction plus exact sim.  
First step: solve for `M` with `M k = 0` for all `k ∈ K`, `M η(0)=0`, and `M η(v_i)=v_i`. Verify:

- `rank(M)=3`
- `|ker(M)|=32`
- image has all 8 vectors
- all seven Fano line sums pass
- exhaustive linearity over all pairs, not just 20 random pairs.

**SECTION C. Reversals / Corrections For Claude**

- at `papers/cascade-derivation/TASK-h-grad-1-b3.md:18` replace “Enumerates the 30 maximal isotropic 4-dim subspaces” with “Enumerates all 270 maximal totally singular 4-dim subspaces, then constructs/filters the 30 Kirmse-Coxeter admissible pointed frames used for B3.”
- at `papers/cascade-derivation/TASK-h-grad-1-b3.md:22` replace “constructs a complement W_0 such that F_2⁸ = L_0 ⊕ W_0. W_0 has 8 elements” with “constructs a 5-dim kernel K and a 3-dim complement T such that F_2⁸ = K ⊕ T. The 8 octonion basis classes are a labelled transversal, not W_0.”
- at `papers/cascade-derivation/cascade-fano-grading-lift.md:167` replace “30 maximal isotropic 4-dim subspaces” with “270 maximal totally singular 4-dim subspaces; the number 30 only appears after fixing extra pointing/incidence/Kirmse-Coxeter data.”
- at `papers/cascade-derivation/cascade-fano-grading-lift.md:170` replace “projecting a 4-dim subspace to its 3-dim ‘Fano subspace’” with “choosing a Kirmse-Coxeter frame supplies a 5-dim kernel and an `F_8 ≅ (F_2)^3` labelling; the 4-dim isotropic alone does not project to the Fano quotient.”
- at `papers/cascade-derivation/cascade-h-grad-1-closure.md:205` replace “μ = deg ∘ π, where π projects along L to a chosen complement W” with “μ_I is the quotient map by a 5-dim kernel K supplied by the Kirmse-Coxeter frame; L_0 is only part of the data.”
- at `papers/cascade-derivation/cascade-h-grad-1-closure.md:407-417` replace the “kernel K as the span of the three relations” wording with “kernel K is computed as the 5-dim nullspace of the solved `3x8` degree matrix; Fano relations are checked by `η(a)+η(b)+η(a+b)+η(0) ∈ L_0`.”

**SECTION D. Route K Standard Model**

Use `F_8` as `F_2^3` with labels exactly as in the task. The standard degree map is:

`μ_std(sum c_x e^x) = sum c_x x`.

Then `K_std = ker μ_std` has dimension 5. The Fano rule is just `x+y+z=0`; the seven nonzero `x` are the seven imaginary octonion basis labels. The repo sim’s job is to transport this model into the current `I/2I_lat` basis by finding `η` and solving the `3x8` matrix `M`.

**SECTION E. Route Q / Attribution Audit**

Do not use QMS-3 to choose Route K data. `insight.md:670-679` gives a Route Q motivation only, and `insight.md:685-699` proposes future god-prime/Fano-triad checks but no explicit triads. `insight.md:878-892` is a separate holonomy/zeta build. Also avoid conflating “Coxeter projection” in `insight.md` with Coxeter’s correction to Kirmse integers.

**SECTION F. Top 3 Next Sim Outputs**

1. `verify_h_grad_1_b3.py` after `verify_h_grad_1.py:399-460`: print raw isotropic geometry: `270` all max singular 4-spaces, `30` through `[1]`, and the count of KC-admissible frames after the Fano filter.

2. At `TASK-h-grad-1-b3.md:29-54`: print the chosen `η` table: `0,[1]` plus `v1..v7`, their 8-bit classes, unit-class provenance, and all seven affine Fano relation PASS lines.

3. At `cascade-h-grad-1-closure.md:385-417`: print the actual `μ_I` data: 5-row kernel basis, `3x8` matrix, rank 3, kernel size 32, image size 8, exhaustive linearity PASS.
