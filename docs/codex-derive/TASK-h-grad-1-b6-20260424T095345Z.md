**SECTION A. Insight / External Content Relevance**

Already in cascade papers:

- B6 target is exactly “all admissible choices of Kirmse-Coxeter frame `(L_0, η, μ_I)` from B3 lie in a single orbit” so `μ_I` is canonical modulo the relevant automorphism action: `TASK-h-grad-1-b6.md:5-9`.
- Task allows either finite sim over `Aut(F_2^8,q_I)` or proof via classical octonion theory: `TASK-h-grad-1-b6.md:16-27`, with guardrail to switch if enumeration exceeds `10^7`: `TASK-h-grad-1-b6.md:90-98`.
- B3 data currently fixed: `L_0` is lex-first maximal totally singular 4-space not containing `[1]`; `K=L_0⊕<[1]>`; `T` is lex-first 3-complement; `μ_I` is the 3x8 map with kernel `K`: `cascade-h-grad-1-closure.md:237-253`, implemented in `verify_h_grad_1_b3.py:185-287`.
- B6 is explicitly the only remaining H-grad-1 build: `cascade-h-grad-1-closure.md:624-628`, `cascade-h-grad-1-catalogue.md:98-104`.
- Current B6 first step is correctly named: define the action on `I/2I_lat`, not abstract octonions, then prove transitivity: `cascade-h-grad-1-closure.md:574-584`.

Only in `insight.md` / external:

- QMS/god-prime content is relevant only as an alternate Route Q: factor `7` in `084473 = 137(7×87+8)-56` is tied to seven Fano triads: `insight.md:670-679`; proposed tests are `insight.md:693-699`. It is not a B6 Route K proof.
- Pentagonal holonomy is orthogonal to H-grad-1: it concerns a `Z[φ]^×` 1-cocycle and a cascade clock, not the additive quotient `I/2I_lat -> (F_2)^3`: `insight.md:878-893`.
- Classical G2 support: Baez states `G2` is `Aut(O)` and that basic octonion triples are carried uniquely to basic triples by octonion automorphisms: [Baez, The Octonions, §4.1, lines 1720-1741](https://webhomes.maths.ed.ac.uk/~v1ranick/papers/baezocto.pdf).
- Classical finite orthogonal support: `Aut(F_2^8,q_I)` is the plus-type orthogonal group. Notation caveat: ATLAS writes `O8+(2)` for the simple/index-two group of order `174,182,400`, with outer triality `S3`: [Bray/Wilson ATLAS page, lines 0-6](https://webspace.maths.qmul.ac.uk/j.n.bray/web/Pres/O8p2.html). The full linear isometry group preserving `q` has order `348,364,800`.
- Classical group proof route should cite Witt’s theorem / polar spaces; Taylor’s table of contents pins the relevant chapters: polar geometry and Witt’s theorem at Ch. 7.5-7.7, orthogonal groups and maximal totally singular subspaces at Ch. 11.1-11.20: [Taylor, Geometry of the Classical Groups, lines 67-78 and 119-139](https://www.heldermann.de/SSPM/SSPM09/sspm09.htm).

**SECTION B. Priority Gaps To Close The Task**

B1 / B6.1 — Define the admissible-frame set precisely.

- Object: finite `G_p`-set  
  `Adm_red(V,q,p,A) = {(L, μ) : L ≤ V, dim L=4, q|_L=0, p∉L, K=L⊕<p>, μ:V↠A, ker μ=K}`, where `V=I/2I_lat`, `p=[1]`, `A=(F_2)^3`.
- Bridges: B3’s lex-first construction to an orbit theorem.
- Source route: new cleanup derivation.
- First step: add lemma: `|{L}|=240`, `|Iso(V/K,A)|=168`, so reduced admissible frames count `240*168=40320`. Raw section choices for `η` add `2^(5*3)` complements and exceed `10^7`; do not enumerate raw `(L,T,η)`.

B2 / B6.2 — Identify the acting group.

- Object/map: `G=Aut(V,q_I)=O^+(8,2)` acting on `V`; `G_p=Stab_G([1])`.
- Group facts: `|G|=348,364,800`; `|G_p|=|G|/135=2,580,480`; `G_p ≅ 2^6 : O^+(6,2) ≅ 2^6:S_8`.
- Bridges: answers the task’s “what is the automorphism group?” question.
- Source route: classical finite orthogonal groups / E8 mod-2 form.
- First step: state notation caveat against ATLAS/Wilson simple `O8+(2)` order `174,182,400`.

B3 / B6.3 — Prove orbit transitivity on reduced B3 frames.

- Proof: `G_p` is transitive on `(L,μ)∈Adm_red`.
- Bridges: B6 canonicality at the `Aut(F_2^8,q_I)` level.
- Source route: classical Witt extension plus explicit hyperbolic-basis proof.
- Concrete lemma: choose hyperbolic basis `e_i,f_i` with `p=e_1`; model any `L` as `span(f_1,e_2,e_3,e_4)`. The stabilizer of `p,L` contains the contragredient `GL(3,2)` on `span(f_2,f_3,f_4)=V/K`, hence matches any quotient label `μ`.

B4 / B6.4 — Pull O-plus canonicality back to “G2 canonicality.”

- Object/map: define the actual octonion pullback, e.g.  
  `κ : {Kirmse-Coxeter/Cayley octonion frames} -> Adm_red(V,q,p,A)`  
  and the induced group homomorphism from the relevant octonion automorphism group into `G_p`.
- Bridges: turns “O^+(8,2)-canonical” into the requested “G2/octonion-canonical.”
- Source route: classical Baez `G2=Aut(O)` + Conway-Sloane Ch. 8 §6 + new cascade pullback.
- First step: decide whether the finite actor is `GL(3,2)` on Fano labels, `G2(2)` on a split mod-2 octonion algebra, or the full `G_p`. `G2(2)` alone cannot be transitive on all 240 `L` choices since `240` does not divide `12096`.

B5 / B6.5 — Downstream naturality for B4/B5.

- Object: commutative/naturality square for `red`, `μ`, and `C_O`.
- Bridges: confirms B6 does not change B4 `red` or B5 characters except by target relabelling / lifted automorphism.
- Source route: new linear algebra.
- First step: prove: if `μ' = h μ g^{-1}` and `g` lifts compatibly through `red`, then `C_O(μ')` is the corresponding relabelled pullback. Without that lift, B5 is canonical relative to the frame, as already stated at `cascade-h-grad-1-closure.md:443-445`.

**SECTION C. Reversals / Corrections**

- at `papers/cascade-derivation/cascade-h-grad-1-closure.md:324` replace  
  `**Status A4:** BUILD REQUIRED (builds B4 + B5, §7). The concrete verification (V1)–(V3) cannot begin until A1, O2, and O3 are supplied.`  
  with  
  `**Status A4:** BUILD COMPLETE relative to the B3/B4 frame (sim-verified 2026-04-24 via B4+B5). Absolute frame-canonicality remains B6.`

- at `papers/cascade-derivation/cascade-h-grad-1-closure.md:389` replace  
  `**Status A4 dual:** BUILD REQUIRED (B5; 8 C_O Pontryagin characters).`  
  with  
  `**Status A4 dual:** BUILD COMPLETE (B5; see §5b).`

- at `papers/cascade-derivation/cascade-h-grad-1-closure.md:630` replace the paragraph saying the primal/dual bridge is not constructed with:  
  `**What this working note does NOT yet prove.** It does not yet exhibit B6: the G2/octonion-pullback canonicality of the B3 Kirmse-Coxeter frame and B4 basis choice. The primal/dual bridge named in §5.4 is now supplied by B4+B5; only absolute frame-canonicality remains part of B6.`

- at `papers/cascade-derivation/WO-H-GRAD-1.md:101` replace  
  `G₂ acts transitively on octonion structures.`  
  with  
  `Classically G₂ acts transitively on octonion basic triples; B6 must identify the induced action on the B3 reduced Kirmse-Coxeter frames in I/2I_lat.`

**SECTION D. Sim vs Proof Recommendation**

Sim is tractable only after reducing the frame definition.

- Good sim: enumerate `240` choices of `L`, `168` quotient labellings each, total `40320` reduced frames, then verify one orbit under generators for `G_p`. This is comfortably below the `10^7` guardrail.
- Bad sim: enumerate raw `(L,T,η)` with arbitrary complements. Count is `240 * 2^15 * 168 = 1,321,205,760`, so it violates the task guardrail.
- Recommendation: implement a small sim as evidence, but write the proof sketch. The proof is short with Witt/hyperbolic-basis language, and the G2 part needs citations plus pullback definitions anyway.

**SECTION E. Attribution Audit**

Do not cite Baez/Conway-Sloane as proving the exact B3 mod-2 frame orbit unless the pullback `κ` is written. They prove the classical octonion/G2 side. The finite quadratic-space orbit is classical orthogonal geometry. The bridge between them is cascade-specific B6.

Route Q should remain parallel: `insight.md:670-679` motivates a possible canonical `Θ_QMS`, but it does not supply the seven triads or a map `I/2I_lat -> (F_2)^3`.

**SECTION F. Top 3 Next Builds**

1. `cascade-h-grad-1-closure.md:574-584`: add B6.1/B6.2 definitions: `V,q,p,A`, `Adm_red`, `G=O^+(8,2)`, `G_p`, counts `240`, `40320`.

2. `cascade-h-grad-1-closure.md:574-584` or new §5c after `:445`: add B6.3 proof using hyperbolic basis/Witt extension; optional sim pointer `verify_h_grad_1_b6.py` enumerating only reduced frames.

3. `cascade-h-grad-1-closure.md:443-445` and catalogue `:24`: add B6.4 pullback paragraph distinguishing `O^+(8,2)` canonicality from octonion/G2 canonicality, citing Baez §4.1, Conway-Sloane Ch. 8 §6, and Wilson/Taylor finite orthogonal group references.
