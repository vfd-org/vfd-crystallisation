I did not run `scripts/codex_derive.sh`: it creates `docs/codex-derive` and tees a saved report (`scripts/codex_derive.sh:49-51,148`), which violates this read-only pass. I inspected it and followed its prompt manually.

**SECTION A — Relevance**
Already in cascade papers:

- `C_O` is defined abstractly, not explicitly: `C_r := {χ : F(χ) ⊆ Q_r}` and `C_O` as Observer-rung characters ([access theorem](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/cascade-derivation/cascade-access-principle-theorem.md:65>), line 76). No `α_a` coordinates.
- `Q_O` is explicit as the octonion algebra with generators `{1,e_1,...,e_7}` ([query presentations](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/cascade-derivation/cascade-query-algebra-presentations.md:192>), lines 198-208).
- `Q_O ≅ Meas(S^7,σ)` is algebra-level only: basis-to-basis and product compatibility ([Q-O bridge](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/cascade-derivation/cascade-q-o-measurement-bridge.md:112>), lines 116-138). It does not construct Pontryagin characters.
- `cascade-fano-grading-lift.md` identifies the target condition: `μ|_{C_O}` bijective to `(Z/2)^3` ([line 136](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/cascade-derivation/cascade-fano-grading-lift.md:136>)), and already corrects the old finite-subgroup error at line 146.
- B3/B4 supply the computable target: B3 defines `μ_I` and Fano labels ([B3 script](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/cascade-derivation/scripts/verify_h_grad_1_b3.py:167>), lines 167-174, 364-389); B4 defines `red` ([B4 script](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/cascade-derivation/scripts/verify_h_grad_1_b4.py:173>), lines 173-217).

Only in `insight.md` / external:

- QMS-3/god-prime gives only a factor-7 clue for Fano triads (`insight.md:670,677,686,696,708`). It is not a `C_O` construction.
- Pentagonal holonomy is a separate multiplicative `Z[φ]^×` connection (`insight.md:878-890`), not the additive mod-2 character quotient needed for B5.
- Classical literature supports B1-B3 style icosian/Fano machinery, not explicit cascade `C_O` coordinates.

Conclusion: `C_O` with explicit eight Pontryagin characters is genuinely new derivation content. The repo has placeholders and target conditions, not the eight `α_a`.

**SECTION B — Priority Gaps**
B1. Observer-character anchor theorem  
Object: `κ_O : {1,e_1,...,e_7} -> Λ^∨/2Λ^∨`, where `Λ = Z[φ]^6`.  
Bridges: `Q_O` algebra basis to actual `C_O ⊂ Ẑ[φ]^6`.  
Source route: new cascade derivation from Def. `C_r`, `π_O`, and Phase-M3 split.  
First step: prove “the eight octonion-basis sections are order-2 Pontryagin eigencharacters of the combined moduli action.”

B2. Primal/dual bridge for B4  
Map: `red^∨ : (I/2I_lat)^∨ -> Λ^∨/2Λ^∨`, or an explicitly justified section of `red`.  
Bridges: B4’s primal matrix to character representatives.  
Source route: standard Pontryagin duality plus new cascade bookkeeping.  
First step: fix trace-dual basis of `Z[φ]`: `τ_0=(3-φ)/5`, `τ_1=(-1+2φ)/5`, so a bit vector `c∈F_2^12` represents `α=(1/2)Σ c_j τ_j`.

B3. Eight explicit representatives  
Object: `α_0,...,α_7 ∈ Q(φ)^6/(Z[φ]^6)^∨`.  
Bridges: AC1-AC2.  
Source route: new derivation, computable once B1/B2 are accepted.  
First step: use lex-first preimages of B3 `η(v_a)` under B4 `red`; candidate bit vectors in `(α_1,β_1,...,α_6,β_6)` order are:
`100000000000`, `101000000000`, `111100000000`, `110100000000`, `100010000000`, `101010000000`, `111110000000`, `110110000000`.

B4. Verification sim  
Object: `scripts/verify_h_grad_1_b5.py`.  
Bridges: AC3-AC4.  
Source route: mechanical exact arithmetic.  
First step: import B3/B4, reduce each `α_a` to mod-2 bits, verify `μ_I(red(α_a))=v_a`, distinctness in `F_2^12`, and preferably distinct `red(α_a)` in `F_2^8`.

B5. Identity convention lemma  
Object: proof deciding whether `χ_0` is the global trivial character or the B3 affine identity class `[1]`.  
Bridges: octonion identity vs Pontryagin degree zero.  
Source route: new derivation.  
First step: prove whether the section `1∈Q_O` is the global unit section of `Γ(G,F)`. If yes, use `χ_0=0`; if no, affine `[1]` may be valid.

**SECTION C — Corrections**
- At `cascade-fano-grading-lift.md:98` replace `C_O ⊂ Ẑ[φ]⁶ forms a subgroup of order 8` with `B5 must represent the octonion-basis frame of C_O by eight order-2 Pontryagin characters in the mod-2 character quotient; it is not assumed to be a literal finite subgroup of the torsion-free lattice.`
- At `cascade-access-principle-theorem.md:76` replace `these are parameterised by ô ∈ S⁷` with `the octonion-basis frame of C_O has eight characters; general ô ∈ S⁷ is a real octonion direction, not an additional basis character.`
- At `cascade-h-grad-1-closure.md:322` replace the paragraph saying B4 and B5 are both still to be constructed with `A4 is PARTIAL: B4 supplies the primal red map; B5 must supply the character-side C_O representatives.`

**SECTION D — Canonicality**
B3 gives a canonical choice only relative to the sim’s Kirmse-Coxeter frame: `L_0`, `T`, `η`, and `μ_I`. `cascade-observer.md` puts observer choices in a `G_2` orbit (lines 154-160), so B5 should state “relative to the B3 frame.” Absolute canonicality belongs to B6.

**SECTION E — Minimum Viable B5**
Minimum viable construction passing AC1-AC4:

1. Define `Λ=Z[φ]^6≅Z^12` and represent characters by `α=(1/2)c` in the trace-dual coordinate basis.
2. Prove B1/B2 above so the chosen eight `c_a` are actually `C_O`, not arbitrary preimages.
3. Use the B3/B4 matrices to verify labels and distinctness.
4. Keep B6 out of scope: canonical up to `G_2`, not absolute.

**SECTION F — Top 3 Next Builds**
1. `cascade-h-grad-1-closure.md:512` — add B5.1 observer-character anchor theorem. This is the real mathematical bridge.
2. `scripts/verify_h_grad_1_b4.py:173` — extend into `verify_h_grad_1_b5.py` with the eight bit representatives and trace-dual conversion.
3. `cascade-access-principle-theorem.md:76` — clarify finite octonion-basis characters vs continuum observer directions before using `C_O` downstream.
