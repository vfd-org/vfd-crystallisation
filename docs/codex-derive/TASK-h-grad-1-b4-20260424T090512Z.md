Read-only honored. I did not run `scripts/codex_derive.sh` because it writes to `docs/codex-derive/...` via `mkdir`/`tee` (`scripts/codex_derive.sh:49-51,142-148`), which conflicts with this read-only round.

**SECTION A. Insight / External Content Relevance**

Already in cascade papers:

- B4 is explicitly the missing primal map: `red : Z[φ]^6 → I/2I_lat`, factoring through `L_O/2L_O` (`cascade-h-grad-1-closure.md:440-451`; catalogue row at `cascade-h-grad-1-catalogue.md:22`).
- `red` is currently hypothetical, not constructed (`cascade-h-grad-1-closure.md:286-292`), and the primal/dual bridge is named as structural gap (`cascade-h-grad-1-closure.md:324-343`).
- `L_O` is only referenced upstream: `cascade-fano-grading-lift.md:175-179` says “Let `L_O ⊂ L_12` be...”, but no generators or construction are supplied.
- `L_12 = E_8 ⊕ Z[φ]^4` is stated architecturally in `cascade-12d-closure.md:56-61`, but the usable combined-level character object is better sourced from `cascade-phase-m3-closure.md:49-55,69`:  
  `π_int(L_12) = σ(I) ⊕ M ≅ Z[φ]^4 ⊕ Z[φ]^2 = Z[φ]^6`.
- Icosian facts are literature-backed: `I` is rank 8 over `Z`, generated by 120 unit icosians, with a Conway–Sloane / Moody–Patera basis (`cascade-algebraic-substrate.tex:480-514`). The star/Galois map is defined at `cascade-algebraic-substrate.tex:516-526`.
- B1/B2/B3 are usable inputs: verified `B_I` basis and `q_I` live in `verify_h_grad_1.py:190-202,310-314,399-460`; `μ_I` construction and checks live in `verify_h_grad_1_b3.py:213-219,347-362,421-430`.

Only in `insight.md` or external/prior-session content:

- QMS/god-prime content is relevant only as an alternative motivation for the Fano quotient, not for B4 `red`: `084473 = 137(7×87+8)-56`, with factor 7 linked to QMS-3 / Fano triads (`insight.md:670-679`). The proposed god-prime Fano-triad checks are at `insight.md:685-699,708-715`.
- Pentagonal holonomy is not a source for `red`: it proposes a `Z[φ]^×`-valued 1-cocycle and cascade clock (`insight.md:878-882`), explicitly a multiplicative/dynamical object not yet in repo (`insight.md:887-890`). Existing catalogue already marks it orthogonal to H-grad-1 (`cascade-h-grad-1-catalogue.md:68-72`).
- External literature needed: Conway–Sloane for `I`, `E_8/2E_8`; Moody–Patera for an equivalent 8-unit basis; classical Kirmse–Coxeter/Witt theory for the B3 Fano quotient.

**SECTION B. Priority Gaps To Close B4**

B4.1 — Construct `L_O`.

- Object: rank-8 `Z`-sublattice  
  `L_O := (π_int|_{L_12})^{-1}(σ(I) ⊕ 0) ⊂ L_12`, if the combined-level splitting is accepted.
- Type: inclusion `ι_O : L_O ↪ L_12`; isomorphism `π_int|_{L_O} : L_O ≅ σ(I)`.
- Bridges: the referenced-but-unbuilt `L_O` in `cascade-fano-grading-lift.md:175-179`.
- Route: new derivation, using Phase-M3 split `π_int(L_12)=σ(I)⊕M`.
- First step: prove the Observer-summand lemma: under `L_12 ∩ (R^4_phys × {0})={0}` (`cascade-phase-m3-closure.md:79`), the inverse image above is rank 8 and canonical relative to the Phase-M3 splitting.

B4.2 — Identify `L_O ≅ I`.

- Map: `θ_O : L_O → I`,  
  `θ_O = σ^{-1} ∘ π_int|_{L_O}`.
- Domain/codomain: `L_O` rank-8 `Z` lattice to icosian ring `I`.
- Bridges: `L_O/2L_O` to `I/2I_lat`.
- Route: classical icosian star-map plus new basis verification.
- First step: choose/verify a `Z[φ]`-basis `g_1,...,g_4` of `I`; check `{g_i, φg_i}` has determinant `±1` against the verified `B_I` basis in `verify_h_grad_1.py:190-202`.

B4.3 — Build the `Z[φ]^6` bridge.

- Map: `Ψ : Z[φ]^6 ≅ σ(I) ⊕ M → π_int(L_12)`.
- Projection: `p_O : Z[φ]^6 = σ(I) ⊕ M → σ(I)`.
- Bridges: the character/winding lattice to the Observer-rung icosian summand.
- Route: new derivation from `cascade-phase-m3-closure.md:49-55,69`; not supplied by `cascade-12d-closure.md` alone.
- First step: fix coordinates  
  `Z[φ]^6 = Z[φ]^4_O ⊕ Z[φ]^2_M`, and prove the first four coordinates are exactly the `σ(I)` summand.

B4.4 — Explicit `red`.

- Map:
  ```
  red = ρ_I ∘ θ_O ∘ p_O
      : Z[φ]^6 → I/2I_lat ≅ F_2^8
  ```
- Coordinate form: for `x=(u,m) ∈ σ(I)⊕M`,  
  `red(x) = [σ^{-1}(u)] mod 2I_lat`.
- Bridges: B4’s required chain `Z[φ]^6 → L_O/2L_O → I/2I_lat`.
- Route: projection route, not QMS or holonomy.
- First step: compute the explicit `8×12` `F_2` matrix of `red` in the chosen `Z`-basis `{1,φ}` of each `Z[φ]` coordinate and the verified `B_I` basis.

B4.5 — Sim / well-definedness.

- Script target: `scripts/verify_h_grad_1_b4.py`.
- Required checks:
  - `rank_F2(red)=8`;
  - image is all `I/2I_lat`;
  - kernel in `(Z[φ]^6/2) ≅ F_2^12` has dimension 4;
  - `μ_I ∘ red` has rank 3 using B3’s `μ_I`;
  - chosen eight preimages of the B3 `η` labels map to all eight Fano values.
- Important boundary: this closes O2/B4 only. It does not identify actual `C_O` Pontryagin characters; that remains B5 (`TASK-h-grad-1-b4.md:90-91`).

**SECTION C. Reversals / Corrections To Prior Rounds**

C1. At `papers/cascade-derivation/cascade-fano-grading-lift.md:157` replace:
```
Z[φ]⁶  ↠  L₁₂ / 2L₁₂  =  F_2^{12}  ↠  (Z/2)³
```
with:
```
Z[φ]⁶ ≅ π_int(L₁₂) → π_int(L₁₂)/2π_int(L₁₂) ≅ F_2^{12} ↠ (Z/2)³
```

C2. At `papers/cascade-derivation/cascade-fano-grading-lift.md:175` replace:
```
Let L_O ⊂ L₁₂ be the 8-dim Observer-rung sublattice (Z-rank 8 component corresponding to the octonion ambient space).
```
with:
```
Build B4 must construct L_O ⊂ L₁₂ as the rank-8 Observer-rung sublattice, equivalently the inverse image of the σ(I) summand under the combined-level internal projection π_int(L₁₂)=σ(I)⊕M.
```

C3. At `papers/cascade-derivation/cascade-h-grad-1-b3-report.md:107` replace:
```
READY (all pieces identified; sim construction is mechanical).
```
with:
```
ROUTE IDENTIFIED; B4.1-B4.5 must still construct the Observer summand, basis-change matrix, and rank/kernel verification before B4 can be status-updated.
```

**SECTION D. Route Audit**

Route P, recommended: Phase-M3 projection route.

- Uses `π_int(L_12)=σ(I)⊕M`.
- Gives canonical `L_O` only if that splitting is accepted as fixed cascade data.
- Produces honest rank accounting: domain rank 12, image rank 8 mod 2, kernel rank 4 mod 2.

Route C, coordinate fallback:

- Pick an arbitrary split `Z[φ]^6 = Z[φ]^4 ⊕ Z[φ]^2` and send first four coordinates to `I`.
- Useful for sim scaffolding.
- Not canonical unless tied back to Phase-M3 / `L_12`.

Route Q, QMS/god-prime:

- Relevant to B3/Fano quotient only.
- Does not construct `L_O` or `red`.

Pentagonal holonomy:

- Multiplicative `Z[φ]^×` dynamics on 600-cell edges.
- Not an additive `F_2` quotient and not a B4 source.

**SECTION E. Rank Mismatch / Image**

`Z[φ]^6` has `Z`-rank 12. Mod 2:
```
Z[φ]^6 / 2Z[φ]^6 ≅ (Z[φ]/2)^6 ≅ F_4^6 ≅ F_2^12.
```

If Route P holds, then:
```
Z[φ]^6 = σ(I) ⊕ M
red(u,m) = [σ^{-1}(u)].
```

So:
- `image(red) = I/2I_lat ≅ F_2^8`;
- `red` is surjective, not injective;
- mod-2 kernel is `M/2M ≅ F_4^2 ≅ F_2^4`;
- integral kernel is `2σ(I) ⊕ M` in the split coordinates.

This is the correct answer to the rank mismatch.

**SECTION F. Top 3 Next Builds**

1. Add the B4 construction lemma in `cascade-h-grad-1-closure.md` after the primal/dual gap at `cascade-h-grad-1-closure.md:324-343`: define  
   `L_O := (π_int|_{L_12})^{-1}(σ(I)⊕0)` and prove `L_O ≅ σ(I) ≅ I`.

2. Implement `verify_h_grad_1_b4.py` using `verify_h_grad_1.py:190-202,310-314` and `verify_h_grad_1_b3.py:347-362`: compute the `8×12` `F_2` matrix, verify rank 8/kernel dimension 4, then apply `μ_I`.

3. Patch the upstream framing at `cascade-fano-grading-lift.md:157,175`: make clear that `L_O` is B4’s constructed object, not already canonical from the current text.
