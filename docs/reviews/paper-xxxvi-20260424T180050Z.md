**Claim Audit**

- `F1: Base permeability` at `papers/paper-xxxvi/paper-xxxvi.tex:156`: established. The proof actually shows the unique positive fixed point and global attraction for `r_{n+1}=1+1/r_n`.

- `F2` proposition, “the space ... is three-dimensional and spanned by `R,E,Q`” at `:219-226`: not established as stated. The proof depends on H-MIN absorbing trace, trace-kinetic, boundary, and curved-background terms, but H-MIN is a gauge convention, not a mathematically specified quotient. The “unique” classification in `:274` is therefore stronger than the proof supports.

- `F2` theorem, “any closure functional ... has the canonical form” at `:265-270`: only valid after turning H-MIN into an explicit hypothesis defining the allowed quotient. As written it still reads like a classification theorem.

- `F3` corollary, “exactly one non-crystallographic rung” at `:328-335`: overclaims. H-NC plus Conway-Sloane can identify any non-crystallographic rung as an `H_4` copy up to conjugacy; it does not by itself exclude repeated/conjugate appearances in a chain unless the chain equivalence relation is separately specified.

- `F3` theorem, “the cascade chain ... is exhibited” at `:349-354`: mostly honest now. It no longer claims maximal uniqueness. But “verified step-by-step” at `:354` is still too strong for steps 2, 4, 5, which are programme selections rather than consequences forced by the stated hypotheses.

- `F4`, “`N_total = 583`” at `:430-439`: established as a definition-driven dimension count. Its force is entirely in Definition `\ref{def:V}`, not in an independent theorem.

- `F5` lemma, equality of `R,E,Q` sums under H-SIG at `:516-520`: established only at the finite-sum level, assuming the discrete invariants commute with the Galois/isometry action as asserted.

- `F5` theorem, factor 2 at `:539-545`: finite discrete factor is supported. The continuum sentence at `:544` is not supported and contradicts `:486`, which correctly says functional convergence is a separate hypothesis. The proof repeats the unsupported claim at `:552`.

- `F6`, GH-convergence target at `:591-597`: honest enough as a conditional reduction, not as an independent proof. The script verifies only the `0 -> 1` numerical ratio `0.499`, consistent with `:602-606`; higher levels remain hypotheses.

- `F7`, “unique candidate rank-2 representation ... localises at D4” at `:706-708`: mostly honest after Round 17. Still, the proof establishes uniqueness only relative to the chosen FP1-FP3 selection criteria, not that physics uniquely requires those criteria.

- `F8`, coefficient matching at `:790-798`: establishes matching, not derivation. The wording “cascade-determined” is acceptable only if read with `:797` and `:812-815`; elsewhere “derived” remains too strong.

- `Cascade Lambda Theorem` at `:875-880`: algebraically follows from H-K, H-PROD, H-COPY-ADD, H-RLA, and the `583` count. It is not a derivation from F1-F7 alone. Lines `:102` and `:842` now correctly include H-K/H-PROD/H-COPY-ADD/H-RLA.

- `Cascade Theorem` at `:931-945`: formally summarizes earlier conditional statements. Its proof adds nothing; its correctness inherits the weaknesses above.

**Internal Consistency**

- Round 17 Lambda-hypothesis omission is fixed at `:102` and `:842`.

- The non-ASCII `§` issue is cleared. `rg` found no non-ASCII characters.

- Major contradiction: `:486` says continuum factor-2 needs a separate functional-convergence hypothesis; `:544` and `:552` say it follows under F6/GH convergence. This must be reconciled.

- `:834-835` says H-MX/H-YM do not feed into Lambda, but `:875-876` includes them in the Lambda theorem’s “thirteen-hypothesis stack.” This is overinclusive, not fatal, but it obscures the actual dependency graph.

- Cross-references appear internally resolvable by label scan. I did not compile because the read-only sandbox prevents auxiliary/temp writes.

**External Consistency**

- Paper V claims are verified locally: `13` masses at `0.014%` under structural/model assumptions are stated in `papers/paper-v/paper-v.tex:60,68,424,596`. XXXVI should keep the “structural correspondence” caveat.

- Paper XXII verifies `alpha^{-1}=137+\pi/87` and `sin^2 theta_W=3/8` as structural/numerical correspondences, not first-principles derivations: `papers/paper-xxii/paper-xxii.tex:46,48,299-309,410-414`. XXXVI lines `:66` and `:1005` saying “cascade-derived” / “derives” are too strong.

- Paper XXXII verifies deuteron `0.0006%`, proton `0.04%`, and six radii: `papers/paper-xxxii/paper-xxxii.tex:72,992,998,1226`.

- Paper XXXV verifies the QM/biology/gravity projection picture and Hopf `600=15x40`: `papers/paper-xxxv/paper-xxxv.tex:47-57,315-319,739-745`. But its Lambda statement gives `0.14%--1.67%` and calls the formula pattern-matched, not derived: `:652,668,754,757`. XXXVI line `:922` claiming `0.88%` “for consistency with SmartXXXV” is not locally supported.

- Paper XXXVII verifies H-CP and tree-level strong-CP/no-axion claims: `papers/paper-xxxvii/paper-xxxvii.tex:246-268`. However it uses superseded “F5 sigma-intertwining” language; XXXVI’s current F5 is finite-sum factor-2 under H-SIG, not the old rational-coefficient intertwining theorem.

- Paper XXX verifies the Born-rule attribution as conditional, with explicit equilibrium, sampling, completeness, and `N>=3` restrictions: `papers/paper-xxx/paper-xxx.tex:51-53,656-763,950-954`.

- Papers XL, XLI, XLII are accurately characterized at XXXVI `:981`: local checks confirm XL as continuum target/dependency paper, XLI as heuristic Schwarzschild target, and XLII as conditional partial-removability.

**Tightness**

- `:101`: replace “Every cascade observable ... is a theorem” with “Every downstream observable is either a conditional theorem or a numerical/structural correspondence with inherited hypotheses.”

- `:544` and `:552`: replace continuum carryover with “A continuum analogue requires a separate functional-convergence hypothesis.”

- `:707`: replace “unique candidate ... relevant to Lambda” with “unique candidate under FP1-FP3.”

- `:797` / `:1005`: replace “derived” language for Paper XXII with “provided as structural-correspondence inputs.”

- `:922`: replace “for consistency with SmartXXXV” with “against the cascade-lambda midpoint benchmark”; Paper XXXV does not quote that benchmark.

**Surface Issues**

- Non-ASCII section symbols are gone.

- No obvious undefined macros in XXXVI by inspection.

- The bibliography entry for Paper XXXVII (`:1042`) undersells its actual title/content; it is not just “Neutrino Sector in H4” but also strong CP.

- `:578` uses `\S7.2`, which is fine LaTeX, but the source consistently uses `\S F2` style without braces; acceptable, though typographically brittle.

**Top Three Fixes**

1. Fix F5 continuum overclaim: `:486` vs `:544`/`:552`. This is the cleanest remaining mathematical contradiction.

2. Downgrade or formalize F2: `:219-250`, `:265-274`, `:281-283`. Either define H-MIN as the actual quotient/gauge convention or stop calling the `R,E,Q` basis unique.

3. Correct external-status language for Paper XXII and Paper XXXV: `:66`, `:797`, `:922`, `:960`, `:1005`. The local sources support structural correspondences and pattern matches, not the stronger “derived” or Paper-XXXV-consistent `0.88%` wording.
