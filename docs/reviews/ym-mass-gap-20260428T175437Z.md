**Claim Audit**
All line numbers refer to `papers/millennium-ym/ym-mass-gap.tex`.

- “P8 ... Der(O) ... dim 14 ... stabilizer ... dim 8” (lines 52-65): dimension counts are locally verified by `verify_der_O_su3_stabilizer.py`. The script verifies dimensions, not independently the full Lie-algebra identification with `su(3)`; that part still rests on the cited classical theorem.
- “P9 (derivable from P8)” (lines 66-75): not established. P8 gives a stabilizer algebra; it does not construct cylindrical `SU(3)` bundles, edge holonomies, refinement compatibility, or gauge equivariance.
- `H_{\rm OS-lift}` (lines 101-115): correctly stated as open. But the rest of the paper repeatedly treats its contents as proved.
- Main proposition (lines 215-243): valid only as a restatement of the hypothesis. The later “proof” overclaims.
- Discrete spectral theorem (lines 245-251; 852-873): the numerical script verifies the nine eigenvalues and multiplicities. The written representation-theoretic proof is not theorem-grade: line 956 says `χ=-φ`, which would give `12+6φ`, not `12+6/φ`; line 964 says “Higher mixings” without a defined representation-theoretic calculation.
- F1-F7 imports (lines 357-415): the scoped statuses mostly match Paper XXXVI. The problem is that later sections use stronger, deprecated versions.
- Continuum measure existence (lines 513-555): not proved. The uniqueness step “force χ_n to approach a unique limit” (lines 543-545) is unsupported, and uses F5/F6 beyond their stated scope.
- Uniform tightness (lines 557-604): false as written. It invokes the old F4 spectral claim at lines 566-569, asserts a Gaussian moment formula for non-abelian YM at lines 578-584, gives an impossible Sobolev summability condition at lines 586-587, and uses Rellich compactness on noncompact `R^4` without localization.
- OS0-OS4 (lines 617-842): not established. OS1 and OS3 use the reconstructed Hamiltonian and mass gap before OS reconstruction is available; OS3 defines the OS Hilbert space using the positivity it is supposed to prove; OS4 again uses the deprecated F4 spectral claim.
- `σ = θ` lemma (lines 794-820): not proved. The cited pregeometry source gives `τ(n)=φ^{-n}`, not Galois conjugation as Euclidean time reflection; the logarithm of `(-1)^n τ^{-1}` is hand-waved.
- Conditional gap proposition (lines 994-1008): acceptable as a conditional statement. But Proposition `prop:conf` (lines 1054-1093) and the main proof (lines 1099-1148) upgrade it back into a derived spectral theorem.
- QCD/pion numerical claims (lines 1036-1044, 1145-1146): invalid and internally contradicted by lines 1030-1033 and 1085-1092, which say such claims were removed.
- Attractor spectral count and dynamics (lines 1164-1214): locally verified by the two cited scripts. These are finite-model diagnostics only, and the paper mostly says so.

**Internal Consistency**
- The paper says the continuum limit is open in `H_{\rm OS-lift}` (lines 101-115, 1291-1298), but states “Continuum measure existence” as a theorem (lines 513-517).
- Section 2 says F4 is only a dimension count (lines 372-381), but tightness and clustering use it as a spectral theorem (lines 566-569, 837-840).
- Section 2 says F5 supersedes `F[σΦ]=F[Φ]` (lines 384-393), but the proofs use exactly that old form (lines 540-541, 741-743).
- The gauge-group discussion conflicts with itself: lines 171-178 correctly use stabilizer of an imaginary octonion; lines 429-431 falsely say `G_2` has maximal compact subgroup `SU(3)` and that `SU(3)` stabilizes a quaternion subalgebra.
- Geometry shifts without control: the target is `R^4` (lines 41, 104), the construction uses `S^3 x R` (lines 441-443), F6 gives only `S^3` convergence (lines 396-403), yet OS2 claims full `E(4)` invariance (lines 703-715).
- Cross-reference semantics are sloppy: `thm:main` is a proposition but later called a theorem; `thm:F2`, `thm:F5`, `thm:F6`, `thm:F7` label hypotheses but are cited as theorems; `thm:gap` labels a proposition but is repeatedly called a theorem.
- Coefficients are called rational (lines 306, 318-320, 740), but the displayed values contain `π`.

**External Consistency**
- Paper XXXVI supports the scoped imports: F2 under H-MIN, F4 as dimension count, F5 under H-SIG, F6 under H-DENS/H-BI2/H-HR, F7 under FP1-FP3/H-FP. It does not support the stronger uses later in this paper.
- The cited `cascade-foundations.md` is a poor citation target for current F2-F7: it has an update banner pointing to Paper XXXVI, but still contains older stronger formulations later. Cite Paper XXXVI directly.
- P3/P9 support is not verified. `cascade-refinement-spaces.tex` explicitly does not define an edge cylindrical direct-limit space; `cascade-infrastructure-plan.md` says P9 is planned and must replace/exclude treating `ym-mass-gap.tex` as if it already constructed continuum YM.
- `cascade-correspondence-foundations/foundations.tex` gives a finite-level gauge projection with modeling choices and an H2 continuum hypothesis. It does not establish the P9 claim as stated.
- `cascade-pregeometry.md` verifies `τ=φ^{-n}` only. It does not verify `σ=θ`.
- `cascade-schrodinger.md` contains a unitarity assertion, but it relies on obsolete global F5-style `σ`-invariance and is not adequate support for OS1/OS3.
- `rh-formal.tex` supports only the descriptive Bridge Axiom Pattern, not a formal meta-theorem.
- The local `cascade-h4-oscillator-law.md` does not contain the exact Lyapunov/PC/MI numbers quoted in the abstract; those are external `aria-chess` artifacts and are not locally verifiable.

**Tightness**
- Lines 123-126: replace “it IS the Clay ... problem itself” with “it is a cascade-specific formulation of the remaining continuum reconstruction and gap-inheritance problem.”
- Lines 148-150: replace “Step (ii) follows” with “Step (ii) would require uniform compactness estimates not proved here.”
- Lines 271-272: replace “Section 4 proves the OS axioms” with “Section 4 records the OS obligations included in `H_{\rm OS-lift}`.”
- Lines 513-517: demote theorem to hypothesis/conditional target.
- Lines 1251-1256: replace “σ-invariance provides reflection positivity” with “σ-invariance motivates a candidate reflection; positivity remains open.”
- Lines 179-181: delete the claim about what mainstream theory “cannot articulate”; it is rhetoric, not mathematics.
- Lines 1036-1044 and 1145-1146: delete outright.

**Surface Issues**
- Missing bibliography entries: `billingsley` and `conwayQ`.
- Undefined or unsafe LaTeX: `\textbeta` needs a package or replacement; `\bm` needs `bm`; `\T_n` should be `\mathcal{T}_n` or a defined macro.
- Unicode symbols such as `✓`, `σ`, `θ`, `γ`, em dashes, and nonbreaking punctuation may break under standard pdfLaTeX.
- Lines 325-329 say the `E_8` roots are “unit norm” but then impose `||v||^2=2`.
- Lines 441-443 say “Lorentzian; Wick-rotated to `S^3 x R`,” which is not a Wick rotation as written.

**Top Three Fixes**
1. Remove the fake proof of the open bridge: demote Theorem `thm:contlim`, Lemma `lem:uniformtight`, OS0-OS4, `lem:sigmatheta`, `prop:conf`, and the main proof to conditional obligations under `H_{\rm OS-lift}` unless real estimates are supplied. Core lines: 513-604, 617-842, 1054-1148.
2. Repair the P8/P9/gauge bridge. State only the verified stabilizer fact, then cite finite-level gauge correspondence with its modeling choices and H2 hypothesis. Do not say P9 is “derivable from P8.” Core lines: 66-75, 427-437, 1107-1113.
3. Delete all QCD/pion/140 MeV remnants and coefficient-rationality language. They contradict the paper’s own scope and reintroduce the Round 3 error. Core lines: 306, 318-320, 740, 1036-1044, 1145-1146.
