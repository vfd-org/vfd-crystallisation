1. **Claim Audit**

- `papers/tau-sigma-construction/tau-sigma-construction.tex:112`: imported setup from Paper 1. Established locally: `V_600`, `G=2I`, `T_tau`, K-multiset, `H=Dic_5`, and `V_24` are present in `papers/bekenstein-incidence/bekenstein-incidence.tex:119`, `:158`, `:162`, `:182`, `:193`, `:278`.

- `tau-sigma-construction.tex:133`: “every non-bulk right coset `Hg` is ... one whole `K=52` cycle and one whole `K=20` cycle.” Established by cited Paper 1 lemma at `bekenstein-incidence.tex:218-255`, and independently checked by `verify.py`.

- `tau-sigma-construction.tex:150`: trace inner product definition. Correct. `verify.py:57-67` implements `Tr_{K/Q}(Re(v conj(w)))` as `2*a`.

- `tau-sigma-construction.tex:173`: reflection obstruction remark. Partially supported, but wording is still inaccurate. Block 3c supports the single-reflection obstruction under its embedding assumptions (`block3c_e8_reflection_obstruction.py:139-151`). Block 3b does **not** show parity-flip candidates “act as identity”; when run, it reports zero closed-and-involution candidates. Rewrite this attribution.

- `tau-sigma-construction.tex:209`: phase-`j` swap. Established directly: the formula is a bijective transposition between two length-10 cycles, hence an involution and `T_tau`-equivariant.

- `tau-sigma-construction.tex:226`: symmetric maximin score. Correctly defined and exactly implemented in `verify.py:149-176`.

- `tau-sigma-construction.tex:249`: Theorem 4.1, phase selection `{0,5}` with table `0,-5/4`. Established computationally. I ran `verify.py`; all five cosets produced exactly the claimed score table.

- `tau-sigma-construction.tex:277`: canonical `tau_sigma` as all-zero phase. Established by definition and verified against `papers/v600-programme/data/tau_sigma_canonical.txt` at `verify.py:181-192`.

- `tau-sigma-construction.tex:291`: worked example cycles 2 and 3. Established: verifier output reports first non-bulk right coset as `K52=2`, `K20=3`, matching the displayed lists.

- `tau-sigma-construction.tex:306`: Theorem 5.1. P1-P4 are established. Antipodal compatibility is true and verified by `verify.py:201-205`, but the prose proof at `tau-sigma-construction.tex:339-344` is sloppy; the clean proof is that every phase-`j` swap commutes with index shift `k -> k+5`.

- `tau-sigma-construction.tex:349`: `Z_2^5` phase ambiguity. Established for P1-P4 by `verify.py:221-234`. The script does not explicitly assert that all 32 maps are distinct or that all 32 are antipodal-compatible; the text mostly only claims P1-P4 for all 32, so this is acceptable but should be tightened if “canonical lifts” is meant to include all properties.

No conjectures are introduced in the derivation.

2. **WO Acceptance Audit**

`paper.tex` criteria:

1. Pure math only: resolved.
2. Paper 1 citations for setup/cosets: resolved.
3. §3 W(H4)/E8 failure: partially resolved. It is now safely non-exhaustive, but the local-script attribution is still not precise.
4. §4 construction, pseudocode, worked example: partially resolved. Construction and worked example exist; explicit pseudocode is absent.
5. §5 theorem P1-P4 plus antipodal: resolved.
6. §6 `Z_2^5`: resolved computationally.

`verify.py` criteria:

1. Builds state from `vfd_v600`: resolved.
2. Rebuilds tau from scratch by Route K: resolved.
3. Compares to canonical data: resolved.
4. Verifies P1-P4, right-coset preservation, antipodal, 32 count: resolved for canonical map; all-32 enumeration is P1-P4 only.
5. Left-coset regression: resolved.

WO open items:

- B1 right-coset carrier: resolved.
- B2 trace metric normalization: resolved.
- B3 literal per-vertex sigma projection theorem: not resolved, but intentionally bypassed by Route K.
- B4 cycle-phase maximin Route K: resolved.
- B5 verification rebuild: resolved.

WO correction items:

- `SCOPE.md:16-19`: mostly fixed.
- `SCOPE.md:44`: still stale: says “left coset” and “unique vertex ... maximising,” contradicting the current right-coset phase-maximin construction.
- `TAU_SIGMA_RESULT.md:12` and `:22`: still stale: uses `c · Dic_5` / left cosets.

3. **Catalogue Audit**

Mostly complete. The catalogue covers D1-D4, L1, R1-R2, T1-T2/T2b, and N1-N3.

Defects:

- `CATALOGUE.md:59-64`: R2 omits `w_0`. The derivation explicitly fixes both `v_0` and `w_0` at `tau-sigma-construction.tex:195-207` and again at `:361-366`.

- `CATALOGUE.md:49-55`: R1 over-specifies “standard E8 generating set” and repeats the “acts as identity” phrasing. That is not what Block 3b establishes.

No derivation theorem/definition is missing from the catalogue. No catalogue theorem/numerical result is wholly unbacked, but R1 needs correction.

4. **Attribution / External Consistency**

- Paper 1 attribution is consistent. The cited local source contains the exact right-coset/left-coset distinction and self-normalizing/non-normal statement at `bekenstein-incidence.tex:218-269`.

- Reflection attribution is not clean. `block3c` supports the single-reflection obstruction, but with embedding caveats. `block3b` reports no closed-and-involution parity-flip candidates, not identity action. Current wording in `tau-sigma-construction.tex:173-181` and `CATALOGUE.md:49-55` should distinguish these.

- The canonical data attribution is consistent: `verify.py` rebuilds the all-zero phase map and matches `papers/v600-programme/data/tau_sigma_canonical.txt`.

5. **Sim Correctness**

`verify.py` ran successfully. The exact score tables, canonical rebuild, P1-P4, antipodal compatibility, right-coset preservation, and 32 phase-combination count all passed.

Caveats:

- `verify_tau_sigma()` P3 only checks K-class swapping, not same-right-coset preservation. `verify.py` separately checks right-coset preservation at `verify.py:207-211`, so the total certificate is fine. The assertion message at `verify.py:198` is stronger than that single helper check.

- The all-32 enumeration checks P1-P4 only. Add distinctness/right-coset/antipodal assertions for all 32 if the paper wants “32 canonical lifts” to mean the full canonical property package.

No chi-square, KL, or statistical null hypothesis is involved; this is exact finite enumeration.

6. **Tightness**

- `tau-sigma-construction.tex:171`: “rules this out” is too strong for a non-exhaustive remark. Use “motivates abandoning this route.”

- `tau-sigma-construction.tex:179`: replace with: “Block 3c supports triviality for the single-reflection route; Block 3b reports zero target hits for the tested parity-flip route.”

- `tau-sigma-construction.tex:342`: replace the antipodal proof sentence with the direct index-shift computation.

- `tau-sigma-construction.tex:371`: “valid canonical” should be “admissible” unless every one is intended to be canonical.

7. **Top Three Fixes**

1. Fix reflection attribution: `tau-sigma-construction.tex:173-181`, `CATALOGUE.md:49-55`.
2. Replace the weak antipodal proof sentence: `tau-sigma-construction.tex:339-344`.
3. Clean stale open-item files: `papers/tau-sigma-construction/SCOPE.md:44`, `papers/cosmological-folding-rate/dynamics/TAU_SIGMA_RESULT.md:12`, `:22`.

8. **Verdict**

Publication ready: no.

The core tau construction is now mathematically verified. The remaining blocker is attribution/tightness, not the phase construction itself: the reflection/parity-flip source claim is still inaccurate, and stale left-coset wording remains in open-item files.
