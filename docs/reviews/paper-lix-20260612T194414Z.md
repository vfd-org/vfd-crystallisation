**Claim Audit**

All line numbers refer to `papers/paper-lix/paper-lix.tex`.

- L30, “every number [the simulator/explorer] displays is derived or recorded here”: not established. `scripts/sm_ledger.py` also prints a cosmology-branch `Lambda` number not recorded in this paper, and the `web/` explorer is not present locally.
- L48, exact spectrum: established. Paper LV supports the closed-form eigenvalues and multiplicities.
- L50, “Mass is closure frequency”: over-scoped. Paper LIII T11-T12 supports the kinematic statement for massive substrate modes and one wavepacket; it does not prove that each SM particle is such a localized closing mode.
- L54-L58, ladder proposition: the round-1 fix mostly holds. It is now explicitly conditional on P1/P2 and does not derive particle integers. Remaining gap: P1’s cited cosmogenesis “Theorem 5.4” is itself only sketched with full recursion deferred; P2 also needs the explicit inverse length-frequency normalization.
- L60-L61, H-shell-96: acceptable as a hypothesis, not as evidence. Paper LII supports prior use of shell 96, not an independent derivation. LVII’s honesty caveats are not imported.
- L68-L92, placements table: numerical values reproduce to rounding. But the muon row mixes conventions: under the anchored `m_P=m_mu phi^96`, the offset is exactly zero; the `-0.0002` offset is from the measured-Planck provenance table. Also “±half shell ≈ ±24%” is technically wrong: half-shell mass error is +27.2% / -21.4%.
- L100-L102, curvature obstruction: valid for additive separable rules `N=a_T+f(g)`. It does not refute all static-quantum-number rules with type-generation coupling.
- L104-L106, budget proposition: valid inside the per-type polynomial class. “Cheapest exact class” is not global, and the 25-bit threshold is a protocol choice, not a theorem.
- L108-L110, sieve scan: script output verifies the eight tested sieves and p-values. “The one surviving non-dynamical class” is too broad; say “the surviving tested class.”
- L112-L114, exclusions/lead: established for the stated one-parameter log dressing and reported regression. It does not exclude general loop dressing.
- L124-L126, “Not finer rungs”: overclaim. The tests reject integer/half/quarter shell grids, not all intermediate geometric refinements.
- L128-L130, W/Z proposition: algebraically correct as on-shell electroweak bookkeeping. Scope it to the on-shell/tree-level relation.
- L132-L134, scheme/binding observation: numbers reproduce. The causal language remains stronger than an `n=13` observation supports.
- L142-L144, atoms: improved but still not publication-clean. The Coulomb calculation is correctly scoped in places, but L143 calls `-13.606 eV` “measured”; the measured hydrogen ionization energy used later is `13.5984 eV`. The “seventy-fold” residual claim is off: with the paper’s numbers it is about 54-fold unless rounded aggressively.

**Internal Consistency**

- L61 and L75-L76 conflict in convention: anchored muon mass gives zero offset, while the table preserves the measured-Planck offset.
- L122 says “recorded causes,” but L124-L126 is only a null against three grids and L132-L134 is explicitly an observation. “Attributions” is safer than “causes.”
- L143 and L144 use two different “measured hydrogen” targets: `-13.606 eV` and `13.5984 eV`.
- Cross-references resolve. No unresolved `\ref`/`\cite` appeared in the compiled aux/log.
- L129 names Papers LVI/LVIII but only cites LVIII; `SmartLVI` is in the bib file but never cited.

**External Consistency**

- Paper LV verifies L48.
- Paper LIII verifies the narrow T11/T12/T13 claims used at L50 and L142, but not the full particle-ontology statement.
- `docs/closure-cosmogenesis.md` contains Theorem 5.4, but its proof is a sketch and says full recursion is deferred. L55 should not imply a completed theorem-grade source.
- Paper LII supports prior shell-96 usage; it does not derive shell 96.
- Paper LVII supports conditional `G` under H-shell-96 and explicitly says non-circularity is only partial/suggestive.
- Papers LVI/LVIII support the “independent hypercharge / Weinberg angle open” statement. Add an actual LVI citation.
- Paper LVIII supports charge magnitude and chirality with caveats; signed dynamical charge and assignments remain open.
- Paper XXII supports `alpha^{-1}=137+pi/87` only as a numerical/structural correspondence; LIX cites no source for the conditional alpha input.

**Tightness**

- L50: replace “A particle is” with “In this framework, a candidate particle mode is modelled as”.
- L55: add “assuming inverse coherence-length frequency normalization.”
- L105: replace “numerology by definition” with “inadmissible under this compression protocol.”
- L109: replace “the one surviving non-dynamical class” with “the surviving tested non-dynamical class.”
- L125: replace “not intermediate rungs” with “not half- or quarter-shell grids.”
- L133: replace “artifacts” with “plausible comparison artifacts.”
- L144: replace “seventy-fold” with the actual computed factor or remove it.

**Surface Issues**

- L48: use `k=0,\ldots,5`, not `0..5`.
- L109: use `$E_8$`, not `E$_8$`.
- L133: use `$\overline{\mathrm{MS}}$`, not “MS-bar.”
- L68: PDG 2024 values need a citation.
- L143: “measured -13.606” is misleading.
- L150/L159: `web/` is not in this repository checkout.
- LaTeX log has overfull boxes at lines 30, 119, 129-130, 143, and 159-160.

**Top Three Fixes**

1. Fix the ladder proof boundary at L50-L58 and L156: P1 is not theorem-grade in the cited source, and P2 verifies mode kinematics, not all particle masses.
2. Fix the hydrogen paragraph at L142-L144: distinguish infinite-proton Coulomb/Rydberg value from measured ionization energy, correct the residual-factor arithmetic, and do not imply QED corrections are already derived.
3. Tighten the rung-selection/deviation claims at L109, L122-L126, and L132-L136: the scripts support specific nulls and observations, not global causal exclusions.
