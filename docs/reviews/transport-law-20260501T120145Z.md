Publication ready: no.

Must-fixes: the two central theorems are mostly intact, but the paper still contains publication-blocking inconsistencies in the front matter and attribution layer.

**1. Claim Audit**

Line refs are to `papers/transport-law/transport-law.tex`.

- `:130-136` Proposition “Two exact symmetries of `\Fcal`”: proof establishes the claim, assuming P2’s shell-adjacency commutant and that the 600-cell edge relation is permuted by `H_4`. No over-claim.

- `:166-172` discrete continuity: correctly labelled as a hypothesis/programme target. Paper XVIII supports only the continuum equation, not this graph version.

- `:177-182` continuum limit: correctly conjectural.

- `:195-196` total-probability conservation: proof establishes the conditional claim exactly.

- `:207-222` Theorem `thm:u1-explicit`: theorem proof is sign-correct and establishes the claim for real-symmetric nearest-neighbour-supported `H`. Diagonal entries are harmless because they contribute zero imaginary part. But the abstract summary at `:59` is false: it gives the opposite current sign and broadens `H` to any shell-adjacency-commutant operator, contradicting `:222`.

- `:263-272` dispersion identification: correctly framed as ansatz/hypothesis, not theorem.

- `:279-289` Theorem `thm:photon-sector`: proof establishes stationarity/frequency claims once the P2 spectrum and connectedness are imported. Add an explicit P2 citation inside the theorem/proof and one sentence that the uniform vector is the trivial `2I` sector. The theorem itself does not prove the dispersion or photon identification, and `:289` correctly says so.

- `:366-369` photon hypothesis: properly conditional; local note really does mark `T_PH_2` both “needs polish” and later “closed”.

- `:382-387` microtubule hypothesis: properly downgraded; local source marks `T_MT_1` as structural conjecture.

- `:422-438`, `:443-458` numerical results: JSON files match the paper’s reported values. The script implementation matches the sign-correct theorem, but `verify_u1_conservation.py` top docstring still has the old sign convention.

**2. Internal Consistency**

- `:59` conflicts with `:208-222`: wrong sign and wrong operator class. This is the main remaining Round 2 defect.

- `:256` says U(1) conservation is “not an unconditional statement” and still part of open item 5. That contradicts `:219`, `:232`, and `:62`, which say the Schrödinger-limit case is closed. Qualify `:256` as “outside the Schrödinger-limit theorem / for the full Langevin process”.

- Cross-references resolve by source scan; no missing `\ref`/`\eqref` labels found.

- Labels are misleading: `thm:continuity-discrete` is a hypothesis, `prop:dispersion` is a hypothesis, `cor:photon` is a hypothesis. Not broken LaTeX, but bad audit hygiene.

**3. External Consistency**

Verified locally:

- P2 supports the 600-cell substrate, shell-adjacency centrality/commutant, and spectrum: `cascade-algebraic-substrate.tex:1084-1094`, `:1116-1161`.

- Paper XVIII supports continuum continuity and current velocity: `paper-xviii.tex:101`, `:115-130`; it supports only equilibrium/leading-order Schrödinger recovery, not global recovery: `:319-328`, `:437-451`, `:530-542`.

- Paper XII supports gradient/projected flow and dissipation: `paper-xii.tex:87-108`.

- Paper XVI supports WKB transport along gradient-flow lines: `paper-xvi.tex:261-289`.

- Paper XIX supports nonlinear residual and exact `L^2` conservation, but not a transport source term: `paper-xix.tex:153-185`.

- Paper XXVII supports the centering identity as compatibility, not dynamical conservation: `paper-xxvii.tex:170-182`.

- Paper XXX supports the Route C/E description and assumptions: `paper-xxx.tex:323-352`, `:656-738`, `:872-880`.

- Paper XXXIV supports `50 = 9+12+14+15` and the convention-status of `87`: `paper-xxxiv.tex:44`, `:151-160`, `:165-189`, `:246-247`.

Not verified / problematic:

- `:310` says ARIA implements an `E_8 \oplus 600`-cell product substrate with learned coupling weights `W`. I did not find that claim in the cited ARIA paper. The ARIA paper repeatedly says no learned weights; the E8 material appears in release-bundle kernel scripts, not as the paper’s active substrate claim. This needs rewriting.

- Bib path `:494` says `papers/aria-chess/`, but local root has `papers/aria-chess-paper/`; `papers/aria-chess/` exists only under `release-bundle/...`.

**4. Tightness**

- `:59` edit: “for `H=L` or any real-symmetric nearest-neighbour-supported operator, `j_{v\to w}:=-2 Im(...)`.”

- `:242` edit: replace “would emerge only” with “would require” for the continuum angular-momentum claim.

- `:463` edit: “the companion ARIA preprint reports an active-regime substrate witness...” not “realises the active regime of Remark...”.

- `:315` edit: remove “at the time of writing” or specify “transport-observable verification is not yet repository-backed.”

**5. Surface Issues**

- `SmartARIAClosureKernel2026` and `SmartXXI` bibliography entries appear unused.

- `:172` says “deferred to the full draft”; this manuscript is already the draft under review. Use “future work”.

- Tectonic produced a PDF but with many overfull boxes, including very large repeated `1204pt` overfull warnings. No visible undefined-reference/citation failure, but typography is not clean.

- `verify_u1_conservation.py:24-34` docstring still states the old sign convention and RK4 residual language, while the actual code is corrected.

**6. Top Three Fixes**

1. Fix the abstract’s U(1) theorem summary at `:59`: sign and operator scope are wrong.

2. Reconcile `:256` with `:219`, `:232`, and `:62`: the Schrödinger-limit theorem is unconditional for `H=L`; only full Langevin continuity remains open.

3. Rewrite the ARIA attribution at `:310` and fix the local-source path at `:494`; the cited ARIA material does not support “learned coupling weights on an `E_8 \oplus 600` product substrate.”
