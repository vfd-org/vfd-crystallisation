Publication ready: no.

**1. Claim Audit**
- Lines 38-47: “if … T2 holds, T3 is made precise and proved, and T4 is realised … then the ansatz should reduce to the Schwarzschild--de Sitter metric at leading weak-field order.” The paper does not establish this. What follows is an inventory of dependencies, not a derivation from the ansatz to Schwarzschild--de Sitter. The framing is mostly honest, but “argues formally” still overstates what is actually done.
- Lines 82-88 and 109-113: the scalar source equation is written as `(\Delta \Phi)(x)=\lambda T(x)` and T1-SS then targets `\phi_N(r)=-GM/r`. These are not internally compatible with the stated hope that `\lambda` be identified with `8\pi G`. In three spatial dimensions, `\nabla^2(-GM/r)=4\pi GM\,\delta^3`, not `8\pi GM\,\delta^3`. This is a substantive normalization error, not just missing rigor.
- Lines 90-94: the radial recurrence is presented as the route to the Newtonian target, but as written it is undefined at the source: the left side contains `2/r` while the right side contains `\delta_{r,0}`. If the recurrence is meant only for `r\ge 1`, the paper needs a separate origin equation or matching condition. As written, there is no well-posed discrete source problem.
- Lines 102-113: T1-SS is correctly labelled heuristic, not proved. Even as a target, it hides two unspoken assumptions beyond the listed bullets: that shell volumes scale as in flat 3-space, and that the graph-distance shell index can be identified with a continuum radial coordinate with uniform spacing. The text partly admits this at lines 94-95, but those assumptions are doing the real work.
- Lines 121-124: T2-SS says, in effect, if the continuum tensorial theory “agrees with Einstein gravity at first post-Newtonian order,” then the metric has the GR first-PN form with `\beta=\gamma=1`. That is logically true but nearly tautological. The paper proves nothing here; it merely restates the desired output as an added premise.
- Lines 132-136: T3-SS is also honestly labelled heuristic. The imported `\Lambda` value is not derived here; that part is fine. What is missing is a regime statement: the error term `O((GM)^2/r^2)` suppresses mixed `\Lambda GM r` and `\Lambda^2 r^4` terms, so the text should say explicitly that both `GM/r<<1` and `\Lambda r^2<<1` are assumed.
- Lines 144-149: the PN observables are correctly framed as textbook consequences, not derived here. No over-claim there.
- Lines 159-165: T4-SS is correctly described as a naming convention for a future construction, not a theorem. This section is appropriately cautious.

**2. Internal Consistency**
- Lines 84, 111: factor-of-two inconsistency. The paper wants the Poisson-type coupling to come from `8\pi G`, but the targeted Newtonian solution requires `4\pi G` in the scalar Poisson equation.
- Line 92: the recurrence is singular at `r=0` while simultaneously carrying the point-source term there. That is a mathematical inconsistency in the displayed target equation.
- Line 127: “Proposition T1-SS” contradicts the paper’s own repeated insistence that T1-SS is “not a proved proposition.”
- Cross-references inside XLI are otherwise fine; I do not see a misleading `\ref`/`\eqref` usage.

**3. External Consistency**
- XL status claims are accurate. XLI lines 54 and 102-108 match XL’s actual status labels: T2 conjecture / conditional corollary, T3 programme target, T4 programme conjecture or integration target.
- XXVII disclaimers are accurately preserved. XLI lines 80-88 match XXVII’s explicit disclaimers: no Einstein equations, no tensorial stress-energy, no continuum limit, no coupling constant.
- XXIX attribution is accurate. XLI lines 161-165 correctly say XXIX gives observer-admissible / closure-basin machinery, not a horizon theorem or “coherence boundary.”
- XXXVI’s Cascade-`\Lambda` dependency load is cited accurately. XLI lines 132-136 preserve the extra assumptions; XXXVI does state the theorem only conditionally on shell amplitude, product closure, and residue-to-`\Lambda` identification.
- XLI line 84 misattributes more to XXXVI F8 than XXXVI actually proves. XXXVI’s F8 fixes action coefficients by matching and explicitly says it does not derive Newton’s constant `G`. It does not justify identifying the scalar Poisson coupling here with `8\pi G`.

**4. Tightness**
- Line 38: replace “argues formally that … should reduce” with “records the conditions under which one would seek a derivation reducing the ansatz to”.
- Line 84: replace “hopes to identify with `8\pi G` under Paper XXXVI’s F8” with “would require an additional normalization argument; XXXVI F8 does not by itself fix the Poisson coupling here”.
- Line 121: replace “then the effective metric functions admit the GR first-PN form” with “then, by assumption, the first-PN metric would coincide with the GR form”.
- Line 132: add “in the joint weak-field regime `GM/r<<1` and `\Lambda r^2<<1`”.

**5. Surface Issues**
- Line 80 uses `\Gcal` without defining it in this file. That is broken LaTeX.
- Line 146 uses `\farcs` without loading a package that defines it. That is broken LaTeX.
- Line 127’s “Proposition T1-SS” is a terminology slip with substantive rhetorical effect.

**6. Top Three Fixes**
1. Lines 84, 111: fix the coupling normalization. The present text conflates the Einstein-field-equation `8\pi G` with the Newtonian Poisson `4\pi G` normalization. Until that is repaired, the central weak-field target is internally inconsistent.
2. Line 92: repair the radial source equation. As written it is undefined at `r=0`. State the recurrence only for `r\ge 1` and give a separate origin/source condition, or rewrite the source-shell formulation entirely.
3. Line 84 and the abstract at lines 38-47: stop attributing the Poisson-coupling identification to XXXVI F8. XXXVI does not supply that result. The paper can remain conditional, but the attribution has to be exact.

The round-1 softening mostly landed. The remaining blockers are not tone; they are the Poisson-coupling mistake, the ill-posed source recurrence, and the over-attribution to XXXVI F8.
