**1. Claim Audit**

B4 Route P is established, not Route C. `cascade-h-grad-1-closure.md:347-364` uses the Phase-M3 split; `cascade-phase-m3-closure.md:49-55` locally states `π_int(L12)=σ(I) ⊕ M ≅ Z[φ]^4 ⊕ Z[φ]^2`. The sim implements that split: first four `Z[φ]` coordinates feed `σ(I)`, last two are `M` and go to zero (`verify_h_grad_1_b4.py:171-214`). Caveat: this assumes Phase-M3’s split; it does not independently reconstruct `π_int` from raw `L12`.

`R` is correct for the stated model. The run reports `rank_F2(red)=8`, image `256`, kernel `16`, and the last four `M` columns are zero. Since kernel dim is 4, the kernel is exactly the `M/2M` summand.

`μ_I ∘ red` is correct: run reports rank `3`, image all eight `(F_2)^3` values. The check enumerates the full `F_2^12` domain (`verify_h_grad_1_b4.py:291-299`), not a sample.

The `{ω_1,ω_2,ω_4,ω_5}` `Z[φ]`-basis check is sound conditional on B1: `verify_h_grad_1_b4.py:111-131` tests that `{g_i, φg_i}` is unimodular in the accepted `B_I` `Z`-basis. Det `±1` is the right criterion.

A4 is correctly **PARTIAL**: primal B4 complete, dual B5 still required (`cascade-h-grad-1-closure.md:384-385`; `WO-H-GRAD-1.md:44`).

**2. WO Acceptance Audit**

A1 resolved. A2 resolved. A3 resolved. A4 partial: B4 only, no `C_O` characters.

O1 unresolved. O2 resolved by B4. O3 unresolved. O4 resolved. O5 is stale: B1 and B4 are now done; remaining V1-V3 blockage is B5, not B4.

**3. Catalogue Audit**

B1-B6 and N1-N5 are present. N5 is backed by the B4 sim (`cascade-h-grad-1-catalogue.md:34`). Defects: `cascade-h-grad-1-catalogue.md:56` still says O5 needs B4; should say B4 complete, B5 required. `cascade-h-grad-1-catalogue.md:97-100` is stale: A3 is not deferred and A4 is not deferred to B4+B5, only B5.

**4. Attribution / External Consistency**

Phase-M3 attribution checks out at `cascade-phase-m3-closure.md:49-69`. The `Q_O ≅ Meas(S^7,σ)` citation supports only algebra-level identification (`cascade-q-o-measurement-bridge.md:112-138`), not Pontryagin characters; the derivation correctly says that at `cascade-h-grad-1-closure.md:268-273`.

**5. Sim Correctness**

`verify_h_grad_1_b4.py` ran cleanly: `B4 OVERALL: PASS`. No statistical tests are involved. One sim-text defect: the header claims the eight `σ(I)` basis elements hit all eight Fano values (`verify_h_grad_1_b4.py:18-19`), but the run shows six distinct generator images; the span is all eight, as the later code correctly says (`verify_h_grad_1_b4.py:351-379`).

**6. Tightness**

Replace stale “future/hypothetical/open” B4 prose at `cascade-h-grad-1-closure.md:286-322`, `397-423`, `490-502`, `566-583`.

**7. Top Three Fixes**

1. Update stale B4/O2 status in `cascade-h-grad-1-closure.md:286-322`.
2. Rewrite B4 build entry `cascade-h-grad-1-closure.md:490-502` as complete via Route P.
3. Fix catalogue stale O5/out-of-scope rows: `cascade-h-grad-1-catalogue.md:56`, `97-100`.

**8. Verdict**

Publication ready: no. B4/O2 passes, but A4 remains blocked on B5.
