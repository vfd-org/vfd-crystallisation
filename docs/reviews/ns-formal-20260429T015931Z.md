Not publication-ready. The new `sec:aria_home` is non-load-bearing in placement, but the paper still has publication-blocking overclaims in the existing load-bearing chain.

**1. Claim Audit**
- “Main, conditional formal consequence” (`ns-formal.tex:142`): follows only if `B_NS` is assumed as a uniform BKM vorticity bound and if the Cascade-NS correspondence is also assumed. The proof does not establish `H_1`, `H_NS`, `H_{P6/P7}`, or `B_NS`; it packages them. Also proof line `595` weakens the data to `s >= 1`, contradicting `s > 5/2` at `149`.
- “Pi_hyd via Whittaker-Shannon... Then vorticity satisfies...” (`162-195`): not proved. The scale sum is over `k in Z` (`179`, `189`) but evaluated as `sum_k phi^{-2k}=1+phi` (`193`), which is false unless restricted to `k >= 0`. Curl is first-order, but the proof invokes “two derivatives” (`203`). Kernel, divergence-free compatibility, and NS evolution compatibility are all deferred.
- “Proposition main is conditional only on ... B_NS_3” (`236-238`): conflicts with the proposition’s four-input hypothesis stack (`143-145`, `154`) and with the abstract’s three-input stack (`36-45`).
- F2 import: “contains no rank-3 or higher ... invariants” (`315-320`) is stronger than Paper XXXVI. Paper XXXVI only gives a rank-2 symmetric, order <= 2, degree <= 2 canonical basis under H-MIN.
- F4 import (`323-334`): correct as a dimension count. The caveat that it is not a spectral theorem is good.
- F5 import (`336-346`): directionally correct but imprecise. Paper XXXVI proves finite-sum equality between the two H4 copies under H-SIG, with the factor 2 appearing only when summing the full `E8` copy pair.
- “Cascade-NS correspondence” theorem (`367-384`): not established by the proof. The proof gives a heuristic term-matching paragraph, not a derivation of incompressible NS from the gradient flow. It also contradicts itself on which term supplies viscosity/convection (`378-380` vs `398-403`).
- Viscosity “on the order of 10^-6 m²/s” (`417-422`): unsupported numerical claim. No formula, parameter values, or calculation are supplied.
- “Rank preservation” lemma (`429-461`): not proved. Linear maps preserving tensor rank does not imply coarse-graining cannot generate higher-order continuum operators. The statement calls vortex stretching rank-3-like (`433-434`), then the proof says it is rank-2 (`458-460`).
- “Bounded macroscopic operators” lemma (`465-473`): only item (1) is standard for sufficiently smooth NS. Items (2) and (3) are circular: they rely on the uniform `L^\infty` vorticity bound later assumed in `B_NS` (`491-499`).
- `B_NS` hypothesis (`506-518`): acceptable as a hypothesis, but it is essentially the needed BKM continuation input. “smooth Leray-Hopf solution” (`507`) is sloppy terminology.
- BKM theorem and global extension (`561-590`): the conditional inference is valid once `B_NS` is assumed. The citation is weak: the listed BKM paper is the Euler breakdown paper, not a precise NS continuation-reference citation.
- Main proof (`594-618`): formally conditional, but Step 2 uses the unsupported Cascade-NS theorem (`601-603`) and Step 4 depends on a corollary whose hypotheses are the proposition’s hypotheses. This is a conditional reformulation, not a proof.
- Spectral count (`636-651`): locally consistent with the scripts and Paper XXXVI caveats. This is evidence only, not a bridge to vorticity.
- Finite-noise diagnostic (`664-685`): matches the local script, but R2/R3 are forced by explicitly projecting onto `P_fix`; they are not independent dynamical evidence.
- aria H4O witness (`687-695`): correctly marked external and not locally reverified.
- Discussion “bounded discrete spectrum (F4)” (`732-734`): wrong attribution. The spectral norm is a separate 600-cell Laplacian fact, not F4.
- “sigma-invariance of F” (`735-738`): reverts to superseded F5 language. Current F5 is finite-sum factor-2 under H-SIG, not continuum sigma-invariance.
- “Our uniform vorticity bound” (`768`): overstates; the bound is hypothesized.
- “cascade provides an alternative proof” for 2D NS (`777-778`): unsupported.
- New ARIA subsection (`785-814`): non-load-bearing claim is explicit and believable (`787-789`). However “sits in the same ... family” and “is the rank-projection” (`791-805`) are too definite. The local ARIA note supports this only as programme-level vocabulary, not as a proved identification. The `aria/vfd-b-anomaly` evidence (`806-809`) is not locally present.

**2. Internal Consistency**
- Hypothesis stack is inconsistent: abstract has `H_1`, `H_NS`, `H_P6/P7` (`36-45`); proposition adds `B_NS` (`143-145`); scope says only `B_NS_3` (`236-238`); later evidence discusses only `H_1` and `H_P6/P7` (`620-700`).
- `H_NS`, `H_1`, `H_P6/P7`, and `B_NS_3` are not formalized as definitions/hypotheses. `B_NS` is formalized.
- F4 is carefully demoted at `323-333`, but later reused as spectral support at `732-734` and in the script language.
- The `Pi_hyd` scale convention conflicts: `k in Z` (`163`, `179`) versus `k >= 0` (`250`).
- Domain mismatch persists: the paper proves/claims NS on `R^3`, while P6/P7 infrastructure is on `S^3`.
- Cross-references and bibliography keys in this file appear to resolve. The RH external label `rmk:strengthened` exists locally. Static check only; I did not compile.

**3. External Consistency**
- Paper XXXVI F2: not faithful as used. Source supports only a restricted H-MIN canonical basis, not the broad “no rank-3” claim.
- Paper XXXVI F4: verified. It is `dim V = 583`, not spectral.
- Paper XXXVI F5: verified only as finite discrete factor-2/equality under H-SIG. Current NS discussion still drifts into obsolete sigma-invariance language.
- P4: local source supports finite-level energy dissipation for `F_n`; it does not support continuum conservation or NS-level regularity.
- P6: local source explicitly says spectral convergence is a conditional program and “not yet Clay-grade theorem-level output.” The abstract’s “theorem-grade spectral stability and convergence” (`42-45`) is too strong.
- P7: local source explicitly makes no unconditional NS regularity, uniqueness, smoothness, or blow-up claim. It gives a conditional weak/distributional NS limit on `S^3` under `H_hyd` plus P6 Hypothesis 6.4.
- `docs/aria-closure-kernel.md`: supports `C_phi = L_M + phi^{-2}I` and the Green kernel, and marks the note non-load-bearing. It does not prove the NS family identification.
- `aria/vfd-b-anomaly`: no local `aria/` directory exists in this checkout. The `r=0.983` claim is external and unverifiable locally.
- Adaptive Closure Transport: verified. It explicitly leaves the edge-space lift and Lyapunov mechanism open.

**4. Tightness**
- `42-45`: replace “the theorem-grade spectral stability and convergence results” with “the conditional P6/P7 spectral and hydrodynamic infrastructure, at the status stated in those papers.”
- `367`: replace theorem environment with hypothesis/proposition: “Assume the Cascade-NS correspondence...”
- `414`: replace “IS Navier-Stokes” with “would reduce to Navier--Stokes under the stated correspondence assumptions.”
- `719`: replace “Why cascade closes the question” with “What the cascade bridge would have to close.”
- `732`: replace “Bounded discrete spectrum (F4)” with “The separate 600-cell Laplacian spectral norm bound.”
- `735`: replace “sigma-invariance of F” with “finite-sum factor-2 equality under H-SIG.”
- `768`: replace “Our uniform vorticity bound” with “The hypothesized uniform vorticity bound.”
- `791-805`: replace “sits in” / “is the rank-projection” with “is proposed to sit in” / “would be modeled as.”

**5. Surface Issues**
- Undefined macros/packages: `\textbeta` (`59`), `\Vcal` (`534`), `\bm` (`648`, `650`).
- Bad TeX notation: `B$_{\text{NS}}$_3` (`237`, `263`) should be a single math expression.
- Typographic/capitalization tone: “IS” (`414`), “BOUNDED” (`459`), “INFINITELY” (`770`) should be normalized.
- Unicode in TeX: `m²/s` (`421`) and `H₄` (`783`) should be TeX-safe unless the build is explicitly Unicode-safe.
- Bibliography missing current Paper XXXVI, P4, P6, P7, and `docs/aria-closure-kernel.md`; `CascadeFoundations` is explicitly superseded for F4/F5/F7.
- `KatoUniqueness` bibliography entry (`844-845`) is incomplete.
- “compactly-supported sinc” (`182-183`) is mathematically confused; sinc is not compactly supported.

**6. Top Three Fixes**
1. Rewrite the main theorem and dependency stack (`36-60`, `142-160`, `236-274`, `594-618`). Define `H_1`, `H_NS`, `B_NS`, `B_NS_3`, and `H_P6/P7` exactly once, and make clear that global regularity follows only after assuming the BKM vorticity bound bridge.
2. Remove or demote the unsupported load-bearing theorem/lemmas: `Pi_hyd` (`162-195`), Cascade-NS correspondence (`367-415`), rank preservation (`429-461`), bounded operators (`465-500`). These are not proved at theorem grade.
3. Repair citation/status drift: P6/P7 are conditional and not Clay-grade (`42-45`), F4 is not spectral (`732-734`), F5 is not continuum sigma-invariance (`735-738`), and the ARIA/LHCb evidence is external programme support (`806-814`), not local proof.
