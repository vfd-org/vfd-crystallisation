Publication-ready: **No**.

Round-2 fixes claimed by the author are mostly present in the text. I verify:
- `(a)` `\lambda` is now discussed as the hoped-for Poisson normalisation `4\pi G`, not `8\pi G`, and F8 is explicitly said not to determine it: lines 87, 91, 122-123.
- `(b)` the radial equation is now split into a bulk relation for `r\ge 1` plus a separate origin/source condition at `r=0`: lines 95-100.
- `(c)` the T1-SS remark now names the hidden flat-shell-volume / uniform-shell-spacing assumptions: line 123.
- `(d)` T3-SS now states the joint regime `GM/r << 1` and `\Lambda r^2 << 1` and includes mixed-error terms: lines 139-143.
- `(e)` `\Gcal` and `\farcs` are in the preamble: lines 21-22.

Those fixes do not get the paper over the line, because two substantive mathematical defects remain and one attribution is still loose.

**1. Claim audit**
- **Lines 93-101, especially 95.** The paper’s bulk radial recurrence does **not** support the advertised Newtonian profile. If the target continuum equation is `\phi'' + (2/r)\phi' = 0`, the centred finite-difference discretisation should contribute `(1/r)(\phi(r+1)-\phi(r-1))`, not `(2/r)(\phi(r+1)-\phi(r-1))`. With the present coefficient, substituting `\phi(r)=C/r` gives a nonzero residual `-2C/(r^3-r)`. So the local ansatz undercuts the very profile the paper says it is targeting.
- **Lines 109-120, with the paper’s own admission at 122-123.** T1-SS is still over-claimed relative to its hypothesis list. The conclusion `\phi_{\mathrm{Newton}}(r)=-GM/r` requires a separate normalisation fixing `\lambda=4\pi G` and an identification of the source parameter `M` with physical mass. The bullets do not include that assumption. The remark immediately below admits that this normalisation argument is missing. As written, the conditional “If … then … `-GM/r`” is not established even at the level of a coherent heuristic implication.
- **Lines 128-135.** T2-SS is correctly labelled as a heuristic target, and the remark correctly says `\beta_{\mathrm{PPN}}=\gamma_{\mathrm{PPN}}=1` is not derived. No over-claim here beyond the dependence on the broken T1-SS antecedent.
- **Lines 139-143.** T3-SS is also correctly caveated as heuristic. The joint weak-field regime and mixed error terms are now stated. No new over-claim beyond inheriting the unresolved T1-SS normalisation problem.
- **Lines 166-171.** T4-SS is appropriately presented as a construction target rather than a theorem. The negative claim about XXIX is supportable.

**2. Internal consistency**
- **Line 134** says “`Proposition~T1-SS`”, but T1-SS is explicitly introduced as “heuristic target, not a proved proposition” at line 109 and reaffirmed at lines 122-123. That is a direct internal contradiction.
- The paper now correctly distinguishes scalar Poisson normalisation from Einstein’s `8\pi G`, but T1-SS still behaves as if the missing scalar normalisation had somehow been supplied. The remark and the target statement disagree on what is actually assumed.
- The `\eqref` uses are otherwise fine. I see no other serious ref/label mismatch.

**3. External consistency**
- **Lines 57, 111-114. Paper XL.** I checked `papers/paper-xl/paper-xl.tex`. XLI’s summary of XL’s T3/T4 status is broadly accurate: XL really does treat T3 as a programme target and T4 as an integration target with a missing substrate bridge; see XL lines 101-107 and 185-199.
- **Lines 41 and 111. “Paper XL’s conditional target T2 holds.”** This is sloppy to the point of being misleading. In XL, T2 itself is an unconditional conjecture at lines 157-159, while the conditional result is a separate proposition at lines 161-167. I cannot verify the phrase “conditional target T2” because XL does not formulate it that way. XLI needs to cite either XL’s `Conjecture T2` or XL’s `Proposition T2 under BI-sharp and halving`, not blur them.
- **Lines 83-87. Paper XXVII.** Verified. XXVII does define the scalar graph-Poisson model and explicitly disclaims Einstein equations, tensorial stress-energy, and continuum limit; see XXVII lines 40, 44, 68, 353, 393-400.
- **Lines 77 and 112. Paper XXIII.** Verified. XXIII does treat the birth-angle order as basepoint-dependent, not a metric time coordinate; see XXIII lines 43, 174, 353-357.
- **Lines 87, 114, 133-135. Paper XXXVI F8.** Verified. XXXVI’s F8 is coefficient matching, not a derivation of the scalar Poisson coupling; see XXXVI lines 588-613. XLI is right on that point.
- **Lines 139-143. Cascade `\Lambda` theorem from XXXVI.** Verified. XXXVI itself demotes the `\Lambda` formula to a conditional statement once the extra shell/product/residue assumptions are spelled out; see XXXVI lines 626-650. XLI now cites that dependency load correctly.
- **Lines 168-171. Paper XXIX.** Verified. XXIX gives observer-admissible configurations and basin-admissibility/LMC machinery, not a horizon analogue; see XXIX lines 164-179 and 251-295.

**4. Tightness**
- **Lines 41-42.** Too strong. Suggested edit: “... then the ansatz would at best furnish a heuristic route toward the Schwarzschild--de~Sitter weak-field form, provided an additional scalar normalisation fixing `\lambda=4\pi G` is supplied.”
- **Lines 109-120.** Too strong. Suggested edit: replace “then ... should ... approach” with “then one may target `\phi_{\mathrm{Newton}}(r)=-GM/r`, but only after adding a separate normalisation assumption fixing `\lambda=4\pi G`.”
- **Lines 93-95.** Too strong mathematically because the displayed recurrence is wrong. Suggested edit after correction: “one is led only heuristically to a bulk recurrence intended to discretise `\phi''+(2/r)\phi'=0`.”

**5. Surface issues**
- **Units are inconsistent.** The paper writes `r_s=2GM` (line 168) and `\omega(r)/\omega_\infty \approx 1-GM/r` (line 152), but keeps explicit `c^2` and `c^3` in the light-deflection, perihelion, and Shapiro formulas (lines 153-155). Either declare `c=1` globally or restore `c` consistently. As written, dimensions are mixed.
- **Line 134.** “`Proposition~T1-SS`” is not just a style glitch; it is a false status label.
- The macro additions are present. I see no must-fix undefined-macro problem.

**6. Top three fixes**
1. **Fix the bulk radial equation at line 95.** The coefficient of the first-difference term is wrong for a discretisation of the 3D radial Laplacian. Until this is corrected, the advertised `1/r` behaviour is not even the right formal target.
2. **Repair T1-SS at lines 109-123 and the abstract at line 41.** Add an explicit assumption fixing the scalar normalisation `\lambda=4\pi G` and the source-to-mass identification, or weaken the conclusion so it no longer claims `-GM/r` follows from the listed bullets.
3. **Fix the XL attribution at lines 41 and 111.** Stop calling T2 a “conditional target.” Cite XL’s conditional proposition or XL’s unconditional conjecture accurately. While doing that, fix the internal status error at line 134 (`T1-SS` is not a proposition).
