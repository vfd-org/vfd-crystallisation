Publication ready: no.

**1. Claim Audit**
- Lines 79-91, “The following properties hold” for `\Fcal`. Items (i)-(iii) are immediate from the definition. Item (iv), “the Hessian … is `H_4`-orbit-independent; its explicit form is computed by the accompanying script,” is not proved in the paper but is correctly framed as script-level computation, not theorem.
- Lines 136-145, “The squared-distance difference between the first two shells is … `1/\varphi`.” The proof is fine.
- Lines 163-163 plus Table at 165-183, “The nearest-neighbour adjacency matrix … is therefore isotypically block-diagonal,” and caption “Complete spectral decomposition (verified computationally).” The block-diagonal statement is established at the representation-theory level. The full caption over-claims: the numerical spectrum is verified computationally, but the irrep labels are, by the paper’s own admission, “imported from the standard `2I` character table” and only matched empirically. The caption should not say the whole spectral decomposition is computationally verified.
- Lines 192-206, Proposition “Isotypic decomposition of the shell-adjacency algebra.” The core invariance claim is established, assuming the standard identification of the vertex set with `2I`. The proof justifies preservation of isotypic summands by each shell matrix, hence by the generated algebra. Two defects remain:
  - The displayed equation at lines 198-200 only treats univariate `f(A_d)`, not general polynomials in the whole family `{A_d}`.
  - The proposition relies on the imported `2I` identification from line 163; it is not self-contained.
- Lines 254-263, “Arithmetic identity … = 137.” Proved.
- Lines 279-283, “`\alpha^{-1} \approx 87 + 50 + \pi/87` … at `0.81` ppm.” As arithmetic plus numerical comparison, yes. As derivation of physical `\alpha`, no; the manuscript now correctly disclaims that.
- Line 291, “The ratio `9/15 = 3/5` matches … `SU(5)` hypercharge normalisation.” As a numerical match, yes. The paper correctly says it does not derive the normalisation.
- Line 295, script-reported coupling ratio `9:0:4:9` and vanishing `g_3`. Properly attributed to `run_e8_gauge_bridge.py`; no theorem-level over-claim remains in the manuscript.
- Lines 301-307, `\mathbb{Z}_3` decomposition and the restricted-Hamiltonian spectrum. The manuscript correctly treats these as script-reported/exploratory outputs. No theorem is falsely claimed here.
- Lines 329-331, “restriction … to `W_int` is an invariant reduction, not a truncation approximation.” Within the shell-adjacency algebra, yes; that follows from Proposition 192.

**2. Internal Consistency**
- Line 48, “Seventeen scripts verify the computational claims,” conflicts with lines 378-378 and 426-426, which state that only most scripts reproduce computations and some are exploratory/incomplete. The abstract is stronger than the body.
- Line 112, “Every angular separation is a multiple of `\pi/5`,” is simply false against Table 1 itself: `\pi/3`, `\pi/2`, `2\pi/3`, and `\pi` are listed at lines 123, 125, 127, 129.
- Lines 163 and 167 are not aligned: line 163 says the irrep labels are empirical/imported, while the table caption at line 167 says the complete spectral decomposition is computationally verified.
- Lines 193-201 overstate their displayed formula: the prose claims preservation by the algebra generated by all shell matrices, but the display only states `P_int f(A_d) P_irr = 0` for one shell at a time.
- By inspection, the manuscript’s `\ref`/`\eqref` usage is otherwise coherent. I cannot certify LaTeX resolution without compiling.

**3. External Consistency**
- Line 57, “Paper V reports 13 numerical mass correspondences … at `0.014%` … under `R_D(4)=15`, a non-unique `(S,w)` scheme, and ten reverse-identified chain ratios.” Verified locally in `papers/paper-v/paper-v.tex` lines 59-68, 315-321, 377-380. The Round 7 caveats are now inherited correctly.
- Line 187, “Paper V … uses the four nontrivial integer eigenvalues `{9,12,14,15}`.” Verified locally in Paper V’s spectrum/material around lines 99-124 and 183-193.
- Line 283, “Paper V’s identical numerical identification … as ‘motivated numerical identification’ rather than derivation.” Verified locally in Paper V lines 235-257.
- Line 216 and line 220, local attribution to `CascadeAlgSubstrate` for the `E_8 -> H_4` projection and the `240 -> 120` pairing. Verified locally in `papers/cascade-algebraic-substrate/cascade-algebraic-substrate.tex` lines 556-625.
- Line 102, “the source papers XVIII, XXI do not derive the physical units `\hbar, m`.” Consistent with local XVIII/XXI: XVIII gives effective/natural-unit parameters (lines 366-374) and XXI explicitly treats physical identification as bookkeeping/conditional (lines 247-258).
- Line 331, “Papers IX-X explore … a gravity-facing structural analogy.” Verified locally in `paper-ix.tex` lines 121-125 and `paper-x.tex` lines 38-44, 61-63.
- Line 309, “Paper II introduces … `f(w)=\varphi^5(w-1)^{1/\varphi}` … yielding `m_\mu/m_e` and `m_\tau/m_e` matches.” I cannot verify this locally: no local Paper II source was found.
- Line 187 and line 321, claims tied to “Paper III” (`\Delta C=15`, closure-invariant correspondence). I cannot verify this locally: no local Paper III source was found.
- Line 319 and line 321, claims attributed to Papers I/IV. I cannot verify these locally: no local Paper I or IV sources were found.
- Bibliographic audit trail is not clean. Several `references.bib` titles for local papers do not match the local `.tex` titles now present in the repo, e.g. `SmartIX`, `SmartX`, `SmartXIX`. That does not falsify the manuscript, but it weakens traceability.

**4. Tightness**
- Line 48: “Seventeen scripts verify the computational claims.”
  - Edit: “Seventeen scripts accompany the paper; most reproduce the listed computations, while some are exploratory or incomplete.”
- Line 112: “Every angular separation is a multiple of `\pi/5`.”
  - Edit: “The shell angles are the standard 600-cell angles, all determined by the same `H_4/\varphi` geometry.”
- Line 167: “Complete spectral decomposition (verified computationally).”
  - Edit: “Numerical spectrum verified computationally; irrep labels imported from the standard `2I` character table.”
- Lines 198-200:
  - Edit: replace `f(A_d)` with `p(A_1,\dots,A_8)` if the intended statement is preservation by the full shell-adjacency algebra.

**5. Surface Issues**
- Line 112 is a factual geometry error, not merely style.
- The reproducibility trail is still dirty at script level:
  - `run_generation_selection.py` lines 15-17 and 245-256 still state that the crystallisation operator “IS a generation selection mechanism.”
  - `run_600cell_spectral_verification.py` lines 11-13 and 196-203 still assert an “integer eigenvalue selection principle.”
  - `run_phi_permeability.py` line 8 is explicitly a placeholder.
- Because line 378 admits that some scripts still overstate, the manuscript is honest about the problem, but the audit trail is not yet clean.
- `references.bib` contains stale/mismatched titles for some local papers.
- I see no obvious undefined macro in `paper-xxii.tex` by inspection.

**6. Top Three Fixes**
1. Lines 48, 378, 426: resolve the script/reproducibility contradiction. The abstract cannot say the scripts “verify the computational claims” while the body admits placeholders, exploratory scripts, and stronger script-internal phrasing. Either weaken line 48 or clean/quarantine the scripts.
2. Lines 309, 321, 187: fix the attribution trail for Papers II/III/I/IV. Those claims are presently not locally verifiable. Either add the local sources, cite exact available sources, or recast these as unsupported programme-level references.
3. Lines 112 and 167: fix the two clean overstatements. Line 112 is false on its face; line 167 overstates what is computationally verified.

Round 7 fixes are in place. The remaining blockers are the abstract-level script overstatement, the still-unclean source trail for nonlocal dependencies, and the new geometric/caption overstatements.
