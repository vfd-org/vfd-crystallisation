**Round-4 status**

`2V_W` vs `2\sigma^2 V_W`: mostly fixed in XXI. The current draft uses `2\VW` in the right places (lines 134, 415).  
`\hbar \in \mathbb{R}` hypothesis: only partially fixed. The self-adjointness theorem now says so (lines 162-167), but the Stone statement still mishandles the `\hbar` scaling.  
Identification vs correspondence: only partially fixed. The prose at line 253 is cautious, but the recovery map still reverts to identification/origin language.  
Recovery-map representation-theory claim: mostly weakened, but one related overclaim remains in the table (`\sigma` as an isometry of the Hilbert structure).

**1. Claim Audit**

- Lines 162-167: “With `\hbar \in \mathbb{R}` ... `\hat H = \hbar \Delta_{H_4}` ... is self-adjoint.”  
Verdict: established. Multiplication of a Hermitian matrix by a real scalar preserves self-adjointness. The proof does what it says.

- Lines 192-200: “Under the Route B generator assumption ... Stone’s theorem yields ... `i\hbar \partial_t \psi = \hat H \psi`.”  
Verdict: conditionally fine, but only after fixing the Stone theorem statement. As written, the theorem at lines 186-188 is mathematically sloppy: if `U(t)=e^{-itA/\hbar}`, then the Stone generator is `A/\hbar`, not `A`. You also need `\hbar \neq 0`, not merely `\hbar \in \mathbb{R}`.

- Lines 238-240: “Papers XVIII-XX establish ... an exact nonlinear paired-process wave equation ... whose leading-order linearisation ... coincides with the Schrödinger equation with Witten Hamiltonian `H_W`.”  
Verdict: fair. This matches Paper XVIII lines 320-353 and 568-574, with XIX/XX handling the residual structure. The draft correctly keeps this formal/perturbative.

- Lines 250-253: “Restoring `\hbar` ... suggests the structural correspondence `\hbar_{\mathrm{Route\,A}} \leftrightarrow \sigma^2` ... bookkeeping correspondence ... not an established physical identification.”  
Verdict: fine. This is the right level of caution.

- Lines 274-278 in Table 1: “Hilbert-space inner product ... isometry group contains `2I` and `\sigma`”; “Planck constant `\hbar` — Route A: noise scale `\sigma^2`.”  
Verdict: not established as written. The `\sigma` action on `\mathcal H` is explicitly only “expected, but not proved” at lines 179-180. And Route A gives, at most, a bookkeeping correspondence for `\hbar`, not a recovery-map identification.

- Line 293: “a proposed structural origin for `\hbar` (Route A as noise scale ...)”  
Verdict: still too strong. Your own disclaimer at lines 253 and 471 says “correspondence,” not “origin.”

- Line 397: “The cascade interpretation (via Route B) further identifies `\Delta\sqrt{\rho}/\sqrt{\rho}` as a projection artefact of the `H_4` graph Laplacian’s action on amplitude modes.”  
Verdict: not established. Route B only posits a generator and gets unitary evolution from self-adjointness. It does not derive any amplitude-mode mechanism, projection formula, or Madelung-term identification.

- Lines 359-365 and 367-373: the `U(N)`/projective Kähler claims.  
Verdict: fine, but purely imported standard geometry under Hypothesis 312-314. The paper says that, which is the correct posture.

**2. Internal Consistency**

- Lines 179-180 vs line 274: direct conflict.  
At 179-180, the `\sigma` action on `\mathcal H` is “expected, but not proved here.” At 274, the table states that the Hilbert-space isometry group “contains `2I` and `\sigma`.” Those cannot both stand.

- Lines 253 and 471 vs lines 278 and 293: direct conflict.  
The body says `\sigma^2 \leftrightarrow \hbar` is only a bookkeeping correspondence, not a physical identification. The recovery table and the “structural origin” bullet revert to stronger language.

- Lines 186-188: Stone theorem is misstated.  
With `U(t)=e^{-itA/\hbar}`, the theorem cannot then say “`A` is its unique self-adjoint generator” without redefining “generator.” This infects the formal presentation of Route B even though the intended physics is obvious.

- Line 163: “With `\hbar \in \mathbb{R}` (in particular `\hbar>0`) ...”  
This is logically malformed. Real does not imply positive. If you want the physical case, say “for the physical case `\hbar>0`; for self-adjointness it suffices that `\hbar\in\mathbb R`.”

- Line 171: “Paper V Theorem 2.1” does not match the local source.  
The spectrum is in Paper V lines 99-124, but I do not see a theorem numbered “2.1” there.

**3. External Consistency**

- Lines 95-99, Paper XVII obstruction: verified.  
Paper XVII lines 141-145 prove the dissipativity statement essentially as quoted.

- Lines 103-115 and 238-240, Paper XVIII/XIX/XX Route A synthesis: verified in substance.  
Paper XVIII lines 320-353 and 568-574 support the equilibrium/leading-order Schrödinger claim. Paper XIX lines 41-43 support the residual-isolation framing. Paper XX lines 49-51, 106-112, 409, 428-430 support the modulus-curvature classification.

- But the cited source base is itself not fully cleaned up on the normalization point.  
Paper XX still contains stale `2\sigma^2 \VW` slips at lines 226 and 275, even though the correct normalized form `2\VW` appears at lines 250, 279, 409, 428. XXI is cleaner than one of the papers it cites.

- Lines 146 and 211-217, Papers XXV and XXXVI: verified, with the same conditionality the draft now acknowledges.  
Paper XXV lines 108-125 give the chain-length separation. Paper XXXVI lines 465-485 give F6 with explicit caveats; lines 610-617 explicitly warn that F8 uses imported couplings.

- Lines 171-175, Paper V spectrum: verified in content, not in citation numbering.  
Paper V lines 99-124 contain the stated eigenvalues and multiplicities.

- Lines 55 and 381-382, Paper XXX use of XXI inputs: verified.  
Paper XXX lines 401-405 and 572-577 explicitly import XXI’s global `U(N)`-invariant Kähler structure and say so.

- Line 397 amplitude-mode / projection-artefact claim: not verified locally.  
I did not find a local source establishing that claim.

**4. Tightness**

- Lines 278 and 293 are stronger than the math supports.  
Edit: “Planck constant `\hbar` — Route A: bookkeeping correspondence with the noise scale `\sigma^2`; Route B: cascade action scale, conditional on F1+F8 normalisations.”

- Line 274 is stronger than the math supports.  
Edit: “Hilbert-space inner product — standard `L^2` form on the 600-cell; `2I` acts by isometries, while a unitary `\sigma` action is conjectural here.”

- Line 397 is much stronger than the paper supports.  
Edit: “The cascade interpretation suggests, but does not derive here, a possible relation between `\Delta\sqrt{\rho}/\sqrt{\rho}` and the `H_4` graph Laplacian in a continuum/amplitude-sector limit.”

- Line 544 is slightly stronger than the draft’s actual state of proof.  
Edit: “... identifying a mathematically coherent and partly conditional route ...”

**5. Surface Issues**

- Line 171: citation-number mismatch to Paper V (“Theorem 2.1” does not match the local source).
- Lines 186-188: Stone theorem statement should be rewritten in mathematically correct form.
- Line 163: parenthetical “in particular `\hbar>0`” is logically wrong.
- Table 1 still contains two high-visibility overclaims (lines 274, 278). In a synthesis paper, the table will be read before the caveats.

**6. Top Three Fixes**

1. Fix the recovery map table and adjacent prose (lines 274, 278, 293).  
This is the main remaining overclaim. It presently asserts a proved `\sigma` isometry and treats `\sigma^2` as `\hbar`/an “origin” of `\hbar`, contradicting the paper’s own caveats.

2. Rewrite the Route B Stone theorem presentation (lines 163, 186-200).  
The self-adjointness theorem is basically fine, but the `\hbar` handling is still mathematically sloppy. State Stone in standard form, then derive the Schrödinger normalisation as a corollary for `\hat H` with fixed `\hbar>0`.

3. Delete or weaken the amplitude-mode “projection artefact” sentence (line 397).  
That claim is not proved in XXI, not delivered by Route B, and I could not verify it in the cited local sources. It is exactly the sort of sentence a referee will circle as bluff.
