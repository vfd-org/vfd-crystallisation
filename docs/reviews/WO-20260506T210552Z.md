**1. Claim Audit**
- `tau-sigma-construction.tex:112-148`: Paper 1 setup and right-coset carrier claims are supported locally by `bekenstein-incidence.tex:119-147`, `:162-187`, `:193-227`, `:257-260`, and `:284-295`. No substantive issue. Minor hidden step: `tau ∈ H` is used at `tau-sigma-construction.tex:137`; it is true from the identity cycle, but should be stated explicitly.
- `tau-sigma-construction.tex:152-164`: Trace form definition is coherent and matches `verify.py:57-67` and `block3a_e8_icosian_lattice.py:51-61`. Established.
- `tau-sigma-construction.tex:46-50` and `:100-104`: Over-claim. Quote: “E8 Weyl reflections and parity flips ... acted as the identity on V600 in every tested case.” `block3c` supports trivial action for the single-reflection route, but `block3b` reports zero target hits / zero closed-and-involution candidates, not “acted as identity.” Remark 3.1 is fixed; abstract and intro are not.
- `tau-sigma-construction.tex:173-191`: Remark 3.1 itself is now appropriately weak. It matches `block3c_e8_reflection_obstruction.py` output and `block3b_parity_flip_tau_sigma.py` output.
- `tau-sigma-construction.tex:217-226`: Phase-`j` swap is well-defined, involutive, and `T_tau`-equivariant by direct index calculation. Established.
- `tau-sigma-construction.tex:234-255`: Symmetric maximin score matches `verify.py:141-179`. Established.
- `tau-sigma-construction.tex:257-277`: Phase-selection theorem is established as an exact computational theorem. `verify.py` asserts the full score table for all five cosets and all ten phases.
- `tau-sigma-construction.tex:285-295`: Canonical phase-zero lift is a definition plus data reference. Established by `verify.py:181-192`.
- `tau-sigma-construction.tex:299-310`: Worked example matches verifier output: coset 1 uses cycles 2 and 3 with the listed vertices.
- `tau-sigma-construction.tex:314-358`: Main theorem proof establishes P1-P4 and antipodal compatibility. The rewritten antipodal proof is sound.
- `tau-sigma-construction.tex:362-386`: `Z_2^5` phase ambiguity is established for phase choices and P1-P4. Avoid reading this as “the 32 maps form a group under composition”; the text mostly avoids that, but “group of phase choices” is the safer phrasing.

**2. WO Acceptance Audit**
- Paper criterion 1, `WO.md:61-62`: Resolved. Current derivation is pure math.
- Criterion 2, `WO.md:63`: Resolved. Paper 1 is cited for setup, K-classes, `Dic_5`, `V_24`, and right/left distinction.
- Criterion 3, `WO.md:64`: Partially resolved. E8/parity route is discussed, but W(H4) failure is absent from the derivation, and the text no longer proves non-isometry as a theorem.
- Criterion 4, `WO.md:65`: Partially resolved. Exact construction and worked example are present; pseudocode is missing.
- Criterion 5, `WO.md:66`: Resolved.
- Criterion 6, `WO.md:67`: Resolved as exact computational proof.
- Verify criterion 1, `WO.md:69-70`: Resolved.
- Verify criterion 2, `WO.md:71`: Resolved.
- Verify criterion 3, `WO.md:72`: Resolved.
- Verify criterion 4, `WO.md:73`: Resolved.
- Verify criterion 5, `WO.md:74`: Resolved.
- Priority gaps B1, B2, B4, B5: Resolved.
- Priority gap B3, `WO.md:30-35`: Not resolved; superseded by Route K. That is acceptable only if the WO explicitly accepts Route K instead of literal per-vertex argmax.

**3. Catalogue Audit**
No catalogue blocker found. D1-D4, L1, R1-R2, T1-T2/T2b, and N1-N3 are all backed by the derivation or verifier. T2b is correctly marked as post-build, not part of Theorem 5.1. No derivation theorem/definition/remark/computational result is missing from the catalogue.

**4. Attribution / External Consistency**
- Paper 1 attribution is consistent: right cosets `Hg` are whole-cycle carriers and left cosets `gH` are not, per `bekenstein-incidence.tex:218-255`.
- Shared code agrees: `right_cosets` uses `Hg` at `group.py:140-167`; `left_cosets` uses `gH` at `group.py:107-137`; non-normality at `group.py:170-172`.
- Reflection attribution is inconsistent only in abstract/intro, not in Remark 3.1. Fix `tau-sigma-construction.tex:46-50` and `:100-104`.
- `SCOPE.md:17` still gives a sloppy reason: “T_tau commutes with right multiplication by H since tau ∈ H.” The correct reason for `Hg` is `T_tau(Hg)=(tau H)g=Hg`.
- `SCOPE.md:19` gives a one-sided maximin formula, not the two-sided symmetric score used in the paper.
- `TAU_SIGMA_RESULT.md:5-14` has a good supersession note, but stale legacy lines `:33-37` still say “each non-bulk coset, in either formulation” contains one K=52 and one K=20 cycle. That is false for left cosets.

**5. Sim Correctness**
`verify.py` implements the derivation’s computable claim correctly: exact `Fraction` arithmetic, right-coset carrier check, full phase score table, phase-zero rebuild, static-data comparison, P1-P4, antipodal compatibility, right-coset preservation, and 32-lift count. No χ²/KL/statistical-null issue applies.

Ran:
- `PYTHONPATH=papers/v600-programme/lib python3 papers/tau-sigma-construction/verify.py` — passed.
- `pytest` for `test_dic5_subgroup.py` and `test_tau_sigma.py` with plugin autoload disabled and no capture — 14 passed.

**6. Tightness**
- Replace `tau-sigma-construction.tex:46-50` and `:100-104` with the exact Remark 3.1 split: block3c single-reflection route acts trivially; block3b parity-flip search has zero target hits.
- `SCOPE.md:18` says “Forces τ_sigma to be a non-isometric vertex permutation.” Current paper only motivates abandoning the reflection route. Use “motivates using” unless an exhaustive W(H4)/E8 proof is added.
- `SCOPE.md:19` should use the two-sided symmetric maximin definition from `tau-sigma-construction.tex:234-247`.

**7. Top Three Fixes**
1. `tau-sigma-construction.tex:46-50`, `:100-104`: fix the false reflection/parity-flip attribution. This is the substantive blocker.
2. `WO.md:64` vs `tau-sigma-construction.tex:167-191`: either add a supported W(H4) failure reference or weaken the WO acceptance criterion. Current derivation does not meet it.
3. `WO.md:65` vs `tau-sigma-construction.tex:217-295`: add a short pseudocode block for the construction/rebuild algorithm.

**8. Verdict**
Publication ready: no.

The core construction and verifier are sound, but the abstract/intro still over-claim what the reflection/parity-flip scripts establish. That is a substantive attribution defect, not a typo.
