Publication ready: **no**.

**1. Claim audit**
File reviewed: [paper-xliv.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/paper-xliv/paper-xliv.tex:1)

- `45-46`: “...motivates a possible CPTP upgrade `\Kcal`, not constructed here.” This is not established as stated, because the paper later defines `\Kcal` to be the **nonlinear partial postselected rule**, not the CPTP upgrade. The CPTP object is `\widetilde{\Kcal}` at `129-130`. This is not a minor notation slip; it changes the mathematical type of the main object.
- `49`: the claim that `\Kcal(\rho)=V\rho V^\dagger/\Tr(V\rho V^\dagger)` is a “candidate normalised state assignment” is established, modulo the domain restriction later imposed in Definition `\ref{def:K}`. But the abstract omits the domain `\Dcal_V`; without that, the formula is false on states with `\Tr(V\rho V^\dagger)=0`.
- `50`: the Lindblad claim is correctly conditional. No over-claim remains.
- `51`: the Born-descent claim is properly hedged and, in the abstract, now states the full Route C + Route E + `N\ge 3` load. That part is materially improved and is supportable by the local Paper XXX text.
- `52`: the Petz statement is only a sketch-level heuristic and is marked as such. No over-claim.
- `90-106`: Definition of `V`. The contraction claim at `105` is fine given the postulated ingredients. The “partial isometry when restricted...” clause is acceptable, but only because the triality decomposition is itself postulated at `96-100`; nothing canonical has been shown.
- `108-109`: the “not by itself a partial trace” claim is correct.
- `112-123`: Definition of `\Kcal` now correctly makes it a **partial** rule on a proper domain. This fixes the main round-10 mathematical issue.
- `129-130`: the upgrade remark correctly distinguishes `\widetilde{\Kcal}` from `\Kcal`. This is the right object-typing.
- `161-168`, `179-195`, `201-215`: the Lindblad / Born / Petz sections are now mostly disciplined. I do not see a fresh over-claim there.
- `225-227`: the toy-model discussion still says “the fate of the cross-block coherence under `\Kcal` depends on the chosen Kraus upgrade” and “some block-diagonal upgrades of `\Kcal`...”. That is type-incorrect. Coherence-preservation/destruction is a property of a linear CPTP map, i.e. `\widetilde{\Kcal}`, not of the nonlinear postselected rule `\Kcal`.

**2. Internal consistency**
- Main failure: the paper still mixes up `\Kcal` and `\widetilde{\Kcal}` outside the cleaned-up middle sections.
  - `45-46`: abstract calls the CPTP upgrade `\Kcal`.
  - `71`, `73`: Scope still says “survive or be lost under `\Kcal`” and Born descent “through `\Kcal`”, while the body correctly uses `\widetilde{\Kcal}`.
  - `134`: section title still says “What Could Survive `\Kcal`”, but the section opening `137` correctly says it is conditional on a CPTP upgrade `\widetilde{\Kcal}`.
  - `225-227`: toy-model discussion again attributes channel behaviour to `\Kcal` rather than `\widetilde{\Kcal}`.
- `180` is weaker than `51` and `264`: it initially summarizes Paper XXX as requiring Route C plus universal completeness and basis-realisability, then only later in `188-189` adds the rest of the Route E uniqueness package. As written, the section opening understates the dependency load.
- Cross-references themselves appear semantically consistent. I did not find a bad `\ref`/`\eqref` usage from the text.
- There is, however, a hard LaTeX consistency failure: `\Dcal_V` is used at `115`, `117`, `119`, `122`, `130`, `249` but never defined.

**3. External consistency**
- `62`, `264` on Paper XXI: verified. Paper XXI does present Schrödinger-type evolution conditionally via two routes, including a posited self-adjoint generator route; see `paper-xxi.tex:48-56`, `147-209`.
- `51`, `264` on Paper XXX: verified. The full dependency load is present locally:
  - Route C five hypotheses at `paper-xxx.tex:323-330`.
  - Route E theorem hypotheses `(U1)-(U4)`, universal completeness, and `N\ge 3` at `paper-xxx.tex:656-670`.
  - Basis-realisability as an additional bridge assumption is repeatedly stated in Route E; e.g. `paper-xxx.tex:668`, `760`, `952`.
- `62`, `264` on Paper XXXI: verified. Paper XXXI explicitly frames itself as a conditional sufficient-condition template under four additional hypotheses and says it does not derive the needed structure from the coupling alone; see `paper-xxxi.tex:73-77` and the four-hypothesis template around `176-196`.
- `86`, `264` on Paper XXXVI: partially verified, and the present paper is careful enough here. Paper XXXVI does give the cascade chain and the Schläfli `600 = \bigsqcup 5\times 24` decomposition; see `paper-xxxvi.tex:308-314`. I do **not** see in Paper XXXVI a vertexwise `5`-to-`1` surjection onto a reference 24-cell or a Hilbert-space `8_v\oplus 8_s\oplus 8_c` decomposition of `\mathbb C^{24}`. XLIV correctly says those are not supplied and are postulated modeling inputs.
- `240`, `264` on Paper XLII: verified. XLII states the `\varphi^{-25}` decoherence-rate consequence only conditionally on coupling to XLIV; see `paper-xlii.tex:176-177`, `192`, `200`.

**4. Tightness**
- `45-46`: replace with “...and motivates a possible CPTP upgrade `\widetilde{\Kcal}`, not constructed here. We do not claim that the partial rule `\Kcal` or any upgrade `\widetilde{\Kcal}` is a literal partial trace...”
- `71`: replace “under `\Kcal`” with “under a CPTP upgrade `\widetilde{\Kcal}`”.
- `73`: replace “descend through `\Kcal`” with “descend through a CPTP upgrade `\widetilde{\Kcal}`”.
- `134`: replace section title with “What Could Survive a CPTP Upgrade `\widetilde{\Kcal}`”.
- `180`: replace first sentence with “Paper XXX’s Born-rule package is conditional on the five Route C hypotheses together with the full Route E uniqueness package...”
- `225-227`: replace each “under `\Kcal`” / “upgrades of `\Kcal`” with “under a chosen CPTP upgrade `\widetilde{\Kcal}`” / “upgrades `\widetilde{\Kcal}`”.

**5. Surface issues**
- `115`, `117`, `119`, `122`, `130`, `249`: `\Dcal_V` is undefined. As written, the file will not compile.
- `45-46`: notation clash between `\Kcal` and `\widetilde{\Kcal}` is not merely stylistic; it creates contradictory object types in the abstract.
- Section-title / scope phrasing still lags behind the corrected body text and should be harmonised.

**6. Top three fixes**
1. Fix the remaining `\Kcal`/`\widetilde{\Kcal}` type errors everywhere they survive, especially `45-46`, `71`, `73`, `134`, `225-227`. This is the main substantive defect still visible after the round-10 cleanup.
2. Define `\Dcal_V` or stop using the macro and write the symbol literally. The current draft has an outright LaTeX failure at `115` and all later references.
3. Make the Paper XXX dependency statement uniform across the paper, especially `180`. The abstract (`51`) and Dependencies paragraph (`264`) are now correct; the Born section opening still understates the Route E load unless the reader keeps reading into `188-189`.

On your four round-10 follow-ups:
- `(1)` yes, the partial-rule/domain fix is present.
- `(2)` yes, `\widetilde{\Kcal}` is introduced as formally distinct at `129-130`.
- `(3)` mostly, but not completely: the cleaned sections are better, yet the abstract/scope/title/toy-model still backslide into `\Kcal`.
- `(4)` yes in the abstract and Dependencies list; not fully uniform in the Born section opening.

So: improved, but still **not** publication-ready.
