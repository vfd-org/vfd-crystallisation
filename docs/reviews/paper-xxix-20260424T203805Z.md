Line references are to `papers/paper-xxix/paper-xxix.tex`.

**1. Claim Audit**

- L203: “Basin locality.” Established only tautologically: the definition of `B_\phi` references `(\Fcal,\phi)`. It does not prove dynamical independence from other minima.
- L211: “basins ... partition a full-measure subset.” Not proved. Needs finite-dimensionality, complete flow, no escape, and Morse-Smale-type hypotheses.
- L257: “Local measurement consistency.” Proof establishes it, but only because Definition 3 hard-wires zero cost on every admitted basin. This is definitional, not a measurement theorem.
- L280: “Basin-admissibility is context-independent.” Follows from LMC, with the same weakness.
- L248/L331: toy model “exhibits a single-probe instance” versus “pointwise at `\Phi_A`.” The latter is honest; the former still overclaims. A point check is not a Definition 3 probe observer.
- L178: “will generically contain fewer than `N`.” Not proved. The caveat helps, but “will” should be “may.”
- L345-L351: “system resolves to that configuration.” Not established. The paper defines an admissibility filter, not selection dynamics.
- L373: “zero-cost locus.” Inconsistent with Definition 2’s `\Ccal_\Ocal \leq \epsilon`; should be sublevel locus unless restricting to the ideal probe case.
- L384: XXIV conditional frame claims are now correctly caveated. This repair works.
- L400: “global frame ... ruled out by construction.” Overclaim. Bounded observer regions do not by themselves rule out a global linear extension or canonical clock on total posets.
- L429: arg min notation and compactness caveat are repaired and consistent with XXVIII.
- L433-L437: “entire field configuration converges” and “biases the resolution.” Not proved; conflicts with L429’s no-global-convergence caveat.
- L442-L455: weak/strong regime is now correctly labelled hypothesis-level. This repair works.
- L503-L519: toy arithmetic is correct: `C(Phi_A)=0`, `C(Phi_B)=4`, exclusion holds for `0<epsilon<4`. It proves only two-point admissibility, not physical measurement.

**2. Internal Consistency**

- L218-L235 conflicts with L129-L133. Probe condition (3) makes `C=0` on whole basins, which are normally open near stable minima. A smooth functional vanishing on an open basin cannot have positive-definite Hessian there. This may make the “probe observer” class empty under the strict stability definition.
- L248 still says local probe functionals exist only if basin membership is locally recoverable. Under the written condition, this is not exactly true unless off-basin positivity or nontrivial readout is imposed.
- L122/L128/L220 mix vertex sets, induced subgraphs, and subgraphs. The clarification helps, but boundedness still says `\Ocal \subsetneq \Gcal` although `O \subseteq \Ecal` is the primitive object.
- L413 says “Linear extension induced by `\Ocal`” without the “intended/conditional” caveat used at L390. The table reintroduces the XXIV bridge overclaim.
- L422 says conscious observers define frames via “induced linear extension”; that construction remains open.
- L433 says crystallisation is global convergence; L429 says global convergence is not established.
- L443 says `\Fcal` “determines which admissible configuration is selected”; L429 says nonunique minimisers require arbitrary selection.

**3. External Consistency**

- L115, XXIV observer definition: verified in `paper-xxiv.tex` L65-L73.
- L384, XXIV simultaneity and endpoint intervals: verified. Simultaneity requires incomparable events; endpoint-interval difference requires unequal weight sums (`paper-xxiv.tex` L116-L124, L143-L165). XXIX now states this correctly.
- L400, XXIII no-global-clock: verified only conditionally on incomparable events (`paper-xxiii.tex` L205-L220). XXIX’s conditionalisation is repaired, but its added bounded-observer “global frame ruled out” gloss is not from XXIII.
- L341/L448, XXVIII weak/strong labels: verified as hypothesis-level only (`paper-xxviii.tex` L229-L254, L285-L292). XXIX now mostly respects this.
- L429, XXVIII crystallisation argmin account: verified (`paper-xxviii.tex` L303-L308).
- L373/L301, XXX Born-rule use of LMC plus completeness: verified (`paper-xxx.tex` L323-L352, L576-L651, L656-L681). XXIX’s summary is acceptable.
- L373, XIV stationary distribution: verified, but uniqueness depends on regularity/irreducibility/confining assumptions (`paper-xiv.tex` L174-L190). XXIX does not currently overstate this.
- Bibliography repair: `SmartXXX` is present; `SmartXXVIII` and `SmartXXII` titles match the local paper titles.

**4. Tightness**

- L351: replace “the system resolves to that configuration” with “only that configuration remains admissible.”
- L395: replace “is the concrete embedding” with “is proposed to be represented by the embedding, once the XXIV bridge is supplied.”
- L413: replace “induced by `\Ocal`” with “intended to be induced by `\Ocal`.”
- L433: replace “the entire field configuration converges” with “the formalism models crystallisation as descent toward constraint satisfaction.”
- L443: replace “determines which admissible configuration is selected” with “constrains the minimiser selection problem.”
- L582: replace “defines a relativistic frame” with “is intended to define a relativistic frame conditional on the XXIV bridge.”

**5. Surface Issues**

- L197-L199 uses `\Phi` both as initial condition and evolving trajectory.
- L211 should say “Morse-Smale” or add the missing hypotheses.
- L240 still says “well-designed measurement apparatus”; better: “idealised detector array under off-basin-silence.”
- L330 uses `\partial_{O_{\phi_j}}\Phi` before formally defining boundary notation for arbitrary vertex subsets.
- L503 uses `\delta^2\Fcal>0` without the coordinate/Hessian clarification now supplied for `\Ccal`.
- `SmartXXVII`, `SmartXVIII`, `SmartXXI`, `SmartXXII` are in the bibliography but not directly cited in this paper.

**6. Top Three Fixes**

1. Fix the probe-observer consistency problem at L218-L235 versus L129-L133. Zero cost on open basins is incompatible with strict positive Hessian unless the stability point is outside the admitted basins or the basin condition is weakened.

2. Remove remaining dynamical-selection language at L345-L351, L433-L443, and L582. This paper defines admissibility constraints; it still does not prove measurement dynamics, convergence, or frame construction.

3. Repair the residual XXIV bridge leakage in tables/conclusion: L413 and L582 still state “induced/defines” as if the structural observer produces `(\leq_O,\mu_O)`. The prose caveat at L144 is good, but not consistently propagated.
