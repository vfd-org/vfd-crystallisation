**1. Claim Audit**

- “Geometric-mean self-similarity selects `\phig`” (line 137): established, but only because the geometric-mean axiom is assumed. No physical derivation of that axiom.
- “Maximal non-resonance” (lines 179-187): still overclaims. The universal inequality stated for `\phig` is not correct as written, and the transfer-operator statement remains motivational, not proved.
- “The `\pi/5` angular constraint selects `2I`” (lines 233-243): proved only under H1-H4. The proof supports the conditional classification claim.
- Corollary “unique maximal finite subset of `S^3` whose pairwise inner products all lie in `1/2 Z[\phig]`” (lines 278-282): not proved. This is substantially stronger than the ADE theorem and needs extra hypotheses, likely `H_4`-invariance/vertex-transitivity.
- “Uniqueness of the closure functional under A1-A4” (lines 365-390): the A4 retreat is present and necessary. But the proof still handwaves the passage from A3 for functionals to product additivity of `R`; state this as an assumption of class `C`, not as derived.
- “Properties of `\Fcal`” (lines 416-423): mostly plausible, but no proof is supplied. “Vertex minimality,” “monotonicity,” and “barrier height” need either a proof or demotion to remarks.
- “Interaction from exclusion” (lines 467-476): algebraically established from `Z_2`. Fine.
- “Properties of `\Fint`” (lines 479-491): repulsion and sharp-limit divergence are established. “Attains its maximum at `\psi_1=\psi_2`” is not proved globally.
- “Pion-exchange channel count” (lines 693-707): the label `\triangleright` is appropriate. The count depends on importing the `P_3` support graph and on treating vertex exclusion as disjoint support, not merely `i \ne j`.
- “Photon exchange” (lines 756-768): zero mode is derived; identifying it with photon exchange and assigning coupling `\alpha` is imported/identified. Current label is appropriate.
- “`(6+\alpha)` separation factor, conditional identification” (lines 831-843): the demotion is present and correct. The proof still assumes a binding pion-sector minimum at `6\pi\lbar_p`, although earlier `\Fint` is repulsive. That is a major unresolved bridge.
- “Deuteron charge radius” (lines 961-966): arithmetic follows from the inputs. The result is conditional on identified `r_{\mathrm{str}}`, not derived from `\Fint`.
- “`Z\alpha` scaling” (lines 1004-1012): pure ansatz/identified extrapolation. No nuclear-structure derivation is given.

**2. Internal Consistency**

- Lines 331-332 still say “three axioms,” but Section F now has A1-A4.
- Line 409 says “all three axioms”; should be “A1-A4” or “the four axioms.”
- Line 1067 says uniqueness satisfies “A1-A3,” contradicting Theorem `thm:F` under A1-A4.
- Line 1071 still calls `thm:factor` a “Theorem”; it is now a proposition.
- Line 1051 still marks `(6+\alpha)` as `\blacktriangleright^\ast + \triangleright`; the requested pure `\triangleright` retreat is not fully propagated.
- Lines 192 and 372 conflict: chordal/geodesic squared distances agree at `O(\theta^4)`, not `O(\theta^3)`.
- Line 372 has a factor-of-two ambiguity: `c_i=|x-v_i|^2/2`, but the parenthetical calls it the squared chordal distance `2(1-\cos\theta)`. The cost is half the squared chordal distance.
- Line 819 still says “triple overdetermination,” contradicting line 785’s “not algebraically independent.”
- Lines 933 and 976 call the separation factor/result “derived correction”; it is conditional/identified.
- All checked `\ref`/`\eqref` labels resolve. All citation keys used in this paper exist in `references.bib`.

**3. External Consistency**

- Paper V / `SmartV`: line 84’s “particle masses at `0.014%` average error” is locally verified in `papers/paper-v/paper-v.tex`, but Paper V repeatedly qualifies it as conditional on postulates, assignments, and correspondences. Current text omits that qualification.
- Paper XXII / `SmartXXII`: `\alpha^{-1}=137+\pi/87` is verified locally, but Paper XXII explicitly calls it a numerical/structural correspondence, not a first-principles derivation. Current Section 5 mostly respects this, except line 819.
- Paper XXII decoupling: line 616 is verified only for shell-adjacency polynomial operators. Applying it to all modes in the present `\Fint` expansion requires an extra assumption.
- Paper XIII / `SmartXIII`: lines 531-537 are verified. Paper XIII indeed used a four-component interaction ansatz with free weights.
- Charge-radius paper / `SmartRadius`: `r_\pi=\pi\lbar_p`, proton `r_p=4\lbar_p`, neutron `-(8/3)\lbar_n^2`, and old LO deuteron are locally verified. They are not derivations from the present `\Fint`.
- Paper VI / `SmartVI`: “gauge structure” is only a structural reinterpretation in the source, not a derivation.
- CODATA/Pohl entries are present in `references.bib`; the table now reports both 2018/Pohl and CODATA 2022 anchors as requested.

**4. Tightness**

- Line 48 “This paper closes that gap.” Edit: “This paper narrows that gap within stated structural assumptions.”
- Line 200 “forced by the permeability structure.” Edit: “forced by the geometric-mean permeability axiom plus the chordal `S^3` convention.”
- Line 408 “only functional satisfying all three axioms.” Edit: “only functional in class `C` satisfying A1-A4.”
- Line 611 “All other modes are suppressed.” Edit: “The remaining modes are assumed negligible at nuclear distances, with electroweak suppression estimated separately.”
- Line 819 “triple overdetermination.” Edit: “three related arithmetic appearances.”
- Line 933 “With the derived separation factor.” Edit: “With the conditional separation factor.”
- Line 1128 “at `0.02\sigma`.” Edit: “at `0.02\sigma` relative to the 2018 anchor and `0.83\sigma` relative to CODATA 2022.”

**5. Surface Issues**

- `R_\alpha` line 361 should say `\alpha>0, \alpha\ne1`.
- The theorem/proposition label `thm:factor` is now misleading.
- The phrase “Paper XXII~\citep{SmartVI,SmartXXII}” at line 599 is sloppy; cite Paper VI and Paper XXII separately.
- Line 1198 says “three independent structural characterisations”; this repeats the independence problem.
- The numerical table’s `0.02\sigma` and factor-100 improvement are not reproducible from the rounded displayed values alone. Add unrounded values or say “approximately.”

**6. Top Three Fixes**

1. Fix the status propagation for the `(6+\alpha)` result: line 1051 must be pure `\triangleright`, line 1071 must say Proposition, and lines 933/976 must stop calling the correction derived.

2. Repair Section F consistency: lines 331, 409, and 1067 still say three axioms/A1-A3 after adding A4. This directly undermines the Faddeev-Khinchin retreat.

3. Remove remaining independence/overdetermination language: line 819 and line 1198 still assert more independence than the math supports. Use “three related arithmetic identities/characterisations, not algebraically independent.”
