**Claim Audit**

- `Definition 3.1` lines 237-263: the three Round 5 clauses landed: zero residual, flow invariance, terminal projection-compatibility. This is stipulative, not proved. It still hides analytic structure: arbitrary `\Phi_k` and `R_k:\Phi_k\to[0,\infty)` do not by themselves define `-\nabla R_k` or a flow `\Psi_k^t`. Add Hilbert/smooth finite-dimensional hypotheses or make the semiflows part of the data without saying they are generated by a gradient.

- Source import at lines 201-213 and Hypothesis 3.4 at lines 305-321: over-claim remains. `CascadeClosureDynamics` proves flow existence/dissipation for the full quadratic `F_n=\alpha R_n+\beta E_n-\gamma Q_n`, and flow intertwining only in the mass-only case `L_n=-\gamma C_n`. This paper’s mechanism uses gradient flow of the residual `R_k`. The cited source does not establish flow intertwining for `-\nabla R_k`. Treat line 312 as “analogous to”, not “the flow-level statement of” the cited theorem.

- `Proposition 3.6` lines 329-343: the proof sketch at lines 346-362 establishes exactly residual-zero propagation and fixed-point propagation for the projected top fixed point, under the two hypotheses. It does not prove occurrence of a Definition 3.1 event from an initial tower. The remark at lines 365-384 correctly says this.

- Carrier claims lines 401-432: mostly verified. The `2I`, `E_8\to H_4`, shell, and spectrum claims are in `cascade-algebraic-substrate.tex`. However the shell-class theorem is `thm:shell-class`; the present paper cites `thm:pi-H` and `thm:icosian-closure` but should cite `thm:shell-class` directly at lines 421-432.

- Spectrum claim lines 412-431: acceptable after Round 5. The source itself calls the spectrum an imported character-table computation, and this paper now says that.

- `\|\Cphi^{-1}\|=\varphi^2` lines 436-448: mathematically follows from the shifted Laplacian bottom eigenvalue. The local script claim is supported, but `verify_kernel.py` writes `results.json`; in this read-only environment the listed command failed at that final write.

- ARIA implementation lines 470-526: “implements” is stronger than the evidence. The paper gives an architecture mapping plus empirical witness, not a formal proof that ARIA satisfies Definition 3.1 closure compatibility.

- ARIA empirical numbers lines 510-520: verified in `papers/aria-chess-paper/paper/main.tex` and sections. The single-seed EEG caveat is present in the source and should remain visible.

- `b\to s\mu^+\mu^-` witness lines 546-558: verified in sibling repo `../BANOMALY-001/vfd-b-anomaly`. The AIC weights `FREE_C9=0.652`, `VFD=0.348` are correct. The source explicitly says sign-uniformity is a consistency check, not evidence over a constant `C9` shift; this paper should repeat that sentence more directly.

**Internal Consistency**

- Major notation conflict: target `R_k` is the residual whose gradient defines the cascade flow; source `R_n` is only the curvature/graph-Laplacian component of `F_n`, while the cited flow-intertwining theorem is mass-only for `Q_n/C_n`. This is the main remaining mathematical inconsistency.

- Line 331 says “gradient flow of `R_N` on `G_N`”; the flow is on `\Phi_N`, not on the carrier `G_N`.

- All internal `\ref` labels and citation keys used in the target file are statically present.

- The Appendix A/E/F repoint mostly landed. Remaining blemish: line 598 says `Appendix~\ref{app:fsa-A} and~E`; make `E` a real direct reference to `\ref{app:fsa-E}`.

- The ARIA branch-pin text landed at lines 744-750. It is still not publication-ready by its own standard because the commit hash is explicitly deferred.

**External Consistency**

- `CascadeClosureDynamics`: theorem labels exist, but the imported flow-intertwining theorem applies only to mass-only `L_n=-\gamma C_n`, not to this paper’s residual flow `-\nabla R_k`.

- `CascadeRefinementSpaces`: `thm:bonding` and `thm:commute` verified locally.

- `CascadeAlgSubstrate`: `thm:pi-H`, `thm:icosian-closure`, `thm:shell-class`, spectrum fact, and `94+26` split verified locally.

- `SmartARIAChess`: `18/18`, `6/6` at seed 42, and HCP `-11.58\sigma` verified locally.

- `ariaChessRepo`: local sibling `../aria-chess/v4_locked_2026-04-29` contains the module files listed. It is not a git checkout here, so no branch or commit can be verified locally.

- `SmartBAnomaly`: verified in sibling repo. Current paper’s AIC-inconclusive wording is correct.

- `SmartV`, `SmartVRev`: files exist and contain the mass-spectrum correspondence material. Since this paper marks it out of scope, no load-bearing use remains.

**Tightness**

- Lines 138-144: replace implication-chain language with “is modelled as propagation under the hypotheses below.”

- Lines 312-314: replace “This is the flow-level statement of…” with “This is the analogue of the flow-level statement proved in the mass-only source case…”

- Line 421: replace “theorem-grade” with “proved in the cited source”.

- Lines 472-475: replace “as one such implementation” with “as one architecture-level realisation/mapping”.

- Lines 548-553: add “sign-uniformity is not independent model-selection evidence over `FREE_C9`.”

**Surface Issues**

- Line 331: `on G_N` should be `on \Phi_N`.

- Line 598: plain `E` should be `Appendix~\ref{app:fsa-E}`.

- Lines 721-725: the rerun command should say `verify_kernel.py` writes `results.json`.

- Lines 746-750: commit pin is still missing; the text admits this.

- “eigvecs” at line 462 is informal; use “eigenvectors”.

**Top Three Fixes**

1. Fix the residual-flow import mismatch at lines 201-213, 305-321, 329-343, and 587-589. Either change the mechanism to the source’s `F_n`/mass-only flow or stop citing the source theorem as support for `-\nabla R_k`.

2. Add the missing analytic hypotheses to Definition 3.1 at lines 237-250: smooth/Hilbert state spaces, differentiability of `R_k`, flow existence, and continuity/linearity of projections.

3. Before publication, replace the ARIA branch pin with a commit pin at lines 744-750 and cite `thm:shell-class` directly for the shell claims at lines 421-432.

Publication ready: **No**.
