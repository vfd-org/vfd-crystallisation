All line numbers below refer to [paper-xxx.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxx/paper-xxx.tex:1>).

Publication-ready now that the scope/attribution fixes are in: **no**. The two round-19 target fixes are in place: line 153 no longer calls the partial-observer extension a “genuine generalisation,” and lines 754-755 now correctly say “imported from.” Proposition `prop:born` at lines 323-345 is also now stated at the right level: a conditional sector-mass decomposition plus sampling lemma with overlap error.

**1. Claim Audit**
- `Proposition \ref{prop:born}` (323-345): **supported as stated**. The proof establishes exactly a conditional sector-mass decomposition plus sampling/completeness lemma, with an explicit overlap error. No overclaim remains.
- `Proposition \ref{prop:suff}` (447-463): **supported as stated**. It proves covariance only for the sector-preserving subgroup and says so.
- `Theorem \ref{thm:chentsov}` (475-485): **acceptable only as an imported theorem block**. The text is explicit that it is imported and not proved here.
- `Proposition \ref{prop:hessian_fisher}` (493-526): **supported as stated**. The proof establishes only a local Laplace-regime Fisher/Hessian identification on configuration-space location families, and the statement now respects that scope.
- `Proposition \ref{prop:fs_assembly}` (544-565): **supported as stated**. It proves only an assembly/packaging result plus a conditional scale-candidate statement, not a closure derivation of projective Kähler geometry.
- `Theorem \ref{thm:noncontextuality}` (594-604): **proof is fine, statement bookkeeping is not**. The theorem assumes `\Psi \in \Mcl(\Ocal_\mathcal{B}) \cap \Mcl(\Ocal_{\mathcal{B}'})` and then separately assumes both observers are complete for `\Psi`; under Definition `\ref{def:closure_manifold}` (416-421), the first already encodes the second. This is a real statement-level inconsistency, not a proof gap.
- `Corollary \ref{cor:U2_derived}` (610-622): **supported as stated**.
- `Theorem \ref{thm:born_unique}` (656-701): **supported as stated**. It is a conditional Gleason theorem under (U1)-(U4) and universal completeness, not a closure-only derivation, and the paper now says that plainly.
- `Corollary \ref{cor:p_excluded}` (703-735): **supported as stated**. The counterexample is explicit and works.
- Toy-model numerical claims (783-796): **supported as schematic arithmetic only**, and the text correctly says the asymmetric state is stipulated rather than derived.

**2. Internal Consistency**
- **Must fix:** lines 233-235 define `P(\Phi_i \mid \Ocal) = w_i(\Ocal)/\sum_j w_j(\Ocal)`. That suppresses state dependence exactly where the rest of the paper insists probability is state-dependent. Route C later uses `w_i^{(C)} = \int_{\Phi_i} |\Psi|^2 d\mu` (316-320). The notation at 233-235 is therefore inconsistent with the paper’s actual construction.
- **Must fix:** line 293 says the Paper XIV stationary distribution “concentrates on the closure manifold `\Mcl`.” In this paper, `\Mcl(\Ocal)` was redefined at lines 142 and 416-421 as a subset of `\mathbb{CP}^{N-1}`. A configuration-space density cannot “concentrate on” that object. This is a type error caused by symbol reuse.
- **Must fix:** lines 595-599 use `\Psi \in \Mcl(\Ocal_\mathcal{B}) \cap \Mcl(\Ocal_{\mathcal{B}'})` and then separately impose completeness for the same `\Psi`. As written, the theorem mixes two equivalent hypotheses as if they were distinct.
- Cross-references otherwise read coherently on inspection. I did not compile the file, so this is a source-level check, not a build-level one.

**3. External Consistency**
- Paper XIV claim at 289-293, 308, 358, 915: **verified locally**. XIV proves the stationary distribution at [paper-xiv.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xiv/paper-xiv.tex:174>) and states uniqueness only under extra irreducibility/regularity assumptions at lines 190-190. Your current draft reflects that correctly.
- Paper XVIII claim at 297-300, 538, 754: **verified locally, with the right caveat**. XVIII treats the complex-structure point as bookkeeping, not a new geometric theorem: see [paper-xviii.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xviii/paper-xviii.tex:258>) and lines 291-300.
- Paper XXI claim at 51, 96, 400-406, 469, 547, 755: **verified locally, with the right caveat**. XXI explicitly presents the projective/Kähler structure and `U(N)` action as imported standard geometric QM on an assumed invariant subspace: see [paper-xxi.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxi/paper-xxi.tex:56>), 318-322, and 364-387.
- Paper XXIX claim at 51, 53, 578-579, 584-590: **verified locally**. XXIX proves basin locality and LMC, and explicitly says Paper XXX only gets non-contextuality of the normalised probability after adding completeness: see [paper-xxix.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxix/paper-xxix.tex:203>), 251-295.
- Combined claim `|\Psi|^2 = \Pst` at equilibrium (297-300 etc.): **verifiable only as a cross-paper inference**, not as a single upstream theorem in one source. That is acceptable; the current draft presents it that way.

**4. Tightness**
- The round-19 tone fixes you flagged are already done. No must-fix overclaim remains on line 153 or lines 754-755.
- Must-fix one-line edit for 233-235: change `w_i(\Ocal)` to `w_i(\Psi,\Ocal)` or explicitly say “for fixed state `\Psi`.”
- Must-fix one-line edit for 293: replace `\Mcl` with the Paper XIV configuration-space constraint manifold notation, or spell it out in words and stop reusing `\Mcl` there.
- Must-fix one-line edit for 595-599: either drop `\Psi \in \Mcl(\Ocal_\mathcal{B}) \cap \Mcl(\Ocal_{\mathcal{B}'})` or drop the separate completeness assumption. Keeping both is sloppy.

**5. Surface Issues**
- No substantive typo-level issues matter compared to the three consistency defects above.
- I did not compile the TeX, so this is not a build audit for undefined macros or unresolved references.

**6. Top Three Fixes**
1. Fix the state-dependence error in the basic weighting formula at lines 233-235. As written, the paper defines observer-only weights and then spends the rest of the paper using state-dependent ones.
2. Fix the `\Mcl` notation/type collision at line 293 against lines 142 and 416-421. Right now the same symbol denotes incompatible objects.
3. Clean up the hypothesis statement of Theorem `\ref{thm:noncontextuality}` at lines 595-599 so `\Mcl` membership and completeness are not redundantly presented as separate assumptions.
