**1. Claim Audit**

- `Theorem [Pentagonal torsion in 2I; sim-verified]` at `papers/millennium-rh-formal/rh-formal.tex:703`: proof establishes the orbit decomposition once an order-10 element exists. The element-order distribution is imported/sim-verified, not proved. Acceptable if the script and classical group fact are cited as data, but “sim-verified” should not be made to carry the group-theoretic classification alone.

- `Theorem [Shell--class bijection; imported]` at `papers/millennium-rh-formal/rh-formal.tex:727`: not proved here. The paper correctly labels it imported. I could not verify the cited Paper XXII claim locally in this pass because repository traversal/read commands on the OneDrive mount stalled; treat this as unverified until checked against `PaperXXII` lines 204–223.

- `Theorem [$K$-multiset for the script-selected τ; sim-verified]` at `papers/millennium-rh-formal/rh-formal.tex:764`: the proof establishes only the script-selected representative case. The theorem now states this limitation, which is good. It still relies on exact-computation output rather than a mathematical derivation. The caveat at `papers/millennium-rh-formal/rh-formal.tex:773`–`775` is essential and must remain in any abstract/main-result phrasing.

- `Theorem [Exact closed form; M1]` at `papers/millennium-rh-formal/rh-formal.tex:800`: the proof establishes the displayed factorisation only after importing the selected-τ `K`-multiset. The theorem statement itself does not restate “for the same script-selected τ as Theorem 2.3”; this is a hidden dependency. Add that phrase to the theorem statement, not merely to the preceding theorem.

- Numerical coefficient claim `[z^{10}]\widehat\zeta = 1,114,945,460,806,394` at `papers/millennium-rh-formal/rh-formal.tex:832`: algebraically follows from the Lucas values if those values are correct. It is not independently proved in text, but the arithmetic is finite and auditable.

- `Theorem [Mellin pole-symmetry; M2]` at `papers/millennium-rh-formal/rh-formal.tex:838`: the palindromic functional equation is proved. The pole-location statement is essentially correct, but the “reciprocal pairs” language mishandles `K=0`: the two locations coincide at `s=1/2`, with periodic imaginary copies, so this is a self-pair/double factor, not an ordinary pair. The overcount recurs at `papers/millennium-rh-formal/rh-formal.tex:957`–`959` and `papers/millennium-rh-formal/rh-formal.tex:1002`–`1005`.

- `Remark [Obstruction to one natural Hecke matching]` at `papers/millennium-rh-formal/rh-formal.tex:892`: the narrowed claim is much safer than earlier versions. However, the obstruction still needs a precise model of the proposed Euler-factor matching. As written, the finite rational function versus infinite Euler product mismatch already blocks a literal global Hecke `L`-function identification; the unitary-on-norm-one argument is only relevant after additional local-factor assumptions are imposed.

- `Theorem [ζ uniqueness]` at `papers/millennium-rh-formal/rh-formal.tex:1047`: true but mostly tautological. `(U1)` plus meromorphic continuation already forces equality on the connected domain; `(U2)` and `(U3)` are consistency checks, not needed hypotheses. The proof admits this at `papers/millennium-rh-formal/rh-formal.tex:1090`–`1095`.

- `Theorem [Cascade zeta identity]` at `papers/millennium-rh-formal/rh-formal.tex:402`: proof establishes only definitional compatibility, not construction. The theorem text correctly says this at `papers/millennium-rh-formal/rh-formal.tex:406`–`414`; keep the caveat.

- `Proposition [Euler product]` at `papers/millennium-rh-formal/rh-formal.tex:1214`, `Proposition [Functional equation]` at `papers/millennium-rh-formal/rh-formal.tex:1244`, and `Proposition [Analytic continuation]` at `papers/millennium-rh-formal/rh-formal.tex:1287`: all are immediate by definition/import from classical ζ. They establish no cascade-native analytic input; the text mostly says this honestly.

- `Theorem [Dedekind factorization]` at `papers/millennium-rh-formal/rh-formal.tex:1392`: standard and adequately proved by local factors. The corollary at `papers/millennium-rh-formal/rh-formal.tex:1412` correctly notes the RH-for-product equivalence, assuming the usual meaning of non-trivial zeros in the critical strip.

- `Lemma [Galois action on the spectrum, conditional]` at `papers/millennium-rh-formal/rh-formal.tex:1503`: proof establishes the stated claim under `(H-Mat)`. The caveat that F5 does not imply `(H-Mat)` is correctly included at `papers/millennium-rh-formal/rh-formal.tex:1536`–`1547`.

- `Hypothesis H_{σ-fix}` at `papers/millennium-rh-formal/rh-formal.tex:1570`: this is exactly RH in new language, as acknowledged at `papers/millennium-rh-formal/rh-formal.tex:1635`–`1647`. No overclaim here.

- `Theorem [Spectral σ-attractor decomposition; imported, sim-verified]` at `papers/millennium-rh-formal/rh-formal.tex:1706`: claim is imported from Paper XXII plus a local script. I could not verify the cited Paper XXII spectrum table locally in this pass. Until verified, this remains an imported assertion, not checked evidence.

- `Theorem H_attr_finite` and its corollary, especially `papers/millennium-rh-formal/rh-formal.tex:1913`–`1920`: the proof establishes convergence for a deliberately constructed polynomial-gradient flow using `p_fix(L)^2`. It does not establish that this is the cascade closure flow, nor that it is canonically forced by the closure functional. The wording “discrete finite-dimensional content of H_attr” and “canonically attached” is too strong.

- `Hypothesis [Candidate cascade Mellin trace identity]` at `papers/millennium-rh-formal/rh-formal.tex:1958`: correctly labelled open. It contains undefined symbolic objects (`K^*`, `λ_K^\pm`, `h_K^{pair}`, `R(h)`) and therefore cannot be used as a mathematical premise without further definitions. The status remark at `papers/millennium-rh-formal/rh-formal.tex:1993`–`2005` is necessary.

- External empirical H4O claims at `papers/millennium-rh-formal/rh-formal.tex:939`–`969` and `papers/millennium-rh-formal/rh-formal.tex:2036`–`2057`: correctly labelled external and unaudited. Do not allow later prose to treat them as evidence for a theorem.

**2. Internal Consistency**

- The abstract relation-to-RH paragraph still compresses the open-input structure into `H_{σ-fix}` at `papers/millennium-rh-formal/rh-formal.tex:233`–`240`. The Round 6 note says the abstract dependency graph was corrected to list all three open inputs (`H_sigma-irr-NP`, `B_sigma-P`, structural completeness), but the visible abstract text names only `H_{σ-fix}`. If the three-input decomposition exists later, the abstract is still not reflecting it.

- `K` is overloaded: number field `K = Q(√5)` at `papers/millennium-rh-formal/rh-formal.tex:438`, cycle weights `K(C)` at `papers/millennium-rh-formal/rh-formal.tex:768`, and the set `K = {0,20,52,72}` at `papers/millennium-rh-formal/rh-formal.tex:1962`. The expression `\sum_{K \in K}` at `papers/millennium-rh-formal/rh-formal.tex:1973` is especially bad notation.

- Pair-count language is inconsistent. The paper alternates between “4 reciprocal pole-location pairs” and a self-reciprocal `K=0` location. Use “three nontrivial reciprocal pairs plus one self-reciprocal `K=0` location” at `papers/millennium-rh-formal/rh-formal.tex:957`–`959`, `papers/millennium-rh-formal/rh-formal.tex:1002`–`1005`, and `papers/millennium-rh-formal/rh-formal.tex:2013`–`2015`.

- `\widehat\zeta(s)` and `\zetacas(s)` are kept mostly separate, but the attractor/trace discussion still risks conflating `\widehat\zeta` poles with classical `ζ` zeros. The caution at `papers/millennium-rh-formal/rh-formal.tex:2020`–`2033` is good; similar caution should be repeated before any “critical-line theorem” language.

- Cross-references visible in the read portions appear conceptually aligned: `thm:K_multiset`, `thm:zhat_form`, `thm:zhat_sym`, `prop:sigmas`, `prop:zeroset-tau`, and `rmk:set-vs-point` are used in the expected places. I could not run a LaTeX compile or full `\ref` audit because shell commands stalled.

**3. External Consistency**

- `PaperXXII` shell-class citation at `papers/millennium-rh-formal/rh-formal.tex:735`–`739`: not verified locally in this pass. The paper should not rely on brittle line references alone; cite a theorem/section label or reproduce the finite table.

- `PaperXXII` spectrum citation at `papers/millennium-rh-formal/rh-formal.tex:1714`–`1719` and `papers/millennium-rh-formal/rh-formal.tex:1732`–`1743`: not verified locally in this pass. This is central to the 94/26 spectral split and must be checked before publication.

- `PaperXXXVI` F5 citation at `papers/millennium-rh-formal/rh-formal.tex:559`–`581`: not verified locally in this pass. The present paper’s statement is appropriately cautious: F5 is not being used as unrestricted operator equivariance.

- `AdaptiveClosureTransport` citation at `papers/millennium-rh-formal/rh-formal.tex:1615`–`1622` and `papers/millennium-rh-formal/rh-formal.tex:2110`–`2118`: not verified locally in this pass. The current text says it does not prove the projector/edge-space lift, which is the safe formulation.

- `SmartCanonicalFramework`, `SmartPaperII`, `SmartPaperC` at `papers/millennium-rh-formal/rh-formal.tex:250`–`269` and `papers/millennium-rh-formal/rh-formal.tex:2491`–`2528`: not verified locally in this pass. These are peripheral and should remain framed as architectural comparison, not support.

**4. Tightness**

- `papers/millennium-rh-formal/rh-formal.tex:800`: replace “Exact closed form; M1” with “Exact closed form for the script-selected clock representative; M1”.

- `papers/millennium-rh-formal/rh-formal.tex:845`: replace “reciprocally paired as `{(5-K)/10,(5+K)/10}`” with “reciprocally paired for `K>0`, with `K=0` self-reciprocal at `s=1/2`”.

- `papers/millennium-rh-formal/rh-formal.tex:916`: replace “Thus `\widehat\zeta` is a finite Artin--Mazur-style dynamical zeta” with “Thus, in this construction, `\widehat\zeta` is best treated as a finite Artin--Mazur-style dynamical zeta”.

- `papers/millennium-rh-formal/rh-formal.tex:1623`–`1627`: replace “supplies the missing canonical object” with “supplies a finite-dimensional toy closure object”.

- `papers/millennium-rh-formal/rh-formal.tex:1913`–`1920`: replace “establishes the discrete finite-dimensional content of `H_attr`” with “establishes an explicit finite-dimensional model consistent with the desired `H_attr` selection”.

- `papers/millennium-rh-formal/rh-formal.tex:2051`–`2055`: replace “established in Theorem H_attr_finite” / “derived directly from the closure functional” with “established for the polynomial flow defined in this paper”.

**5. Surface Issues**

- Literal Unicode star `★` is used at `papers/millennium-rh-formal/rh-formal.tex:928`. The header comment claims pdfLaTeX support, but `inputenc` plus `textgreek` does not by itself guarantee U+2605. Define it or replace with `\(\star\)`.

- “4 reciprocal pole-location pairs” at `papers/millennium-rh-formal/rh-formal.tex:957` and `papers/millennium-rh-formal/rh-formal.tex:1003` should be corrected for the `K=0` self-pair.

- `\sum_{K \in K}` at `papers/millennium-rh-formal/rh-formal.tex:1973` is notation abuse. Rename the set to `\mathcal K`.

- `m^K_K` at `papers/millennium-rh-formal/rh-formal.tex:1973` is visually unreadable and overloaded. Use `m_{\mathcal K}(K)` or `m_K`.

- `λ_K^\pm = (12 ± sqrt(12^2 - 4K^*))/2` at `papers/millennium-rh-formal/rh-formal.tex:1976` introduces `K^*` without definition. The later caveat admits this, but undefined notation in a displayed hypothesis is still poor form.

- “positive-integer numerators in the Euler product” at `papers/millennium-rh-formal/rh-formal.tex:2246`–`2248` is odd phrasing; the Euler product has factors with real coefficients. Say “real Dirichlet coefficients”.

**6. Top Three Fixes**

1. Fix the abstract/open-input dependency graph at `papers/millennium-rh-formal/rh-formal.tex:233`–`240`. If the real Round 7 status depends on `H_sigma-irr-NP`, `B_sigma-P`, and structural completeness, the abstract must name those inputs explicitly instead of collapsing everything into `H_{σ-fix}`.

2. Downgrade the closure-flow claims at `papers/millennium-rh-formal/rh-formal.tex:1623`–`1627`, `papers/millennium-rh-formal/rh-formal.tex:1913`–`1920`, and `papers/millennium-rh-formal/rh-formal.tex:2051`–`2055`. The theorem proves convergence for a designed polynomial flow, not for the cascade closure functional itself.

3. Make the selected-clock dependence explicit in M1 and repair the pole-pair count: add “same script-selected `τ`” to `papers/millennium-rh-formal/rh-formal.tex:800`–`807`, and replace “4 reciprocal pairs” with “three reciprocal pairs plus the `K=0` self-reciprocal location” at `papers/millennium-rh-formal/rh-formal.tex:957`–`959` and `papers/millennium-rh-formal/rh-formal.tex:1002`–`1005`.
