Publication-ready: **No**. As a conditional RH reformulation it is closer, but there are still substantive math/attribution blockers.

Line references are to `papers/millennium-rh-formal/rh-formal.tex`.

**1. Claim Audit**
- **“from the permeability axiom F1 alone”** (`114-121`, `606-609`, `866-873`): not established. The scripts verify computations once `2I`, `τ`, shells, and `κ` are chosen. They do not derive the icosian ring, the 600-cell clock, or the cocycle from F1 alone. Edit to “using the φ/600-cell cascade substrate”.
- **“All K-values and multiplicities are forced by the 2I-conjugacy / shell structure”** (`128-132`, `668-689`): overclaims. The exact script verifies the selected `τ`/`κ` computation, but the proof is enumeration plus “Codex prediction,” not a derivation of force/uniqueness. State it as exact computer-assisted enumeration for the chosen cocycle.
- **F5 hypothesis** (`491-503`): attribution mismatch. Paper XXXVI proves a factor-2 equality of finite sums over conjugate `H4` copies for σ-fixed closure fields under H-SIG; it does not prove the stated `F[σΦ]=F[Φ]` operator-style assertion. This must be restated to match Paper XXXVI or removed as unused background.
- **U4/H4O theorem** (`829-864`): not established locally. It is external empirical material, explicitly unaudited. A theorem environment saying the law “exhibits” those values is too strong. Demote to “external report” or supply local artifacts.
- **Spectral attractor theorem** (`1540-1569`): proof does not establish the exact spectrum as written. It cites “known closed form from H4 representation theory” but gives no reference or exact characteristic-polynomial computation; the script is floating-point matching. Add a source or exact verification.
- **Closure-flow/spectral bridge** (`1610-1620`, `1714-1757`, `1995-2000`): still a major gap. The spectral count is for the adjacency Laplacian `L`; the RH bridge concerns zeros of `ζ` and/or the closure operator `F|_{H4}`. No theorem identifies `F|_{H4}` or the closure flow with `p(L)`, `P_fix`, or the Mellin-zero map. The text mostly admits this, but `1997-2000` still overstates “explicit candidate self-adjoint operator… together with spectral data.”

**2. Internal Consistency**
- Cross-references and bib keys resolve by text search.
- The centered Mellin convention is substantively fixed (`544-561`, `1762-1800`): inversion now gives `s -> 1-s`.
- The paper correctly distinguishes holomorphic `s -> 1-s` fixed point `{1/2}` from the critical line via `τ(s)=1-\bar{s}` (`1802-1855`).
- Remaining inconsistency: `σ` is sometimes said to “act” on the Mellin variable (`369-371`, `1762-1768`) but the later proof says this is a convention, not a Galois action on positive reals (`1780-1799`). Use “we define the Mellin-level σ-pairing” consistently.
- The B11 script cited as verification still contains stale prose conflating the fixed set of `s -> 1-s` with the critical line. The manuscript is corrected; the verification artifact should be corrected too.

**3. External Consistency**
- **Paper XXII / `run_icosian_exact.py`**: verified locally. It supports shell = conjugacy class and the 9-shell sizes used at `639-645`.
- **Paper XXXVI F5**: source exists and supports H-SIG factor-2 finite-sum equality, but not the RH paper’s exact `F[σΦ]=F[Φ]` wording.
- **CascadeRH seam note** (`1334-1348`): the RH paper’s downgrade is accurate. The cited file is informal and overclaims; no formal Proposition 4.1 supports the spectral-zero correspondence.
- **TransportLaw / AdaptiveClosureTransport** (`1457-1467`, `1610-1614`, `1697-1699`): they support “open/conditional adaptive selection” language, not the specific σ-paired mode suppression or closure-projector identification. The RH paper should cite them only as motivation/open-status context.
- **Smart / aria-chess claims**: not locally verifiable inside this repository. The manuscript should not put them in theorem-grade claims.

**4. Tightness**
- `114-121`: replace “from F1 alone” with “from the cascade φ/600-cell substrate, after choosing the stated clock and cocycle.”
- `129-132`: replace “forced” with “computed exactly for the specified cocycle and normalisation.”
- `491-503`: replace the F5 statement with the exact Paper XXXVI factor-2 finite-sum theorem.
- `829`: replace `\begin{theorem}` with `\begin{remark}` or “External empirical report.”
- `1997`: replace “supplies an explicit candidate self-adjoint operator” with “suggests a candidate operator layer; the operator/zero identification is not constructed here.”

**5. Surface Issues**
Only substantive surface blockers:
- `\textdelta` may be undefined under ordinary pdfLaTeX unless an appropriate package/engine is used.
- Unicode Greek letters and em dashes may break a plain pdfLaTeX build unless the build is XeLaTeX/LuaLaTeX or configured for them.
- `132` says “Definition~\ref{thm:K_multiset}” but the target is a theorem.
- Abstract repeats the Smart parenthetical twice (`225-229`).

**6. Top Three Fixes**
1. Fix the **F5 attribution** (`491-503`): restate exactly as Paper XXXVI’s H-SIG factor-2 finite-sum result, or remove it if unused.
2. Downgrade the **F1-alone / forced K-multiset rhetoric** (`114-132`, `668-689`): the proof is exact enumeration for chosen data, not derivation from F1 alone.
3. Separate the **Laplacian σ-attractor evidence from RH content** (`1540-1757`, `1995-2000`): add an explicit missing bridge `F|_{H4}`/closure flow/`P_fix`/Mellin-zero map, or state that the spectral attractor material is only diagnostic and not part of the conditional RH reduction.
