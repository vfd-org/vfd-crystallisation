**1. Claim Audit**
- `paper-xxx.tex:323-338` Proposition `Conditional Born weighting from stationary sector measure`: the proof establishes only the second equality in (333), and only approximately. The first equality in (333) is assumption (4) restated. So this is not a derivation of measurement statistics from the stationary measure; it is a sector-decomposition lemma plus a sampling postulate. The proposition also does not assume completeness, so it does not yet recover the standard unconditional Born rule.
- `paper-xxx.tex:444-448` Proposition `Sector-preserving unitaries induce sector-probability covariance`: basically correct in the exact sector-supported regime, but the statement omits load-bearing hypotheses used in the proof. You use exact `P_i(\Psi)=|c_i|^2` at `452-462`, which requires exact sector support, normalised sector modes, and that `T\Psi` remains in the admissible class.
- `paper-xxx.tex:475-484` Theorem `Chentsov 1972; Petz quantum extension`: there is no in-paper proof; this is imported. More seriously, part (2) is not what Petz’s monotone-metric theorem proves in this form. What you need here is the standard classification of the `U(N)`-invariant Kähler metric on `\mathbb{CP}^{N-1}`, not a theorem established in this manuscript.
- `paper-xxx.tex:493-506` Proposition `Closure Hessian as the Fisher metric...`: the proof does establish a local Laplace-regime asymptotic for the translated density family it defines. The statement is correctly local and correctly says this is not yet a metric on `\mathbb{CP}^{N-1}`.
- `paper-xxx.tex:544-551` Corollary `Fubini--Study identification`: not established. The proof asserts, rather than proves, the crucial bridge from the Fisher metric of a configuration-space translation family to the Fubini–Study metric on the real slice of projective Hilbert space. The dimensional map is missing, the pullback is not defined, and `559-567` are doing conceptual work that has not been earned.
- `paper-xxx.tex:596-601` Theorem `Non-contextuality of sector probability for complete observers`: the proof establishes a restricted claim only. Given two localised-probe observers sharing the same canonical probe and given completeness for the fixed state `\Psi`, the normalised ratio agrees. That is fine as a restricted theorem. It is not general non-contextuality.
- `paper-xxx.tex:612-620` Corollary `Restricted (U2)...`: this does not supply the strength of `(U2)` later needed in Theorem `\ref{thm:born_unique}`. It gives context-independence only on a restricted observer subclass, with Route C separation hypotheses, and effectively state-by-state.
- `paper-xxx.tex:650-669` Theorem `Born weighting uniqueness...`: as an abstract theorem, the proof is standard and acceptable once `(U1)-(U4)` and `\Mcl=\mathbb{CP}^{N-1}` are granted. What the manuscript does not establish is that VFD gives you those hypotheses in the needed global form. Calling this “closure-derived” uniqueness is over-claiming.
- `paper-xxx.tex:701-706` Corollary `Exclusion of |c|^p for p\neq2`: proved correctly.
- `paper-xxx.tex:781-796` Toy-model numerical claims: the symmetric `1/2,1/2` split is fine by symmetry. The asymmetric `|a|^2,|b|^2` result is only an illustration under an inserted state, and the text honestly says so.

**2. Internal Consistency**
- `153-154` versus `196-200`: the manuscript first distinguishes “complete for `\Psi`” from “universally complete,” then later says statewise completeness is “equivalently” `\Mcl(\Ocal)=\mathbb{CP}^{N-1}`. That is false. The latter is the universal condition.
- `53`, `102`, `619`, `895`, `948-949` versus `650-662`: the paper repeatedly says non-contextuality is no longer an external assumption, but Theorem `\ref{thm:born_unique}` still assumes full `(U2)` on all projectors and all states in `\mathbb{CP}^{N-1}`. What you derived is narrower.
- `151` versus `173-181`: `\Pi_i` is called the projector onto the “sector minimum,” but later the paper carefully distinguishes the configuration-space minimum/basin from the Hilbert-space basis vector. The terminology slips.
- `169-181`, `245-255`, `316-320`: the manuscript cycles among `\Phi_i(\Ocal)`, `\Phi_i`, `B_i`, `\phi_i`, and `\Phi_i^*` for different objects. The typing is not stable enough for a paper that leans so hard on geometry.
- Cross-references appear internally resolved by inspection. I did not compile the file, but I do not see an obvious missing `\label`.

**3. External Consistency**
- `289-293`, `308`, `913`: Paper XIV does support the stationary density claim at `paper-xiv.tex:174-190`. It also qualifies uniqueness by irreducibility/regularity/confining-boundary assumptions. Your sentence at `289-293` suppresses those hypotheses.
- `297-300`, `538`, `548-549`: Paper XVIII does support the Nelson wavefunction and equilibrium/near-equilibrium Schrödinger recovery at `paper-xviii.tex:291-306` and `575-579`. It gives an almost-complex structure and equilibrium-tangent integrability. It does not supply a global Kähler structure by itself.
- `437-469`, `547`, `653-657`: Paper XXI does support the `\mathbb{CP}^{N-1}` projective manifold, Kähler structure, and `U(N)` isometries at `paper-xxi.tex:170-189` and `207-216`. The scope is explicitly equilibrium-tangent only, which your draft mostly remembers.
- `159-164`: Paper XXIX’s admissibility definition is verified at `paper-xxix.tex:163-168`.
- `582-591`, `594-605`: Paper XXIX does contain basin locality, localised probe observers, LMC, and basin-admissibility context-independence at `paper-xxix.tex:202-203`, `216-283`, `287-291`.
- `596-606`: what XXIX gives downstream at `paper-xxix.tex:298-303` is context-independence of the unnormalised integrated mass. Your theorem adds the completeness step to make the normalised ratio context-independent. That extra step is yours, not XXIX’s.
- `181`, `328`, `407-418`: I do not find in XVIII or XXI a construction of sector-supported basis vectors with densities supported on the measurement basins. Your own text admits at `181` that this is a modelling assumption, not something XVIII actually provides.
- `761`, `906`: Paper XIX does support the claim that the residual vanishes at equilibrium and is small near equilibrium; see `paper-xix.tex:41-43`, `58`, `118`, `463-470`.

**4. Tightness**
- `51-55`: “fixes the unique compatible probability assignment as the Born measure” is too strong. Edit to: `yields a conditional uniqueness argument for the Born measure once (U1)-(U4) and the global geometric inputs are granted.`
- `302`: “the effective Born density ... coincides” overstates what Route C buys you. Edit to: `at equilibrium, the Nelson density coincides with the stationary density, providing the candidate weighting measure.`
- `354-360`: “need not be introduced as an independent measurement axiom; it is inherited” ignores assumption (4). Edit to: `conditional on the sampling hypothesis, the quadratic law is inherited from the equilibrium measure.`
- `821`: “Derived for complete observers; generalised for partial observers” is too strong. Edit to: `Conditionally recovered for complete observers; a conditional extension is proposed for partial observers.`
- `952-956`: “VFD also makes a structural prediction that extends standard quantum mechanics” outruns the actual observer modelling. Edit to: `Within the sector-supported partial-observer model, VFD assigns a conditional-detection probability law extending the complete-observer case.`

**5. Surface Issues**
- `196-197`: the “equivalently” clause is mathematically wrong, not just stylistically weak.
- `206`: the explicit formula for partial observers needs the same sector-supported-decomposition caveat later added at `954-955`.
- `483`, `487`: write `(dc_i)^2`, not `d c_i^2`.
- `151`: “projector onto the sector minimum” should be “projector onto the sector basis vector associated with that minimum.”
- `597`, `609`: `\Ocal_\mathcal{B}` and `\Ocal_{\mathcal{B}}` are used inconsistently.
- `140`: “Finite-dim” is journal-sloppy; write `finite-dimensional`.
- No obvious undefined macros jump out by inspection. I did not compile, so this is not a compile audit.

**6. Top Three Fixes**
1. Repair Route E’s central bridge or cut it back hard. The failure is at `544-568`, and it infects `650-669`, `856-859`, and `948-952`. Right now the paper does not prove the local-Fisher-to-global-Fubini–Study identification it advertises.
2. Stop claiming that `(U2)` has been derived in the strength needed by Theorem `\ref{thm:born_unique}`. The gap is between `612-623` and `650-662`, and the overstatement recurs at `895` and `948-949`.
3. Recast Route C honestly as a conditional sampling postulate plus sector-mass lemma. The problem is at `323-343`, with over-interpretation at `346-350`, `855`, `870`, and `944-948`. As written, the “derivation” is doing less than the prose says.
