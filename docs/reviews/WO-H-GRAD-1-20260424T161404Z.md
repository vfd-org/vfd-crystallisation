**1. Claim Audit**

- PASS: stale `BUILD REQUIRED` at `cascade-h-grad-1-closure.md:324-327` and `389-390` is gone; these now read `BUILD COMPLETE` / PASS.
- PASS: the B6 isotropy error is corrected: `[1]` is “isotropic (not anisotropic)” and the proof now uses “external isotropic vector with nonzero bilinear pairing” at `cascade-h-grad-1-closure.md:468-499`.
- FAIL: B6 §7 is not fully relabelled to `G_p`. The heading is fixed, but the body still claims “single orbit of the relevant G₂ / Fano-automorphism action” and “well-defined up to G₂” becomes “an actual theorem” at `cascade-h-grad-1-closure.md:698-708`.

**2. WO Acceptance Audit**

A1-A4 and O2-O5 are marked complete in the status tables, but stale WO-level closure text remains. `WO-H-GRAD-1.md:3` still says `Status: OPEN` and “H-grad-1 remains open.” O1 is contradictory: table says proof-sketch complete at `WO-H-GRAD-1.md:45`, but §3 still says “G₂-canonicality remains as build B6 (deferred to a separate round)” at `WO-H-GRAD-1.md:97-104`.

**3. Catalogue Audit**

Catalogue B6 is acceptable at frame-level: it says `G_p`-canonicality proof sketch, G₂ pullback outlined/not executed at `cascade-h-grad-1-catalogue.md:24`. It also correctly marks `G_p` sufficiency as a “declared judgement” at `cascade-h-grad-1-catalogue.md:99-109`.

**4. Attribution / External Consistency**

Not re-audited beyond the requested stale-frame checks. No new blocking attribution defect found in the requested narrow scan.

**5. Sim Correctness**

No sim file was supplied for this review round. I did not audit scripts.

**6. Tightness**

The `G_p` sufficiency claim is not consistently downgraded. The catalogue and honest assessment do it correctly, but `cascade-h-grad-1-closure.md:549-552` still asserts it as fact, and `WO-H-GRAD-1.md:45` repeats the assertion without “declared judgement” language.

**7. Top Three Fixes**

1. Replace `cascade-h-grad-1-closure.md:700-708` with `G_p`-level wording only.
2. Update `WO-H-GRAD-1.md:3` and `95-104`; current text still says open/deferred.
3. Downgrade `cascade-h-grad-1-closure.md:549-552` and `WO-H-GRAD-1.md:45` to declared judgement.

**8. Verdict**

Publication ready: no. Frame-level B1-B6 is close, but stale G₂ theorem/open-status wording still overclaims closure.
