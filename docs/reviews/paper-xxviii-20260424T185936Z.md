Line refs are to `papers/paper-xxviii/paper-xxviii.tex`.

1. **Claim Audit**

- “Paper XXII establishes a `240`-to-`120` two-to-one map…” (l. 82): verified in Paper XXII. The softened “two-to-one pairing, not double cover” language is source-faithful. Minor caveat: Paper XXII itself imports/records part of this from the local `CascadeAlgSubstrate` derivation, so “establishes” is slightly stronger than “records/imports and uses.”

- “Paper XXII constructs `\Fcal` as a log-sum-exp…” (l. 90-94): verified. The formula now matches Paper XXII exactly: denominator `2\varepsilon^2`, not `\sigma`, and the distinction from stochastic `\sigma^2` is correct. Tiny wording issue: source calls `\varepsilon > 0` the smoothing parameter, not “parameter `\varepsilon^2`.”

- “At the Paper XIV equilibrium… yields the Schrödinger equation…” (l. 130): verified against Papers XVIII/XIX/XXI. The “global-phase invariant” wording is now appropriately conservative.

- “When the relative phase parameter `\theta` admits multiple overlapping configurations…” (l. 122): not established as written. Paper XV supports a complexified multi-path ansatz and interference; Paper XXIII supports phase-overlap birth ordering. The bridge claim that this same `\theta` produces “multiple comparable minima” of `\Fcal` is not proved in either cited source.

- “the stationary amplitude … takes the form `A=e^{-\Fcal/\sigma^2}`” (l. 126, repeated l. 196): partly verified. Paper XV gives `A(\psi)=sqrt(P_st)=N exp(-\Fcal/\sigma^2)`. I did not find the “evaluated on the Euler-Lagrange solution” qualification in Paper XV; either cite where that comes from or remove it.

- Proposition “Combinatorial linear-extension count as an `\eta`-indicator” (l. 258-265): the antichain and total-order cases are correct. The intermediate bound `1 < L < n!` is true, but the proof is too loose: “adding a comparable pair strictly reduces `L`” is only true for adding a new non-redundant relation between previously incomparable elements. More importantly, the statement “not by `\eta` alone” is asserted, not proved. Add an explicit counterexample, e.g. on four elements, two posets with two comparable pairs can have different numbers of linear extensions.

- “The ordering fraction `\eta` parameterises this transition” (l. 276; echoed l. 412): not proved. Earlier text correctly says `\eta` is only a bookkeeping statistic. Replace “parameterises this transition” with “labels this proposed interpolation.”

- “Crystallisation … is the process by which the event-order geometry transitions…” (l. 313-318) and “provides the geometric bridge…” (l. 322): overclaim. The later limitation (l. 342) says this is not established; the section itself should use conditional language throughout.

- “What this paper establishes and does not establish” (l. 333-343): now honest. It correctly separates the table, bookkeeping triple, and combinatorial proposition from unproved identification and transition claims.

- Conclusion (l. 423-429): now honest-conditional. It no longer claims a unification theorem and correctly lists the needed identification theorems.

2. **Internal Consistency**

- l. 72 says the paper “establishes a structural bridge”; l. 294 and l. 427 call it a hypothesis. Use “proposes” at l. 72.

- l. 78 says both tracks share “the same underlying substrate” comprising `(\Ecal,\Fcal,\Gcal)`, but l. 110-111 and l. 339-340 say the triple is only bookkeeping and not derived. l. 78 should be rewritten.

- l. 118 and l. 142 say the listed regime features are “all established.” That is too broad: the source papers establish pieces, but the regime packaging is interpretive.

- l. 146 says “birth-angle parameter `\theta`.” In Paper XXIII, `\theta` is the relative rotation parameter; birth angles are `\theta_b(e)`. Use that notation.

- Cross-references checked: all `\ref` targets used in the paper resolve to defined labels. No unresolved citation keys: all cited keys `SmartXIV` through `SmartXXVII` used here are present in `papers/paper-xxviii/references.bib`.

3. **External Consistency**

- `SmartXX` is present in `references.bib`; the Round 2 missing-reference issue is fixed.

- Paper XXII formula and two-to-one pairing: verified locally in `papers/paper-xxii/paper-xxii.tex` lines 87-90 and 234-238.

- Paper XIV stationary distribution `P_st propto exp(-2\Fcal/\sigma^2)`: verified.

- Paper XV complexified Onsager-Machlup action and interference: verified. But the target’s `\theta`/multiple-minima linkage and “Euler-Lagrange stationary amplitude” wording are not verified.

- Paper XVII dissipative obstruction: verified.

- Paper XVIII Nelson pairing, Witten Hamiltonian, exact equilibrium and formal near-equilibrium Schrödinger recovery: verified.

- Papers XIX/XX residual properties: verified; “global-phase invariant” is acceptable and avoids the earlier gauge overclaim.

- Paper XXI Route A/Route B distinction and non-identification of operators: verified.

- Paper XXIII basepoint-dependent strict event order and conditional incomparability: verified.

- Paper XXIV linear-extension observers, simultaneity, and endpoint-interval analogue under separate hypotheses: verified.

- Paper XXV Hasse graph and metric on connected components: verified.

- Paper XXVI graph-curvature indicators as toy/reference-dependent indicators: verified.

- Paper XXVII source candidates, graph-Poisson model, `K_{\mathrm{pot}}`, no Einstein/stress-energy/geodesic-from-`u`: verified. The table row now correctly points `K_{\mathrm{pot}}` to XXVII.

4. **Tightness**

- l. 72: replace “It establishes a structural bridge” with “It proposes a structural bridge hypothesis.”

- l. 78: replace “same underlying substrate” with “a common bookkeeping vocabulary over related 600-cell-derived structures.”

- l. 118: replace “all established” with “assembled from the following source-paper results and interpretive correspondences.”

- l. 276: replace “the system transitions” with “the proposed labelling interpolates.”

- l. 313: replace “is the process by which” with “would be the process by which, if the bridge hypothesis is later proved.”

- l. 322: replace “provides the geometric bridge” with “is proposed as a possible geometric bridge.”

- l. 370: replace “Regime transition on `\Gcal`” with “Regime-label correspondence on event posets.”

5. **Surface Issues**

- l. 110-111: “`\Fcal` … defined differently in each track” is inaccurate; the gravitational papers do not define admissibility/source content via `\Fcal`. Say “track-specific or absent role.”

- l. 126 and l. 196: remove “Euler-Lagrange solution” unless sourced.

- l. 146: use `\theta_b(e)` for birth angle.

- l. 233-239: Definition of `\eta` should include `|\Ecal|\ge 2`, not leave it to the following prose.

- No undefined local macros found in the target file. No missing bibliography keys found.

6. **Top Three Fixes**

1. Fix the remaining transition/crystallisation overclaim at l. 276 and l. 313-322. This is the main place where the paper still reintroduces the unproved mechanism.

2. Repair Proposition 1’s proof at l. 258-268. The bounds are true, but the “not by `\eta` alone” part needs an explicit counterexample, and the strict-monotonicity sentence needs qualification.

3. Correct the Paper XV source-fidelity issue at l. 122, l. 126, and l. 196. The source supports complexified path amplitudes and interference, not the full `\theta`-driven multi-minimum bridge or the “Euler-Lagrange stationary amplitude” phrasing as written.
