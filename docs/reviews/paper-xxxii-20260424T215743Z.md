**Claim Audit**

- `L137-146` Proposition `prop:phi`: proof establishes the unique positive solution of `r = 1 + 1/r`, conditional on the geometric-mean axiom and positive scales. Fine.
- `L233-244` Theorem `thm:600cell`: proof establishes uniqueness of `2I` only within `(H1)-(H4)`. The statement is now properly conditional.
- `L278-293` Corollary `cor:phi_determined`: over-claimed. The preceding ADE theorem does not prove “unique maximal finite subset of `S^3` whose pairwise inner products all lie in `1/2 Z[phi]`.” The paragraph asserts this via `H_4` classification but does not prove it.
- `L365-390` Theorem `thm:F`: axiom count is now A1-A4, but the proof still needs tightening. A2 is stated with geodesic distance at `L338`, while the theorem uses chordal cost at `L372`; “small-distance limit” does not satisfy A2 as written globally.
- `L416-424` Proposition `prop:properties`: no proof is supplied. Several items are plausible, but “barrier height controlled by epsilon” is not a mathematical proposition as stated.
- `L446-458` Proposition `prop:Z2`: established.
- `L467-477` Theorem `thm:Fint`: algebraically established from the exclusion partition function, assuming `eta < 1`.
- `L479-491` Proposition `prop:Fint_props`: repulsion and hard-core limit are mostly established; cluster decomposition is only shown for states near basin centres (`L517`), but the proposition states it unqualified.
- `L693-708` Proposition `prop:pion`: the count `2 x |V(P_3)| = 6` is conditional on the assumed `P_3` supports and disjoint support localisation. Vertex exclusion alone does not prove two three-vertex support graphs are disjoint.
- `L756-769` Proposition `prop:photon`: graph zero-mode part is standard; the physical photon/coupling contribution is identified, not derived. Status is acceptable.
- `L831-843` Proposition `thm:factor`: now correctly demoted to a conditional identification. The proof establishes only the perturbative algebra after importing `-g'/K_pi = pi lbar_p`.
- `L961-967` Proposition `prop:deuteron`: the numerical result follows from the conditional `(6+alpha)` input. With precise Compton wavelengths it gives the stated sigmas; with the rounded constants printed at `L943`, it does not exactly reproduce `0.02 sigma`.
- `L1004-1013` Proposition `prop:Zalpha`: over-stated as a proposition. No derivation is given; it is an assumption/prediction.

**Internal Consistency**

- Round-1 axiom-count issue is mostly fixed: `L332` says four axioms, `L365/L376/L409/L1067` say A1-A4. Remaining inconsistency: `L98` still says “vertex set + extensivity uniquely characterise Fcal,” omitting A1, A2, and A4. Edit to: “Within the A1-A4 soft-min class…”
- Chordal/geodesic mismatch remains serious: A2 uses geodesic distance (`L338`), theorem/proof use chordal distance (`L372/L396`). Either make A2 chordal for embedded `S^3`, or use geodesic costs throughout.
- Cross-references and citations resolve by static scan. No unresolved `\ref`, `\eqref`, or bibliography keys found.
- `L606-609` still says the two mode classes are “therefore independent” and combine as tensor factors. Different operator spaces do not prove physical/statistical independence or tensor-product factorisation.
- `L1071` places the first-order minimisation under “What is derived.” The caveat “once the kinematic identifications are granted” saves it, but it would be clearer as “conditional algebra” rather than derived physics.

**External Consistency**

- SmartRadius claims are mostly verified locally: proton boundary resolvent and `r_p=4 lbar_p` at proton-radius `L224-255`; neutron formula at `L284-300`; pion half-period at `L306-314`; old LO deuteron at `L336-354`.
- The imported NLO coefficient in Paper XXXII (`L838-L843`, `L890-L897`) is not found as a derivation in SmartRadius. SmartRadius supports `r_pi = pi lbar_p`, not the perturbative identity `-g'/K_pi = pi lbar_p`.
- Paper XXII claims are verified with the right status: integer eigenvalues and sector decoupling at paper-xxii `L194-222`; `137 + pi/87` at `L272-301`; non-independence of the three `87` identities at `L283-289`.
- Paper V supports the `0.014%` mass-error claim only conditionally; see paper-v `L38-43` and `L507-510`. Paper XXXII’s `L84-88` should say “conditional numerical correspondences,” not simply “produced.”
- Paper XIII supports the old four-component ansatz with unspecified weights; see paper-xiii `L108-144`.

**Tightness**

- `L48` is good: “narrows that gap within stated structural assumptions.”
- `L66-71`: “Spectral decomposition of Fint identifies two surviving mode sectors” is too strong. Suggested edit: “A spectral decomposition motivates two retained mode sectors…”
- `L98`: too strong. Suggested edit: “A1-A4 uniquely characterise Fcal within the stated soft-min class.”
- `L774-775`: “key derivation from Paper XXII” is too strong; Paper XXII calls it a numerical correspondence. Suggested edit: “key arithmetic identification from Paper XXII.”
- `L1113`: “is no longer a construction” is too strong. Suggested edit: “is no longer merely postulated within the stated soft-min class.”
- `L1004`: change “For a nucleus…” to “Assuming linear charge scaling for the photon contribution…”

**Surface Issues**

- `L611`: `F_int` appears in text mode and will break LaTeX because of `_`. Use `$\Fint$`.
- `L595`: `\Delta_A` is introduced without definition.
- `L943/L1105`: rounded `lbar_p=0.21031` gives `r_str=3.96907`, not `3.96905`; the stated sigmas require more precise constants. Print the precision used.
- `L976`: caption is now appropriately “conditional NLO correction.”
- `L361`: Renyi domain `alpha > 0, alpha != 1` is fixed.
- `L192`: `O(theta^4)` correction is fixed.
- `L1197`: independence language is fixed for the triple appearance of factor 4.

**Top Three Fixes**

1. Fix the geodesic/chordal inconsistency in A2 versus Theorem `thm:F` (`L338`, `L372`, `L396`). As written, the theorem does not satisfy its own sharp-limit axiom.
2. Demote or prove the remaining unsupported mode claims: “two surviving mode sectors” (`L66-L71`) and “therefore independent…tensor factors” (`L603-L609`).
3. Recast `Z alpha` scaling as an explicit assumption/prediction, not a proposition (`L1004-L1013`), and print enough numerical precision to reproduce the `0.02 sigma / 0.83 sigma` claims (`L943`, `L1052`, `L1105`).
