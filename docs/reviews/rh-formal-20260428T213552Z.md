**1. Claim Audit**

- “Cascade zeta identity” (`rh-formal.tex:402`): established only because `\zetacas` is defined as the classical Euler product/continuation at `1173–1182`. The proof supports compatibility, not a cascade derivation.
- “Critical line as fixed set” (`417`): established, but only for the composite anti-holomorphic involution `\tau_{\mathrm{crit}}=\iota\circ\bar{ }`, not for holomorphic `\sigma` alone. The statement is now appropriately caveated.
- “Dedekind reduction” (`434`): established by definition of the prime-ideal Euler product and the standard quadratic factorization.
- “Conditional critical-line theorem” (`467`): formally correct, but tautological: the hypothesis at `1576–1585` is exactly RH in cascade notation.
- “Pentagonal torsion” (`703`): proof establishes the orbit count once the order-10 element/order distribution is accepted. Fine as finite-group/sim-supported.
- “Shell--class bijection” (`727`): not proved here; imported. I verified Paper XXII supports the shell/class claim locally.
- “K-multiset” (`764`): established only for the script-selected representative. The theorem correctly does not prove class-independence.
- “Exact closed form” (`800`): follows from the selected `K`-multiset and Lucas identity. The quoted Lucas numbers and `[z^{10}]` coefficient match the exact script output.
- “Mellin pole-symmetry” (`840`): established. It gives a pole staircase, not critical-line concentration.
- “Obstruction to one natural Hecke matching” (`894`): acceptable only as a narrow obstruction. It is not a classification of global `L`-function identifications, and the text says so.
- “`\zeta` uniqueness” (`1053`) and corollary (`1106`): true; U2/U3 are redundant once U1 and meromorphic continuation are assumed.
- `\eta_p` convergence (`1144`), Euler product (`1220`), functional equation (`1250`), analytic continuation (`1293`): all proved only by definition/classical import. The paper mostly says this.
- Dedekind product/factorization/corollary (`1382`, `1398`, `1418`): established, with standard meanings of non-trivial zeros.
- “Galois action on the spectrum, conditional” (`1509`): proof supports the restricted claim for eigenvalues lying in `\mathbb Q(\varphi)` under H-Mat.
- “Spectral σ-attractor decomposition” (`1712`): locally verified against Paper XXII and the spectrum script.
- “σ-paired mode suppression” (`1848`): the proof establishes suppression for the deliberately defined polynomial filter `\Psi_t`. It does **not** establish this for the original cascade closure functional. The title and later corollary overstate this.
- “Discrete-level statement of H_attr” (`1919`): over-claim. The theorem proves a custom filter lemma, not the former closure-flow attraction hypothesis.
- Candidate trace identity (`1964`): not established and not yet mathematically well-defined; `K^*`, `\lambda_K^\pm`, `m^K_K`, and `h^{pair}_K` are symbolic placeholders.
- Fixed-set and zero-set propositions (`2198`, `2271`, `2280`): established, with the caveat that `s\mapsto1-s` is a convention on the Mellin variable.

**2. Internal Consistency**

- All `\ref`/`\eqref` targets resolve.
- The largest conflict is terminology around `\Psi_t`: `1794–1801` call it a “canonical closure flow,” while `1828–1845` admit it is not the original closure functional or a physical/pre-existing dynamics. The later corollary at `1919–1927` reverts to the stronger language.
- `1813` says the original closure functional `F[\Phi]` is from `§\ref{sec:zhat}`. It is actually defined in the Cascade Framework section, `Definition~\ref{def:F}` at `542–557`.
- The “unconditional content” summary at `2361–2369` omits the paper’s own principal unconditional `\widehat\zeta` results M1–M3 (`170–209`). Either say “of the classical-RH reformulation part” or include M1–M3.
- The trace-identity notation at `1964–1997` is internally unstable: `K` is both a set and an index, and the left side tests Laplacian eigenvalues while the right side uses undefined “character-Lucas” locations.

**3. External Consistency**

- Paper XXXVI/F5: verified locally. `rh-formal.tex:560–581` matches Paper XXXVI `539–562`: factor-2 finite-sum equality under H-SIG, not unrestricted operator equivariance.
- Cascade Foundations supersession: verified locally; banner lines `1–8` mark F4/F5/F7 as superseded by Paper XXXVI.
- Paper XXII shell/spectrum: verified locally. Paper XXII `204–223` supports shell = conjugacy class and `177–193` gives the spectrum; the `\varphi` forms in RH are equivalent to the `\sqrt5` forms there.
- Adaptive Closure Transport: verified locally. Lines `327–328` and `377–382` support the claim that the edge-space/projector lift is open.
- Transport Law: verified as broad motivation only, not as proof of the selection mechanism.
- aria-chess/H4O: not verifiable in this repository. The exact torus/eigenvalue/PAC numbers at `950–972`, `2042–2057`, and `2165–2172` remain external reports.
- Smart framework citations: primary files are not present in this repository. `docs/convergence-with-smart.md` repeats the architectural claims, but it is not an independent primary verification.
- Millennium companion drafts: BSD, Hodge, YM, and NS summaries are broadly consistent with their local files. P vs NP is not: RH bibliography `2823–2828` lists `H_{\sigma-irr-NP}` plus structural completeness, but `pnp-formal.tex:192–197` requires three inputs: `H_{\sigma-irr-NP}`, `B_{\sigma-P}`, and structural completeness.

**4. Tightness**

- `1794`: replace “canonical closure flow” with “explicit polynomial filter flow.”
- `1796–1799`: replace “upgrades ... from hypothesis to theorem” with “proves a finite-dimensional filter analogue of the former hypothesis.”
- `1919–1927`: replace the corollary by a remark; it does not discharge `H_attr`.
- `2042` heading “Empirical support” should be “External empirical context”; line `2059` says “not as evidence,” contradicting the heading.
- `1964`: call the trace identity a “schematic target,” not a hypothesis “holds,” until the symbols are defined.

**5. Surface Issues**

- Wrong section reference at `1813`: `§\ref{sec:zhat}` should point to `Definition~\ref{def:F}` or `§\ref{sec:cascade}`.
- `m^K_K` and `\sum_{K\in K}` at `1979` are poor notation and likely to confuse readers.
- “double location” at `2021` should be “coincident reciprocal location” or “double pole at the same location,” depending on intended multiplicity.
- No unresolved LaTeX refs or missing bibliography keys found.

**6. Top Three Fixes**

1. Fix the `\Psi_t` over-claim at `1794–1801`, `1848–1877`, `1919–1927`, and `2057–2061`. It proves a custom polynomial filter theorem, not a cascade closure-flow theorem.

2. Either formalize or demote the trace identity at `1964–1997`. As written, it is underdefined and ill-typed despite being labelled a hypothesis.

3. Correct the P vs NP companion citation at `2823–2828`: include `B_{\sigma-P}` as a required bridge input, matching `pnp-formal.tex:192–197`.
