No clean exit yet. Round-4 fixed the three named issues only partially.

**1. Claim Audit**
- L136: Proposition `prop:phi` is proved, assuming `d_1^2>0` and `\Delta>0`.
- L177-L186: Hurwitz claim is false as written. `|\varphi-p/q| > 1/(\sqrt5 q^2)` does not hold for every rational `p/q`.
- L199-L204: `\pi/5` follows; the “complete set of pairwise cosines” is not proved from vertex-transitivity alone.
- L232-L275: Theorem `thm:600cell` is established within H1-H4 and ADE, assuming the table’s NN-angle facts.
- L277-L283: Corollary `cor:phi_determined` is still overclaimed. It is labelled derived, but the text admits the maximality proof is not supplied.
- L355-L391: Theorem `thm:F` is a plausible proof sketch within class `\mathcal C`; it still needs explicit statement that one dimension-compatible regulariser family is being axiomatized.
- L406-L414: Proposition `prop:properties` has no proof. Vertex minimality, barrier height, and uniform Hessian need supporting estimates.
- L436-L453 and L457-L467: `Z_2` factorisation and `\Fint=-\varepsilon^2\log(1-\eta)` are established.
- L469-L519: `prop:Fint_props` is only partially proved. Cluster decomposition uses an unstated “near basin centres” hypothesis; global maximum at `\psi_1=\psi_2` is not proved.
- L550-L555: `eq:Fint_spectral` is now correctly flagged as heuristic/open.
- L557-L561: The “low-pass filter” statement remains asserted, not proved.
- L655-L665: Fibre flatness is proved, but only because the lift is trivial.
- L678-L693: `prop:pion` overclaims the derived part. The two-state exclusion `i\ne j` does not prove two three-vertex nucleon support graphs are disjoint.
- L741-L754: Photon zero mode is graph-theoretic; photon exchange and coupling `\alpha` are identified, not derived. The label is acceptable.
- L816-L898: `thm:factor` proves only a conditional algebraic perturbation statement after importing the key coefficient. The numerical `O(\alpha^2)` bound at L892-L897 is unsupported without a bound on the hidden constant.
- L945-L956: Deuteron arithmetic checks out, but the proposition should be explicitly “computed conditional on Proposition 9,” not theorem-like.
- L1185-L1193: `prop:triple` is valid algebra given the SmartRadius assignment rules, but those rules are modelling inputs.

**2. Internal Consistency**
- L277-L283 conflicts with the epistemic classification: a derived corollary cannot simultaneously be an asserted classification result with no proof.
- L278 conflates “`H_4`-invariant” with “closed under the `2I` action.” These are not equivalent.
- Cross-references resolve: I found no undefined `\ref`/`\eqref` labels and no duplicate labels.
- The chordal/geodesic cleanup is mostly consistent at L191 and L362.
- The spectral heuristic is still used too strongly later: L851-L852 says `g(r)` is “evaluated from” the heuristic formula.
- `thm:factor` labels a proposition; not fatal, but sloppy.
- No missing citation keys found in `references.bib`.

**3. External Consistency**
- Paper V mass claim at L83-L84 is locally verified in `papers/paper-v/paper-v.tex`, but Paper V explicitly frames the 0.014% result as conditional numerical correspondence, not theorem-level derivation.
- Paper VI gauge-structure claim at L85-L86 is only a structural reinterpretation; Paper VI says this explicitly, so “produced gauge structure” is too strong.
- Paper XIII ansatz claim at L89-L90 and L521-L527 is verified: four weighted components with unspecified weights.
- Paper XXII claims are verified: closure functional as definition, integer eigenvalues `{9,12,14,15}`, `94+26` split, and `\alpha^{-1}=137+\pi/87` as numerical correspondence.
- SmartRadius claims are verified: `r_\pi=\pi\bar\lambda_p`, `r_p=4\bar\lambda_p`, neutron radius, and LO deuteron decomposition all appear locally.
- Paper IV is not directly verifiable as `papers/paper-iv/paper-iv.tex`; no such path exists. Similar heat-kernel/cumulant material appears in `papers/pemr/pemr.tex`, but the cited source path is ambiguous.
- P2/cascade-algebraic-substrate verifies the 600-cell spectrum and `94+26` split, with character-table imports.

**4. Tightness**
- L99: change “Spectral decomposition ... isolates” to “A heuristic spectral projection motivates.”
- L277: change corollary to “Remark/Conjectural classification note,” remove `\blacktriangleright`.
- L557: change “act as a low-pass filter” to “are assumed/modelled as a low-pass filter.”
- L696: change “is a derived structural count” to “is conditional on the support-graph and disjoint-support assumptions.”
- L896: change “well below experimental precision” to “below experimental precision if the omitted coefficient is order one.”
- L1094: change “is no longer a construction” to “is characterised within the stated axiomatic class.”
- L1109: remove “bringing all six charge radii ... within `1\sigma` against both anchors”; that is broader than what is established here.

**5. Surface Issues**
- L928 gives `137+\pi/87 = 137.0361058...`; actual value is `137.036110260...`.
- L928 also says `0.78 ppm`; using the actual value gives about `0.811 ppm` and about `5290\sigma` for CODATA 2022 uncertainty `2.1e-8`.
- L410 uses `d_{\min}` without defining it in this paper.
- L278 “finite `H_4`-invariant subsets ... i.e. `2I` action” is a notation/substance error.
- L521 “with no free parameters” is too broad once later sections import `\alpha`, `\pi\bar\lambda_p`, and the kinematic coefficient.
- Several theorem/proposition labels do not match epistemic status: L277, L945, L1185.

**6. Top Three Fixes**
1. Fix L277-L283: either supply the `H_4`-invariant classification proof or downgrade `cor:phi_determined` to an unproved classification assertion. Also separate `H_4` from `2I`.
2. Fix L678-L693 and L816-L898: the six-channel count and `(6+\alpha)` factor are still not derived from `\Fint`; make them explicitly conditional throughout, including the summary table.
3. Fix hard mathematical/numerical errors: Hurwitz at L177-L186, unproved `\Fint` property claims at L469-L519, and the wrong alpha decimal/ppm at L928.
