Publication ready: no.

**1. Claim Audit**
- `"Direct diagonalisation ... recovers 9 distinct numerical eigenvalues ... each numerical eigenvalue agrees ... with an imported exact algebraic representative"` (lines 100-101). This is not proved in the manuscript. It is a computational report backed by a script, and the paper is honest that the exact forms are imported, not certified here. Acceptable as a computational proposition, not as an internal proof of the exact algebraic spectrum.
- `"Among the eleven regular polytopes ... the 600-cell is the unique one whose vertex-graph Laplacian spectrum simultaneously contains ... irrational √5-values and the rational eigenvalue 15"` (lines 127-132). The proof at lines 135-162 does establish this, conditional on the imported spectral tables for the non-600-cell cases and the standard `λ_max(L) ≤ 2d` bound. Narrow claim, adequately proved.
- `"Direct computation ... shows that at shells d in {2,3,4} vertices split into two populations ... The reflection coefficient R=c/b is:"` (lines 190-191). No proof is given in the manuscript. This is another script-backed computation, not a manuscript proof.
- `"R_D(4)=15 exactly ... via first-passage"` (lines 214-225). The computer-assisted proof at lines 228-257 is adequate for the first-passage ratio, conditional on the equitable-partition enumeration carried out in code. It does not prove that this first-passage ratio is the correct shell-4 coefficient in the mass ansatz. The paper correctly discloses that gap at lines 259-260 and 302.
- `"Assume the standard Hopf decomposition ... The Laplacian eigenvalues of each C_10 fiber are ..."` (lines 265-270). Fine as stated: conditional on the decomposition, the cycle-spectrum computation is exact.
- `"Then the mass-formula exponent takes the numerical value ... 15.61742, matching the Paper I/IV exponent 1265/81 ..."` (lines 310-315). The arithmetic checks. This corollary proves only a numerical proximity under the adopted ansatz and the adopted use of `R_D(4)=15`; it does not recover Paper I/IV algebraically. The paper is careful about that.
- `"α^{-1} = 87 + 50 + π/87 ≈ 137.036"` with `~0.81 ppm` error (lines 345-350). Arithmetic is correct. This is a numerical identification, not a derivation, and the text says so.
- `"Five entries (Z, charm, W, down, bottom) are forward-derivable from the model's geometric primitives"` (line 425). This is overstated. What is forward-derivable is the coefficient decomposition once the chain architecture is already postulated. The particle-level chain step is not derived from the primitives. The script named at line 578 verifies coefficient identities, not particle correspondences.
- `"13 particle-mass correspondences ... average relative error 0.014%"` (lines 431-459). The displayed error column averages to about `0.01377%`, so `0.014%` is numerically fair. But this remains a mixed-target summary over incompatible mass schemes and several model conventions; the paper now says that, which is the right qualification.

**2. Internal Consistency**
- The central new strengthening is not propagated consistently. The paper says `5 forward + 5 reverse` at lines 41 and 425, but still says `"ten reverse-identified chain ratios"` at lines 60, 73, and 631, and `"the chain ratios ... are reverse identifications rather than forward derivations"` at line 68. That is a direct contradiction on the paper’s main update.
- The manuscript oscillates between the careful formulation `"five forward-derivable chain ratios"` (line 578) and the stronger particle-level wording `"Five entries ... are forward-derivable"` (line 425). Those are not the same claim.
- Cross-references appear internally coherent by inspection: `Theorem~\ref{thm:RD4}`, `Corollary~\ref{cor:proton}`, `Table~\ref{tab:chain}`, and the section references point to the right objects. I do not see a semantic mismatch in the `\ref`/`\eqref` usage.

**3. External Consistency**
- Paper I attribution is not locally auditable as cited. The bibliography entry `SmartI` in [references.bib](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/paper-v/references.bib#L1) names `"Proton-to-Electron Mass Ratio from Closure Geometry and Graph Invariants"`, but I do not find a local source file with that title. The claims at lines 50 and 283 therefore cannot be verified locally as cited.
- Paper IV attribution is locally verifiable. [pemr.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/pemr/pemr.tex#L20) matches the cited title. It does contain the proton/electron ansatz `m_p/m_e = φ^{1265/81}` and a tensor-product/state-counting derivation of `C(S)` with a uniqueness proposition at [pemr.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/pemr/pemr.tex#L281) and [pemr.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/pemr/pemr.tex#L321).
- Paper III attribution is locally verifiable. [spectral-dimension.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/spectral-dimension/spectral-dimension.tex#L37) states the exact `λ=15=ΔC` correspondence and the F4 extension.
- Paper II attribution is locally verifiable. [lepton-generations.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/lepton-generations/lepton-generations.tex#L39) contains the shell-extension no-go theorem and the conditional winding operator; the boundary assignment `μ,τ : {1}, w=2,3` appears at [lepton-generations.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/lepton-generations/lepton-generations.tex#L196).
- Paper XXX attribution is locally verifiable. [paper-xxx.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/paper-xxx/paper-xxx.tex#L46) explicitly presents the Born-rule discussion as conditional on stated hypotheses. That matches line 329 here.

**4. Tightness**
- Line 425: `"Five entries ... are forward-derivable"` is too strong. Edit to: `Five chain coefficients are forward-traceable to explicit 600-cell primitives once the chain architecture is fixed.`
- Lines 60, 73, 631: `"ten reverse-identified chain ratios"` is now false. Edit to: `ten chain correspondences, split into five forward-traced coefficients and five reverse-identified correspondences.`
- Line 350: `"the same spectral data supports"` is too strong for a numerology-level identity. Edit to: `the same spectral data admits the numerical identification`.
- Line 614: `"The strongest internal support is"` overstates what is mostly narrow uniqueness plus numerical coincidence. Edit to: `The main internal motivation is`.

**5. Surface Issues**
- Line 319: `1/(φ^3) = (√5 - 2)/1 = 2φ - 3` is sloppy. The `/1` is pointless and looks like a typo.
- The old wording about `"ten reverse-identified chain ratios"` survives in multiple places. That is not cosmetic; it reads as uncorrected copy.
- The paper relies heavily on scripts for Propositions at lines 100 and 190. If this is meant for journal submission, the manuscript should mark them uniformly as computer-assisted statements or give compressed proof sketches in the text.

**6. Top Three Fixes**
1. Fix the central contradiction about chain status at lines 60, 68, 73, and 631. As written, the paper both claims and denies the new `5 forward / 5 reverse` strengthening.
2. Repair the Paper I attribution at lines 50 and 283. On the stated criterion, this is not publication-ready while the cited source is not locally identifiable and auditable.
3. Downgrade `"forward-derivable"` at lines 411-425 to what is actually shown: coefficient identities, not particle-level derivations. The current wording oversells the mathematical content of the new script check.
