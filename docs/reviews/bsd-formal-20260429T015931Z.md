Line numbers refer to `papers/millennium-bsd-formal/bsd-formal.tex`.

**1. Claim Audit**
- Line 151: Proposition “Formal consequence...” is only a tautology from `B_BSD,rank` and `B_BSD,spec`, and implicitly `B_BSD,conv` because `T_E(1)` is otherwise undefined. The proof at lines 469-486 cites a remark as “Theorem”; rewrite the proof to cite the three hypotheses directly.
- Line 233: Termwise bound is false as stated. The proposition claims `≤ 2 log p`, but the proof derives `2 φ log p` at lines 247-250 and then “absorbs” `φ` into an exact inequality. That is not legitimate.
- Line 265: Absolute convergence on `Re(s)>1` is essentially correct only after fixing the termwise constant and the operator domain. Line 280 falsely says `Σ_p log p p^{-s}` equals `-ζ'/ζ`; that Dirichlet series includes prime powers.
- Lines 283-290: Numerical “bounds” are finite truncations from primes `p≤1000`, not bounds on the infinite operator. The `Re(s)=1.0` value is especially misleading because the infinite Hasse majorant diverges at `s=1`.
- Line 330: `B_BSD,conv` is too weak/incomplete. It allows a simple pole at `s=1` but later uses `T_E := T_E(1)`. A finite-part or residue prescription must be part of the hypothesis itself.
- Line 347: `B_BSD,spec` is underdefined. The proof needs geometric multiplicity equal to `dim ker T_E`; the hypothesis itself does not say geometric/algebraic/spectral multiplicity.
- Line 358: “Classical Eichler-Shimura” is misstated. Eichler-Shimura is naturally for modular curves/Jacobians/newform quotients, not a direct canonical isomorphism `H^1(E,Z)⊗Q ≅ S_2(...)^{f_E} ⊕ ...` as written.
- Line 412: The corollary is just `B_BSD,rank` restated. Acceptable only if labelled as a restatement, not as derived content.
- Line 437: Correctly says the spectral identity is unproved, but line 475 then invokes it as a theorem. This is a serious internal contradiction.
- Lines 537-543 and 558-566: Examples are standard/plausible, but the cascade statements are only restatements of bridge hypotheses. They do not verify the cascade kernel.
- Lines 588-604: The 600-cell spectral count is locally supported by the cited script, but the script uses numerical eigendecomposition/rational recognition, not a fully exact symbolic verification. “Classification is exact” is stronger than the local artifact establishes.
- Lines 606-613: The transfer to BSD is entirely conditional on a new assumption: `T_E` polynomial in the 600-cell Laplacian. That assumption is not one of the three BSD bridge hypotheses and is not established in Section `sec:T_E`.
- Lines 717-738: New ARIA subsection is non-load-bearing as advertised. It names the three BSD bridge hypotheses correctly at lines 728-731, but `T_E = T_E(s)` at line 728 is sloppy: `T_E` is the regularised/f finite-part value at `s=1`, not the whole family.

**2. Internal Consistency**
- `T_E` is defined on `H^1(E,Z[φ])` at lines 221-230, but lines 391-407 admit this space is too small and the operator must live on an enlarged Hecke module. This is the central unresolved inconsistency.
- `v_φ(p)` at lines 228-230 is not defined coherently. Since `φ` is a unit in `Z[φ]`, a “φ-adic valuation of p” is not a standard valuation.
- Line 332 references `Definition~\ref{def:TE}` but means the `s`-regularised operator in `Definition~\ref{def:T_E_s}`.
- Cross-references resolve syntactically, but several labels misrepresent status: `thm:BSD` labels a proposition; `thm:specL` labels a remark explicitly saying no theorem is proved; `thm:cascES` labels a warning remark.
- The paper alternates between `B_{rm BSD,*}` and `H_{mathrm{BSD-*}}` at lines 571-573 and elsewhere. Use one notation.
- Line 607 cites Section `sec:T_E` for “`T_E` is a polynomial in `L`”; that section contains no 600-cell Laplacian construction.

**3. External Consistency**
- `CascadeFoundations` at lines 185 and 449: local foundation material supports φ/σ/closure vocabulary. I did not find a local trace formula identifying BSD `T_E` spectra with `L(E,s)`. The warning at lines 447-451 is accurate.
- `ConvergenceWithSmart` at line 576: local document supports only a descriptive bridge-axiom pattern for RH-like reductions, not BSD-specific hypotheses.
- `CascadeRHformal` at line 672: locally consistent. The RH paper’s strengthened remark treats the spectral bridge as conditional; it supports caution, not a BSD proof.
- `CascadeAttractorSpectrum` at line 590: locally supports `94/120 = 78.3%` and the displayed eigenvalue families, but not a BSD transfer.
- `CascadeAttractorDynamics` at line 622: source implements R1/R2/R3; R2/R3 are forced by explicit projection onto the σ-fixed subspace, so this is not independent BSD evidence.
- `ariaH4Olaw` at lines 645-650: not verifiable in this repository. The cited `aria-chess/...` paths are absent.
- New ARIA citation `docs/aria-closure-kernel.md` at lines 723-726: local file supports `C_φ = L_M + φ^{-2}I` and `G(x)=(φ/2)e^{-|x|/φ}`. The LHCb `r=0.983` claim at lines 733-735 points to external `aria/vfd-b-anomaly`, absent here.

**4. Tightness**
- Line 102: replace “`T_E`'s convergence theorem and its spectral identification together are...” with “If the boundary and spectral bridge hypotheses are assumed, `T_E` is intended to...”
- Line 171: replace “conditions are equivalent... to classical Kolyvagin and Gross-Zagier input” with “not known to be weaker than the corresponding classical BSD input.”
- Line 610: replace “the `78.3%` count transfers to spectral support” with “the count would be compatible with such a transfer, assuming the missing correspondence.”
- Line 735: replace “family-canonical member” with “candidate member of the same programme-level response-operator family.”

**5. Surface Issues**
- Line 25: `\newcommand{\Sha}` may conflict with `amssymb`; use `\renewcommand` only if needed or avoid redefining.
- Line 75: `\textdelta` is likely undefined with the loaded packages; use `$\delta$`.
- Line 280: remove literal `∎`; `amsthm` supplies proof endings.
- Lines 600 and 603: `\bm` is used without loading `bm`.
- Bibliography: Eichler-Shimura entry is not a usable citation; Wiles alone is not the full modularity theorem for all elliptic curves over `Q`.
- Lines 121-128 omit bad Euler factors while calling the product `L(E,s)`. Say “good-prime part” or include bad local factors.

**6. Top Three Fixes**
1. Fix the operator architecture: define the actual enlarged Hecke module, define `v_φ(p)`, and stop presenting `T_E` as a well-defined operator on `H^1(E,Z[φ])` while later disavowing that domain. Lines 221-230, 338-344, 391-407.
2. Repair the conditional proof: include `B_BSD,conv`, define finite-part `T_E(1)`, define geometric multiplicity, and cite hypotheses instead of `thm:specL`. Lines 330-351, 469-486.
3. Demote or quarantine the evidence sections: the 600-cell and ARIA material is non-load-bearing and does not verify BSD bridges. Lines 606-613 and 717-738 need softer wording and explicit “programme context only” language.

Verdict: not publication-ready. The new ARIA subsection is non-load-bearing, but the paper still has residual must-fixes in the load-bearing operator definition and conditional proof.
