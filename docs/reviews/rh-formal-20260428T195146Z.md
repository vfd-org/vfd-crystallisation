**1. Claim Audit**

- “`ζ_cas(s) ... is identically equal to ... ζ(s)`” (`rh-formal.tex:400-410`): established, but only because `ζ_cas` is defined as the classical Euler product plus analytic continuation. This is a tautological identification, not a construction.
- “Critical line as fixed set...” (`415-429`): established. The proof correctly uses the anti-holomorphic involution `s -> 1 - \bar{s}`, not holomorphic `s -> 1-s`.
- “Dedekind reduction” (`432-441`): established by the later Euler-product comparison and standard quadratic factorization.
- “Conditional critical-line theorem” (`465-470`): established, but the hypothesis is exactly RH in different notation (`1633-1645`).
- “Splitting law for rational primes” (`644-668`): established by standard quadratic-field theory.
- “Pentagonal torsion in `2I`” (`701-708`): proof establishes the orbit count once the order-10 element and `|2I|=120` facts are accepted.
- “Shell--class bijection” (`725-729`): locally verified in Paper XXII; import is legitimate.
- “`K`-multiset for the script-selected `τ`” (`762-773`): established only for the selected representative. The no-class-independence caveat is necessary and correctly present.
- “Exact closed form; M1” (`798-819`): established conditional on the `K`-multiset. The displayed `[z^10]` arithmetic is consistent.
- “Mellin pole-symmetry; M2” (`836-855`): algebraically established. Wording should say periodicity comes from the exponential parametrisation, not “multi-valuedness of `log φ`.”
- “Hecke obstruction” (`899-912`): plausible but underproved as written. To rule out the proposed local matching, spell out the norm-one idele quotient/ratio argument; the current paragraph gestures at Neukirch but does not prove the obstruction.
- “ζ uniqueness” and corollary (`1045-1100`): established; in fact `(U1)` plus meromorphic continuation already does the work.
- `η_p` convergence (`1136-1145`): established.
- Euler product / functional equation / analytic structure (`1212-1238`, `1242-1269`, `1285-1311`): established only as classical imports from the definition `ζ_cas := ζ`.
- `ζ_cas^{Z[φ]} = ζ_K`, factorization, and RH/GRH product corollary (`1374-1440`): established.
- “Galois action on the spectrum, conditional” (`1501-1522`): established under H-Mat. It does not follow from F5, and the paper now says so.
- “Spectral σ-attractor decomposition” (`1704-1727`): locally verified against Paper XXII. The `φ`-form matches the `√5` table.
- “σ-paired mode suppression by the cascade closure flow” (`1840-1868`): proof establishes suppression for the specifically constructed polynomial filter `Ψ_t`. It does not establish this for the original closure functional.
- “Discrete-level statement of H_attr” (`1911-1918`): overclaims. It treats the designed polynomial filter as if it discharged the prior closure-flow hypothesis.
- “Candidate cascade Mellin trace identity” (`1956-1988`): not a well-defined mathematical hypothesis. `K^*`, `λ_K^\pm`, `h_K^{pair}`, `R(h)`, and `m^K_K` are symbolic placeholders, not defined objects.
- Mellin convention and zero-set propositions (`2190-2228`, `2263-2290`): established as a convention plus classical functional-equation symmetry.
- External H4O numerical claims (`946-966`, `2034-2050`): only partially verified in the neighbouring `aria-chess` artifacts. The Lyapunov and 8-pair covariance values are present; the exact torus-certification numbers and `MI=0.046` were not found in the cited artifact.

**2. Internal Consistency**

- Major conflict: lines `281-294` say `\widehat ζ` encodes arithmetic of `Z[φ]` prime ideals and observer closure. But `\widehat ζ` is later explicitly a finite Artin--Mazur zeta, not an Euler product over primes (`892-917`), while prime-ideal arithmetic belongs to the separate Dedekind extension (`1331-1336`). This is the largest remaining internal overclaim.
- Cross-references and bibliography keys resolve by static inspection.
- Lines `1804-1806` and `1824-1826` refer to the original closure functional as being in `§zhat`; it is defined in `§cascade`, Definition `def:F`.
- `H_trace` notation is internally unusable: `K` is both set and index, and `m^K_K` is not defined as a function.
- The paper simultaneously says `Ψ_t` is not the original closure dynamics (`1820-1837`) and then uses it to “discharge” finite `H_attr` content (`1911-1928`, `2048-2053`). That is inconsistent in force.

**3. External Consistency**

- Paper XXXVI supports the narrowed F5 import: factor-2 finite-sum equality under H-SIG, not unrestricted operator equivariance (`paper-xxxvi.tex:539-562`).
- Paper XXII supports the shell-class import and `94+26` spectral split (`paper-xxii.tex:204-223`), and the spectrum table matches after rewriting `√5` in terms of `φ`.
- `AdaptiveClosureTransport` supports the claim that the edge-space/projector lift remains open (`adaptive-closure-transport.tex:327-328`, `377-382`).
- `TransportLaw` is only programme-level motivation; the RH draft correctly does not treat it as proving the missing selection mechanism.
- `docs/convergence-with-smart.md` supports architectural similarity, not formal equivalence.
- Smart Paper II and Paper C claims are broadly verified in neighbouring repos: ID* is a postulate, Bombieri--Lagarias is the conditional endpoint, and Paper C has Bridge/Shadow/Non-Circularity statements.
- Companion Millennium drafts do contain bridge hypotheses. However the Hodge draft still contains residual contradictory wording around “five” classes (`hodge-formal.tex:248-287`, `874-877`) despite also saying only three are complete and two weaker.

**4. Tightness**

- `281-294`: replace with “This paper’s `\widehat ζ` uses the `Z[φ]`/600-cell substrate but does not encode prime ideals; prime-ideal arithmetic enters only in the separate Dedekind extension.”
- `890-912`: replace “is obstructed” with “is obstructed by the following norm-one quotient argument...” and then supply that argument.
- `1786-1791`, `1911-1918`: replace “cascade closure flow” with “a canonical polynomial filter attached to `L`.”
- `1956`: replace the Hypothesis environment with an “Informal template” remark until all symbols are defined.
- `2034-2050`: replace “consistent with” by “not inconsistent with, but not evidence for.”

**5. Surface Issues**

- No unresolved `\ref`, `\eqref`, or bibliography keys found.
- Line `467`: `Hypothesis~\textup{H$_{\sigma\text{-fix}}$}` is typographically awkward; use `Hypothesis~$\mathrm{H}_{\sigma\text{-fix}}$`.
- Lines `844-845`: “multi-valuedness of `log φ`” is the wrong explanation; use “periodicity of the exponential parametrisation.”
- Lines `2491-2493`: “released January 2026” conflicts with the abstract’s “December 2025 and January 2026” for Smart Paper II.
- Several literal Greek characters are used; the preamble comments address this, so this is acceptable if pdfLaTeX mapping actually works.

**6. Top Three Fixes**

1. Fix the prime-ideal/rung-crossing overclaim at `281-294`. The paper does not prove that `\widehat ζ` encodes `Z[φ]` prime ideals. This conflicts with the finite dynamical-zeta description at `892-917`.

2. Demote or fully define `H_trace` at `1956-1988`. As written it is not a mathematical hypothesis; it is an equation-shaped placeholder with undefined symbols.

3. Stop calling `Ψ_t` the closure flow in theorem-level language (`1786-1791`, `1911-1928`, `2048-2053`). The proof only covers a hand-built polynomial filter, not the original cascade closure functional or any continuum selection principle.
