All line references are to `papers/millennium-pnp-formal/pnp-formal.tex`.

**1. Claim Audit**

- L173-183, “Restricted-model separation…”: not established cleanly. The stated proposition assumes both `H_{\sigma-irr-NP}` and `B_{\sigma-P}`, but the downstream corollary at L402-405 assumes only `H_{\sigma-irr-NP}` while its proof uses `B_{\sigma-P}` at L410.
- L192-198, “Conditional P vs NP…”: proof at L501-525 does not establish the claim. It assumes only Conjecture 4.1 explicitly, uses Lemma L520 and Hypothesis L508 without restating their hypotheses, and asserts unsupported absence of rung-local, `\varphi`-adic, and `2I` structure at L512-515.
- L253-255, CT equivalence: only supported locally by `cascade-dynamics.md` proof sketches, while `cascade-infrastructure-plan.md` says D5-D10 are non-citable framework notes. Not theorem-grade as cited.
- L275-279, “Generic σ-irreducibility”: not proved. Statement says any `\alpha>0`; proof only gives decay for `\alpha>1` at L301-302. The per-clause `≤1/2` bound at L289-293 is asserted, not proved.
- L307-310, corollary on generic 3-SAT: only follows from the flawed theorem, and only after restricting to `\alpha>1`.
- L350-369, complexity status of σ-irreducibility: internally unstable. Definition L264-265 uses a two-block partition `A \sqcup B`, but L355-356 invokes Bell-number growth for arbitrary set partitions. Also, “Verifying `P` takes polynomial time” at L365-367 is not shown: checking equality of all restrictions over assignments to `A` is exponential unless a further structural certificate is supplied.
- L379-388, “at least as hard as NP=coNP”: not proved. This needs a reduction; the current text only gestures at “full Bell-partition combinatorics,” which conflicts with the binary partition definition.
- L392-398, Conditional NP-containment: tautological and established.
- L402-413, Separation corollary: false as stated. Missing `B_{\sigma-P}` in the assumptions.
- L455-459, structural completeness conjecture: acceptable as conjecture, but “Equivalently” is not formal because “exploits” and “has none of these properties” are undefined.
- L531-547, BGS evasion proposition: proof does not establish non-relativization. Oracle-independence of the input predicate is not enough; the lower-bound hypothesis itself would need oracle-stable formulation.
- L551-570, RR evasion proposition: establishes at most “not literally a Razborov-Rudich function property.” It does not prove that an eventual separation proof avoids naturalization.
- L574-589, AW evasion proposition: field specificity does not prove non-algebrization. Needs a formal algebraic-oracle analysis.
- L593-601, “Cascade evades three barriers”: not established. Should be a remark, not a theorem.
- L619-634, 600-cell spectral count: arithmetic is internally correct given the displayed spectrum: fixed multiplicity `94`, paired `26`, ratios `78.3%/21.7%`. But the cited script is numerical/heuristic, not an exact proof of the spectrum.
- L648-667, finite-noise diagnostic: locally matches the cited script as a simulation result. The later interpretation at L690-692 overstates it as “the spectral form of the cascade closure functional in the continuum limit”; the script implements an explicit projector.
- L671-677, aria H4O witness: correctly labelled external, but the cited local paths are not present.

**2. Internal Consistency**

- L168 says `P_\sigma^{cas}` is “strictly smaller” unconditionally. That is exactly the open `B_{\sigma-P}` content later.
- L433-435 says Theorem 1.1 follows under `H_{\sigma-irr-NP}` and Conjecture 4.1. Wrong: it needs `B_{\sigma-P}`, not structural completeness.
- L501 proof of “Theorem” 1.2 is actually proof of Proposition 1.2 and omits two hypotheses in the opening sentence.
- L512, “σ-irreducible instances have NO σ-symmetry (by definition)” is false. Irreducibility under the stated partition condition is not the same as absence of σ-symmetry.
- L724 calls `thm:genirr` a Heuristic, but it is declared as a theorem at L275.
- `thm:sigmaPlim` labels a hypothesis, yet L410, L508, and L541 call it a theorem.
- `thm:separation` and `thm:conditional` label propositions, yet the text repeatedly calls them theorems.

**3. External Consistency**

- `CascadeDynamics`: local source contains CTM/TM equivalence sketches, but not a theorem-grade bit-level simulation proof. Repo infrastructure notes explicitly warn not to cite D5-D10 as theorems.
- `CascadeRHformal`: verified. Its strengthened conditional remark supports only the same kind of conditional/evidential architecture, not a bridge theorem.
- `CascadeAttractorSpectrum`: supports the numerical spectral diagnostic, but not the manuscript’s “exact in `\mathbb Q(\sqrt5)`” proof status.
- `CascadeAttractorDynamics`: supports R1/R2/R3 simulation values and the fact that the explicit projector dominates.
- `ariaH4Olaw`: not locally verifiable; cited files are absent.
- `ConvergenceWithSmart`: I found no local formal meta-theorem establishing a “Bridge Axiom Pattern”; use only as descriptive analogy.

**4. Tightness**

- L168: replace “is strictly smaller” with “is intended to define a restricted subclass; strictness is Hypothesis `B_{\sigma-P}`.”
- L275: replace theorem heading with “Heuristic/conditional estimate” unless the per-clause bound is proved.
- L402: add `B_{\sigma-P}` to the corollary assumptions.
- L593: replace theorem with “Remark: the present formulation is not obviously of the standard relativizing/natural/algebrizing forms.”
- L690-692: replace “the spectral form of the cascade closure functional in the continuum limit” with “the explicit spectral projector used in this diagnostic.”
- L774-775: replace “If Conjecture… can be proved” with “If Conjecture 4.1, `H_{\sigma-irr-NP}`, and `B_{\sigma-P}` can all be proved.”

**5. Surface Issues**

- Undefined macros: `\textgamma` at L76/L202/L427, `\bm` at L631/L633, `\HH` at L637.
- Unicode Greek `σ` appears directly in math/text throughout; this will break or be fragile under pdfLaTeX unless the toolchain is explicitly Unicode-capable.
- The bibliography item `ariaH4Olaw` points to missing paths.
- The theorem labels are misleading: `thm:*` used for propositions and hypotheses.
- L718 says Theorem 6.4 was “demoted,” but it is still a theorem environment.
- Capitalization/style is inconsistent: “boolean” L263, “INPUT FORMULA” L538, “FORMULAS” L557.

**6. Top Three Fixes**

1. Fix the main dependency logic at L402-413, L433-435, L501-525, and L774-775. Every separation/P-vs-NP statement must explicitly require all needed inputs: `H_{\sigma-irr-NP}`, `B_{\sigma-P}`, and, for classical P vs NP, structural completeness.

2. Rebuild σ-irreducibility from the definition up: define the σ-action on variables/formulas, resolve binary partition vs Bell partition, and either prove or withdraw the polynomial verification and generic irreducibility claims at L275-303 and L350-388.

3. Demote the barrier-evasion theorem and unsupported evidence transfer: L531-601 should become programmatic remarks, and L619-692 must distinguish exact spectral arithmetic, numerical simulation, and unproved closure-functional/NP-witness transfer.
