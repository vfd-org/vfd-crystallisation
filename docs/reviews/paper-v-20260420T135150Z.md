Publication ready: no.

**1. Claim Audit**
- `[Lines 100-103]` “Direct diagonalisation … recovers 9 distinct numerical eigenvalues … Each numerical eigenvalue agrees … with an imported exact algebraic representative.” This is not proved in the text. It is a computational claim deferred to a script plus imported tables. Framed as such, it is acceptable; it is not a self-contained proposition.
- `[Lines 127-132]` “Among the eleven regular polytopes … the 600-cell is the unique one whose vertex-graph Laplacian spectrum simultaneously contains (i) values in $\mathbb Q(\sqrt5)\setminus\mathbb Q$ and (ii) the rational eigenvalue 15.” The proof at lines 135-160 does establish exactly this stated theorem, conditional on imported spectral tables for the non-600-cell cases and on Proposition `thm:eigenvalues` for the 600-cell. This is the cleanest theorem in the paper.
- `[Lines 190-191]` “Direct computation … shows that at shells $d\in\{2,3,4\}$ vertices split into two populations … The reflection coefficient $R=c/b$ is:” No proof is supplied. A table follows. This is an assertion plus script pointer, not an established proposition in the manuscript.
- `[Lines 214-225]` “$R_D(4)=15$ exactly … via first-passage …” The proof at lines 228-256 is adequate for the stated computer-assisted theorem. The equitable-partition step is the load-bearing move; once accepted, the linear-system calculation yields the stated ratio. The theorem proves the first-passage ratio, not the mass-formula coefficient as such.
- `[Lines 265-270]` “Assume the standard Hopf decomposition … The Laplacian eigenvalues of each $C_{10}$ fiber are …” This is correct as a conditional proposition. Given the assumption, the cycle-spectrum computation is trivial.
- `[Lines 310-315]` “Then the mass-formula exponent takes the numerical value … $15.61742$ …” The proof at lines 318-323 establishes the numerical substitution under the stated assumptions. No issue.
- `[Lines 345-350]` “$\alpha^{-1}=87+50+\pi/87\approx137.036$ … match is at the $\sim0.81$ ppm level.” Arithmetic only. Established as a numerical coincidence, not as a derivation. The text is mostly honest about that.
- `[Lines 59-60, 68, 431-459, 630]` “13 non-reference particles … average relative error $0.014\%$.” The manuscript reports this but does not derive it in-text beyond the table. That is acceptable as a computational summary. What is not acceptable is pretending this sits on a wholly “strictly certified” formula domain; it does not, because shell 4 still enters the ansatz only through a model-level identification, and shell 5 is openly conventional.
- `[Lines 39-43]` “12 of the 13 non-reference entries are within the strictly certified domain of the mass formula.” Over-claimed. Only one open convention is shell 5, but shell-4-dependent entries also rely on the unproved identification “first-passage ratio = coefficient in Eq. (2)”. That is not theorem-level certification.

**2. Internal Consistency**
- `[Line 260]` “Theorem~\ref{thm:RD4} therefore closes precisely the coefficient $R_D(4)$ appearing in Eq.~\eqref{eq:exponent}.” This conflicts with the paper’s own more careful language at `[lines 302-303]` and `[line 619]`, which say the identification of the first-passage ratio with the mass-formula coefficient is a model-level convention. Theorem `thm:RD4` does not “therefore” close the coefficient. It closes a quantity later adopted as that coefficient.
- `[Lines 302 and 541]` the manuscript invokes a “shell-1 single-step value $c/b=1/6$,” but shell 1 is not covered by Proposition `thm:dual` or Table `tab:reflection`. Either prove/state the shell-1 intersection numbers, or stop citing shell 1 as established.
- `[Lines 39-43]` the abstract’s “strictly certified domain” language does not sit well with `[lines 302-303, 619-620]`. The paper itself says shell 4 enters Eq. `eq:exponent` by convention. That is not “strict certification.”
- Cross-references are mechanically fine. I found no broken `\ref`/`\eqref`.

**3. External Consistency**
- Paper I attribution at `[lines 50, 69, 283, 539, 598]`: partially verifiable if `papers/master-mass/master-mass.tex` is indeed the local source for Paper I. That file has the exact title from `references.bib`, states the closure invariant and $\Delta C=15` at `[master-mass lines 39-43, 148-153]`, and gives the three-order law and `1265/81` at `[236-245]`. If this is meant to be Paper I, the attribution is broadly consistent.
- Paper II attribution at `[lines 50, 515-516, 599, 632]`: cannot verify locally. I could not find the cited Paper II source in the repository.
- Paper III attribution at `[lines 50, 600, 613, 632]`: cannot verify locally. I could not find the cited Paper III source in the repository.
- Paper IV attribution at `[lines 50, 69, 283, 601, 606, 621, 632]`: cannot verify locally. I could not find the cited Paper IV source in the repository.
- Paper XXX attribution at `[lines 329, 622]`: verifiable and correctly hedged. `papers/paper-xxx/paper-xxx.tex` states the Born-rule result is conditional at `[lines 51-53]` and again at `[lines 857-861]`. Paper V’s usage here is accurate.

**4. Tightness**
- `[Lines 39-43]` Too strong. Suggested edit: “The strange-quark entry depends on an open shell-5 boundary convention; the other 12 avoid that specific open coefficient, though shell-4 uses a model-level identification of a proved first-passage ratio.”
- `[Line 260]` Too strong. Suggested edit: “Theorem~\ref{thm:RD4} supplies the first-passage value later adopted as the shell-4 coefficient in Eq.~\eqref{eq:exponent}.”
- `[Line 613]` Slightly too strong given the missing local source for Paper III. Suggested edit: “The strongest internal motivations offered here are …”
- `[Line 630]` Slightly too strong if read as theorem-backed. Suggested edit: “reports high-accuracy numerical correspondences” is fine; keep it and avoid any further suggestion of certification.

**5. Surface Issues**
- `[Line 319]` `(\sqrt{5} - 2)/1` is pointless and ugly.
- `[Lines 302, 541]` shell-1 `c/b=1/6` is used without prior statement of shell-1 intersection numbers.
- `[Line 245]` `\texttt{Python}'s \texttt{fractions}` is clumsy typesetting.
- `[Lines 329, 622]` “Born-rule” / “Born rule” hyphenation is inconsistent.
- `[Lines 489-500]` the assignment table tracks dependence on `R_D(4)` only. The strange row also depends on the shell-5 convention, which is the paper’s advertised open item. That omission is misleading.

**6. Top Three Fixes**
1. Fix the abstract over-claim at `[lines 39-43]`. “12 of 13 … within the strictly certified domain” is not defensible while shell-4 is still connected to Eq. `eq:exponent` only by convention.
2. Fix the theorem-to-ansatz slippage at `[line 260]`, and align it with `[lines 302-303, 619]`. Theorem `thm:RD4` proves a first-passage ratio, not the coefficient in the mass formula by itself.
3. Fix the attribution gap for Papers II-IV at `[lines 50, 69, 283, 515-516, 598-601, 613, 632]`. Either include the cited local sources in the repository or weaken those attributions to “claimed in prior preprints not included here.” On a narrow attribution criterion, this is still a publication blocker.

On the narrow substantive-math/attribution criterion: the new uniqueness wording is fine, and the `R_D(4)` qualification is substantially improved. The remaining blocker is the abstract’s “strictly certified domain” language, plus the fact that major internal-paper attributions still cannot be checked locally.
