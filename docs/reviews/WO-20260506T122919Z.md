**1. Claim Audit**
- “`V_600` is the set of `120` unit icosians…” `bekenstein-incidence.tex:118`: established by exact construction plus closure/norm checks in `verify.py:70-88`.
- “`σ(V_600)` meets `V_600` exactly along the 24 axis/half-type vertices” `bekenstein-incidence.tex:137`: established computationally by `verify.py:152-155`, but this is not separately catalogued as its own claim.
- “`T_tau`: 12 cycles each of length 10” `bekenstein-incidence.tex:161`: established by `verify.py:90-95`.
- “K-multiset `{72:1, 0:1, 52:5, 20:5}`” `bekenstein-incidence.tex:181`: established; shell ordering is exact in `group.py:65-112`.
- “`H` is the union of K=72 and K=0 cycles, `|H|=20`” `bekenstein-incidence.tex:192`: established by `verify.py:97-109`.
- “`H ≅ Dic_5` with `a^10=1`, `b^2=a^5`, `bab^{-1}=a^{-1}`” `bekenstein-incidence.tex:197`: established by `find_dic5_presentation` and generated-subgroup equality, `verify.py:111-131`.
- “Right cosets are whole `T_tau`-cycle unions; left cosets are 2-per-cycle” `bekenstein-incidence.tex:217`: statement is verified by tests, but the proof has a real logical gap. `bekenstein-incidence.tex:235-240` uses self-normalising `H` to infer that `g^{-1}τg ∈ H` forces `g ∈ H`; self-normalising `H` alone does not imply this for a single element `τ`. Add the stronger checked fact `g^{-1}τg ∉ H` for all `g∉H`, or prove `N_G(<τ>)=H`.
- “`H` is self-normalizing and non-normal” `bekenstein-incidence.tex:250`: established by explicit all-100 outside-elements check, `verify.py:136-147`.
- “`V_24` is the σ-fixed sublattice, 8 axis + 16 half-type points” `bekenstein-incidence.tex:277` and `:285`: established.
- “Coset incidence: every left/right coset has 4 fixed, 16 mobile” `bekenstein-incidence.tex:303`: established by `verify.py:159-195`; theorem is computational, not theoretical.
- “τ-independence over all 24 order-10 elements; 6 distinct conjugate subgroups” `bekenstein-incidence.tex:341`: established by `verify.py:197-305`.
- “`S/A=1/4` per coset and aggregate `20/80`” `bekenstein-incidence.tex:402`: follows from the incidence theorem and is verified.
- No conjectures are present.

**2. WO Acceptance Audit**
- B1-B7: resolved except B1/B5 have the proof-gap noted above; implementation verifies both coset sides.
- AC1: substantively resolved, but filename is `bekenstein-incidence.tex`, not literal `paper.tex`.
- AC2-5: resolved.
- AC6: partially resolved; theorem is correct, but `lem:cosetkpair` proof needs the stronger normalizer/conjugacy statement.
- AC7-9: resolved; LQG normalisation caveat is present.
- AC10: mostly resolved; appendix documents `verify.py`, but not the shared pytest command.
- AC11-12: resolved. I ran both: `verify.py` exits 0; shared pytest reports `35 passed`.
- Open questions: author line resolved; venue and length not touched; `τ_σ` is only out-of-scope; Hawking/PBH corollary omitted in the derivation.

**3. Catalogue Audit**
- All main D/L/T/N objects are represented.
- Defects: catalogue labels/sections are loose. `CATALOGUE.md:11-12` says `def:galois` is §2, but it is in §4 at `bekenstein-incidence.tex:271`. D2 omits actual label `def:icosian`; D3-D7 omit available labels.
- The intersection identity `V_600 ∩ σ(V_600)=V_24` is only folded into L6 notes, not given its own ledger entry despite being a verified computational claim.
- No catalogue entry is unsupported by the derivation.

**4. Attribution / External Consistency**
- WO references to programme scope check out: `NARRATIVE.md:60` and `:68` say Paper 1 is group-theory and `4/16=1/4`.
- `NOTATION.tex:7-10` and `:26` define the shared symbols as claimed.
- `icosian.py:1-9` and `:112-157` support exact arithmetic and the 120-vertex construction.
- `cascade-algebraic-substrate.tex:918-928` does say the `2I/2T` quotient language is not used as theorem support.
- Local tests do cover the claims, but `test_dic5_subgroup.py:47-48` still has a false docstring saying left cosets are exactly one K52 cycle union one K20 cycle. The assertions at `:81-96` correctly test 2-per-cycle.

**5. Sim Correctness**
- `verify.py` is a deterministic exact certificate, not a statistical simulation; χ²/KL/null-hypothesis questions do not apply.
- It implements the main finite claims correctly: construction, closure, Dic5 presentation, self-normality/non-normality, left/right incidence, aggregate ratio, and τ-independence.
- Weakness: it does not explicitly assert the proof-level claim `g^{-1}τg ∉ H` for `g∉H`; it only verifies the resulting left-coset 2-per-cycle structure via shared tests.

**6. Tightness**
- `bekenstein-incidence.tex:235-240`: replace the self-normalising inference with the stronger finite check or a lemma `N_G(<τ>)=H`.
- `bekenstein-incidence.tex:87-96`: “reproduces the universal coefficient” should be “matches the numerical coefficient under the stated interpretation.”
- `SCOPE.md:5` and `:25` remain stronger than the derivation; do not ship those lines unchanged as public-facing scope.

**7. Top Three Fixes**
1. `bekenstein-incidence.tex:235-240`: fix the invalid proof inference in `lem:cosetkpair`.
2. `papers/v600-programme/tests/test_dic5_subgroup.py:47-48` and `group.py:140-142`: remove stale “left cosets are whole cycle unions” prose.
3. `CATALOGUE.md:11-32`: correct labels/sections and add a ledger entry for `V_600 ∩ σ(V_600)=V_24`.

**8. Verdict**
Publication ready: no.

The theorem spine and certificate pass, but the current written proof of `lem:cosetkpair` still contains a substantive group-theoretic inference that does not follow from the cited hypothesis.
