Requested round-3 checks first.

(a) Fixed: the old “no longer an external assumption” overclaim has been replaced by the restricted-derivation / upgrade-requires-completeness language at lines 641 and 895.

(b) Mostly fixed: lines 399 and 761 now say the `N=2` case is “not covered here.” But line 950 still says “with `N = 2` handled separately.” That reintroduces the old overclaim.

(c) Fixed: line 856 now correctly says the closure Hessian gives only a “candidate local scale,” conditional on an unproved bridge theorem.

(d) No broken Paper XXI cross-references remain. I found no unresolved `\ref`/`\eqref` labels by static pass, and no lingering XXI theorem-number citations that point nowhere.

**1. Claim audit**

- Lines 323-341, Proposition `Conditional Born weighting from stationary sector measure`:
  “Then … `P(\Phi_i \mid \Ocal)=...=|c_i|^2+O(\text{overlap})`.”
  This is proved as stated, but only as a five-hypothesis conditional lemma. The proof does not derive measurement sampling from closure; it assumes it in (4). The text is now honest about that.

- Lines 446-450, Proposition `Sector-preserving unitaries induce sector-probability covariance on sector-supported states`:
  “`P_{f(i)}(T\Psi)=P_i(\Psi)`.”
  The proof does not establish the proposition as written. At lines 454-464 it silently replaces the paper’s conditional probability by the unconditional formula `P_i=|c_i|^2`. That identification in this paper requires completeness/state-normalisation assumptions from Route C, which are not assumed in Proposition 446. If the observer is partial, the denominator matters. The statement may still be salvageable, but the proof needs either:
  1. an added completeness hypothesis, or
  2. a rewritten proof showing covariance of the normalised ratio, not just of the numerator.

- Lines 495-509, Proposition `Closure Hessian as the Fisher metric...`:
  This establishes exactly what it says: a local Laplace-regime Fisher/Hessian identification on a configuration-space location family. The proof is adequate for that limited statement. No overclaim here.

- Lines 546-557, Proposition `Fubini--Study identification...`:
  This is now correctly framed. The proof establishes only that Paper XXI supplies the global projective/Kähler structure and that the closure-side computation gives at most a candidate local normalisation conditional on a missing bridge theorem. That matches the stated conclusion.

- Lines 596-602, Theorem `Non-contextuality of sector probability for observers complete-for-Ψ`:
  The proof establishes only a restricted, state-specific result. The theorem says that, and the proof matches it.

- Lines 612-620, Corollary `Restricted (U2)...`:
  The claim is directionally right, but the wording is too strong. “satisfies axiom (U2) of Theorem `born_unique`” imports a global theorem axiom into a restricted setting. What is actually shown is a restricted analogue of (U2) on the specified observer/state class.

- Lines 650-669, Theorem `Born weighting uniqueness...`:
  Given hypotheses (U1)-(U4) and `\Mcl(\Ocal)=\mathbb{CP}^{N-1}`, the proof is fine. The theorem itself is conditional and honest. The problem is not the theorem; it is the surrounding prose, which still sometimes misstates what is imported, what is derived, and what the bridge theorem is needed for.

- Lines 701-707, Corollary `Exclusion of |c|^p for p ≠ 2`:
  Correctly proved.

- Lines 735-736, Remark `N=2`:
  Correctly scoped as open. This is the right replacement.

**2. Internal consistency**

- Lines 950 vs. 399, 761, 735:
  Internal contradiction. The paper now says in the Route E body that `N=2` is “not covered here,” but the conclusion reverts to “handled separately.” It is not handled separately in this paper. It is left open.

- Lines 754-755 vs. 641, 662, 895:
  Another contradiction. At line 754 the paper says non-contextuality is “now derived” from basin locality and LMC. That is not what the paper actually establishes. What it derives is a restricted, state-specific form inside the complete localised-probe-observer subclass. The paper itself says exactly that at 641, 662, and 895.

- Lines 758-759 and 859 vs. 556, 571, 856:
  The dependency chain is still muddled. Several places now correctly say the closure Hessian gives only a candidate local scale and is not operative in the proof of Theorem `born_unique`. But lines 758-759 and 859 still mix the bridge theorem / Hessian scale-fix into the uniqueness claim. If Gleason is doing the uniqueness work once the imported FS/Kähler structure is granted, then the bridge theorem is not a condition for uniqueness. It is only a condition for the stronger closure-side normalisation story.

- Cross-references:
  No unresolved internal labels found. No remaining broken Paper XXI cross-refs found.

**3. External consistency**

Verified locally:

- Paper XIV for the stationary measure:
  Verified at `paper-xiv.tex` lines 175-190 and 399-403. It does state `P_st \propto exp(-2F/\sigma^2)` and conditions uniqueness on irreducibility/regularity/confining assumptions. The present paper’s cautious phrasing is consistent with that.

- Paper XVIII for Nelson pairing / `\Psi=\sqrt{\rho}e^{iS/\sigma^2}`:
  Verified at `paper-xviii.tex` lines 215-218. The geometric “multiplication by i” bookkeeping is also there at lines 258-280. What I do not find there is any explicit construction of the sector-supported basis used in Paper XXX; the present paper is right to call that a modelling assumption at lines 181-182.

- Paper XXI for the Kähler / projective structure:
  Verified at `paper-xxi.tex` lines 307-323, 340-346, 364-378, 382-388. XXI explicitly says this projective/Kähler geometry is standard geometric QM applied on a finite-dimensional invariant subspace under an explicit hypothesis. Paper XXX is now mostly consistent with that.

- Paper XXIX for admissibility, basin locality, localised probe observers, LMC, and canonical-probe convention:
  Verified at `paper-xxix.tex` lines 164-169, 203-209, 217-236, 251-259, 290-295. Crucially, XXIX itself says LMC alone gives only numerator coincidence and that completeness is additionally needed downstream for full probability non-contextuality. That means line 754 of Paper XXX is still too strong.

Unverified / overstated attribution:

- Line 580:
  “Within the VFD framework, (U2) is a derived consequence of two results established in Paper XXIX...”
  That is not what XXIX proves. XXIX proves LMC and explains that completeness is additionally needed downstream; it does not prove global (U2).

**4. Tightness**

- Line 754:
  Replace “`now derived`” with:
  “`derived only in a restricted, state-specific form within the complete localised-probe-observer sub-class`”.

- Lines 758-759:
  Replace with:
  “`...and Gleason's theorem identifies the Born rule once the global (U2) hypothesis is imposed; the bridge theorem concerns only the candidate local scale, not the uniqueness proof.`”

- Line 859:
  Replace “`uniqueness from the imported ... Kähler structure ... plus the local closure-Hessian computation ... plus Gleason's theorem`” with:
  “`uniqueness from the imported Paper XXI Kähler structure plus Gleason's theorem; the local closure-Hessian computation contributes only a conditional candidate scale-fix.`”

- Line 950:
  Replace “`with N = 2 handled separately`” with:
  “`with N = 2 not covered here`”.

**5. Surface issues**

- Line 950: stale phrase; must be corrected.
- Labeling sloppiness: line 546 is a `proposition` labeled `cor:fs_from_closure`. Not fatal, but careless.
- Static source pass found no unresolved `\ref` or `\eqref`.
- No remaining broken Paper XXI cross-ref machinery detected.

**6. Top three fixes**

1. Lines 446-465: fix Proposition `prop:suff`.
   The current proof illegitimately uses `P_i=|c_i|^2` without the completeness hypothesis. This is a real proof gap, not cosmetic.

2. Lines 754-759 and 859: clean up Route E dependency claims.
   The paper still oscillates between three positions:
   restricted non-contextuality is derived;
   global (U2) is still a hypothesis;
   the bridge theorem is or is not needed for uniqueness.
   Pick one consistent dependency chain and enforce it everywhere.

3. Line 950: remove “`N=2 handled separately`.”
   That directly contradicts the repaired body text at 399, 761, and 735. As written, the conclusion backslides into the old overclaim.
