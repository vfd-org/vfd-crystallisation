All line numbers refer to [papers/paper-xxi/paper-xxi.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxi/paper-xxi.tex:1>).

1. **Claim audit**
- Lines 48-49: “the sector-superposition state manifold is `\mathbb{CP}^{N-1}` ... and `U(N)` acts as Kähler isometries.” Over-claimed in the abstract. Section 4 later makes this conditional on Hypothesis 172-174, but the abstract suppresses that load-bearing assumption.
- Lines 120-120: “The residual is proved to be ... amplitude-homogeneous.” Not established by XIX. `Amplitude homogeneity` is proved in XX, not XIX; XIX proves phase invariance, locality away from nodes, vanishing at equilibrium, and `O(\varepsilon)` smallness.
- Lines 137-139: “The closure programme establishes ... whose Fréchet linearisation ... is the Schrödinger equation.” Acceptable as a synthesis of XVIII-XIX-XX, but XXI does not prove this itself. It should be presented as imported, not as a theorem of XXI.
- Lines 177-177: “`\Mcl(\Ocal)=\mathbb{CP}^{N-1}` ... can be read as implying Hypothesis 172-174.” Unsupported. XXX treats `\Mcl(\Ocal)=\mathbb{CP}^{N-1}` as an observer-side support/completeness hypothesis, not as `H_W`-invariance of a chosen subspace.
- Lines 205-206: Proposition claiming Schrödinger flow is Hamiltonian “with respect to the symplectic form (193).” The result is standard, but the proof does not establish the statement as written. Earlier, lines 195-201 say the descended form on projective space is only a constant multiple of Fubini-Study, so a normalization/time-scale issue remains. As written, the proposition is too crisp for the proof supplied.
- Lines 219-220: Proposition claiming both that `U(N)` preserves the Kähler structure and that the full isometry group is exactly `PU(N)`. The proof establishes only the forward direction. The converse is delegated to a citation. That is not a proof of the stated proposition.
- Lines 227-228: Corollary that Schrödinger flows form a one-parameter subgroup of `U(N)`. Fine, conditional on the forward half of Proposition 219 and Hypothesis 172-174.
- Lines 319-320: restricted unitarity claim for the equilibrium tangent regime is properly scoped and is the strongest clean claim in the paper’s own geometric addendum.

2. **Internal consistency**
- Lines 172-174 use `\begin{hypothesis}...\end{hypothesis}`, but no `hypothesis` theorem environment is defined in the preamble. The file does not compile as written.
- Line 177 uses `\Mcl` and `\Ocal` without defining either macro anywhere in XXI. More broken TeX, and imported notation appears without setup.
- Lines 195-201 distinguish ambient forms on `T_\Psi \mathcal H_O` from descended forms on `T_{[\Psi]}\mathbb{CP}^{N-1}`, but line 206 then refers back to equation (193) as if the same `\omega` lives on projective space. Notation is being used for two different objects.
- Lines 120 and 302-303 split the programme’s staging inconsistently. The text attributes amplitude homogeneity to XIX; the status table correctly places the structural classification work in XX.
- Line 322 says the residual’s status as “gauge-removable” remains open, but line 301 says XIX proved gauge non-removability under phase and linear-unitary transforms. Those statements can coexist only if line 322 is narrowed to “broader, nonstandard transformations.”
- Line 177 says Hypothesis 172-174 “holds exactly when the chosen sector basis diagonalises, or is adapted to, `H_W`.” That is not the same as subspace invariance. Diagonalising is sufficient, not necessary; “adapted to” is undefined.

3. **External consistency**
- XXI 77-82 attributing the closure evolution equation to XVI is locally verifiable in XVI 142-149. Caveat: XVI explicitly labels the derivation formal (XVI 153-154); XXI should not present it as fully rigorous.
- XXI 86-92 quoting the dissipative obstruction is supported by XVII 136-145. Caveat: XXI suppresses XVII’s hypotheses on `\Fcal`, `M`, and the integration-by-parts domain.
- XXI 96-105 summarising XVIII’s continuity equation, irrotational phase, Madelung pair, and nonlinear wave equation is supported by XVIII 115-128, 136-154, 162-192, and 220-228.
- XXI 120 attributing amplitude homogeneity to XIX is not verifiable because it is false. XX, not XIX, proves amplitude homogeneity (XX 131-146).
- XXI 197 citing XVIII for the complex structure is only partly right. XVIII 291-300 is explicit that this is a bookkeeping observation about multiplication by `i`, not a substantive new theorem.
- XXI 242 saying XXX uses this section’s Kähler structure in a Petz-based uniqueness argument is verifiable in XXX 399-401, 544-551, and 856-859. But XXI overstates the Hessian’s role: XXX 506 and 528-529 say the closure Hessian gives only a local normalization on a configuration-space family, not an “alternative Riemannian metric” on `\mathbb{CP}^{N-1}`.
- XXI 177’s inference from XXX’s hypothesis `\Mcl(\Ocal)=\mathbb{CP}^{N-1}` is not verifiable and is contradicted in spirit by XXX 672-675, which says that hypothesis is observer-side completeness and does not by itself establish the support/invariance properties XXI wants.

4. **Tightness**
- Line 48: replace with “under the finite-dimensional invariant-subspace hypothesis of Section 4, the projective state manifold is `\mathbb{CP}^{N-1}` and the equilibrium-tangent Schrödinger flow is Hamiltonian.”
- Line 120: replace with “The residual is proved to be phase-invariant, modulus-dependent, local away from nodal sets, exactly zero at equilibrium, and `O(\varepsilon)` near equilibrium.”
- Line 177: replace with “Paper XXX additionally assumes `\Mcl(\Ocal)=\mathbb{CP}^{N-1}`; that hypothesis is compatible with, but does not imply, Hypothesis 172-174.”
- Line 242: replace with “Paper XXX uses the Kähler structure of this section together with a closure-Hessian normalization at equilibrium in its Petz-based uniqueness argument.”

5. **Surface issues**
- Undefined environment: `hypothesis` at lines 172-174.
- Undefined macros: `\Mcl`, `\Ocal` at line 177.
- Inconsistent encoding/spelling: line 163 uses literal `Kähler`, while the rest of the file uses `K\"ahler`.
- Line 177: “its Theorem’s hypothesis” is bad English and reads like an editing artefact.
- The nodal-set caveat for expressions like `\Delta|\Psi|/|\Psi|` is missing in XXI even though XIX repeatedly restricts locality claims to regions where `|\Psi|>0`.

6. **Top three fixes**
1. Rebuild the geometric addendum so its claims match its hypotheses. The abstract and Section 4 currently state conditional projective/Kähler results as if they were unconditional. The worst lines are 48-49, 172-177, 195-206, and 219-225.
2. Correct the source attributions. The XIX/XX boundary is sloppy at line 120, and the XXX-based inference at line 177 is not justified by the cited source.
3. Fix the TeX breakage and notation hygiene before anything else. As written, the file has an undefined theorem environment and undefined macros at 172-177, so it is not even mechanically sound enough for serious review.
