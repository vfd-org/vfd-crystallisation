**1. Claim audit**

- “Direct computation gives the spectrum `\{0,\, 5-\sqrt 5,\, 6-\sqrt 5,\, 9,\, 12,\, 14,\, 6+\sqrt 5,\, 15,\, 7+\sqrt 5\}` with multiplicities `\{1,4,9,16,25,36,9,16,4\}`...” ([paper-xxi.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxi/paper-xxi.tex:170>)-171). This is not established here, and worse, it conflicts with Paper V’s explicit table. Paper V gives `0, 9-3\sqrt5, 10-2\sqrt5, 9, 12, 14, 10+2\sqrt5, 15, 9+3\sqrt5` with the same multiplicities ([paper-v.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-v/paper-v.tex:99>)-124). As stated, XXI’s numerical claim is wrong for the Laplacian it defines in (3.3).

- “Paper XXXVI Foundation F5 ... supplies an additional discrete symmetry of the generator” ([paper-xxi.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxi/paper-xxi.tex:51>)-52). The surrounding prose is mostly repaired: it no longer says unitarity comes from F5. Good. But this sentence still slides from a theorem about the scalar functional `F` to a claim about the generator. The later remark correctly retreats to “expected, but not proved here” for the action on `\mathcal H` and commutation with `\Delta_{H_4}` (lines 174-176). The abstract should match the remark.

- “The operator `\hat H=\hbar\Delta_{H_4}` ... is self-adjoint” ([paper-xxi.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxi/paper-xxi.tex:162>)-164). Established. In finite dimension, real symmetric implies Hermitian. The proof is adequate.

- “Under the Route B generator assumption ... every trajectory ... satisfies `i\hbar \partial_t|\psi(t)\rangle=\hat H|\psi(t)\rangle`” ([paper-xxi.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxi/paper-xxi.tex:188>)-193). Established, conditional on the generator postulate. No complaint.

- “Papers XVIII–XX establish ... whose leading-order linearisation at that equilibrium coincides with the Schrödinger equation with Witten Hamiltonian `H_W`” ([paper-xxi.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxi/paper-xxi.tex:234>)-236). Substantively fair, but only because the sentence immediately says “formal/perturbative.” Without that caveat it would over-claim. Keep the caveat.

- “The effective parameters (`m_eff = 1/\sigma^2`, `V_eff = V_W`) are determined...” ([paper-xxi.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxi/paper-xxi.tex:246>)). This is only true in the `\hbar_eff=1` normalisation. The same paper later says “Planck constant `\hbar`: Route A: noise scale `\sigma^2`” (line 274). Those cannot both be stated without an explicit units convention.

- “Schrödinger as Hamiltonian flow on `\mathbb{CP}^{N-1}`” ([paper-xxi.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxi/paper-xxi.tex:341>)-342). Fine as a standard imported fact under Hypothesis 1. The proof is schematic but adequate given the citation.

- “Every unitary `U(N)` action preserves the Kähler structure; conversely the full isometry group ... is exactly `PU(N)`” ([paper-xxi.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxi/paper-xxi.tex:355>)-356). The forward direction is fine. The converse is not proved here, only cited. That is acceptable if presented explicitly as imported standard geometry, not as if newly shown.

- “`F5 as a consistency symmetry ... \sigma`-automorphism ... commutes with `\Delta_{H_4}`, giving a conserved discrete symmetry” ([paper-xxi.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxi/paper-xxi.tex:452>)). This is not established. It directly overshoots Remark `\ref{rem:F5_role}`, which says the commutation is only expected, not proved.

**2. Internal consistency**

- The recovery map now does split Route A and Route B correctly in the Schrödinger row and the evolution-operator row (lines 272-275). That specific round-3 fix is present.

- Section 3/Route B no longer says unitarity comes from F5. The operative text now correctly makes unitarity follow from self-adjointness plus Stone (lines 174-176, 180, 283, 404). That specific round-3 fix is present.

- The `\end{remark>` typo is absent from XXI. I found no `remark>` token in the file. That specific round-3 fix is present.

- There is a live contradiction between Remark `\ref{rem:F5_role}` (lines 174-176: commutation with `\Delta_{H_4}` is “expected, but not proved here”) and the status table row at lines 452-452, which states commutation as fact.

- There is a live normalisation conflict for Route A. Lines 217-226 package both routes into `i\hbar \partial_t\psi=\hat H\psi`, line 246 reads off `m_eff=1/\sigma^2`, while line 274 says `\hbar` on Route A is `\sigma^2`. Those statements can be reconciled only by explicitly declaring whether Route A is being written in `\hbar=1` units or not.

- All local `\ref`/`\eqref` targets in XXI exist. I checked: 48 labels, 48 references, no undefined local references.

**3. External consistency**

- Paper XVIII support is real. XXI’s claims about exact continuity, irrotational phase, exact nonlinear wave equation, and formal near-equilibrium Schrödinger recovery are present in XVIII at lines 115-145, 162-242, and 319-374.

- Paper XIX support is real. XXI’s claims about residual normal form, equilibrium vanishing, locality, `O(\varepsilon)` near equilibrium, exact norm conservation, and gauge non-removability are present in XIX at lines 70-84, 94-135, 153-177, and 218-234.

- Paper XX support is real. XXI’s claims about amplitude homogeneity, modulus-curvature classification, and equilibrium-tangent limit are present in XX at lines 131-146, 240-279, and 181-191.

- Paper XXV support is mostly fair. XXI says XXV gives chain-length/event-order structure but not a global real-valued time parameter (line 146). That is consistent with XXV, which builds separation functions and a graph metric, not a global physical time parameter.

- Paper XXX support is real. XXI says XXX uses the equilibrium-tangent Kähler structure as an input to its Petz/Gleason Born-rule route (lines 55, 299, 377-379). XXX says exactly that at lines 47-53, 405, 546-559, 858, 876.

- Paper XXXVI support is mixed.
  - F5 as `\sigma`-intertwining of the scalar closure functional is verified at XXXVI lines 409-441.
  - F6 is explicitly conditional and caveated, as XXI says, at XXXVI lines 465-485.
  - F8 caveats about imported couplings are verified at XXXVI lines 610-614.
  - But XXI’s sentence “In the continuum limit of F6, `\mathcal H` embeds into `L^2(S^3)` via a spectral truncation” (lines 152-153) is not something I could verify in XXXVI. F6 is a GH-convergence statement for refined vertex sets; the spectral-truncation Hilbert embedding is an extrapolation, not a theorem I found there.

- Paper V does not support XXI’s stated spectrum. It supports the multiplicity pattern, but not the eigenvalues written in XXI. On this point XXI is externally inconsistent with the cited local source.

- Paper XL is mentioned as future work (line 213). I cannot verify it locally; I did not find a `paper-xl` file.

**4. Tightness**

- Lines 47-49: “two independent routes lead to Schrödinger evolution.”
  Suggested edit: “two conditional routes lead to Schrödinger-type evolution in different senses.”

- Lines 51-52: “F5 ... supplies an additional discrete symmetry of the generator.”
  Suggested edit: “F5 supplies a discrete symmetry of the closure functional; any induced symmetry of the chosen generator is conjectural here, not proved.”

- Lines 226-226: “What Routes A and B share is ... the class of generator (self-adjoint).”
  Suggested edit: “What Routes A and B share is, at the relevant linearised/assumed level, a self-adjoint generator.”

- Lines 283-283: “The Schrödinger equation itself ... is an operator-theoretic consequence rather than a free postulate.”
  Suggested edit: “In Route B, once the self-adjoint generator is posited, Schrödinger evolution is an operator-theoretic consequence.”

**5. Surface issues**

- The exact spectrum listed at line 171 is numerically wrong for the Laplacian defined in line 156. This is not cosmetic; fix it.

- The status table row at line 452 should not state unproved commutation as fact.

- Route A’s `\hbar`/mass normalisation is inconsistent across lines 217, 246, and 274.

- I found no undefined local refs and no `\end{remark>` typo in XXI.

- I did not compile the file. In this sandbox, compiling would require writing aux files.

**6. Top three fixes**

1. Fix the H4 spectrum claim at lines 170-171.
   The eigenvalues conflict with Paper V’s exact spectrum for the same graph Laplacian. This is the worst error in the draft because it is concrete, checkable, and false.

2. Fix the F5/`\sigma` symmetry over-claim in the status table at line 452.
   The body text carefully says commutation with `\Delta_{H_4}` is only expected; the table reverts to stating it as proved. That undermines the round-3 repair.

3. Make Route A’s units consistent across lines 217-226, 246, and 274.
   Decide whether Route A is written with `\hbar_eff=1` or `\hbar_eff=\sigma^2`, then rewrite the recovery map and effective-parameter sentence accordingly. Right now the paper tries to say both at once.
