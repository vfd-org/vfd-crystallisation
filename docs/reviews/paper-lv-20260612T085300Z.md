All line references are to `papers/paper-lv/paper-lv.tex`.

**1. Claim Audit**

- Status claim, line 33: not fully verified in current repo state. `verify_rendering_layer.py` 21/21, `verify_gap_strengthening.py` 22/22, `verify_gr_closure.py` 39/39, and `verify_residual_closure.py` 24/24 pass. But `verify_crystal_forcing.py` currently fails GE1b, so the imported GE0-GE3 self-embedding support is not clean.

- Proposition “Intrinsic arena”, lines 64-65: overstates. Pairwise inner products determine geodesic distances after the embedding is fixed, but the pentagonal clock is not merely a function of distances; it uses a chosen group element and left multiplication. The proof supports “intrinsic to the fixed quaternionic/group structure,” not “a function of geodesic distances” for every listed object.

- Theorem “Square-multiplicity law”, lines 70-78: proof establishes the claim, conditional on identifying the graph as the Cayley graph of `2I` with the stated conjugacy-class generating set. Distinctness is numerical, not analytic, but is explicitly stated as such.

- Theorem “Harmonic identification; exact dispersion”, lines 80-88: establishes the unprimed `k=0..5` blocks, assuming McKay branching and using SR1 for irreducibility. The primed-block statement is cited/standard, not proved here; acceptable if cited properly.

- Corollary “Spectral dimension 3”, lines 90-91: supports “connectivity corroborates the S3 harmonic signature.” It does not prove that dimension 3 is derived without prior arena input, because the substrate is already defined in `S^3` at lines 43 and 68. The phrase `L \to -2a^2\Delta_{S^3}` is also too strong without an explicit refinement family; this paper has a fixed 120-vertex graph.

- Theorem “Canonical kernel”, lines 104-112: correct as a K4-selection result. It does not prove uniqueness from K1-K3, and the manuscript now mostly says that. Add `r>0` and connected graph as explicit hypotheses.

- Lemma “Equivariance; locality”, lines 114-116: equivariance is exact. Exponential locality and monotone coherence length are empirical over the tested `r` range, not theorem-grade. The continuum Yukawa statement is formal low-band/continuum language.

- Remark “correction”, lines 118-123: correct. The upstream `docs/closure-cosmogenesis.md` has in fact been patched.

- Proposition “Canonical observer frame”, lines 130-134: correct given the fixed quaternionic basis and orientation. It is not basis-free canonical; the text admits this, but the title should not hide it.

- Definition “Anchor chart; rendering map; scene”, lines 136-140: internally under-specified. The continuum formula with `G_r(d)` is not shown to equal the finite graph resolvent on vertices, yet line 139 declares exact equality on vertices by definition. This needs two maps: a discrete rendering and a chosen interpolation.

- Theorem “Scene decomposition”, lines 142-146: the linear split is immediate and valid. “Common to all resolutions up to smoothing” is qualitative, not equality. The bulk-invariance input is imported from the cosmogenesis/L-boundary generator story, not from Paper XXXIII’s explicit residual minimizer.

- Lemma “Clock reversal is inner”, lines 154-156: established; the 10 reversers are verified.

- Hypothesis H-equiv, lines 158-160: correctly framed. Paper XXXIII gives weaker symmetry-class invariance of minimizer sets, not residual-law equivariance. This revised conditionality is appropriate.

- Proposition “Second-order effective time”, lines 162-164: still overclaims. H-equiv plus inner reversal excludes spatial-rotation-invariant first-order time terms. It does not classify all effective equations. “Unique second-order stencil” is only true after assuming a three-point local centered stencil. State those assumptions.

- Remark, lines 168-170: “survives at every cascade rung” is not proved here. Cite a rung-ladder theorem or weaken.

- Proposition “Simultaneity, linear class”, lines 176-178: true for vertex readouts inside the chart domain under the discrete kernel definition. “For every chart anchor `u`” is too loose.

- General nonlinear simultaneity paragraph, line 180: the quadratic instance is verified. The general “must be the gradient” reduction needs smoothness, unique minimizers, correct readout type, and a simply connected chart; otherwise it is too compressed.

**2. Internal Consistency**

- The input-vs-corroboration framing is improved at line 68, but lines 52, 61, and 91 still read as if dimension 3 is derived outright from nothing. The paper should consistently say: `S^3` is input via the substrate definition; the bare connectivity independently corroborates the `S^3` harmonic signature.

- Lines 138-139 conflict between continuum interpolation and exact discrete rendering. A sampled continuum Yukawa kernel is not automatically `(I+r^2L)^{-1}`.

- The chart-radius condition at line 137 conflicts with the tested coarse kernels: line 115 reports `xi(4)=25.5` graph edges, so `xi(r)a` is far larger than `pi`. The condition cannot hold for all tested `r`.

- Cross-references in this file appear to resolve: all `\ref` targets are defined.

- Line 145 says “radial profile”; the verified result is “function of graph-distance to the bulk set,” not observer-homogeneous isotropy. Avoid language that sounds like spherical isotropy around an observer.

**3. External Consistency**

- Paper LIV: the conditional forcing argument exists and is explicitly conditional on W1-W3. The irrep-dimension claim used at line 77 is locally present in LIV. However, the GE self-embedding support used at lines 33, 68, and 188 is not fully verified now: GE1b fails in `verify_crystal_forcing.py`.

- Paper XXXIII: line 159 is accurate in its revised form. XXXIII establishes a candidate projection map and conditional symmetry-class invariance, but leaves multi-observer compatibility open. It does not prove H-equiv.

- Paper LIII: line 164 is locally supported. LIII consumes the rendered wave operator and closes T10; `verify_gr_closure.py` passes 39/39.

- Paper LVII: line 169 says LIII/LVII consume the result, and LVII does locally refer to the time-step/T10 continuum control. But LVII is not cited in Paper LV’s bibliography or at line 169.

- Cosmogenesis import at line 145 is locally supported by `docs/closure-cosmogenesis.md`, but that document still notes that equivalence with the explicit XXXIII residual minimizer remains a tightening.

**4. Tightness**

- Line 36: “three theorem-grade” is too strong for the chart/scene construction. Edit: “three structural results and one conditional time result.”

- Line 53: “unique canonical family” should be “canonical once K4 is imposed.”

- Line 68: replace “bare graph … independently carries” with “the adjacency spectrum and self-embedding independently corroborate.”

- Line 91: replace `L \to -2a^2\Delta_{S^3}` with “on the low harmonic band, and in the design-refinement interpretation, `L` has the dictionary…”

- Line 115: replace “rows decay exponentially” with “rows fit an exponential decay envelope over the tested range.”

- Line 133: replace “substrate hands every observer” with “given the fixed quaternionic basis, each anchor inherits…”

- Line 163: replace “the unique second-order stencil” with “the unique three-point centered second-difference stencil.”

- Line 169: replace “survives at every cascade rung” with “is expected to transfer to cascade rungs where the same inner-reversal structure is present.”

- Line 180: add “for smooth unique minimizers on a simply connected chart.”

**5. Surface Issues**

- Line 33: reproducibility status is false until `verify_crystal_forcing.py` GE1b is fixed or the imported claim is downgraded.

- Line 83 says McKay branching is “cited,” but there is no McKay bibliography entry in `papers/paper-lv/references.bib`.

- Line 169 mentions Paper LVII without a citation or bibliography entry.

- Line 36 has a grammar ambiguity: “the remaining three hold for it as theorems, which is automorphism-equivariant…” The antecedent of “which” is unclear.

- Line 77 says “Paper LIV, Theorem 3” without a citation or stable label. Use `\citep{SmartLIV}` plus theorem name.

- Line 137’s chart-radius condition is dimensionally and quantitatively problematic for large `r`.

**6. Top Three Fixes**

1. Fix the R4 overclaim at lines 162-164: make H-equiv, locality, spatial-invariance, and three-point-stencil assumptions explicit. The proposition currently proves less than it states.

2. Repair the dimension/self-embedding reproducibility problem at lines 33, 68, and 188. Either fix GE1b in `verify_crystal_forcing.py` or downgrade “pointwise reconstruction” to the claims that currently pass.

3. Separate discrete rendering from continuum interpolation at lines 136-140 and line 177. Define `\mathcal R^{disc}=\pi_rF` exactly, then define a continuum interpolant and state whether it is exact, approximate, or merely canonical by choice.
