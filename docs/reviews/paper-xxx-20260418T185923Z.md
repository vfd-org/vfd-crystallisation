Publication ready: no.

**1. Claim Audit**
- `Proposition 1` “Conditional Born weighting from stationary sector measure” (`paper-xxx.tex:323-341`): the proof establishes exactly the conditional sector-mass statement it claims. It does not establish a Born-rule derivation from closure dynamics alone; assumptions (4) and (5) do real work, and the paper now says so plainly. No must-fix here.
- `Proposition 2` “Sector-preserving unitaries induce sector-probability covariance” (`447-453`): established for the narrow subgroup only. The proof supports that limited claim. No over-claim.
- `Theorem 3` “Chentsov...; Petz quantum extension (imported; not proved here)” (`475-485`): correctly flagged as imported. No issue in this paper, provided it stays explicitly imported.
- `Proposition 4` “Closure Hessian as the Fisher metric...” (`493-507`): the proof supports the local Laplace-regime statement on a translation family in configuration space. It does not support any global projective-state metric claim; the paper now mostly respects that limit.
- `Proposition 5` “Imported Fubini-Study structure...” (`544-555`): this is a packaging proposition, not a derivation. The proof matches that weaker status.
- `Theorem 4` “Non-contextuality of sector probability for observers complete-for-Ψ” (`594-600`): proof is adequate for the state-specific, restricted claim. No over-claim.
- `Corollary 1` restricted `(U2)` (`610-618`): supported as a restricted ingredient, not a full global frame-function theorem. Fine.
- `Theorem 5` “Born weighting uniqueness...” (`656-676`): as a theorem about an already-given global `P`, the proof is fine. As a theorem about the VFD observer-indexed probabilities, it proves less than some summary prose suggests, because the bridge from `P(\Pi,\Psi;\Ocal)` to a single global `P(\Pi,\Psi)` still depends on basis-realisability and universal completeness and is not derived here. This is still the main logical vulnerability.
- `Corollary 2` exclusion of `|c|^p` for `p≠2` (`703-735`): proof is correct.
- `Example` two-basin model (`776-799`): internally honest; the asymmetric state is stipulated, not derived.

**2. Internal Consistency**
- Check (a): fixed. The route-comparison table now gives Route E as “imported Kähler structure + Gleason” at `848`, not “Petz+Gleason`.
- Check (b): fixed. The “Logical status” remark at `355-363` now explicitly carries both the sampling postulate and state-completeness.
- Check (c): fixed in substance, but the line is now `404`, not `403`: it does list universal completeness, basis-realisability, and axioms `(U1)-(U4)` on a global `P`.
- Remaining must-fix inconsistency: the paper still oscillates between two objects:
  - the abstract global frame function of `Theorem 5` (`656-676`);
  - the observer-indexed conditional probability actually constructed by VFD.
  This distinction is explained later (`642-647`), but the abstract, introduction, synthesis, and conclusion still sometimes slide over it. That is not a cosmetic issue.

**3. External Consistency**
- `Paper XIV`: verified locally. It proves the stationary distribution `P_st ∝ e^{-2F/σ^2}` and only gives uniqueness under added irreducibility/regularity/confining assumptions (`paper-xiv.tex:174-190`). The present paper represents that correctly.
- `Paper XVIII`: verified locally. It defines `Ψ_N = √ρ e^{iS/σ^2}` and explicitly labels the complex-structure point as a bookkeeping observation, not new geometry (`paper-xviii.tex:258-310`). The present paper represents that correctly.
- `Paper XXI`: verified locally. It explicitly presents the projective/Kähler structure and `U(N)` action as imported standard geometric QM on an invariant subspace (`paper-xxi.tex:340-387`). The present paper now represents that correctly.
- `Paper XXIX`: verified locally. It contains the admissibility definition (`164-170`), basin locality (`203-209`), localised probe observers (`217-237`), LMC (`251-284`), and explicitly says downstream full probability non-contextuality needs completeness in addition (`294-295`). The present paper represents that correctly.

**4. Tightness**
- `49-50`: replace “weights determined by the structure of the closure dynamics rather than by primitive stochastic postulates” with “weights tied to closure dynamics only after additional measurement and observer hypotheses.”
- `53`: replace “The result is a conditional derivation of the Born rule” with “The result is a conditional recovery of the Born rule.”
- `55` and `956-958`: replace “This is a genuine extension of standard quantum mechanics” with “This is a formal extension of the paper’s observer model beyond the complete-measurement regime; its physical scope remains open.”

**5. Surface Issues**
- No publication-blocking LaTeX or macro failure is evident by inspection.
- I did not compile the file, so I am not certifying that all refs resolve mechanically.

**6. Top Three Fixes**
1. Stop blurring the abstract global `P` of `Theorem 5` with the VFD observer-indexed probability. This needs to be made explicit at the theorem site and in the conclusion (`404`, `642-647`, `656-676`, `950-954`).
2. Remove the residual overstatement in the abstract that closure dynamics determines the weights without primitive postulates. The paper itself shows otherwise: Route C needs sampling, Route E needs global `(U1)-(U4)` plus completeness machinery (`49-53`, `347-363`, `656-668`).
3. Soften the “extension of standard QM” language for partial observers unless and until the paper supplies a physical measurement model showing such observers are realised and operationally testable (`55`, `216`, `807-808`, `956-958`).
