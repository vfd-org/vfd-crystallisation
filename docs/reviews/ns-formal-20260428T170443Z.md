**Claim Audit**
- `Main, conditional` (`ns-formal.tex:140`): not established as stated. The proof assumes Kato smooth local existence from `H^s, s >= 1` (`:566-570`), which is false at this regularity. It also relies on Cascade-NS correspondence (`:572`) and the vorticity “Theorem” (`:576`) that is actually a hypothesis.
- `Pi_hyd` lemma (`:150`): proof does not establish the claim. The sum is over `k in Z` (`:167`) but evaluated as `sum_{k>=0}` (`:181`, `:237-239`). Also vorticity is one derivative, not “two derivatives” (`:190-192`), and finite 600-cell samples are not a Whittaker-Shannon lattice.
- F2 (`:303`): imported, not proved here. Source has an invariant-classification assumption; current paper states it as unconditional.
- F4 (`:309`): conflicts with the paper’s other F4 usage. Here F4 is “spectrum concentrated at phi^{-1} with multiplicity 583”; elsewhere F4 is the 600-cell Laplacian spectrum/operator norm (`:195-197`, `:228-234`, `:607-614`).
- F5 (`:315`): over-stated. The local foundations note’s old F5 required rational coefficients and is marked superseded; P4 also says dissipation, not conservation.
- Cascade-NS correspondence (`:338`): not proved. The proof is a heuristic homogeneisation paragraph; it does not derive convection, pressure, incompressibility, viscosity, or arbitrary initial-data representation.
- Viscosity numerical check (`:388-393`): unsupported. No formula or calculation is given.
- Rank preservation (`:400`): false/irrelevant as argued. The proof’s rank argument does not control vortex stretching; it even calls vortex stretching rank-3 (`:404-405`) and rank-2 (`:429-430`).
- Bounded macroscopic operators (`:436`): circular. Items (2)-(3) depend on the later vorticity hypothesis and would already imply the desired continuation control.
- `B_NS` (`:477`): acceptable only as a hypothesis. It assumes essentially the BKM-controlling estimate; it is not an intermediate theorem.
- BKM theorem (`:532`): usable as a continuation criterion, but the citation is the Euler BKM paper and the statement should be sourced for NS or stated as standard NS continuation.
- Global extension corollary (`:543`): valid only conditional on `B_NS`; text incorrectly cites `Theorem~\ref{thm:vorticity}` (`:549`), but that label is on a hypothesis.
- Spectral count (`:607-622`): the count is locally supported by `rh-formal.tex` and the spectrum script. However line `:231` gives the wrong eigenvalues `7±sqrt(5)`; the correct pair is `12-4phi`, `8+4phi`.
- Finite-noise diagnostic (`:635-657`): matches the RH paper’s diagnostic, but R2/R3 equal 1.0000 by explicit projection, not by discovered dynamics. Treat only as a sanity check.
- aria H4O empirical witness (`:660-666`): cannot be verified in this repo. The cited `aria-chess/...` paths are absent; the local H4 oscillator law derives a flow law, not the claimed torus/eigenpair artifact.

**Internal Consistency**
- `H_1`, `H_NS`, and `H_P6/P7` are never formalized as hypotheses usable in Theorem `:140`.
- `B_NS`, `B_NS_3`, and `H_NS` are used as if interchangeable (`:37`, `:225`, `:251`, `:477`), but only `B_NS` is stated.
- `\begin{hypothesis}` is used (`:477`) without `\newtheorem{hypothesis}`.
- Cross-reference `Theorem~\ref{thm:vorticity}` points to a hypothesis (`:274`, `:549`, `:576`).
- The paper alternates between `R^3`, `S^3`, 600-cell subsets of `R^4`, ball-averaging coarse-graining, and Whittaker interpolation without one compatible map (`:150-171`, `:321-329`, P7 is on `S^3`).
- Discussion contradicts the repaired hypothesis status: “Cascade provides the bound” (`:697`) and “yielding the uniform vorticity bound” (`:706-707`) contradict `B_NS` as open (`:488`, `:516-519`).
- Sigma-invariance “conserving cascade energy exactly” (`:703`) contradicts the paper’s own statement that P4 proves dissipation, not conservation (`:508-512`).

**External Consistency**
- P4: verified. It proves finite-level flow and dissipation (`cascade-closure-dynamics.tex:640-653`), and explicitly no continuum/PDE claim (`:1339-1344`). NS uses the dissipation point correctly at `:511`, but overuses P4-style dynamics elsewhere.
- P6: not as strong as claimed. It marks spectral convergence as a conditional program, not Clay-grade (`cascade-schlafli-convergence.tex:1341-1363`, `:1406-1428`). This conflicts with `ns-formal.tex:40-43`.
- P7: not as strong as claimed. It gives only a conditional weak NS limit on `S^3` under `H_hyd` (`cascade-hydrodynamic-projection.tex:1010-1065`) and explicitly no smooth/unique/regularity result (`:1138-1149`, `:1264-1272`).
- `CascadeFoundations`: current header says F4/F5 are superseded by Paper XXXVI. NS should not cite the old formulations as theorem-grade without updating.
- `CascadeAttractorSpectrum`: verified for the 94/120 spectral count; fresh spectrum run also matches. It does not support the wrong `7±sqrt(5)` line.
- `CascadeAttractorDynamics`: source/RH paper support the printed R1/R2/R3 values, but also say R3 is not a true control and projection dominates by construction.
- `ariaH4Olaw`: not locally verifiable as cited. No `aria-chess` repo is present; the local oscillator-law document does not contain the torus-attractor claims.

**Tightness**
- `:73-98`: replace “The cascade supplies exactly this” with “The conditional bridge would have to supply this.”
- `:216-218`: replace “standard Sobolev-regularity argument” with “an open compatibility theorem with Sobolev-analytic components.”
- `:385`: replace “IS Navier-Stokes” with “would reduce to Navier-Stokes if the stated asymptotic identification is proved.”
- `:697-707`: replace the whole paragraph with “Under `B_NS`, the cascade substrate would supply the BKM bound.”
- `:716-719`: replace “Cascade answers” with “The conditional cascade proposal would answer, if the bridge hypotheses are proved.”
- `:737-738`: delete “cascade provides an alternative proof.”

**Surface Issues**
- Undefined theorem environment: `hypothesis` (`:477`).
- Undefined macros/packages: `\Vcal` (`:505`), `\bm` (`:619`, `:628`), `\textbeta` (`:57` unless a package defines it).
- Unicode likely unsafe under pdfLaTeX: `m²` (`:392`), `H₄` (`:743`), sigma characters in theorem names.
- Inconsistent spelling: `Navier-Stokes` (`:344`, `:385`) vs `Navier--Stokes`.
- Wrong eigenvalue display at `:231`.
- `sinc` described as compactly supported (`:170-171`), which is not sinc.
- Explicit `\qed` inside a proof environment (`:206`) may duplicate the proof tombstone.

**Top Three Fixes**
1. Demote or restate Cascade-NS correspondence and `Pi_hyd` as hypotheses/open bridges (`:150`, `:338`). These are still the real proof gaps.
2. Repair the main theorem’s logical statement (`:140-147`): require smooth initial data/high enough Sobolev regularity, define all hypotheses formally, and stop citing a hypothesis as a theorem.
3. Align external dependency claims with P6/P7/P4 (`:40-43`, `:572-577`, `:697-707`): P6 spectral convergence is not Clay-grade; P7 gives conditional weak NS on `S^3`, not global smooth regularity on `R^3`.
