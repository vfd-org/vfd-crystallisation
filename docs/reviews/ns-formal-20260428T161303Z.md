**1. Claim Audit**

- “Under Hypotheses H$_1$, H$_{\mathrm{NS}}$, and H$_{\text{P6/P7}}$ … has a unique solution $u\in C^\infty$” (lines 140-147): not established. The proof assumes Kato smooth local existence from $H^s$, $s\ge1$ (lines 604-608), which is false at that regularity. It also relies on the unproved hydrodynamic bridge and the unproved vorticity theorem.

- Lemma $\Pi_{\mathrm{hyd}}$ (lines 150-183): does not establish its claim. The sum is over $k\in\mathbb Z$ (line 167), but $\sum_k\varphi^{-2k}$ diverges; later the paper silently changes to $k\ge0$ (lines 237-239). The curl should introduce one scale derivative, not “two derivatives” (lines 190-192), unless the operator is a Laplacian, not vorticity. The sinc reconstruction is not specified on a genuine lattice, is not shown divergence-free, and is not shown compatible with NS viscosity.

- “The remaining open lemma B$_{\text{NS}}$_3…” (lines 251-262): this is accurate in spirit, but it contradicts the theorem/lemma register used earlier. If B$_{\text{NS}}$_3 is open, Lemma 1.1 and Theorem 1.1 cannot be presented as proved mathematical results.

- F2 rank structure (lines 303-307): imported, not proved here. Even if accepted, it does not imply any bound on vortex stretching.

- F4 bounded spectrum (lines 309-313): mis-stated relative to the cited foundations. The local foundations file says F4 is now a graded closure-field dimension/count statement, not the operative 600-cell Laplacian spectral bound used later.

- F5 $\sigma$-invariance (lines 315-317): imported, not proved here. More seriously, the later proof uses it as conservation of cascade energy (lines 546-549), which does not follow from $\sigma$-invariance.

- Cascade-NS correspondence theorem (lines 338-355): not established. The “proof” is an outline with unspecified coefficient tracking, no convergence theorem, no topology, no error estimate, and no derivation of incompressibility or pressure beyond naming them.

- Numerical viscosity claim “$10^{-6}$ m²/s, matching water” (lines 388-393): unsupported. No formula evaluation, units audit, parameter values, or uncertainty are given.

- Rank preservation lemma (lines 400-406): false as stated. Linear averaging of fields does not imply nonlinear differential operators preserve a meaningful “rank” in the sense needed. It also contradicts itself: line 405 lists vortex stretching as rank-3/uncontrolled, while lines 429-430 say it exists and is rank-2.

- Bounded macroscopic operators lemma (lines 436-445): not proved. Item 3 is essentially global regularity in Sobolev norms. The proof is circular: it reduces item 2 to Theorem 5.1 (lines 466-470), while Theorem 5.1 later invokes this lemma (lines 529-530).

- Uniform vorticity bound theorem (lines 477-486): not proved. The theorem omits the bridge hypotheses from its statement, applies cascade coarse-graining to an arbitrary smooth NS solution, asserts an unproved $L^\infty$ bound on vortex stretching (lines 511-522), and then replaces a time-growing estimate (lines 532-537) by a time-uniform one via unsupported claims about conserved cascade energy (lines 540-557).

- BKM theorem (lines 570-577): acceptable as a continuation criterion if properly cited for NS, but the bibliography item is the Euler paper (lines 785-787). The paper should cite an NS continuation version or state the adaptation.

- Global extension corollary (lines 581-599): formally follows only if Theorem 5.1 is true. Since Theorem 5.1 is the main unproved bridge, this corollary is conditional on the missing result, not established.

- Spectral count $94/120=78.3\%$ (lines 645-660): locally verified numerically by the cited script, but the paper overstates “exact in $\mathbb Q(\sqrt5)$ arithmetic” (line 659). The script uses exact construction of the graph but numerical eigendecomposition.

- Dynamical diagnostic R1/R2/R3 (lines 673-694): the reported interpretation is mostly honest. However R2 and R3 equal 1.0000 because the script explicitly projects onto $P_{\mathrm{fix}}$; this is not evidence that natural closure dynamics produces the projector.

- Aria H$_4$O empirical witness (lines 698-704): not locally verified in this repository. The cited path is external; the local H$_4$ oscillator note proves a candidate law with empirical/free-parameter layers, not the claimed uniform torus/vorticity bridge.

**2. Internal Consistency**

- The hypotheses H$_1$, H$_{\mathrm{NS}}$, H$_{\text{P6/P7}}$, and B$_{\text{NS}}$_3 are not formalized consistently. The main theorem points to Section 4 (line 145), but Section 4 contains lemmas, not definitions of those hypotheses.

- The paper simultaneously says the bridge is not rigorously proved (lines 50-58, 251-262) and then proves the main theorem using it as if available (lines 603-627).

- F4 means at least three different things: closure operator eigenvalue $\varphi^{-1}$ with multiplicity 583 (lines 309-313), 600-cell Laplacian eigenvalues and operator norm 15.708 (lines 228-234), and “substrate dimension 583” (line 517). These are not interchangeable.

- The scale sum is inconsistent: $k\in\mathbb Z$ in the reconstruction (line 167), but $k\ge0$ in the convergence claim (line 238).

- The term identifications in the Cascade-NS theorem conflict: $\alpha R$ gives convection at line 349 but Laplacian plus advective terms at lines 369-370; $\gamma Q$ gives viscosity at line 351 but quadratic material derivative and viscosity at lines 373-374.

- Cross-references mostly resolve, but several point to weaker material than claimed: Lemma 1.1 is a sketch, Section 4 does not define the theorem hypotheses, and RH Remark `rmk:strengthened` supports only a conditional analogy.

**3. External Consistency**

- `papers/cascade-derivation/cascade-foundations.md`: does not support the target paper’s use of F4/F5. Its update says F4/F5 formulations were superseded; F4 is a dimension/count statement, and F5 is not an energy-conservation theorem.

- P6, `papers/cascade-schlafli-convergence/cascade-schlafli-convergence.tex`: explicitly says spectral convergence is a conditional program and “not yet Clay-grade theorem-level output” (lines 76-87). The target’s “theorem-grade spectral stability and convergence” language is too strong.

- P7, `papers/cascade-hydrodynamic-projection/cascade-hydrodynamic-projection.tex`: explicitly makes no unconditional NS regularity, smoothness, or blow-up claim (lines 114-120). It gives a conditional scaling-limit program, not an $L^\infty$ vorticity/BKM bridge.

- `test_sigma_attractor_spectrum.py`: supports the 94/120 count, but also reports no eigenvector localization on the 24-point fixed set and says conversion to analytic zero/bridge claims is not established.

- `test_sigma_attractor_dynamics.py`: supports the R1/R2/R3 numbers only as a toy diagnostic. The “closure” result is implemented by explicit projection onto $P_{\mathrm{fix}}$.

- `docs/convergence-with-smart.md`: supports only architectural similarity, not a formal meta-theorem. This agrees with lines 636-638, but not with any stronger “programme-level” evidentiary use.

**4. Tightness**

- Lines 95-98: replace “standard Sobolev analysis” with “an open analytic bridge lemma.”
- Line 385: replace “the macroscopic limit IS Navier-Stokes” with “would reduce to Navier-Stokes under the stated hydrodynamic scaling hypothesis.”
- Lines 477-486: rename theorem to “Conditional vorticity-bound bridge” and include H$_{\mathrm{NS}}$ in the statement.
- Lines 546-549: delete; $\sigma$-invariance is not conservation.
- Lines 735-745: replace “Cascade provides the bound” with “The proposed cascade bridge would provide the bound.”
- Lines 755-757: replace “Cascade answers” with “The conditional cascade mechanism would answer, if the bridge lemma is proved.”

**5. Surface Issues**

- `\textbeta` is undefined without an added package or macro (line 57).
- `\bm` is used but the `bm` package is not loaded (lines 657 etc.).
- Unicode `σ`, `φ`, `H₄`, `Δ`, superscript `²`, and arrow-like text may fail under pdfLaTeX.
- `B$_{\text{NS}}$_3` is malformed notation; use `$B_{\mathrm{NS},3}$`.
- Line 154 has malformed mathematical prose: `\mathbb{R}^4 \to \mathbb{R}^3` inside a subset declaration.
- “compactly-supported sinc” (lines 170-171) is contradictory.
- Bibliography path `Papers/cascade-derivation/...` (line 798) has wrong capitalization for a case-sensitive system.
- `KatoUniqueness` is not a usable bibliographic citation (lines 811-812).
- All-caps mathematical emphasis (“BOUNDED”, “WITHOUT GROWTH IN TIME”, “IS”) should be removed.

**6. Top Three Fixes**

1. Fix or demote the vorticity theorem and main theorem proof (lines 477-558, 603-627). As written, the proof is circular and uses the open bridge as a proved estimate.

2. Replace Lemma 1.1 with a precise conjectural bridge statement (lines 150-219). The current version has a divergent scale sum, wrong derivative scaling, undefined reconstruction, and no divergence-free/viscous compatibility.

3. Align all infrastructure citations with what the local papers actually claim (lines 40-58, 309-316, 631-724). P6/P7 and foundations do not supply theorem-grade NS regularity or $L^\infty$ vorticity control.
