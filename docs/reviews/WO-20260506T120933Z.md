**1. Claim Audit**

- “`V_600` is the 120 set ... 8+16+96 coordinate orbits” (`bekenstein-incidence.tex:117-133`): established by explicit construction plus `verify.py`; OK.
- “`σ(V_600)` meets `V_600` exactly along the 24 axis/half-type vertices” (`bekenstein-incidence.tex:136-139`): established by `verify.py:149-155`; OK.
- Lemma “120 vertices, group closure, unit norm” (`bekenstein-incidence.tex:141-153`): established by exhaustive 120 norms + 14,400 products; OK.
- “For `τ` of order 10 ... 12 cycles each length 10” (`bekenstein-incidence.tex:160-166`): OK, but write `ord(τ)=10`; `|τ|=10` conflicts with unit norm notation.
- K-multiset `{72:1,0:1,52:5,20:5}` (`bekenstein-incidence.tex:180-188`): established by exact computation; OK, proof text is terse but appendix/script carries it.
- “`H = (K=72) ∪ (K=0)`, `|H|=20`, `H ≅ Dic_5`” (`bekenstein-incidence.tex:191-214`): established, including closure and `<a,b>=H`; OK.
- “`H` is self-normalizing” (`bekenstein-incidence.tex:216-228`): established by all 100 outside elements having `gH != Hg`; OK.
- “`V_24` has 24 vertices ... form a regular 24-cell, the unique self-dual regular convex 4-polytope” (`bekenstein-incidence.tex:251-256`): count and 24-cell claim OK; “unique self-dual” is false as stated, since the regular 4-simplex is also self-dual. Must fix.
- Main coset theorem, left/right `4/16` (`bekenstein-incidence.tex:269-305`): established by exact enumeration; OK.
- τ-independence theorem: 24 choices, 6 distinct `H_τ`, all conjugate, all 4/16 both sides (`bekenstein-incidence.tex:307-326`): established by `verify.py`; I also checked every `H_τ` has the same K-multiset, contains its τ, and `<a,b>=H_τ`; OK.
- Structural coefficient theorem `S/A=1/4` (`bekenstein-incidence.tex:358-380`): follows from incidence counts; OK.

**2. WO Acceptance Audit**

Open items:
- B1 resolved: left/right cosets, non-normality, no quotient assumption.
- B2 resolved: Dic5 presentation certificate.
- B3 resolved: current group path uses exact `Q(√5)` sign comparison, no float ranking.
- B4 partially resolved: fixed set/count OK; 24-cell uniqueness overclaim must be fixed.
- B5 resolved: six left + six right coset tables printed.
- B6 resolved: scope firewall now mostly honest.
- B7 resolved: `verify.py` exits 0; shared tests exit 0.

Numbered criteria:
1. Partially: content exists in `bekenstein-incidence.tex`, not `paper.tex`.
2. Resolved.
3. Resolved.
4. Resolved.
5. Partially: cites exist, but 24-cell uniqueness and Moody-Patera bib issue remain.
6. Partially: incidence theorem resolved, but the WO-requested non-bulk “one `K=52` + one `K=20` cycle” lemma is not in the manuscript, only in tests.
7. Resolved.
8. Resolved.
9. Resolved, with comparison wording still slightly over-simple for LQG.
10. Resolved.
11. Resolved; per-coset ratio is implicit from asserted `4/16`.
12. Resolved. I ran both commands; `verify.py` passed and shared tests report `33 passed`.

**3. Catalogue Audit**

- All main derivation objects are present in the catalogue: D1-D7, L1-L5, T1-T3, N1-N2.
- Defect: catalogue claims stable LaTeX labels, but definition labels are absent in the manuscript. Example: `CATALOGUE.md:11-12` cites `def:galois`, but no `\label{def:galois}` exists; definitions at `bekenstein-incidence.tex:117`, `160`, `169`, `191`, `237`, `243`, `358` are unlabeled.
- Minor defect: `G := 2I = V_600` (`bekenstein-incidence.tex:156`) is a new standing definition but not catalogued explicitly.
- No unbacked lemma/theorem entries found, aside from the manuscript’s false “unique self-dual” wording.

**4. Attribution / External Consistency**

- WO citations to `NARRATIVE.md`, `SCOPE.md`, `NOTATION.tex`, `icosian.py`, and tests check out.
- `group.py:65-99` really does use exact sign comparison, not float ordering.
- `block2e_bulk_subgroup_check.py` is weaker than the current paper: it uses float shell sorting at lines `75-79` and does not give the current presentation-grade proof. Treat it as historical, not as theorem support.
- `cascade-algebraic-substrate.tex:918-925` does warn against using `2I/2T` quotient language; WO attribution checks out.
- Attribution defect: `bekenstein-incidence.tex:133` cites `MoodyPatera1993`, but `references.bib:66-72` is a different Moody-Patera paper/year/title than the local cascade anchor `Quasicrystals and icosians` at `cascade-algebraic-substrate.tex:2257-2260`. Fix the bib entry or drop that cite.

**5. Sim Correctness**

`verify.py` implements the advertised finite claim. No statistical null, χ², KL, or sampling issue exists; this is deterministic exact enumeration.

Non-blocking code issues: `verify.py:243` has unused `a_t_pow`, and `verify.py:300-302` computes unused `gH`. They do not affect correctness.

**6. Tightness**

- `bekenstein-incidence.tex:161`: replace `|τ| = 10` with `\operatorname{ord}(\tau)=10`.
- `bekenstein-incidence.tex:255-256`: replace “the unique self-dual regular convex 4-polytope” with “the exceptional self-dual regular 24-cell.”
- `bekenstein-incidence.tex:82-83`: soften “Each reproduces” to avoid implying LQG gives `1/4` without its usual normalization choice.

**7. Top Three Fixes**

1. `bekenstein-incidence.tex:251-256`: remove the false uniqueness claim for the 24-cell.
2. `papers/bekenstein-incidence/references.bib:66-72`: correct `MoodyPatera1993` to the icosian paper actually used as the repo anchor, or remove it from `bekenstein-incidence.tex:133`.
3. `bekenstein-incidence.tex:283-305`: add the missing WO lemma that each non-bulk left coset is one `K=52` cycle plus one `K=20` cycle, or explicitly drop it from the acceptance target.

**8. Verdict**

Publication ready: no.

The main incidence theorem and τ-sweep certificate are theorem-grade. The blockers are narrower: one false mathematical overclaim about the 24-cell and one wrong/mismatched external citation.
