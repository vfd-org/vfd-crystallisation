**1. Claim Audit**

- `def:Eobs`, `E(v):=|v-\sigma(v)|^2_{\mathrm{Eucl}}`, `papers/v600-hawking-quantum/v600-hawking-quantum.tex:129`: established. The rationality argument at line 137 is valid because `sigma(d)=-d` for `d=v-sigma(v)`.
- `lem:vanish`, “`E(v)=0` iff `v in V_24`”, `tex:140`: established, assuming the Euclidean norm is the real positive-definite norm.
- `lem:coord`, “exactly two nonzero coordinates, each `±sqrt(5)/2`”, `tex:149`: substantively established and verified by `verify.py`. But the proof contains a false identity: `sigma(phi/2)=(1-sqrt5)/4 = 1/(2phi)-1/2`, `tex:165`. Correct is `(1-sqrt5)/4 = -1/(2phi)`. Fix this before it gets quoted.
- `thm:Eq`, spectrum `24 delta_0 + 96 delta_{5/2}`, `tex:185`: established by the coordinate lemma plus imported vertex classification. The symmetry paragraph only proves constancy on the mobile orbit, not the value `5/2`; it needs one coordinate computation. So the abstract’s “single value follows ... from symmetry” phrasing at `tex:46-51` is too strong unless rewritten as “constancy follows”.
- Bilateral orbit claim, `Gamma={v -> gvh}`, `tex:212-223`: established computationally. I separately checked `V_24` closure, right-commutation, and full bilateral commutation: all true. `verify.py` itself only asserts left-commutation.
- `def:coset_thermo`, `S,A,E,T_H`, `tex:230`: definition is fine, but `T_H=E/A` silently requires `A>0`. Paper 1 gives `A=16`, so harmless here.
- `thm:firstlaw`, per non-bulk coset `S=4,A=16,E=40,T_H=5/2`, `tex:241`: established if Paper 1 incidence is accepted. Aggregate is explicitly only over five non-bulk left cosets, `tex:253`, which matches the proof.
- `rem:BH`, “standard structural relations `E_horizon=T_H A`, `S_BH=A/4`, `T_H S=E/4`”, `tex:278-288`: over-claim. `S=A/4` is standard; `E=T_H A` and `T_H S=E/4` are not standard black-hole first-law identities. This is the most dismissible physics-language defect.
- PBH observable, “predicts ... discrete cutoff ... testable ... by gamma-ray burst observations”, `tex:318-329`: not established. With no cascade-unit calibration, no spectrum map, and no observational model, this is a conjectural motivation at best. As written it is an unsupported prediction.

**2. WO Acceptance Audit**

- D1 `paper.tex`: partially resolved. The artefact is `v600-hawking-quantum.tex`, not `paper.tex`; content mostly matches, no PBH numerics or `tau_sigma`.
- D2 Introduction: resolved. Bekenstein/Hawking and Paper 1 are cited; one observable is stated.
- D3 Setup: resolved. `K`, `sigma`, `V_600`, `V_24`, `Dic_5`, cosets imported.
- D4 Sigma-pair excitation: resolved, except the algebra typo at `tex:165`.
- D5 Main theorem: partially resolved. Spectrum and coordinate proof are present; symmetry/orbit explanation is present, but wording should say symmetry proves constancy, not the numerical value.
- D6 First-law structure: resolved mathematically.
- D7 Interpretation/scope: partially resolved. Scope box is careful, but §7 still makes an unsupported PBH prediction.
- D8 Appendix verification: resolved.
- D9 `verify.py`: resolved for required assertions; it passes. Coverage gap: bilateral σ-commutation is only partly asserted.

Open items / top builds:
- F1 convention correction: resolved in current derivation and `sigma.py`; stale contradiction remains in `SCOPE.md:45`.
- F2 orbit certificate: resolved by `verify.py` and `symmetry.py`, with the commutation coverage caveat.
- F3 math spine from Paper 1: resolved.

**3. Catalogue Audit**

Defects:
- `CATALOGUE.md:41` says “Table in §5 / verify output”; there is no table in §5 of the derivation.
- The PBH “predicted observable” at `tex:318-329` is an introduced conjectural/predictive claim but is absent from the catalogue.
- Catalogue does not record the Bekenstein-Hawking analogy remark `rem:BH`, `tex:278`, despite it introducing nontrivial interpretive relations. Either catalogue it as interpretive/non-theorem or weaken/remove the remark.
- Otherwise D1, D2, L1, L2, T1, T2, N2 match the derivation.

**4. Attribution / External Consistency**

- Paper 1 import of `V_600=2I`: supported by `papers/bekenstein-incidence/bekenstein-incidence.tex:143-146`.
- Paper 1 import of `V_24=Fix(sigma)`: supported by `bekenstein-incidence.tex:284-305`.
- Paper 1 import of `H=Dic_5`, order 20, K-special cycles: supported by `bekenstein-incidence.tex:193-216`.
- Paper 1 self-normalizing/non-normal claim: supported by `bekenstein-incidence.tex:257-260`.
- Paper 1 per-coset `4/16` incidence: supported by `bekenstein-incidence.tex:310-321`; aggregate `20/80` supported by `bekenstein-incidence.tex:409-415`.
- Local bibliography has Bekenstein/Hawking/Carr entries, but it does not locally establish the physical equations or PBH observability claims.

**5. Sim Correctness**

`verify.py` passes and implements the spectrum, coordinate certificate, orbit sizes, left/right non-bulk coset first law, and aggregate left-boundary identity.

Issues:
- `verify.py:106-111` checks only left multiplication commutes with `sigma`; the derivation claims bilateral `g v h` commutation. My direct check confirms right/bilateral commutation, but the certificate should assert it.
- `verify.py:87-91` contains a misleading general comment about squaring `a+b sqrt(5)`; the actual assertion avoids the mistake by checking coordinates directly.
- Pytest was not runnable here: it failed before collection because the sandbox has no writable temp directory.

**6. Tightness**

- `tex:49-51`: change “single value follows ... from symmetry” to “constancy on the mobile sector follows from symmetry; the value is computed by the coordinate lemma.”
- `tex:281-284`: replace “standard structural relations” with “formal relations in this finite model, shaped to echo `S=A/4`.”
- `tex:320-329`: deflate to “A calibrated future model would need to decide whether a cutoff signature exists.”

**7. Top Three Fixes**

1. Fix the false algebra at `papers/v600-hawking-quantum/v600-hawking-quantum.tex:165`.
2. Remove or sharply weaken the unsupported PBH prediction at `tex:318-329`; it is not derived by the paper.
3. Replace the false “standard structural relations” language at `tex:281-284`; the current wording invites an immediate physics objection.

**8. Verdict**

Publication ready: no.

The finite `E_q=5/2` spectrum and first-law arithmetic are basically solid. The blocker is not the core computation; it is the over-claimed Hawking/PBH physics wrapper plus one visible algebra error in the coordinate proof.
