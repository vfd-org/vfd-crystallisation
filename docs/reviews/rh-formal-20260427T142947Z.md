Publication ready? **No.**  
Approaching publication-ready as a **conditional reduction/reformulation**? **Yes**, but only after the remaining theorem-grade overclaims are demoted or repaired.

**1. Claim Audit**

- `rh-formal.tex:348`: “\(\zeta_{\rm cas}(s)=\zeta(s)\).” Established only because \(\zeta_{\rm cas}\) is later defined to be the classical zeta function at `rh-formal.tex:1021`. Valid as a reformulation, not as a derivation.

- `rh-formal.tex:363`: “The critical line is precisely the fixed locus of \(\tau=\iota\circ\sigma\).” Established. This is now correctly anti-holomorphic; it is not falsely claiming that \(\sigma\) alone fixes the line.

- `rh-formal.tex:394`: “Assume \(H_{\sigma{\rm fix}}\). Then RH holds.” Established, but tautological: `rh-formal.tex:1387` explicitly makes \(H_{\sigma{\rm fix}}\) equivalent to RH.

- `rh-formal.tex:379`: Dedekind factorisation for \(\mathbb Q(\sqrt5)\). Established by the standard quadratic-character argument.

- `rh-formal.tex:549`: “\(p\) splits iff \(p\equiv\pm1\pmod5\).” Established.

- `rh-formal.tex:598`, `rh-formal.tex:622`, `rh-formal.tex:647`: pentagonal-clock order, shell classes, and \(K\)-multiset. Established computationally by the cited local scripts; I verified the scripts reproduce order \(10\), shell sizes, and \(K=\{0,20,52,72\}\) with multiplicities \(1,5,5,1\).

- `rh-formal.tex:637`: “unique lowest-degree polynomial.” Not established as stated. Uniqueness needs a normalization clause; otherwise affine/scalar variants are not excluded by the text.

- `rh-formal.tex:673`: “exact closed form” for \(\widehat\zeta(z)\). Established from the verified \(K\)-multiset. The Round 5 sign fix is substantively closed: `rh-formal.tex:699` uses \(\sigma(\phi^K)=(-1)^K\phi^{-K}\) and notes all four \(K\)’s are even.

- `rh-formal.tex:711`: palindromic Mellin symmetry. Established. The Round 5 correction is present: `rh-formal.tex:724` has \(\widehat\zeta(w^{-1})=w^{24}\widehat\zeta(w)\), and `rh-formal.tex:727` uses \(w^{-12}\widehat\zeta(w)\).

- `rh-formal.tex:760`: “cannot coincide with any global Hecke \(L\)-function over \(\mathbb Q(\sqrt5)\).” Not established. The proof assumes a local Euler-factor comparison that is never derived from an equality of functions. As written this is an overclaim.

- `rh-formal.tex:811`: “\(8\) \(\sigma\)-paired pole pairs.” Not established and internally mismatched with the arithmetic data. The zeta side has four distinct reciprocal pole-location pairs, with multiplicities \(1,1,5,5\), not eight pole pairs unless an extra Coxeter-plane doubling convention is added and proved.

- `rh-formal.tex:859`: “\(\sigma\)-paired 8-mode signatures.” Not established as a theorem. The cited bridge script itself treats this as structural analogy, not an eigenvalue-to-pole theorem.

- `rh-formal.tex:914`: uniqueness from U1-U3. Mostly established, but U1 alone already gives equality on \(\Re s>1\) plus meromorphic continuation. The “critical strip filled from boundary” phrasing is analytically imprecise.

- `rh-formal.tex:1299`: zeros-as-resonances is now correctly a Conjecture. Round 5 fix closed.

- `rh-formal.tex:1325`: H-Mat lemma. Established under the corrected integer-matrix hypothesis. Round 5 fix closed; the weaker \(\mathbb Z[\phi]\) version is explicitly refuted at `rh-formal.tex:1366`.

- `rh-formal.tex:1509`: finite \(\sigma\)-attractor spectral split. Established only as script-verified/numerical finite data, not as a representation-theoretic closed-form proof unless that proof is supplied.

- `rh-formal.tex:1612`: finite-noise diagnostic. The statement matches the script design, but R2/R3 are projection tests by construction. I could not get that script run to complete in the sandbox; I reviewed the source instead.

- `rh-formal.tex:1962`: “explicit candidate self-adjoint operator, \(F|_{H_4}\).” Not established. The paper gives a finite \(600\)-cell Laplacian testbed and conditional closure hypotheses; it does not prove a self-adjoint \(F|_{H_4}\) with the required zero spectrum.

**2. Internal Consistency**

- The largest conflict is the zeta-side counting: `rh-formal.tex:176` says four reciprocal pole pairs, while `rh-formal.tex:823` and `rh-formal.tex:865` say eight \(\sigma\)-paired poles/signatures. These are not the same statement.

- `rh-formal.tex:1409` and `rh-formal.tex:1978` say Galois-stability follows from “F5 + H-Mat.” The actual lemma uses H-Mat alone. F5 may motivate the operator, but it is not used in that proof.

- `rh-formal.tex:967` says the paper “constructs” a cascade-derived Dirichlet series, but `rh-formal.tex:1021` defines it to be the classical \(\zeta\). Say “defines a reformulation,” not “constructs.”

- Cross-references and citations appear syntactically resolved in source; I found no missing `\ref`/`\cite` keys by static check. I could not compile because `pdflatex` is unavailable.

**3. External Consistency**

- F5 attribution is still weak. The bibliography key points to `papers/cascade-derivation/cascade-foundations.md`, but that file says its F4/F5/F7 material is superseded by `papers/paper-xxxvi/paper-xxxvi.tex`. The corrected conditional F5 is verifiable in Paper XXXVI, not cleanly in the cited working note.

- Paper XXII claims about \(600\)-cell/\(2I\), shell classes, and \(94+26\) split are locally verifiable in `papers/paper-xxii/paper-xxii.tex` and its script.

- The “zeros-as-resonances” attribution to the older cascade seam is correctly demoted: the local seam file is programmatic/informal and does not prove the former lemma.

- Transport-law and ARIA-attractor references are consistent only as conditional/programmatic support. They do not prove the spectral-zero bridge.

- The Smart comparison is fixed in the main paper, but the cited local note `docs/convergence-with-smart.md` still says “same torsion order: 12” and uses stronger “forced” language. That undercuts the Round 5 fix unless the note is updated or the citation is made non-load-bearing.

**4. Tightness**

- `rh-formal.tex:760`: Replace “cannot coincide with any global Hecke \(L\)-function” with “no global Hecke \(L\)-function identification is established under this Mellin parametrization.”

- `rh-formal.tex:823`: Replace “8 \(\sigma\)-paired pole pairs” with “four reciprocal pole-location pairs, with multiplicities \(1,1,5,5\), unless the Coxeter-plane doubling convention is explicitly imposed.”

- `rh-formal.tex:859`: Demote Theorem B14 to “Structural Bridge / Conjectural Correspondence.”

- `rh-formal.tex:1607`: Replace “collapses to \(\sigma\)-fixed” with “is conjectured to collapse to \(\sigma\)-fixed.”

- `rh-formal.tex:1962`: Replace “supplies an explicit candidate self-adjoint operator” with “supplies a finite \(H_4\) Laplacian testbed and an abstract conditional closure-operator slot.”

**5. Surface Issues**

- `rh-formal.tex:219` repeats the same Smart/Bridge-Axiom sentence structure from the preceding paragraph.

- Literal proof-ending `□` symbols are unnecessary beside theorem environments and may be fragile depending on engine.

- The `CascadeFoundations` bibliography path/citation should point to Paper XXXVI for the corrected F5 statement.

**6. Top Three Fixes**

1. Fix the \(8\)-mode/\(8\)-pole overclaim at `rh-formal.tex:811`, `rh-formal.tex:823`, and `rh-formal.tex:859`. This is the main remaining mathematical mismatch.

2. Rewrite the Hecke obstruction at `rh-formal.tex:760`. It currently proves less than it claims.

3. Clean up F5/H-Mat attribution and dependency at `rh-formal.tex:487`, `rh-formal.tex:1409`, and `rh-formal.tex:1978`, and update the Smart convergence note so it no longer contradicts the softened “12-related cyclic structure” wording.
