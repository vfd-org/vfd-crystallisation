**1. Claim Audit**

- “`|gH ∩ V_24| = 4` … for every left/right coset” (`bekenstein-incidence.tex:43-46`, `250-262`): numerically supported by `verify.py` for the deterministic `H` chosen by `build_state()`. Not yet theorem-grade because the certificate does not fully certify the group/subgroup inputs.
- “Closure … verified … on each of the `120^2 = 14,400` products” (`bekenstein-incidence.tex:126-140`): over-claimed. `verify.py` checks only 200 random products (`verify.py:44-52`). Local exhaustive check passes, but the supplied certificate does not do what the paper says.
- “Every element has norm 1” (`bekenstein-incidence.tex:126-129`): true locally, but not asserted by `verify.py`.
- “`σ(V_600)` meets `V_600` exactly along the 24 … vertices” (`bekenstein-incidence.tex:121-124`): not proved or asserted by `verify.py`. Local check supports it, but the artefacts do not certify it.
- “Fix `τ ∈ G` with `|τ|=10`” then define the bulk subgroup from its cycles (`bekenstein-incidence.tex:147-178`): hidden choice. Different order-10 `τ` choices give different bulk `H` subsets. Local check found 6 distinct such `H`s. Either specify a canonical `τ` or quantify and verify all choices.
- “K-multiset `{72:1, 0:1, 52:5, 20:5}`” (`bekenstein-incidence.tex:164-173`): supported by `verify.py:54-59` for the chosen state. Proof is just “direct computation”; acceptable only if the certificate is promoted to the proof object.
- “`H = Dic_5` … closed … presentation” (`bekenstein-incidence.tex:180-197`): partially established. `verify.py` finds relations, but does not assert `H` is closed, does not assert `<a,b>=H`, and prints only indices, not an explicit coordinate certificate.
- “`b` is the unique element of `H` satisfying …” (`bekenstein-incidence.tex:191-194`): false. Local check gives 10 such `b` elements for the chosen `a`.
- “The `24` pairs `g ∈ G \ H` for which `gH != Hg`” (`bekenstein-incidence.tex:205-208`): false. Local check gives all 100 elements outside `H`.
- “Equivalently, `|G:N_G(H)| < |G:H|`” (`bekenstein-incidence.tex:207-208`): false. Local check gives `N_G(H)=H`, so `|G:N_G(H)| = |G:H| = 6`. Nonnormality is true, but this proof is wrong.
- “`V_24` has cardinality 24 … regular 24-cell” (`bekenstein-incidence.tex:232-238`): count proof is adequate; regularity is citation-backed, not derived. Fine if the paper only needs the incidence count.
- “verification script … exhibits the explicit four-element intersection” (`bekenstein-incidence.tex:272-276`): false. The script prints counts, not the four elements.
- Table incidence counts (`bekenstein-incidence.tex:288-313`): matches `verify.py` output for the chosen `H`; not an independent certificate.
- “`S(C)/A(C)=1/4` for every coset” (`bekenstein-incidence.tex:328-341`): follows from the incidence theorem once its certificate gaps are fixed.
- “reproduces the Bekenstein-Hawking entropy coefficient” (`bekenstein-incidence.tex:53-56`): over-claim. The math proves a dimensionless finite ratio. The physical coefficient requires an unproved calibration, which the paper later admits (`bekenstein-incidence.tex:386-391`).
- Scope claim “Established exact, unconditional” (`bekenstein-incidence.tex:349-364`): too strong until the certificate actually checks the theorem inputs exhaustively.

**2. WO Acceptance Audit**

Priority gaps:

- B1 coset-side/normality: partially resolved. Both sides are verified, but the nonnormality proof contains false numbers.
- B2 `Dic_5` presentation certificate: partial. Relations found; generation, closure, uniqueness wording, and explicit coordinates are missing.
- B3 exact shell ordering: resolved in `group.py:65-99`.
- B4 `V_24 = Fix(σ)` proof: partial. Count is proved; `V_600 ∩ σ(V_600)` is not certified.
- B5 incidence certificate: partial. Counts printed; explicit intersections and partition checks absent from `verify.py`.
- B6 interpretation firewall: partial. Scope is cautious, but title/abstract/conclusion still sell the physical coefficient too hard.
- B7 reproducibility harness: resolved. `verify.py` exits 0; shared tests pass.

Numbered WO items:

- 1: partial. No `paper.tex`; current file is `bekenstein-incidence.tex`. Abstract still says “reproduces” the BH coefficient.
- 2: resolved with caveat. Intro cites Bekenstein/Hawking/String/LQG and says finite arithmetic, but physical phrasing remains too strong.
- 3: partial. Exact model exists; closure/unit claims are not fully certified by `verify.py`.
- 4: partial. Bulk subgroup section exists; subgroup and presentation certificate are not complete.
- 5: partial. `V_24` count is present; only Coxeter is cited, not the full requested Coxeter/Conway-Sloane/Moody-Patera trio.
- 6: partial. Main theorem exists; non-bulk `K=52/K=20` coset lemma is not in the derivation, only tests.
- 7: partial. Ratio theorem exists; interpretation still reads like a physical match in places.
- 8: partial. Scope box is present, but does not explicitly exclude `τ_σ`.
- 9: resolved. Comparison table is compact and makes no superiority claim.
- 10: partial. Appendix describes the script, but the script is weaker than the appendix/paper need.
- 11: partial. `verify.py` does not assert full closure, unit norm, `H` subgroup closure, generated subgroup equality, coset partitions, or explicit intersections.
- 12: resolved. Commands pass; current shared baseline is `33 passed`, not the WO’s stale `28 passed`.

Top-build items F1-F3: F1 partial under a different filename; F2 partial for certificate reasons; F3 mostly done, but the `group.py:94` line reference in WO is stale.

**3. Catalogue Audit**

No catalogue/ledger is supplied under `papers/bekenstein-incidence`. That fails catalogue audit if the programme requires one.

Uncatalogued derivation objects: definitions at `112-119`, `147-162`, `175-178`, `218-226`, `318-326`; lemmas at `126-140`, `164-173`, `180-209`, `232-246`; theorems at `250-286`, `328-341`; numerical result table at `288-313`.

**4. Attribution / External Consistency**

- WO citations to `NARRATIVE.md:60` and `:68` check out.
- WO citation to `SCOPE.md:20` checks out, but `SCOPE.md:5` still says only “left coset” in the pitch while the theorem now needs both sides.
- WO citation to `NOTATION.tex:7` checks out for `2I`, `V_600`, `Dic_5`, `V_24`, `σ`, `1/4`.
- WO citations to `icosian.py` check out for exact `Fraction` arithmetic and vertex construction.
- WO citations to shared tests check out, but the paper script itself is weaker than the shared tests.
- Derivation claims about `verify.py` are not all true: no explicit triple (`bekenstein-incidence.tex:194-196`), no explicit four-element coset intersections (`273-276`), no exhaustive closure (`137-140` versus `verify.py:44-52`).
- SCOPE inherited old scripts include float shell ordering; current `vfd_v600` fixed this. Do not cite the old scripts as theorem-grade proof.

**5. Sim Correctness**

`verify.py` implements the main incidence count and exits 0. It is not a sufficient certificate.

Bugs/gaps:

- Random closure only (`verify.py:44-52`).
- No full `H` subgroup closure assertion (`verify.py:61-66`).
- No generated-subgroup equality check for `<a,b>=H` (`verify.py:68-72`).
- No coset partition/disjoint-union assertions (`verify.py:82-105`).
- No explicit coset representatives or intersection elements despite the paper claiming they are exhibited.
- Latent bug in `group.py:248`: the unit check uses `and` where a strict non-unit skip should use `or` or simply `nsq != (1,0)`. Harmless here because vertices are unit, but bad certificate hygiene.
- No statistics are involved; χ²/KL/null-hypothesis questions do not apply.

**6. Tightness**

- `bekenstein-incidence.tex:23`: change title to “A 1/4 Incidence Identity on the 600-Cell and its Bekenstein-Hawking Analogy.”
- `bekenstein-incidence.tex:53-56`: replace “reproduces” with “has the same dimensionless numerical value as.”
- `bekenstein-incidence.tex:191-194`: delete “unique.”
- `bekenstein-incidence.tex:205-209`: replace with “For every `g∉H`, enumeration gives `gH≠Hg`; hence `N_G(H)=H`, so `H` is self-normalizing and not normal.”
- `bekenstein-incidence.tex:397-408`: replace “admits a finite-arithmetic realisation” with “has a finite-arithmetic analogue.”

**7. Top Three Fixes**

1. Fix the false `H`/normalizer/presentation statements at `bekenstein-incidence.tex:191-209`. These are direct mathematical errors.
2. Upgrade `verify.py:44-115` into the actual certificate promised by the paper: exhaustive closure, unit norm, `H` closure, `<a,b>=H`, coset partitions, `V_600 ∩ σ(V_600)`, explicit coset reps/intersections.
3. Reduce BH-coefficient language in title/abstract/conclusion (`bekenstein-incidence.tex:23`, `53-56`, `397-408`) and either specify the chosen `τ` canonically or verify the theorem for all order-10 `τ`-derived bulk subgroups.

**8. Verdict**

Publication ready: no.
