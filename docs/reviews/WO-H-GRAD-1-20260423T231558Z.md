**1. Claim Audit**
- “A1 ... BUILD COMPLETE” at `cascade-h-grad-1-closure.md:35` and `:116`: established by the sim, with a caveat. I independently checked `det_basis_scaled_coords = 16`, `rank = 8`, all 120 units have integral coordinates, and the basis vectors are themselves unit icosians. Since local `I` is defined as the subring/Z-span generated by the 120 unit icosians, this is an actual Z-basis, not merely a spanning set.
- “A2 ... BUILD COMPLETE” at `:36` and `:164`: essentially established. The script computes `q([x]) = Tr(N(x)) mod 2` directly, not by fitting labels. I additionally checked the polar form has F2-rank 8; with 136 zeros, this is the canonical Arf-0 E8/2E8 quadratic form.
- Defect: stale reversed counts remain: “120 isotropic ... 135 anisotropic” at `:157-158` and the sim outline repeats it at `:334-335`. This contradicts the corrected claim at `:164-170`.
- A3/B3 claims at `:225-230`, `:385-417`: correctly marked open. No Fano quotient is constructed.
- A4/B4/B5 claims at `:293-296`, `:419-443`: correctly marked open. No `red` or `C_O` characters exist.
- B6 at `:445-455`: only a target, not proved.

**2. WO Acceptance Audit**
- A1: resolved; WO correctly flips it at `WO-H-GRAD-1.md:41`.
- A2: resolved in WO at `:42`, but derivation has stale contradictory counts.
- A3: not resolved (`:43`, `:83-86`).
- A4: not resolved (`:44`, `:88-91`).
- O1: not resolved (`:45`, `:97-101`).
- O2: not resolved (`:46`, `:103-107`).
- O3: not resolved (`:47`, `:109-112`).
- O4: resolved by B1 sim; WO correctly flips it at `:48`.
- O5: partial only; B1 done, B4 still missing (`:49`).

**3. Catalogue Audit**
No H-grad-1 catalogue was supplied/found. The only local catalogue, `cascade-observer-zeta-catalogue.md`, is for another note and omits B1/B2/B3/B4/B5/B6. If it is intended as the ledger, it is defective.

**4. Attribution / External Consistency**
- `cascade-algebraic-substrate.tex:480-499` supports the 120-unit definition, rank 8, and existence of an eight-unit basis.
- `cascade-q-o-measurement-bridge.md:112-138` supports only `Q_O ≅ Meas(S7, sigma)`, not explicit Pontryagin characters; the derivation states this correctly at `cascade-h-grad-1-closure.md:242-247`.
- `insight.md:670-678` supports the factor-7 motivation only; no `Theta_QMS` map.
- `cascade-fano-grading-lift.md:163-169` still has the old wrong 120/135 count and an over-strong Fano-subspace projection claim.

**5. Sim Correctness**
`verify_h_grad_1.py` passes all requested sections: B1 120/120, B2 `1+135+120=256`, sanity 60 classes. No χ²/KL/statistical null is involved; this is exact enumeration. Hidden assumption: the encoded 120-unit list is the Conway-Sloane list.

**6. Tightness**
Edit `cascade-h-grad-1-closure.md:157-158` and `:334-335` to `135 isotropic, 120 anisotropic`.

**7. Top Three Fixes**
1. Fix stale reversed A2 counts at `cascade-h-grad-1-closure.md:157-158` and `:334-335`.
2. Add polar-rank/nondegeneracy output to `verify_h_grad_1.py` near `:368-381`.
3. Add an H-grad-1 catalogue entry set for B1/B2 numerical results and B3-B6 open builds.

**8. Verdict**
Publication ready: no. B1/B2 sim closure is credible; the full WO remains open and the derivation still contains contradictory A2 statements.
