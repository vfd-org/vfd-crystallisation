**1. Claim Audit**
- `ns-formal.tex:142` Proposition main: only follows if `B_NS` is assumed exactly as a uniform-in-`T` BKM bound. The proof weakens/changes the data to `s >= 1` at `:595`, which is not Kato smooth local theory. Step 2 also uses a converse to the cascade-NS theorem not stated.
- `:162` Lemma `Pi_hyd`: not established. `sum_{k in Z} phi^{-2k} = 1+phi` at `:193` is false; only `k >= 0` converges. Curl is one derivative, not two (`:202-204`). A finite 600-cell shell is not a Shannon lattice, and no bandlimit/divergence-free compatibility is proved.
- `:315` F2 import: broadly consistent with Paper XXXVI only under H-MIN, but the wording omits Paper XXXVI’s order/degree/symmetric-sector restrictions.
- `:323` F4 import: verified as Paper XXXVI’s dimension count, not a spectral theorem. Correct as scoped.
- `:336` F5 import: verified as Paper XXXVI’s H-SIG finite-sum factor-2 statement. Later wording at `:735-738` drifts back to “sigma-invariance of F”, which is not the same claim.
- `:367` Cascade-NS correspondence: not proved. The proof is heuristic coefficient matching and conflicts with P7, which gives only a conditional weak NS limit on `S^3`, not an unconditional theorem on `R^3`.
- `:429` Rank preservation: false as used. Linear averaging does not control nonlinear differential operators, and rank <= 2 does not bound vortex stretching.
- `:465` Bounded macroscopic operators: overclaims global Sobolev boundedness without assuming `B_NS`. This is essentially regularity smuggled into a lemma.
- `:506` `B_NS`: acceptable as a hypothesis, but `:504` calling it “the key theorem” is wrong.
- `:561` BKM theorem: usable as a continuation criterion, but the citation is the Euler BKM paper; cite a Navier-Stokes continuation formulation or state the adaptation.
- `:572` Global extension: valid only because `B_NS` assumes the needed uniform vorticity bound. It adds no independent content.
- Numerical claims: spectrum at `:636-651` is verified by the local script, but `:243` gives wrong eigenvalues `7 ± sqrt(5)`. The viscosity estimate at `:417-422` is unsupported.

**2. Internal Consistency**
- F4 means different things in adjacent places: `:240-246` has one spectrum, `:243` misprints the middle pair, and `:636-642` has the correct script-verified spectrum.
- `H_NS`, `B_NS`, `B_NS_3`, and `Pi_hyd` are not one formal object. `:236-238` says the proposition is conditional only on `B_NS_3`, contradicting `:142-145`.
- The coarse-graining maps conflict: ball average of `partial_t Phi` at `:350-358`, sinc reconstruction from scaled 600-cell vertices at `:176-183`, and P7’s bump-kernel projector on `S^3`.
- All in-file `\ref`/`\eqref` targets appear present, but `Theorem~\ref{thm:F2}` at `:431` and `:442` refers to a `hypothesis` environment.
- The proof’s `s >= 1` at `:595` conflicts with the proposition’s `s > 5/2` example at `:149`.

**3. External Consistency**
- Paper XXXVI: F2/F4/F5 scoped imports are broadly verified locally. Paper XXXVI lines 219-226 and 265-274 require H-MIN; lines 430-447 make F4 a dimension count; lines 539-562 make F5 H-SIG finite-sum factor-2 only.
- P4: verified. It proves dissipation, not conservation, at `cascade-closure-dynamics.tex:640-653`, and explicitly no PDE/hydrodynamic theorem at `:1339-1344`.
- P6: not as strong as `ns-formal.tex:42-45`. P6 says spectral convergence is a conditional program and “not Clay-grade” at `cascade-schlafli-convergence.tex:76-87` and `:1406-1428`.
- P7: not as strong as imported. It gives conditional weak NS limits under `H_hyd` at `cascade-hydrodynamic-projection.tex:1010-1066`, and explicitly no smoothness/uniqueness/regularity at `:1138-1149` and `:1264-1272`.
- `CascadeAttractorSpectrum`: verified 94/120 fixed and 26/120 paired; it also reports no 2T localization.
- `CascadeAttractorDynamics`: source shows R2/R3 reach fixed support because the code explicitly applies `P_fix`; this is not independent evidence of closure dynamics.
- `aria-chess/...`: cannot verify locally; `aria-chess` is absent. The local `cascade-h4-oscillator-law.md` derives a law with free couplings, not the torus-attractor/8-pair empirical claim at `:689-695`.

**4. Tightness**
- `:414` replace “Hence the macroscopic limit IS Navier-Stokes” with “Under a P7-style scaling hypothesis, the projected limit would be Navier--Stokes.”
- `:719` replace “Why cascade closes the question” with “What `B_NS` would close.”
- `:768` replace “Our uniform vorticity bound” with “Hypothesis `B_NS`”.
- `:98-100` replace “standard Sobolev analysis” with “an open reconstruction theorem.”
- `:777-778` delete “cascade provides an alternative proof”; no 2D proof is given.

**5. Surface Issues**
- Undefined/unsafe LaTeX: `\textbeta` at `:59`, `\Vcal` at `:534`, `\bm` at `:648` and `:650`.
- Unicode likely unsafe under pdfLaTeX: `m²` at `:421`, `H₄` at `:783`, and raw `σ` throughout.
- Missing bibliography entry for Paper XXXVI despite repeated “per Paper XXXVI” claims.
- `B$_{\text{NS}}$_3` at `:237` is malformed notation.
- Inconsistent capitalization/spelling: `Navier-Stokes` at `:373`, `:414` versus `Navier--Stokes` elsewhere.

**6. Top Three Fixes**
1. Replace `Pi_hyd` lemma and Cascade-NS theorem with explicit hypotheses aligned to P7; fix the false `k in Z` summation and derivative scaling (`:162-205`, `:367-415`).
2. Delete or demote the rank-preservation and bounded-operators lemmas; they do not prove bounded vortex stretching or global Sobolev control (`:429-500`).
3. Repair external-status claims: P6/P7 are conditional weak-limit infrastructure, `aria-chess` is unverifiable locally, and the F4 spectrum typo at `:243` must be corrected (`:42-45`, `:243`, `:689-695`).
