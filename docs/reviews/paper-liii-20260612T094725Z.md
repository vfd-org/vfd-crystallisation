**Claim Audit**

Line refs are to `papers/paper-liii/paper-liii.tex`.

- L33, L329: “39/39 PASS” is reproducible: `scripts/verify_gr_closure.py` reports 39 PASS. It also emits an empty-slice runtime warning in the radial-profile test, so the numerical evidence should be reported with that caveat.
- L89-L95, stress tensor theorem: the proof establishes the free, source-free calculation only. It needs `J=0` or inclusion of source stress, `\alpha>0`, and a consistent `\Box` convention.
- L104-L109, discrete conservation: plausible for symmetric `L`, but the proof is only a sketch. The local formula uses scalar squares although `F` is quaternionic/vector-valued.
- L116-L117, dust limit: no proof is supplied. The massive equation needs the massive stress tensor, including the `\mu^2 F^2` term; the cited simulation is not a theorem.
- L136-L142, tensoriality: establishes only spatial-frame `SO(3)` covariance under `2I` actions. It does not establish a four-dimensional Lorentz rank-2 field with 10 spacetime components.
- L144-L154, Wishart action: proves a local covariance-rate quadratic term for independent Gaussian modes near equilibrium. It does not prove a two-derivative kinetic action for `h_{\mu\nu}`.
- L162-L167, chart gauge: the Lie-derivative statement is standard, but “therefore the effective action is invariant” is an extra physical assumption, not a consequence of the computation.
- L182-L191, Fierz-Pauli uniqueness: the classical theorem is true under locality/Lorentz/two-derivative assumptions, but the paper’s SVD over random samples is not a proof “for all.” The two-polarisation count also needs a proper constraint/gauge analysis.
- L194-L195, variation equals `G^{lin}`: no derivation is given, only numerical random checks.
- L202-L203, conserved coupling: the integration-by-parts argument works with boundary assumptions, but uniqueness of the substrate stress tensor and conservation in interacting/source sectors are not proved.
- L206-L207, universality: “one substrate” does not by itself prove one effective coupling for all rendered sectors. This is an assumption.
- L214-L229, trace-reversed equation: standard if Fierz-Pauli plus conserved `T` is accepted. The normalization is matched to Newton, not derived. The exact point-source claim is stronger than the displayed 3% grid profile witness.
- L231-L237, compactness/light bending: standard consequences of the weak-field metric, but “unconditional” is false given the residuals; light bending is asserted, not derived.
- L243-L249, nonlinear completion: correctly labelled conditional, but “the substrate supplies all premises” is not established. Self-coupling is not derived from `\Theta_{00}\ge0`.
- L276-L282, Schrödinger envelope: standard KG nonrelativistic reduction, but the sign convention conflicts with L116 and `\hbar`/mass normalization are imported.
- L284-L286, equivalence principle: “agree identically” is too strong; the reported values differ by `2.1e-4` and support only an `O(\epsilon^2)` agreement.
- L296-L298, Maxwell: standard if `A_\mu -> A_\mu+\partial_\mu\chi` is assumed. The chart-phase redundancy and charge sector are not derived.

**Internal Consistency**

- `\Box` is inconsistent. L76 defines `\Box=c^{-2}\partial_t^2-\nabla^2`, while the action with signature `(-,+,+,+)` at L79/L83 gives the opposite standard d’Alembertian. L116 says `(\Box-\mu^2)F=0`, but L277 uses the stable KG sign `\partial_t^2F=c^2(\nabla^2-\mu^2)F`.
- “Closes the chain” / “unconditional” at L38, L42, L231 conflict with residuals at L306-L312 and the second-pass caveats at L316-L322.
- L300 says the non-abelian gauge group content is open; L322 says the gauge-group inventory is derived. These cannot both stand as written.
- L316 and L359 say `verify_residual_closure.py` is 23/23. Running it gives `SUMMARY: 24 PASS, 0 FAIL`.
- L128-L137 define a 10-component spacetime field, but the tensoriality proof only treats spatial frame components.
- I found no unresolved `\ref`/`\eqref` targets in the source.

**External Consistency**

- `SmartKappa`: verified locally. It supports Newtonian response and the prior conditional Schwarzschild/Fierz-Pauli obstacle state, but with continuum assumptions and `G` as calibration.
- `SmartRendering`: verified locally for kernel/chart/inner clock reversal. It relies on an equivariance hypothesis and uses a normalization `L -> -2a^2\Delta_{S^3}`, not simply L76’s `L/a^2 -> -\nabla^2`.
- Paper XL: verified. It is explicitly a programme paper with conditional/open T1-T4 targets, not a continuum theorem.
- Paper L: not verified for L252. Paper L gives `\Omega_\Lambda=2/3≈0.667`, 2.7% from Planck, not `0.6844` at `-0.083%`.
- Paper XVIII: partially verified. It handles equilibrium and near-equilibrium Nelson/Schrödinger structure; generic interacting recovery remains nonlinear/open.
- Paper LII: verifies the muon shell `96` claim, but not the formula for `G`; that formula is in `docs/residual-closure-derivation.md`, and is conditional.
- `docs/gauge-group-from-arenas.md` contradicts L322: it says the mechanism gives `SU(3) × SU(2)` with a distinguished circle inside `SU(2)`, not an independent `SU(3) × SU(2) × U(1)` product.

**Tightness**

- L38: replace “This paper closes the chain” with “This paper gives an effective-field closure of the chain, modulo the named residuals.”
- L59: replace “there is nothing left of it” with “the specific quartic-in-`F` objection no longer applies.”
- L63: replace “must be gauge invariant” with “is assumed to be gauge invariant under chart redescription.”
- L187: replace “carries exactly two physical polarisations” with “has the standard Fierz-Pauli two-polarisation content, under the usual constraint analysis.”
- L207: replace “There is therefore a single coupling” with “The programme postulates a single rendered coupling; the quantitative check is T12.”
- L252: replace the `0.6844` sentence unless the cited local source is changed.

**Surface Issues**

- Use `\langle \partial_\mu\delta F,\partial_\nu\delta F\rangle` at L131; the current expression is ambiguous.
- Use norms/inner products, not `F_v^2`, for quaternionic fields at L108.
- L319 says finite-moment i.i.d. modes for Cramér; the local derivation requires bounded/sub-Gaussian or finite moment-generating function near the mean.
- `BoulwareDeser1975` at L248 is not a clean exclusion of all massive alternatives; either scope it or remove it.
- The verification count `23/23` must be updated to `24/24` or the script/docs reconciled.

**Top Three Fixes**

1. Fix the `\Box`/mass-sign convention across L76, L79, L83, L116, and L277. This affects the stress tensor, KG bridge, and field equations.
2. Downgrade “closed/unconditional/theorem” language at L38, L42, L231, L284, and L296 to match what is actually proved.
3. Correct the external inconsistencies: Paper L’s `\Omega_\Lambda` value at L252, the residual-script count at L316/L359, and the SM gauge-group claim at L322.
