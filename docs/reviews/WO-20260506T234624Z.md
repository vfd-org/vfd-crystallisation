**1. Claim Audit**
- `Σ_{V_600}` structural spine, `papers/v600-unified/v600-unified.tex:117`: **not established as stated.** The tuple omits the Galois map `σ`, but IT2 and IT3 explicitly need `σ` (`:254-255`, `:278`, `:335`, `:338`). Therefore the central factorisation claim “none uses datum outside `Σ`” at `:342` is false unless `σ` is added to the spine.
- `V_24 / Dic_5` lemma, `v600-unified.tex:184`: **supported.** Paper 1 supports `|V_24|=24`, `|Dic_5|=20`, distinctness, and 4 fixed vertices per left/right coset. The main text states the distinction correctly here.
- Phase-independence lemma, `v600-unified.tex:202`: **mostly supported, slightly over-broad.** IT1/IT2 do not reference `τ_σ`; Paper 3 supports `(P1)-(P4)` for all 32 lifts; Paper 4 supports trace-ratio phase-independence. But “All four foundation theorems” is too broad because IT3’s explicit phase-0 map is phase-specific.
- IT1 Bekenstein incidence, `v600-unified.tex:225`: **supported by Paper 1.** No new proof, no overclaim.
- IT2 Hawking quantum / first-law identity, `v600-unified.tex:244`: **supported by Paper 2.** The finite identity is imported correctly. The `T_H` notation remains physically suggestive but Paper 5’s scope firewall prevents a physics derivation claim.
- IT3 `τ_σ` involution, `v600-unified.tex:261`: **supported by Paper 3.**
- IT4 trace ratios / admissibility / Layer 2 / Layer 3, `v600-unified.tex:291`: **supported by Paper 4 and layer-separated correctly.** No Paper 4 Layer-1 trace identity is promoted into a Paper 5 unified-physics claim.
- Synthesis proposition, `v600-unified.tex:328`: **fails as written** for the same reason as `Σ`: `σ` is used but not included in `Σ`.
- F1 CMB future build, `v600-unified.tex:368`: **scope is right, wording is wrong.** “from `σ`-fixed bulk states” at `:372` leaks the old `bulk = σ-fixed` conflation. `Dic_5` is 20 vertices; only 4 are in `V_24`.
- Verification certificate, `v600-unified.tex:417`: **overclaims.** It says `verify.py` “asserts each structural-spine fact” (`:420`), but the script does not assert self-normalising/non-normal `Dic_5` or the left-coset-not-whole-cycle carrier fact.

**2. WO Acceptance Audit**
- B1 imported theorem registry, `WO.md:22`: **resolved** in `v600-unified.tex:225`, `:244`, `:261`, `:291`.
- B2 structural spine, `WO.md:28`: **partially resolved.** Table exists, but `σ` is missing from `Σ`; this breaks the factorisation claim.
- B3 `V_24` / bulk relation, `WO.md:35`: **partially resolved.** Main lemma and sim are correct; F1 wording and catalogue F1 still say “σ-fixed bulk states.”
- B4 Layer-3 cosmology firewall, `WO.md:42`: **resolved in the derivation.** Paper 4 Layer 2/3 is not promoted.
- B5 CMB bridge, `WO.md:48`: **partially resolved.** It is out-of-scope, but the wording still imports the bad σ-fixed-bulk phrase.
- B6 dipole audit, `WO.md:54`: **resolved.** Marked future work with frozen-protocol prerequisites at `v600-unified.tex:379-388`.
- B7 prediction manifest, `WO.md:60`: **resolved.** Marked future work at `v600-unified.tex:390-398`.
- Top build 1, `WO.md:96`: **resolved.**
- Top build 2, `WO.md:98`: **partially resolved** because `Σ` is missing `σ`.
- Top build 3, `WO.md:100`: **partially resolved.** The derivation is clean, but `SCOPE.md:57-58` and `SCOPE.md:74` still describe a CMB-bulk argument as synthesis content.

**3. Catalogue Audit**
- Present and backed: D1, L1, L2, IT1-IT4, S1, R1, F1-F3 all correspond to derivation objects.
- Defect: `CATALOGUE.md:32-34` repeats the defective `Σ` without `σ`, while IT2/IT3 later say `σ` is used from `Σ` (`:71-82`).
- Defect: `CATALOGUE.md:102-105` repeats the false “none uses any datum outside `Σ`” claim.
- Defect: `CATALOGUE.md:120-125` repeats “σ-fixed bulk states.”
- Missing catalogue object: the computational verification certificate in `v600-unified.tex:415-439` / `verify.py` is not catalogued as a computational result (`N1` or equivalent). “Verification entry points” is not a status entry.

**4. Attribution / External Consistency**
- Paper 1 attributions check out: K-multiset, `H=Dic_5`, right-coset carriers, non-normality, `V_24`, incidence, and τ-choice incidence are at `papers/bekenstein-incidence/bekenstein-incidence.tex:182-187`, `:193-204`, `:218-226`, `:257-260`, `:284-297`, `:310-320`, `:348-356`.
- Paper 2 attributions check out: spectrum and first-law identity are at `papers/v600-hawking-quantum/v600-hawking-quantum.tex:189-200`, `:245-264`; physical-calibration caveats at `:285-301`.
- Paper 3 attributions check out: right-coset construction, phase selection, canonical map, theorem properties, and `Z_2^5` are at `papers/tau-sigma-construction/tau-sigma-construction.tex:139-156`, `:265-276`, `:327-337`, `:356-369`, `:410-434`.
- Paper 4 attributions check out: layer split, traces, admissibility, phase-independence, observations, hypothesis are at `papers/v600-cosmic-tensions/v600-cosmic-tensions.tex:43-46`, `:192-199`, `:215-224`, `:268-276`, `:290-296`, `:356-383`.
- `docs/closure-cosmogenesis.md` does contain the conditional/open CMB prerequisites, but it also says the 20-vertex bulk is σ-fixed at `docs/closure-cosmogenesis.md:23` and `:36`, which conflicts with Paper 1. Paper 5 must not echo that language.

**5. Sim Correctness**
I ran:

`PYTHONPATH=papers/v600-programme/lib python3 papers/v600-unified/verify.py`

All assertions passed.

The sim correctly checks the headline finite arithmetic: `|V_600|`, K-multiset, `|Dic_5|`, `|V_24|`, `Dic_5 != V_24`, coset incidence, σ-pair energy, canonical `τ_σ`, trace ratios, and trace phase-independence.

Defects: it does not check every structural-spine fact it claims to check. Missing: `Dic_5` self-normalising/non-normal, left cosets not whole-cycle carriers, and Paper 1 τ-choice independence over all 24 order-10 choices. No χ²/KL/null-hypothesis issues apply; this is exact enumeration, not statistics.

**6. Tightness**
- `v600-unified.tex:121-129`: add `\sigmaG` to `Σ_{V_600}`.
- `v600-unified.tex:203`: replace “All four foundation theorems” with “The theorem-grade identities and properties imported below.”
- `v600-unified.tex:372`: replace “σ-fixed bulk states” with “bulk-supported / σ-invariant sector states, not identified with `V_24`.”
- `v600-unified.tex:420`: replace “asserts each structural-spine fact” with “asserts the listed headline cross-checks.”
- `SCOPE.md:57-58`, `:74`: remove the claim that the synthesis adds a CMB-bulk argument.

**7. Top Three Fixes**
1. Add `σ` to the structural spine and catalogue: `v600-unified.tex:121-129`, `:328-343`; `CATALOGUE.md:32-34`, `:102-105`.
2. Remove the σ-fixed-bulk conflation from F1: `v600-unified.tex:368-377`; `CATALOGUE.md:120-125`.
3. Either add sim checks for the missing spine facts or narrow the verification claim: `v600-unified.tex:417-421`; `verify.py:84-199`.

**8. Verdict**
Publication ready: no.

The main mathematical defect is not cosmetic: the central “all four factor through `Σ_{V_600}`” claim is false until `σ` is part of `Σ`. The CMB future-build wording also still leaks the exact `V_24` / `Dic_5` conflation this round was supposed to eliminate.
