Line references are to `papers/millennium-pnp-formal/pnp-formal.tex`.

**1. Claim Audit**
- “Restricted-model separation… Under H$_{\text{σ-irr-NP}}$ and $B_{\sigma-P}$” (155-165): conditionally correct only if the proof route is repaired. The later corollary omits $B_{\sigma-P}$ (384-392), and line 415 misstates the needed assumptions.
- “Conditional P vs NP… under H, $B_{\sigma-P}$, and structural completeness” (174-179): not proved. The proof assumes only Conjecture 4.1 (483-484), then uses $B_{\sigma-P}$ (490), H (502), and the unproved assertion that generic σ-irreducible SAT has no rung/φ-adic/$2I$ structure (493-497).
- CT equivalence (235-237): locally supported by `papers/cascade-derivation/cascade-dynamics.md` D2/D5/D6.
- “Generic σ-irreducibility… for any $\alpha>0$” (257-260): not proved. The proof only gives decay for $\alpha>1$ (283-284), and the key per-clause probability bound (271-275) is asserted.
- “Generic 3-SAT is σ-irreducible” (289-292): inherits the defective theorem.
- $B_{\sigma-P}$ (299-303): acceptable as an explicit open hypothesis; later citations must not call it a theorem.
- “$L_{\rm σ-red}\in NP$… verifying $P$ takes polynomial time” (345-349): not established. The definition quantifies over all σ-orbit-constant assignments (248-250); no polynomial verifier is given.
- “$L_{\rm σ-irr}\in coNP$” (350-351): depends on the previous unproved verifier and on a clean relative-complement convention.
- “H$_{\text{σ-irr-NP}}$ is equivalent… at least as hard as NP=coNP” (362-370): overclaimed. No reduction is supplied.
- Lemma “Conditional NP-containment” (374-379): tautological and correct.
- Corollary “Under H, $P_\sigma^{cas}\subsetneq NP^{cas}$” (384-386): false as stated; proof uses $B_{\sigma-P}$ at 392.
- Structural completeness conjecture (437-441): fine as conjecture, but “exploits” and “structural property” are undefined, so the claimed equivalence is not formal.
- Barrier propositions (513-579): not proved. They show at most “not obviously subject to” BGS/RR/AW, not barrier evasion.
- Spectral count (601-616): verified locally by the cited spectrum script; 94/120 and 26/120 are supported.
- Finite-noise diagnostic (630-649): supported by local RH source and script text; I did not complete an independent fresh run in this shell.
- aria H$_4$O claim (653-659): not locally verifiable; the cited `aria-chess/...` path is absent, though the paper correctly marks it external.

**2. Internal Consistency**
- Abstract lists the restricted separation as conditional on H and structural completeness (40-53), but the separation actually requires H and $B_{\sigma-P}$; structural completeness is only for the classical bridge.
- Line 415 says Theorem/Proposition 1.1 follows under H and Conjecture 4.1. Wrong: it needs H and $B_{\sigma-P}$.
- Labels conflict with environments: `thm:sigmaPlim` is a hypothesis but cited as “Theorem” (392, 490, 523); `thm:separation` and `thm:conditional` are propositions but called theorems (182, 483).
- Definition 244-252 uses a bipartition $A\sqcup B$; later discussion invokes Bell-number “all partitions” combinatorics (335-338, 367). Pick one.
- σ on variables/orbits is never defined, yet σ-orbit-constant assignments are central to the definition (248-250).
- Cross-references exist, but `\S\ref{rmk:siNP-hypothesis}` (157) points to a remark, not a section.

**3. External Consistency**
- `CascadeDynamics`: verifies the imported CTM/TM and class-equivalence claims.
- `CascadeRHformal`: verifies the strengthened-conditional disclaimer, exact 94/120 spectral count, and the fact that spectral data alone does not prove the bridge.
- `ConvergenceWithSmart`: supports only descriptive architectural similarity, not a formal meta-theorem.
- `CascadeAttractorSpectrum`: verified by local run; numbers match.
- `CascadeAttractorDynamics`: source supports the quoted setup/results; fresh run did not finish here.
- `CascadeFoundations`/Paper XXXVI: does not support “two axioms + F1-F8” as a complete finite inventory without additional structural hypotheses; Paper XXXVI explicitly carries a thirteen-hypothesis stack.

**4. Tightness**
- 40-56: replace with “restricted separation needs H and $B_{\sigma-P}$; the classical bridge additionally needs structural completeness.”
- 150: replace “is strictly smaller” with “is a restricted subclass; strictness is conditional below.”
- 257: change theorem to heuristic/conjecture unless the σ-action and probability bound are proved.
- 513-579: replace “evades” with “is not shown to fall under the standard formulations of.”
- 756-757: replace “If Conjecture… proved, we have a proof” with “If H, $B_{\sigma-P}$, and the structural bridge are proved, the argument would imply $P\neq NP$.”

**5. Surface Issues**
- `hypothesis` environment is used at 299 but never declared.
- `\textgamma` (58, 184, 409), `\bm` (613, 615), and `\HH` (619) are undefined in the preamble.
- Unicode σ inside math identifiers such as `L_{\rm σ-irr}` is engine-sensitive; use TeX macros.
- Status section calls `thm:genirr` a “Heuristic” (706) while the body makes it a theorem (257).
- All-caps proof prose (“FIXED”, “NO”, “ARBITRARY”) should be normalized.

**6. Top Three Fixes**
1. Fix the dependency graph: lines 40-61, 384-395, 415-417, and 483-506 must consistently include $B_{\sigma-P}$ and any extra “no hidden structure” bridge.
2. Rebuild σ-irreducibility formally: define σ on variables, resolve bipartition vs Bell partitions, and prove or withdraw the NP/coNP and generic-density claims (244-370).
3. Demote the barrier-evasion theorem and classical P-vs-NP bridge unless a real normal-form/lower-bound theorem is supplied (437-441, 493-497, 513-579, 756-757).
