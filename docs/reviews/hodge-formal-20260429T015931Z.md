**1. Claim Audit**

- Line 195: “`H_{4a}^{alg}/Omega => H_{4a}^{alg}/X` holds whenever `iota_X` pulls back algebraic cycles…” Not established. Pullback preserving algebraic cycles is not enough; you also need the relevant `sigma`-fixed classes on `X` to lie in the pullback image. Add a surjectivity/transport hypothesis or remove the implication.

- Lines 204-218: Main conditional reduction. As stated, it is true only if `H_{4a}^{alg}/X` is assumed directly. The advertised “via pullback along `iota_X`” is not proved from `H_{4a}^{alg}/Omega`.

- Lines 355-406: `sigma` action / fixed-rational lemma. The integral version is not justified: `H^*(X,Z[phi]) = H^*(X,Z) tensor Z[phi]` fails without torsion hypotheses. Use the repository’s rational theorem instead: `cascade-sigma-rationality.tex` / foundations lines 2632-2663.

- Lines 478-492: Bruhat/Schubert statements are classical but imprecise. For `G/P`, specify connected complex reductive algebraic `G`. Also `[\bar C_w] in H^{2 ell(w)}` has the usual dimension/codimension indexing problem.

- Lines 534-542: Danilov theorem overclaims. `rank H^{2p}` is not the number of `p`-dimensional cones; there are relations. The generation statement is enough for the later proof.

- Lines 594-608: CM abelian status is acceptable as a cited status statement.

- Lines 610-615: “cascade contribution… identify CM abelian varieties as cascade-embeddable” is unsupported and false as written for general CM fields: a CM field need not carry a `Z[phi]` action.

- Lines 641-671: Complete-intersection theorem is still a serious overclaim. The proof asserts broad primitive-middle algebraicity from Noether-Lefschetz without supplying hypotheses. Line 670 contradicts the paper’s own weaker-status caveats.

- Lines 683-709: Proposition `embedhodge` repeats the same pullback gap: algebraicity on `Omega` does not imply algebraicity for arbitrary `sigma`-fixed classes on `X` without a surjectivity/image hypothesis.

- Lines 719-739: “Langlands-cascade reduction” is not theorem-grade. The proof sketch assumes a motivic Langlands result and an embedding construction not supplied. It also contradicts lines 902-906, which say no Langlands-cascade theorem is asserted.

- Lines 790-818: Spectral count verified locally by `test_sigma_attractor_spectrum.py`: `94/120 = 78.3%`, `26/120 = 21.7%`. But the same script reports no `2T` localization. If this script is cited as evidence, that null result should be disclosed.

- Lines 819-842: Dynamics numbers are finite simulation diagnostics only. I could not complete local verification in this pass; keep them non-load-bearing.

**2. Internal Consistency**

- `Theorem~\ref{thm:main-cond}` points to a `proposition` environment at lines 204-218. Either rename it theorem or call it Proposition consistently.

- Lines 243-245 say the main theorem is strictly conditional on `H_{4a}`, but lines 206-208 require both `H_{4a}` and `H_{4a}^{alg}`. Fix the scope sentence.

- Lines 274-281 and 880-890 correctly weaken complete intersections, but lines 641-671 still prove/claim too much.

- Lines 719-739 assert a Langlands theorem; lines 902-906 deny asserting one. Remove/demote the theorem.

- `\cite{Danilov}` and `\cite{CDK}` are undefined; bibliography has `Danilov1978` and `CDK1995`.

**3. External Consistency**

- `H_{4a}` is verifiable in `papers/cascade-correspondence-foundations/foundations.tex:2802`: it is explicitly open and gives embeddability plus compatibility, not algebraicity.

- `Pi_sigma` fixed-rationality is verifiable in foundations lines 2632-2663 and in `cascade-sigma-rationality.tex`: coefficientwise fixed part equals rational part over `Q(phi)`.

- The new ARIA note is verifiable in `docs/aria-closure-kernel.md:84-116`: it states the Hodge `sigma`-projector is a rank-1 / spectral-projection limit of the family. This is programme positioning, not a proof.

- The `aria/vfd-b-anomaly` evidence cited at lines 939-942 is external to this repo; I could not verify it locally.

**4. Tightness**

- Line 113: “classical mathematics does not have” is too strong. Edit: “the cascade formalism packages this boundary as a distinguished operator, conditionally on the bridge axioms.”

- Line 197: replace the implication by “would imply, provided the pullback image contains the relevant fixed classes.”

- Line 610: replace with “A possible cascade interpretation would be…”

- Line 719: replace theorem with “Speculative route.”

- Line 808: if the script is the source, replace “exact” with “numerically recovered and algebraically recognizable,” unless an exact proof is supplied.

**5. Surface Issues**

- Undefined citations: `Danilov`, `CDK`, `Voisin2002`, `Hodge1941`, `BorelHirzebruch`.

- Title says “main theorem”; environment is `proposition`.

- `Q[\pphi]` at line 406 should be standardized to `Q(\pphi)`.

- “This IS” at line 452 should be “This is”.

- `\S H${}_{4a}$` citation formatting is awkward; prefer `\S~H_{4a}` or a real label.

**6. Top Three Fixes**

1. Fix the pullback gap in lines 195-199 and 683-709. Add an explicit surjectivity/image hypothesis or assume `H_{4a}^{alg}/X` directly.

2. Demote/remove the complete-intersection proof at lines 641-671. It still claims full low-codimension Hodge from unsupported primitive-middle assertions.

3. Remove or demote the Langlands-cascade theorem at lines 719-739. It is speculative and contradicts lines 902-906.

Not publication-ready. The new ARIA subsection is mostly non-load-bearing and honestly framed, but it should soften “ask precisely” at lines 936-938 because `H_{4a}` concerns compatibility, while `H_{4a}^{alg}/Omega` and `/X` are separate algebraicity bridge assumptions.
