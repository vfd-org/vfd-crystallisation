**1. Claim Audit**

- `v600-cosmic-tensions.tex:131-148` — “Cycle space” and “K-class projectors.” Definitions are clear. The rank facts at `150-159` are not proved there; they depend on Paper 1 / `build_state()`. That dependency is acceptable and verified by `verify.py`.

- `v600-cosmic-tensions.tex:161-168` — Baseline rule. Correctly framed as definitional, not uniqueness under `tau_sigma`.

- `v600-cosmic-tensions.tex:173-186` — Operators `Hhat = I_C + P_72`, `Chat = I_C - P_0`. Established by definition; self-adjointness follows from diagonal form.

- `v600-cosmic-tensions.tex:190-211` — Theorem 4.1 trace identities. Proof establishes the claim.

- `v600-cosmic-tensions.tex:213-242` — Theorem 4.2 K-saturated admissibility. The rewritten `Q^2 = Q` argument is sound inside `A_K`. It proves exactly the narrow claim: rank-one projectors in the K-saturated diagonal algebra are `P_72` and `P_0`.

- `v600-cosmic-tensions.tex:244-252` — R1. The warning is right, but the example is still imprecise. A `tau_sigma`-invariant rank-one projector is built from a paired `K=52/K=20` two-cycle subspace, e.g. `span(e_52 ± e_20)`, not “inside a single K=52 or K=20 sub-block.”

- `v600-cosmic-tensions.tex:264-284` — Phase-independence lemma. Established, assuming Paper 3’s phase-lift theorem. The local and Paper 3 verifiers both pass.

- `v600-cosmic-tensions.tex:298-326` — Numerical observation. H0 arithmetic is fine. S8 central arithmetic is fine. The residual is not: predicted `0.7627` is below `0.766`, so the directional KiDS asymmetric error is `-0.014`, giving about `0.24σ`, not `0.16σ`. If using the larger error as a conservative/symmetrized convention, say that explicitly.

- `v600-cosmic-tensions.tex:328-338` — Rank-5 ablation. Trace ratios `17/12` and `7/12` are established. “Far outside any published H0 or S8 tension band” is too broad; say “the cited H0/S8 bands.”

- `v600-cosmic-tensions.tex:348-370` — Hypothesis H1. Correctly labelled as hypothesis, but it still maps the second observable as bare `sigma_8` while the observation table uses `S_8`. Applying the same trace ratio to `S_8 = sigma_8 sqrt(Omega_m/0.3)` needs an extra assumption about the `Omega_m` factor, or H1 must be rewritten directly for `S_8`.

**2. WO Acceptance Audit**

- B1 K-saturated admissibility: resolved in `tex:213-242`.
- B2 trace theorem: resolved; `verify.py` passes.
- B3 baseline rule: resolved in `tex:161-168`.
- B4 empirical table: partially resolved. Values fixed, but S8 residual handling is wrong/underspecified.
- B5 coupling hypothesis: partially resolved. It is Layer 3, but `sigma_8`/`S_8` mismatch remains.
- B6 phase-independence: resolved; all 32 phase choices checked pointwise in `verify.py:162-175`.
- B7 OD1 reconciliation: mostly resolved in `tex:384-394`, but stale repo artefacts still overclaim.
- B8 verification hygiene: resolved; `verify.py` uses shared exact `vfd_v600` code and passes.

Open WO fixes:
- C1-C3: resolved in `SCOPE.md:5,21,43`.
- C4: partially resolved; `SCOPE.md:53` still uses stale bare `sigma_8`, `0.7435`, and KiDS/DES wording.
- C5-C6: resolved.
- C7: only partly resolved. The target sentence is softened at `od1_operator_derivation.py:185-187`, but `od1_operator_derivation.py:219-229` still says “OD-1 DERIVATION CLOSED,” “No tunable parameters; no choices.”
- F1-F2: resolved.
- F3: partial because sign/coupling is separated, but S8/σ8 and stale SCOPE text remain.

**3. Catalogue Audit**

- D1-D3, T1, T2, L1, R2, H1 are present.
- Missing catalogue object: the projector-rank and resolution-of-identity computational result from `tex:150-159` / `verify.py:84-92`.
- R1 repeats the bad/imprecise invariant-projector example at `CATALOGUE.md:68-74`.
- N3 overstates verification: `CATALOGUE.md:96-103` says band-inconsistency is verified by `verify.py`; the script verifies only traces/ratios.
- N4 inherits the S8 residual problem at `CATALOGUE.md:108-110`.
- H1 catalogue entry uses `H_0 / sigma_8` at `CATALOGUE.md:119-123`; should match the S8 row or state the Ωm assumption.

**4. Attribution / External Consistency**

Repo-local attributions check out:
- Paper 1 K-multiset and bulk subgroup: supported by `bekenstein-incidence.tex:162-186` and `193-203`.
- Paper 1 right-coset carrier: supported by `bekenstein-incidence.tex:218-255`.
- Paper 3 phase swaps and `2^5` lifts: supported by `tau-sigma-construction.tex:225-238`, `265-287`, `410-434`; its verifier also passes.
- OD1 reversal: supported by `OD1_STATUS.md:3-6`, `83-87`, and `MISSING_MATH.md:3-10`.

External spot-checks support the cited benchmark values: Planck A&A for H0/σ8/S8, Riess/SH0ES via SciXplorer, Heymans KiDS multi-probe via UCL Discovery, and Asgari KiDS cosmic shear via UCL Discovery. The local `references.bib` entries are bibliographic pins, not exact value pins with table/DOI/value notes.

**5. Sim Correctness**

`PYTHONPATH=papers/v600-programme/lib python3 papers/v600-cosmic-tensions/verify.py` passes.

The finite algebra checks are correct: K-multiset, projector ranks, identity resolution, trace identities, ratios, all 32 phase choices with pointwise `H_perm == H`, `C_perm == C`, and ablation traces.

Defects:
- `verify.py:196-205` computes S8 residual using `0.020` while prediction is below central. Directional asymmetric residual is about `0.24σ`; combined Planck+directional KiDS is about `0.18σ`.
- It does not verify the N3 claim “outside published bands”; it only verifies ablation trace ratios.
- It relies on Paper 3 for phase admissibility, which is acceptable, but then Paper 4 should not imply it independently proves the phase-selection theorem.

**6. Tightness**

- `tex:29-31`: title should say `H0 and S8 benchmarks`, not `sigma_8 tensions`.
- `tex:289-290`: replace “currently published tension benchmarks” with “selected published benchmarks.”
- `tex:333`: replace “any published H0 or S8 tension band” with “the cited H0 and S8 bands.”
- `tex:375-378`: “parameter-free prediction” is too strong under a postulated coupling rule. Use “parameter-free consequence of Hypothesis H1.”
- `verify.py:204`: print asymmetric KiDS uncertainty or state “using +0.020 as a conservative symmetric scale.”

**7. Top Three Fixes**

1. Fix the `sigma_8`/`S_8` mismatch in `tex:348-370`, `tex:379-381`, `CATALOGUE.md:119-123`. Either make H1 an `S_8` coupling hypothesis or add the missing Ωm assumption.

2. Fix the S8 residual convention in `tex:309`, `tex:320-323`, `tex:415-417`, `verify.py:196-205`, `CATALOGUE.md:108-110`.

3. Clean remaining overclaims outside the main derivation: `SCOPE.md:53` stale σ8 row and `od1_operator_derivation.py:219-229` “DERIVATION CLOSED / no choices” language.

**8. Verdict**

Publication ready: no.

The core Layer-1 trace theorem and T2 admissibility proof are now sound. The remaining blockers are the S8 statistical handling and the unresolved σ8/S8 coupling mismatch.
