**Claim audit**

1. `Definition [Closure functional]` and `Proposition [Two exact symmetries of \Fcal]` do not yet fully establish what they claim.  
Lines 106-117 define `\Fcal: \C^{V_{600}} \to \R` by
> `C[\psi] = \tfrac12 (\psi,L_C\psi)`  
with only the parenthetical gloss
> `(quadratic closure term; L_C = shell-adjacency polynomial)`.
Two hidden hypotheses are missing:
- You need `L_C` to be self-adjoint / Hermitian, otherwise `C[\psi]` need not be real and `\Fcal: \C^{V_{600}} \to \R` is not established.
- You need `L_C` explicitly in the shell-adjacency algebra with real coefficients, otherwise the `H_4`-invariance claimed in lines 124-130 and proved at line 134 is not actually justified.  
The new `0\log 0:=0` convention fixes the entropy-term gap. It does not fix the quadratic-term gap.

2. `Hypothesis [Programme target: discrete continuity analogue of Paper XVIII]` is honestly fenced.  
Lines 156-163 explicitly say the discrete equation is a programme target and not proved. No over-claim there.

3. `Proposition [Total-probability conservation under Hypothesis...]` is proved correctly, conditional on exactly the stated assumptions.  
Lines 185-191 are fine. The proof establishes conservation of `Q` for any evolution satisfying the hypothesised continuity law plus antisymmetry. The only mild overstatement is the phrase “along the closure flow” at line 186: the proof does not use any closure-flow structure beyond the hypothesis.

4. `Hypothesis [Heuristic Schrödinger-limit dispersion identification]` is honestly fenced.  
Lines 224-234 make clear that this is not a theorem and enumerate the extra assumptions. No substantive objection.

5. `Hypothesis [Conditional photon-identification target]` is mostly honest, but one attribution still overreaches.  
Lines 304-308 correctly weaken the source note by foregrounding the “needs polish” caveat. But line 305 still says the later LaTeX papers use `\Zphi^2` “under an explicit rank-convention translation”. What I can verify locally is weaker:
- the source note uses `\Z[\phi]^4` and also elsewhere speaks as if the photon chain closes;
- `Cascade12D` and `SmartXXXIV` adopt the `\Zphi^2` convention;
- `SmartXXXIV` explicitly says no repository source carries out the downstream rewrite of the markdown-note arguments into that convention.  
So the attribution is still slightly too strong as written.

6. `Hypothesis [Microtubule as ARIA-class active substrate]` is honestly fenced as hypothesis.  
Lines 320-325 do not pretend to prove it.

7. `Numerical result` status is handled conservatively.  
The paper repeatedly says it establishes no numerical claims in this draft (lines 56, 360-369). That is consistent with the body.

**Internal consistency**

- The Round 9 fix at line 113 works. The entropy term is now well-defined on the stated finite-domain field space because of `0 \log 0 := 0`.
- The Round 9 fix at line 143 works. The unverified `/120`-type quantitative residual bound is gone.
- The tone repair at line 76 works. “candidate implementation-level testbed” now matches the later ARIA section.
- The math-mode fix at line 325 is present: `$T_{\text{MT}\,5}$` and `$T_{\text{MT}\,4}$` are in math mode.
- Cross-references and citation keys used in this file appear internally consistent on manual inspection; every `\citep{...}` key used in the text has a matching `\bibitem`. I did not compile the file.

The one real internal defect is the same one noted above: lines 99 and 106-117 claim `\Fcal: \C^{V_{600}} \to \R`, but the text never states the hypotheses on `L_C` needed to make that true.

**External consistency**

Verified locally:

- `Paper XII` claim at lines 69, 333: verified. `paper-xii.tex` lines 87-99 define both unprojected and projected gradient flow.
- `Paper XVI` claim at lines 70, 334: verified in substance. `paper-xvi.tex` lines 261-289 derive WKB transport with density advected along gradient-flow characteristics.
- `Paper XVIII` claim at lines 71, 157, 335: verified. `paper-xviii.tex` lines 115-130 give the exact continuity equation; lines 319ff state exact equilibrium and leading-order near-equilibrium Schrödinger recovery.
- `Paper XIX` claim at lines 143, 203, 336: verified. `paper-xix.tex` gives the nonlinear residual, exact `L^2` conservation, and continuity-equation discussion, but not a transport-law source term.
- `Paper XXVII` claim at lines 73, 288, 337: verified. `paper-xxvii.tex` line 182 explicitly says the centered identity is not a dynamical conservation law.
- `Paper XXX` claim at line 338: verified in substance. `paper-xxx.tex` repeatedly states the Route-E uniqueness package with completeness, basis-realisability, covariance, frame-function, continuity, eigenstate certainty, and `N \ge 3`.
- `Paper XXII` disclaimer at line 217: verified. `paper-xxii.tex` does not provide a theorem-level fractional-charge derivation; `docs/gaps.md` still records that gap.
- `P2 / CascadeAlgSubstrate` claims at lines 49, 134, 198, 225, 233: verified. `cascade-algebraic-substrate.tex` proves shell-class centrality at lines 1085-1096 and lists the nine Laplacian eigenvalues, with `\lambda=0` on the trivial irrep, at lines 1153-1161.
- `Paper XXXIV` claim at line 339 and the `50 = 9+12+14+15` arithmetic at lines 237, 339: verified. `paper-xxxiv.tex` explicitly treats `87` as a convention under Kostant-gauge and `50` as the rational-eigenvalue sum.
- `CascadeAlphaChain` photon-source-note caveat at line 305: verified. The markdown note says `T_PH_2` “needs polish” at line 101 but elsewhere speaks as if the photon chain closes (e.g. lines 131, 263-266).

Not verified / over-attributed:

- `Paper V calibration` for the coefficients `\alpha,\beta,\gamma` at line 121, echoed at line 377: I could not verify this in `paper-v.tex`. Paper V calibrates a mass-formula ansatz; I do not find a source there for these three closure-functional coefficients. This attribution should be removed or re-sourced.
- `Cascade12D/SmartXXXIV` as carrying an explicit translated `T_PH_2` chain from `\Zphi^4` to `\Zphi^2`: not verified. What is verified is only later adoption of the `\Zphi^2` convention plus an explicit notation-reconciliation remark, not a translated proof of the photon descent claim.

**Tightness**

- Lines 121-122: too strong.  
Current: “The coefficients `\alpha,\beta,\gamma` are ... assumed to be those of the mass-formula calibration of Paper V.”  
Edit: “The coefficients `\alpha,\beta,\gamma` are left unspecified here; no local source in this paper fixes them.”

- Line 186: slightly too strong.  
Current: “Then the total probability ... is conserved along the closure flow.”  
Edit: “Then the total probability ... is conserved along any evolution satisfying Hypothesis~\ref{thm:continuity-discrete}.”

- Line 305: still too strong on translation status.  
Edit: replace “under an explicit rank-convention translation” with “under a later `\Zphi^2` notation convention together with an asserted rank-convention reconciliation; no translated proof of `T_{\text{PH}\,2}` is carried out in the repository.”

**Surface issues**

- No used `\citep{...}` key is missing a `\bibitem`.
- `AriaChess`, `SmartXXI`, `Hirani2003`, `KogutSusskind1975`, and `Prigogine1967` are in the bibliography but not cited in this file. Harmless, but untidy.
- Line 140 is conceptually sloppy: “the `\Fcal`-conservation familiar from standard Hamiltonian QM” is not the right formulation. Standard QM conserves the Hamiltonian expectation under appropriate hypotheses; it does not conserve an arbitrary functional `\Fcal`.

**Top three fixes**

1. Lines 106-116, 124-135: state the structural hypotheses on `L_C` explicitly.  
As written, `\Fcal: \C^{V_{600}} \to \R` and the exact-symmetry proposition are under-justified. You need `L_C` self-adjoint and explicitly in the real shell-adjacency algebra.

2. Lines 121 and 377: remove or re-source the claim that `\alpha,\beta,\gamma` come from a “Paper V calibration.”  
I cannot verify that attribution in `paper-v.tex`. This is an avoidable citation overreach.

3. Line 305: weaken the `\Zphi^4`/`\Zphi^2` translation wording one notch further.  
The repository supports later `\Zphi^2` usage and a notation-reconciliation remark, but not a translated `T_{\text{PH}\,2}` proof chain. Do not imply more than that.

Publication ready: **no**. The Round 9 fixes landed, but the quadratic-term definition/symmetry gap and the two attribution overstatements are still substantive enough to block it.
