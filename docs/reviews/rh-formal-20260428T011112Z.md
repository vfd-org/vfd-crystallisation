**1. Claim Audit**

All line numbers refer to `papers/millennium-rh-formal/rh-formal.tex`.

| Line | Claim | Audit |
|---:|---|---|
| 372 | “Cascade zeta identity” | Established only because `\zetacas` is defined as classical `\zeta` at L1123-L1132. Correct, but definitional, not a cascade derivation. |
| 387 | “Critical line as joint fixed set” | Established, provided “cascade σ” means the Mellin convention of L1868-L1878 plus complex conjugation. Not σ alone. |
| 404 | “Dedekind reduction” | Established by definition of the prime-ideal Euler product. No new analytic content. |
| 436 | “Conditional critical-line theorem” | Proof works, but the hypothesis is exactly RH in different language, as admitted at L445-L453 and L1558-L1566. |
| 528 | F5 hypothesis | Correctly downgraded to a hypothesis imported from Paper XXXVI. Not used as unrestricted operator equivariance. |
| 614 | Prime splitting in `Z[φ]` | Standard and correctly proved/cited. |
| 664 | Pentagonal torsion | Established; local script reproduces order distribution, `τ^5=-1`, 12 cycles of length 10. |
| 688 | Shell-class bijection | True by local Paper XXII/script, but this paper gives no proof environment. Treat as imported theorem, not locally proved. |
| 717 | `K`-multiset | Established only for the script-selected `τ`; class-independence is explicitly not proved. Good. |
| 753 | Exact closed form | Established from the `K`-multiset. Integrality follows algebraically; degree-30 coefficient check is only a finite script check. |
| 791 | Mellin pole symmetry | Established. The poles form a staircase, not a critical-line statement. B11 now reports the fixed-set distinction coherently. |
| 846 | Hecke obstruction | Properly scoped to one tentative matching. It does not classify all possible global `L`-function identifications. |
| 892, 951, 1720 | H4O / aria claims | Correctly reported as external and unaudited. No theorem-grade status. |
| 1003 | ζ uniqueness | True, but U1 alone determines `f` by analytic continuation; U2/U3 are redundant consistency conditions. |
| 1094 | `η_p` convergence | Correct. |
| 1170, 1200, 1242 | U1-U3 for `\zetacas` | Established, but entirely by definition/import from classical ζ theory. |
| 1326, 1342, 1362 | Dedekind product/factorization/corollary | Established. The RH-equivalence for the product is acceptable if “non-trivial zeros” is understood and no cancellation in the strip is possible. |
| 1403, 1678, 1704 | Spectral/attractor conjectures | Correctly stated as conjectures and not used as proved inputs. |
| 1427 | Galois action on spectrum | Established under H-Mat. F5 alone would not suffice; the paper says this. |
| 1610 | Polynomial-flow eigenspace stability | Established. |
| 1629 | 600-cell spectral split | Locally verified: spectrum and 94/26 split match `test_sigma_attractor_spectrum.py` and Paper XXII. |
| 1741 | R1/R2/R3 finite-noise diagnostic | Locally reproduced: R1 `0.6639`, R2/R3 `1.0000`. This is a toy projector diagnostic, not evidence of an actual closure-flow theorem. |
| 1868 | σ-Mellin convention | Established as a definition/convention. It is not a Galois action on arbitrary positive reals; L1901-L1905 correctly says so. |
| 1942, 1951 | Fixed set and zero-set invariance | Established. Set-invariance is not pointwise RH; the paper correctly warns this. |

**2. Internal Consistency**

No unresolved `\ref`, `\eqref`, or missing `\bibitem` keys found.

The main remaining conflict is scope language. L41-L45 says the programme derives structure from “a single self-consistency axiom” F1. But the paper itself later imports E8, Paper XXXVI, H-SIG, H-Mat, and other hypotheses. This is too strong. Replace with: “The programme begins from F1 and adds the E8/maximality and named structural hypotheses documented in the cascade papers.”

L95-L99 and L124-L129 still make “F1 → Z[φ] → 2I → …” sound forced. F1 gives `φ`; the icosian/600-cell substrate is an additional geometric construction. Say “F1-motivated” or “F1-compatible”, not “F1-derived”, unless the full derivation is cited and hypothesis-loaded.

`\widehat\zeta(z)` and `\widehat\zeta(s)` are used interchangeably. Define `\widehat\zeta(s):=\widehat\zeta(z(s))` before using the `s` notation.

The label taxonomy is sloppy: `thm:hecke_obs`, `thm:h4o_bridge`, `thm:B14`, and `thm:dyn_attractor` label remarks, not theorems. It compiles, but it degrades referee trust.

**3. External Consistency**

Paper XXXVI: verified. Its F5 theorem is exactly conditional on H-SIG and concerns finite sums on σ-fixed fields, lines 539-562 of `paper-xxxvi.tex`. The RH paper’s current F5 usage is consistent. Change citation text from `\S 539--562` to “lines 539--562” or an actual section label.

Cascade Foundations: verified. It has a banner saying F4/F5/F7 are superseded by Paper XXXVI. The RH paper correctly acknowledges this at L547-L549.

Paper XXII: verified. Shell-class and 94/26 spectral decomposition are supported locally by Paper XXII L204-L223 and by `run_icosian_exact.py`. The RH paper’s spectral formulas match the Paper XXII table after rewriting `√5` in terms of `φ`.

Adaptive Closure Transport: verified. The cited open edge-space/projector caveat is present around L327-L328 and L377-L382. RH paper’s statement at L1539-L1545 is accurate.

Transport Law: verified as broad motivation/hypothesis audit only, not a proof source. RH paper does not overuse it.

Companion Millennium drafts: verified at the level claimed. BSD has two hypotheses; NS has three; P vs NP has two major hypotheses. The RH bibliography now correctly acknowledges multiple bridge hypotheses for BSD/NS/PvNP. Hodge and YM each have their own local bridge hypothesis.

Convergence note: exists and supports broad architectural similarity, but the note itself contains stronger rhetoric than the RH paper now uses. The RH paper’s softened “motivation, not theorem” language is appropriate.

Cascade capstone: verified as a coalgebraic abstraction with remaining hypotheses. RH paper correctly says the capstone is not the support relevant to the RH reduction.

Smart and aria citations are not repository-local. I did not verify them locally; the paper correctly marks aria as external and unaudited.

**4. Tightness**

L657: “with every step sim-verified” is still too broad. Edit to: “with the finite clock, shell, cocycle, and coefficient arithmetic sim-verified.”

L41-L45: “starting from a single self-consistency axiom” is too strong. Add the E8/maximality and named-hypothesis load.

L2075: “Steps (1)--(5) are rigorous” is acceptable but should read “rigorous, largely definitional/classical” to avoid overselling.

L2237-L2251 and L2321-L2332 are now mostly scoped correctly. “Programme-level motivation” at L2332 is the right strength.

L292-L296 is acceptable: “more than 165 years … since 1859” is accurate in 2026.

**5. Surface Issues**

Round 21 polish checks: passed. B11 lines 138-140 are coherent fixed-set commentary; the bridge subsection title is demoted at L2254-L2255; “programme-level evidence” is gone; the requested draft-history blocks are removed from the old target locations; raw box markers are absent; PAC numerics at L920 are in math mode; “IS zeta” does not appear; BSD/NS/PvNP bibliography entries acknowledge multiple hypotheses.

Publication hygiene still needs work: “Earlier drafts…” remains at L912, L1137, L1222, L1263, L1473, L1912. These are useful audit notes, but journal text should not narrate draft history. Convert to mathematical caveats.

Raw Unicode Greek and symbols appear throughout (`σ`, `ζ`, `δ`, `★`, arrows, section symbols). This may fail under pdfLaTeX despite `inputenc`; use LaTeX macros or require LuaLaTeX/XeLaTeX explicitly.

No compile was attempted because the workspace is read-only and TeX would need to write aux/output files.

**6. Top Three Fixes**

1. Fix the remaining “single axiom / F1-derived substrate” overclaim at L41-L45, L95-L99, L124-L129, and L652-L658. This is the most serious remaining mathematical overstatement.

2. Make the imported theorem status explicit for the shell/spectrum layer: L688 should be an imported theorem/fact with a proof citation, not a bare local theorem without proof. Likewise, keep L1629 tied to Paper XXII plus the verified script.

3. Remove draft-history prose and theorem-style labels on remarks. The mathematics is now mostly scoped as a conditional reduction, but the presentation still reads like an internal audit log.

Verdict: the core is now publication-ready only as a conditional reformulation/reduction, not as a proof of RH. I would not call the manuscript publication-ready until the three fixes above are made.
