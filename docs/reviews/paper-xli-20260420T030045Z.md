**Publication ready?** No.

**1. Claim Audit**
- `"T1-SS (heuristic target, not a proved proposition)"` [lines 109-121]. The paper does not pretend to prove this, and the text correctly discloses the missing ingredients: only conditional `T2` in `XL`, undefined `T3`, non-precise `T4`, separate scalar normalisation, and an extra local-Euclidean step. No over-claim here.
- `"T2-SS (heuristic target, not a proved proposition)"` [lines 129-135]. Again, properly caveated. The statement is only as strong as the extra assumption that the absent tensorial theory reproduces first-PN GR. The paper does not establish that; it says so.
- `"T3-SS ... the target exterior metric is the exact Kottler form"` [lines 140-143]. The caveat is mostly adequate: this only follows if one already assumes a full Einstein-with-`\Lambda` vacuum sector plus Birkhoff-de Sitter. The problem is precision, not logic: an “exact Kottler form” is a full metric, but the paper writes only `g_{00}`. If you mean only the temporal component, say so. If you mean Kottler, write the line element.
- `"T4-SS (construction target, not a proved proposition)"` [lines 169-170]. Properly labelled as naming a target, not establishing anything.
- The PN formulas [lines 153-156] are not presented as derived from the paper; that is honest. But PN1 is mis-stated relative to the paper’s own setup: `\omega(r)/\omega_\infty` imports an asymptotic observer at infinity, which the paper has spent effort disclaiming.
- The numerical claim `\Lambda \ell_P^2 = 2\varphi^{-583}` [line 144] is acceptable only as an imported conditional value from `XXXVI`. The paper mostly says that. It then undermines itself later; see line 192.

**2. Internal Consistency**
- The main internal inconsistency is the redshift formula. After explicitly rejecting global asymptotic flatness and replacing it with a local pointed Euclidean regime [abstract line 41; lines 117-121], the paper still writes
  `" \omega(r)/\omega_\infty = \sqrt{1-2GM/r} "` [line 153].
  That is the wrong reference observable for the regime the paper claims to inhabit. Use a finite reference radius or state that this is the asymptotically-flat Schwarzschild benchmark only.
- `"exact Kottler form"` [line 140] is inconsistent with displaying only `g_{00}` [lines 141-143]. The subsequent PN observables depend on the full metric, not just the temporal potential.
- Cross-references looked consistent in source. I did not find a bad `\ref`/`\eqref` by inspection. I did not compile the file.

**3. External Consistency**
- `XXVII` attribution is accurate. XLI says `XXVII` defines a scalar graph-Poisson model `\Delta_\mathcal G u = S-\bar S` and disclaims Einstein/tensorial/continuum claims [XLI lines 83-91]. Verified in `XXVII` [lines 40-44, 68, 131-145, 378-380].
- `XL` attribution is accurate. XLI says unconditional `T2` is still a conjecture, with a conditional `T2 under BI-sharp and halving`; `T3` is a programme target; `T4` is not a precise mathematical conjecture and needs the missing `H_4\to D_4` bridge [XLI lines 57, 109-121]. Verified in `XL` [lines 99-107, 161-166, 185, 195, 209, 215-216].
- `XXXVI F8` attribution is accurate. XLI says F8 is coefficient matching and does not by itself fix the Newtonian scalar normalisation `\lambda=4\pi G` [XLI lines 87, 114-115, 121]. Verified in `XXXVI` [lines 588-611].
- `XXIX` attribution is accurate. XLI says `XXIX` supplies observer-admissible/localised-probe machinery but no horizon analogue [XLI lines 169-172]. Verified in `XXIX` [lines 165-170, 217-259]; I found nothing horizon-like there.
- One attribution fails: `"Paper XXXVI's conditional Cascade Λ-Theorem, with its additional hypotheses ... discharged inside the programme"` [line 192]. I could not verify that. The local record points the other way. `XXXVI` says the theorem is conditional on the shell-amplitude rule, product-closure rule, and residue-to-`\Lambda` identification, and that these are not all theorems from the axioms; the product-closure rule is explicitly called a model choice [XXXVI lines 635-650]. `XL` preserves that same dependency load [XL line 195]. Line 192 is therefore an overstatement.

**4. Tightness**
- Too strong [line 192]: `"discharged inside the programme"`.
  Suggested edit: `"... imported with its additional assumptions still explicit (shell-amplitude rule, product-closure rule, residue-to-\Lambda identification)."`
- Too strong/too sloppy [line 140]: `"the target exterior metric is the exact Kottler form"` followed by only `g_{00}`.
  Suggested edit: `"... the target temporal component is \(g_{00}(r)=-(1-2GM/r-\Lambda r^2/3)\); the full Kottler line element would require the corresponding spatial sector as well."`
- Too strong relative to the paper’s own asymptotic caveat [line 153]: `\omega_\infty`.
  Suggested edit: `\omega(r)/\omega(r_0) \approx 1 - GM(1/r - 1/r_0)` for a finite static reference radius `r_0`, or else label the formula explicitly as the asymptotically-flat Schwarzschild benchmark.

**5. Surface Issues**
- No substantive LaTeX breakage jumped out by inspection.
- The only substantive surface-level defect is conceptual notation leakage at line 153: `\omega_\infty` reintroduces the asymptotic-flatness language the revision was supposed to remove.

**6. Top Three Fixes**
1. Delete or rewrite line 192. As written, it falsely suggests the `XXXVI` `\Lambda` theorem’s extra assumptions have been discharged elsewhere in the repository. The local sources do not support that.
2. Fix PN1 at line 153. `\omega_\infty` is incompatible with the paper’s own “pointed local-Euclidean, not globally asymptotically flat” framing.
3. Fix T3-SS lines 140-143. Either present the full Kottler metric or stop calling a single displayed `g_{00}` component “the exact Kottler form.” That is mathematically imprecise and infects the interpretation of the PN section.
