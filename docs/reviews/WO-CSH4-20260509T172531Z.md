**Claim Audit**

Artefact inventory: `papers/cascade-selection-h4/` contains only `WO-CSH4.md`. There is no derivation `.tex`, catalogue, or sim to audit.

1. `WO-CSH4.md:34-38` defines `\widetilde A_N` as a scaled vertex Laplacian acting on the `V_8` component. This is incompatible with clause 1. P4’s actual coboundary projects `F^0` only to `F^1 = Im(O)` (`papers/cascade-closure-dynamics/cascade-closure-dynamics.tex:313-330`), and `A_n` acts trivially on `V_{H_4}, V_{D_4}, V_{16}` and the real octonion direction (`:412-425`). Therefore the cited operator cannot have kernel equal to `F^0`-valued constants.

2. `WO-CSH4.md:41-46` claims `ker \widetilde A_N ≅ F^0` via constant functions. False for the cited P4 operator. On a connected graph, the actual P4 vertex-block kernel contains arbitrary vertex-dependent fields in the ignored summands, plus constant imaginary-octonion component. Dimension is not `32`; it is `25|V(G_N)| + 7`.

3. `WO-CSH4.md:48-58` claims convergence at rate `lambda_2(\widetilde A_N)`, “the second eigenvalue.” Convergence to kernel projection is standard for finite PSD operators, but the rate is the smallest positive eigenvalue on `ker^\perp`, not necessarily the second eigenvalue of the full operator. With kernel multiplicity greater than one, the second eigenvalue is still `0`.

4. `WO-CSH4.md:60-81` claims equivariant initial data select into constant `Psi_N((F^0)^{W(H_4)})`. False for the cited operator. At `N=0`, the function `f(v)=v` in the `V_{H_4}` summand is `W(H_4)`-equivariant, lies in the P4 kernel because `V_{H_4}` is ignored, and is not constant.

5. `WO-CSH4.md:70-81` gives `dim (F^0)^{W(H_4)} = 28`. This count is conditionally correct from P3 `def:coxeter`: `W(H_4)` acts standardly on `V_{H_4}` and trivially on `V_{D_4}, V_{16}, V_8` (`papers/cascade-refinement-spaces/cascade-refinement-spaces.tex:1172-1188`). It still does not prove the selection claim because the flow kernel is larger than constant fibres.

6. `WO-CSH4.md:83-96` claims `(O1)` is discharged. Over-claim. The mechanism API requires a top-rung state with residual zero and an initial datum converging to that state (`papers/cascade-mechanism/cascade-mechanism.tex:609-614`). It also says selection of `phi_N^*` is not provided by cascade closure (`:637-639`). A 28-dimensional allowed subspace is not a selected state or ray.

7. `WO-CSH4.md:86-90` says any fixed nonzero `xi^*` is residual-zero for every closure functional whose mass block restricts trivially to the kernel. This adds a new hypothesis not in the API. P4’s mass block projects onto `V_{D_4}` (`papers/cascade-closure-dynamics/cascade-closure-dynamics.tex:443-455`), so it is not trivial on a general `xi^* in (F^0)^{W(H_4)}`.

8. `WO-CSH4.md:209-217` names `(H-uniqueness)` as open, requiring extra structure to choose a single ray. That directly contradicts the claimed `(O1)` discharge unless cascade-mechanism is explicitly changed to accept a subspace-valued selection. Locally, it does not.

**WO Acceptance Audit**

A1: not resolved. No `.tex`, and the proposed theorem is false as written.

A2: not resolved. No `.tex` sections 1-10 exist.

A3: partially resolved in the WO style, but defective: compat `def:scaled-curvature` is cited at `WO-CSH4.md:138` and does not exist. The local label is `def:Atilde` (`papers/cascade-refinement-compat/cascade-refinement-compat.tex:279`).

A4: not resolved. The WO asks for a future non-claims box; no `.tex`.

A5: partially resolved in the WO only. The hypotheses are named at `WO-CSH4.md:207-224`, but `(H-uniqueness)` is a blocker to the claimed theorem, not just a handoff.

A6: partially resolved. The 28 count is sketched, but no theorem-grade `.tex` proof exists.

A7: not resolved. No worked numerics section.

A8: not resolved. No `.tex` review can return publication-ready.

Open hypotheses: `(H-uniqueness)` unresolved and load-bearing; `(H-physical-interpretation)` unresolved but honestly scoped. Risks R1/R2/R4 are manageable. R3 is not mitigated; the selection is mostly passive and, with the cited operator, not even restricted to constant fibres.

**Catalogue Audit**

No math-catalogue is supplied. If catalogue is optional, there is nothing to compare. If this WO is treated as mathematical content, the named theorem, `\widetilde A_N`, `\Psi_N`, `(F^0)^{W(H_4)}`, `d_H4^fix`, and the two open hypotheses are currently uncatalogued.

**Attribution / External Consistency**

P3 labels mostly exist and support the fibre/action bookkeeping. P4 `thm:flow-exists` exists, but for the general `L_n = alpha A_n + beta B_n - gamma C_n`, not specifically for a newly defined full `F^0` Laplacian. Compat is scalar pure-midpoint only and explicitly does not lift to the full P3/P4 tower without open hypotheses (`papers/cascade-refinement-compat/cascade-refinement-compat.tex:219-251`, `:1063-1071`). P2 has `def:V600`, `def:icosian-ring`, and `prop:24-in-600`, but I did not find P2 character data proving irreducibility of the standard `W(H_4)` reflection representation; its character-table import is for `2I` (`papers/cascade-algebraic-substrate/cascade-algebraic-substrate.tex:1128-1140`).

**Sim Correctness**

No sim supplied.

**Tightness**

`WO-CSH4.md:12-14`: replace “discharged ... with ... a precise selected state” with “reduced to a finite-N kernel/flow theorem plus an explicit remaining selection principle.”

`WO-CSH4.md:83-96`: replace “(O1) is discharged” with “candidate fixed subspace identified; `(O1)` remains conditional on choosing a specific `xi^*` and initial datum projecting to it.”

`WO-CSH4.md:264-270`: “meaningful selection” is too strong. The 28-dimensional fixed subspace is mostly the trivially acted-on fibre.

**Top Three Fixes**

1. `WO-CSH4.md:34-46`: decide the operator. If it is P4’s operator, rewrite the kernel and selection theorem. If you need `F^0`-constant kernel, define a new full `F^0` graph Laplacian and stop claiming it is the P4/compat operator.

2. `WO-CSH4.md:83-96` and `:209-217`: fix the `(O1)` API mismatch. Either prove cascade-mechanism accepts a 28-dimensional selected subspace, or downgrade the theorem to a conditional statement requiring `(H-uniqueness)`.

3. `WO-CSH4.md:136-144`: repair attributions. Use compat `def:Atilde`, remove the nonexistent `def:scaled-curvature`, and do not attribute `W(H_4)` reflection irreducibility to P2 character data.

**Verdict**

Publication ready: no.
