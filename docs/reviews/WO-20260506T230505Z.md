**1. Claim Audit**

- “K-multiset `{72:1, 0:1, 52:5, 20:5}`” — `v600-cosmic-tensions.tex:53-55`, `125-130`. Established, but imported. Paper correctly says it does not re-prove this at `116-117`.
- “Cycle space `C_Q ≅ Q^12`” — `tex:133-139`. Definition only; fine.
- “K-class projectors” and ranks/resolution — `tex:141-161`. Established from imported K-multiset; verified by `verify.py`.
- “Baseline operator is `I_C` by definition” — `tex:163-170`. Correctly stated as a definition, not uniqueness theorem.
- “Operators `Hhat = I_C + P_72`, `Chat = I_C - P_0`” — `tex:175-188`. Definition; diagonal/self-adjoint follows.
- “Exact trace identities” — `tex:192-213`. Proof establishes exactly this.
- “Only rank-one diagonal projector corrections…” — theorem at `tex:215-243` is correct under `Q ∈ A_K`, `Q^2=Q`, rank one. But the abstract says “only rank-one corrections” at `tex:58-60`, and conclusion repeats it at `tex:420-421`. Literal rank-one operators in `A_K` include scalar multiples like `2P_72`; the proof only classifies rank-one projectors. Fix wording.
- “Hypotheses are essential” — `tex:246-256`. Substantively correct, but “dropping either” is slightly loose because `A_K` already enforces diagonality. Not fatal.
- “Phase-independence across 32 lifts” — `tex:261-288`. Established given Paper 3’s imported 32 phase lifts. Local Paper 3 supports this.
- Numerical table — `tex:302-334`. Arithmetic is correct: `67.36*13/12 = 72.9733`; `0.832*11/12 = 0.7627`; residuals match.
- Ablation — `tex:336-346`. Trace arithmetic correct; “far outside” is supported.
- Hypothesis H1 — `tex:356-382`. Correctly marked as hypothesis, not theorem.
- OD1 caveat — `tex:397-408`. Correct attribution in substance, but `od1_cycle_spectrum.py` has a stale header claiming the opposite before later reporting `17/12`.

**2. WO Acceptance Audit**

- B1 K-saturated admissibility: resolved, with projector-wording cleanup needed.
- B2 trace theorem: resolved.
- B3 baseline rule: resolved.
- B4 empirical table: resolved; S8 handling is honest.
- B5 coupling rule hypothesis: resolved.
- B6 phase-independence: resolved in proof and verifier.
- B7 OD1 reconciliation: resolved in paper; source hygiene issue remains in `od1_cycle_spectrum.py`.
- B8 verification hygiene: resolved.

Surgical corrections C1-C7 are resolved in substance. Residual scope cleanup remains outside the required artefacts: `SCOPE.md:14` still says “rank-1 corrections forced by structure,” and `SCOPE.md:76` still contains a pre-registration timestamp question despite the stated Round-4 removal.

**3. Catalogue Audit**

- D1-D3, L1, T1-T2, R1-R2, N0-N4, H1 are all present and correspond to derivation objects.
- Defect: `CATALOGUE.md:34-40` says `A_K` is the span of “five mutually-orthogonal idempotents” including `I_C`. False: `I_C` is not orthogonal to the `P_K`; the four `P_K` are mutually orthogonal and resolve `I_C`.
- No derivation theorem/lemma/hypothesis is missing from the catalogue.
- No unsupported catalogue theorem, lemma, or hypothesis found beyond the D4 algebra wording defect.

**4. Attribution / External Consistency**

- Paper 1 K-multiset, bulk subgroup, and right-coset carrier facts are locally supported by `bekenstein-incidence.tex:162-186`, `193-203`, `218-255`.
- Paper 3 phase swaps, canonical properties, and 32 lifts are locally supported by `tau-sigma-construction.tex:225-238`, `356-406`, `410-434`.
- OD1_STATUS supports “not closed” and mode-counting failure at `OD1_STATUS.md:3-6`, `83-87`.
- `MISSING_MATH.md:3-10`, `16-30` supports the 13-live-modes gap and H4 route.
- External numerical anchors check out: Planck table gives `H0=67.36`, `S8=0.832`; SH0ES gives `73.04 ± 1.04`; KiDS multi-probe gives `S8=0.766 +0.020/-0.014`; KiDS cosmic shear gives `0.759 +0.024/-0.021`. Sources: Planck A&A/table, SH0ES abstract, KiDS multi-probe, KiDS cosmic shear.

**5. Sim Correctness**

`verify.py` implements the stated Layer-1 claims: K-multiset, projector ranks, trace identities, ratios, all 32 phase combinations, and rank-5 ablations. It does not assert cosmology, which is correct.

Ran:
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=papers/v600-programme/lib python3 papers/v600-cosmic-tensions/verify.py` — pass.
- Shared test file: `9 passed`. Needed `-s` because the read-only sandbox has no writable temp directory for pytest capture.

**6. Tightness**

- `tex:58-60`, `420-421`: replace “rank-one corrections” with “rank-one projector corrections.”
- `SCOPE.md:14`: “forced by structure” is still too strong.
- `SCOPE.md:76`: remove the pre-registration timestamp question if Round-4 scope removal is meant to be complete.
- `CATALOGUE.md:34-40`: replace “five mutually-orthogonal idempotents” with “four mutually orthogonal K-projectors; `I_C` is their sum.”

**7. Top Three Fixes**

1. `papers/v600-cosmic-tensions/v600-cosmic-tensions.tex:58-60`, `420-421`: say “rank-one projector corrections,” not “rank-one corrections.”
2. `papers/v600-cosmic-tensions/CATALOGUE.md:34-40`: fix the false “five mutually-orthogonal idempotents” statement.
3. `papers/cosmological-folding-rate/dynamics/od1_cycle_spectrum.py:1-10`: update stale header, or line-pin the citation to the later `17/12` result.

**8. Verdict**

Publication ready: no.

The core derivation and verifier are sound, but there are still substantive precision defects in the paper summary wording and catalogue algebra.
