**1. Claim Audit**  
B1-B5 are established at frame level: exact sims pass for the basis, mod-2 form, Fano quotient, `red`, and `C_O` pairing (`cascade-h-grad-1-closure.md:116`, `:171`, `:237`, `:374`, `:410`). B6’s quoted claim, “`G_p` acts transitively on `Adm_red`” (`:481`), is only a proof sketch, but the stated Witt-extension argument is plausible for frame-level closure (`:483-518`). It does not prove G2-canonicality or P-A equivalence, and the note correctly says so (`:545-558`).

**2. WO Acceptance Audit**  
A1-A4: resolved at frame/sim level (`WO-H-GRAD-1.md:41-44`).  
O1: resolved only at `G_p` frame level; G2 pullback remains open (`:45`, `:97-108`).  
O2-O5: resolved (`:46-49`). Full downstream P-A is still not updated: `cascade-access-principle-theorem.md:231-233`.

**3. Catalogue Audit**  
Catalogue matches B1-B6 and N1-N7 (`cascade-h-grad-1-catalogue.md:17-36`). Ledger defect: `Adm_red` and Theorem B6.3 are folded into B6/N7, not separately catalogued as definition/theorem (`cascade-h-grad-1-closure.md:455`, `:481`). Non-gating for frame-level math.

**4. Attribution / External Consistency**  
Local citations check out: substrate gives rank-8/basis dependence (`cascade-algebraic-substrate.tex:493-499`); Phase-M3 gives the `σ(I) ⊕ M` split (`cascade-phase-m3-closure.md:49-55`); Q-O bridge gives algebra isomorphism only, not Pontryagin chars (`cascade-q-o-measurement-bridge.md:112-138`); `insight.md` gives factor-7 motivation, not triads (`insight.md:670-678`).

**5. Sim Correctness**  
Ran all five H-grad-1 scripts. All PASS. B6 sim is honestly only counts/type sanity, not full orbit enumeration (`cascade-h-grad-1-closure.md:560-568`).

**6. Tightness**  
The three requested fixes landed: old status phrase gone (`:3`, `:11-19`); Route Q no longer claims B6 supplies G2 orbit identification (`:668-677`); §6 is archival (`:579-591`). Tighten `Status O1: BUILD COMPLETE` at `:575` to “complete at `G_p` proof-sketch level; G2 pullback open.”

**7. Top Three Fixes**  
1. Reword O1 status at `cascade-h-grad-1-closure.md:575`.  
2. Catalogue `Adm_red` and B6.3 separately.  
3. Do not upgrade full P-A until `cascade-access-principle-theorem.md:231-233` is revised or equivalence is proved.

**8. Verdict**  
Publication ready: yes, for frame-level B1-B6 closure. Full G2/P-A closure: no.
