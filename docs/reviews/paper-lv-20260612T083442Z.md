**1. Claim Audit**

- `paper-lv.tex:33`: “All computational claims verified…” Mostly true: I ran the named suites and they pass. But `paper-lv.tex:162` cites gravity-chain `sim T10`, which is not named in the status/reproducibility manifest; `verify_gr_closure.py` also passes.
- `64-65`: “Every structure… every projection class… hence… arena is… S^3.” Not proved in the paper. “Every projection class” is far too broad, and the argument assumes the quaternionic/S^3 embedding it later claims to derive.
- `68-71`: “Square-multiplicity law.” Essentially established, conditional on Paper LIV’s representation dimensions and the numerical distinctness check. The proof should explicitly state the 12-neighbour set is one conjugacy class; locally this is true.
- `78-81`: “Harmonic identification; exact dispersion.” Partly established. The character calculation gives the eigenvalues once irreducibility/branching is assumed; the paper does not prove the McKay branching or irreducibility of `V_k|_{2I}` for `k<=5`. It only samples degree 1 and 2 harmonics in the proof.
- `88-90`: “Spectral dimension 3… No step inputs a dimension.” Overclaim. The data support an S^3 harmonic band and a numerical Weyl fit `d=3.09`; they do not prove a continuum limit from a single fixed 120-point graph, and S^3/SU(2) is already input.
- `102-105`: “(K1)--(K4) are satisfied by exactly one family.” Formally yes, but vacuous as a derivation: K4 essentially defines the resolvent.
- `111-113`: “Equivariance; locality.” Equivariance follows. Strict monotone coherence length, exponential fit, continuum Yukawa identification, and b-anomaly inheritance are not proved in the manuscript; they are numerical/programmatic claims.
- `127-130`: “Canonical observer frame… with no choice involved.” True only after choosing the quaternionic basis `(i,j,k)`, orientation, and left trivialisation. “No choice” is too strong.
- `133-136`: “On vertices it agrees with `π_r F`.” Not established and likely false as written if `G_r` is the continuum Green kernel. The derivation note says “approximately”; the paper upgrades this to exact equality without proof.
- `139-143`: “Scene decomposition.” The linear split follows if bulk time-invariance is assumed. “All temporal change” relies on boundary-only dynamics, not proved here from Paper XXXIII’s arg-min residual. The radial-background statement is numerical, not a theorem in the text.
- `151-153`: “Clock reversal is inner… exactly 10 elements.” Plausible and numerically verified. The paper needs the short group-theoretic proof; also distinguish inner conjugation from left multiplication.
- `155-157`: “(H-equiv)… inherited.” Not verified. Paper XXXIII gives conditional symmetry-class invariance of minimisers, not the stated Aut(`V_600`) residual-generation law `ρ(PF;PΠ0)=Pρ(F;Π0)`.
- `159-163`: “Second-order effective time… forcing the wave operator.” Overclaim. The argument excludes unpaired first-order time terms in a real scalar effective equation under extra symmetry assumptions; it does not force a nonzero `∂_t^2`, Lorentzian form, speed `c=a/τ_tick`, or the unique stencil.
- `173-175`: “Simultaneity, linear class.” Tautologically true only if the scene sample is defined by the same discrete kernel row. It depends on the unsupported exact-agreement claim at `133-136`.
- `177`: “Quadratic case is proven… general case residual.” Not proved in the paper; it is imported from gap-strengthening scripts/notes. The open status is correct.

**2. Internal Consistency**

- `43`, `65`, `89`: the paper both assumes `V_600 ⊂ S^3 ⊂ H` and says “no dimensional input.” These are inconsistent.
- `99-104` versus `112`/`136`: K4 uses the source-normalised resolvent `(I+r^2L)^{-1}`; the continuum Green-kernel prose uses the unnormalised `C_φ^{-1}` form. The scaling/prefactor is not consistent.
- `136` says exact vertex agreement with `π_r`; `173-175` depends on that. If agreement is only approximate, Proposition `simul` must be weakened.
- `160` combines full-graph `L -> -2a^2Δ`; Paper LIII’s gravity chain uses `L_∂/a^2 -> -∇^2`. The operator being passed to gravity changes.
- `151-166`: “inner automorphism” and “left multiplication” are conflated. Inner automorphism is conjugation; left multiplication is a graph automorphism/translation.
- Cross-references resolve. No undefined `\ref`/`\eqref` issues found.

**3. External Consistency**

- `43`: Paper XXXIII supports a proposed closure-projection readout (`paper-xxxiii.tex:47-51`) but explicitly frames it as proposed/conditional, not an established projection theory (`paper-xxxiii.tex:402-420`).
- `43`: Paper LIV supports the forced-crystal claim only under W1--W3 (`paper-liv.tex:64-85`), and it correctly supplies representation dimensions (`paper-liv.tex:103-106`).
- `75`: “Paper LIV, Theorem 3” is locally verifiable: the dimensions are there.
- `156`: Cannot verify the claimed inherited `H-equiv` in Paper XXXIII. The closest result is conditional symmetry-class invariance (`paper-xxxiii.tex:162-176`), not the residual law stated here.
- `166`: Paper LIII consumes the rendering-layer inputs directly (`paper-liii.tex:76-77`). Paper LVII consumes them indirectly through LIII and exact-rate continuum control (`paper-lvii.tex:92-103`), but does not directly cite LV.
- `85`: “rung-ladder intertwiner theorem” is in `docs/rung-dimension-ladder.md`, not cited as a paper. Add a citation or reproduce the needed lemma.
- `112`: b-anomaly support is only a structural/programme witness in `docs/aria-closure-kernel.md`; it is not evidence that the rendering kernel is empirically validated.

**4. Tightness**

- `36`: Replace “supplies that bridge in four theorems” with “supplies a conditional bridge, with R4 depending on H-equiv and continuum/stencil assumptions.”
- `45`: Replace “each derived rather than assumed” with “each derived or reduced under stated imported hypotheses.”
- `89`: Replace “No step inputs a dimension” with “The graph spectrum independently corroborates the S^3 harmonic dimension.”
- `103`: Replace “satisfied by exactly one family” with “K4 selects the resolvent family.”
- `112`: Replace “therefore also a witness” with “shares the same response-operator family; this is at most indirect support.”
- `160-162`: Replace “forbids first-order… forcing the wave operator” with “excludes unpaired first-order time terms under H-equiv; with the centered second-difference ansatz this yields the wave operator.”

**5. Surface Issues**

- Unused bibliography entries: `SmartLIII`, `SmartXXXIII`. Prose mentions Paper XXXIII/LIII/LVII should be actual citations.
- Missing bibliography entry/citation for Paper LVII if it remains in `166`.
- Overfull boxes in the compiled log at manuscript lines `33-34`, `161-163`, and `185-186`.
- `k=0..5` should be `k=0,\ldots,5`.
- “sims R1a” etc. is too informal for a paper; use “verification item”.
- `τ` is overloaded between clock generator and tick scale; `O(τ^2)` at `162` is ambiguous.
- Units: `c=a/τ_tick` uses `a=π/5`, a dimensionless geodesic angle unless a physical length scale is supplied.

**6. Top Three Fixes**

1. Fix R4 (`155-163`). Either prove/import the exact `H-equiv` residual law and add the missing scalar/locality/stencil hypotheses, or weaken the claim to symmetry exclusion of odd time terms.
2. Fix the rendering kernel/scene interface (`111-136`, `173-175`). Decide whether the scene is discrete-resolvent exact or continuum-Green approximate; the current text uses both.
3. Fix the dimension claim (`64-90`). Separate “given the icosian S^3 substrate” from “graph spectrum detects S^3-like harmonic dimension,” and import/prove the McKay irreducibility needed for `78-81`.
