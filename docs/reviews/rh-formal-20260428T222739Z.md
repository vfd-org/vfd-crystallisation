**Claim Audit**
- `rh-formal.tex:402` “Cascade zeta identity.” Established, but only definitionally: `\zetacas` is defined as the classical Euler product/continuation at `1173-1182`. No cascade-native derivation is proved.
- `417` “Critical line as fixed set.” Fixed-set claim is proved. The zero-set invariance part at `429-430` is proved later in Proposition `2289`, but the theorem proof at `2326-2336` should explicitly cite it.
- `434` “Dedekind reduction.” Established by the standard Dedekind Euler product and factorisation; no new content.
- `467` “Conditional critical-line theorem.” Correct but tautological: the hypothesis at `1577-1584` is exactly RH in different language.
- `646` “Splitting law for rational primes.” Standard and correctly stated.
- `703` “Pentagonal torsion in `2I`.” Proof establishes the orbit count once an order-10 element is fixed.
- `727` “Shell-class bijection.” Verified locally in Paper XXII lines `204-223`; imported claim is supported.
- `744` “cocycle unique up to additive and multiplicative normalisation.” Not established. Additive/multiplicative changes alter the `K` values and hence the Lucas factors; “same `K`-multiset up to relabelling” at `753-754` is too strong.
- `764` “`K`-multiset for the script-selected `\tau`.” The proof establishes only a script result for one enumeration-selected representative. It does not give an in-paper representative for `\tau`, nor a reproducible cycle table, and correctly does not prove conjugacy-class independence.
- `800` “Exact closed form.” Established conditional on the preceding `K`-multiset. The integer-coefficient claim follows from the Lucas factorisation, not merely the degree-30 check.
- `840` “Mellin pole-symmetry.” Established for `\widehat\zeta`; it is a pole statement for the finite dynamical zeta, not a zero statement for classical `\zeta`.
- `894` “Hecke obstruction.” Adequately scoped as one tentative matching only. It is not a classification theorem.
- `1053` “`\zeta` uniqueness.” Correct; (U1) plus meromorphic continuation already does the work, so (U2)/(U3) are largely redundant.
- `1144` “Convergence domain of `\eta_p[x^{s-1/2}]`.” Series calculation is correct, but `x^{s-1/2}` is not actually in `L^2(\mathbb R_+,dx/x)`. Treat as a formal test family, not a Hilbert-space vector.
- `1220`, `1250`, `1293` Euler product / functional equation / analytic continuation. Established only by the definition `\zetacas := \zeta`; the paper admits this.
- `1382`, `1398`, `1418` Dedekind zeta and factorisation. Standard and correct; the RH/GRH equivalence for the product is fine.
- `1509` “Galois action on the spectrum.” Correct under H-Mat, but only for the `\mathbb Q(\varphi)` part of the spectrum.
- `1693` “Eigenspace stability under `L`-polynomial flows.” Correct.
- `1712` “Spectral sigma-attractor decomposition.” Verified against Paper XXII table lines `177-193`; the `6+6\varphi` endpoint is correct.
- `1851` “mode suppression by explicit polynomial filter.” The proof is correct for the designed filter `e^{-t p_{\rm fix}(L)^2}`. It does not prove anything about the original cascade closure functional.
- `1971` “Candidate cascade Mellin trace identity.” Not proved, and key terms `K^*`, `\lambda_K^\pm`, `h_K^{pair}` are undefined. The text admits this.
- `2074` finite-noise diagnostic. Script exists locally, but the result is simulation-reported. R2/R3 equal 1 by explicit projection, not by dynamics; the text mostly acknowledges this.
- `2207`, `2280`, `2289` Mellin convention, fixed set, zero-set invariance. Correct. The critical line comes from combining the chosen `s\mapsto1-s` convention with classical complex conjugation.

**Internal Consistency**
- `550` says `\alpha,\beta,\gamma\in\mathbb Q`. This conflicts with Paper XXXVI and the local `cascade-foundations.md` update, which explicitly says F5 works for arbitrary real coefficients and no rationality is needed.
- `1816` and `1836` refer to the original closure functional as being in `§\ref{sec:zhat}`. It is defined in `§\ref{sec:cascade}`.
- Round 8 demotion is incomplete. `1629-1633` still calls `\Psi_t` a “cascade-internal closure flow” supplying the “missing canonical object”; `1804` titles the definition “Cascade closure functional and flow”; `2069-2070` says the finite-dimensional content is “derived directly from the closure functional.” These contradict `1797-1799` and `1831-1848`, where it is correctly described as a custom polynomial filter.
- `2678-2679` says each companion Millennium draft carries “its own per-paper Bridge Axiom.” Several carry multiple bridge inputs. This is not just wording; the bibliography summaries then omit required hypotheses.
- Static parse found no duplicate labels, no unresolved `\ref/\eqref`, and no undefined `\cite` keys.

**External Consistency**
- Paper XXII citations check out: shell-class theorem and spectrum table support `727-731` and `1712-1735`.
- Paper XXXVI F5 citation checks out at lines `539-562`; it also exposes the stale rationality claim at `550`.
- `AdaptiveClosureTransport` citation checks out: local lines `327-328` and `377-382` leave the edge-space/projector lift open.
- `TransportLaw` supports only broad motivation, not a selection theorem; the RH manuscript states this correctly.
- BSD local source now has three bridge hypotheses `B_{\rm BSD,conv}`, `B_{\rm BSD,rank}`, `B_{\rm BSD,spec}` and the rank-1 example `37a1` at `bsd-formal.tex:558-563`. RH bibliography `2805-2809` still only says “more than one” and names a principal spec bridge; it should name all three.
- Hodge local source requires both `H_{4a}` and `H_{4a}^{alg}` (`hodge-formal.tex:206-217`). RH bibliography `2813-2815` compresses this to one projector canonicity axiom.
- P vs NP local source requires `H_{\sigma-irr-NP}`, `B_{\sigma-P}`, and structural completeness (`pnp-formal.tex:71-73`). RH bibliography `2834-2837` omits `B_{\sigma-P}`.
- YM `\|L_{H4}\|=6+6\varphi` is corrected locally. However the YM file still contains stale “positive rational” language for `\gamma` at `ym-mass-gap.tex:319-320` and `475-476`; RH does not repeat it, but the claimed Round 8 fix is not globally applied.
- Smart framework and aria-chess claims cannot be verified locally; those repositories are not present. `docs/convergence-with-smart.md` supports only the broad architectural-similarity claim, not the exact Smart theorem locators.

**Tightness**
- `1629-1633`: replace “cascade-internal closure flow … missing canonical object” with “the explicit polynomial filter flow gives a finite-dimensional toy selection mechanism.”
- `1804`: rename to “Polynomial filter functional and flow.”
- `2069-2070`: replace “derived directly from the closure functional” with “proved directly for the polynomial filter functional.”
- `2572-2575`: “Analogous selection rules” is too strong. Use “loosely analogous projection/selection language.”
- `2678-2683`: replace singular “per-paper Bridge Axiom” with “per-paper bridge hypotheses, with multiplicities and names as stated in the local drafts.”

**Surface Issues**
- Literal Unicode `★` at `930` is risky under pdfLaTeX; no `\DeclareUnicodeCharacter` is provided.
- Label `cor:H_attr_discrete` at `1922` is attached to a `remark`, not a corollary.
- The manuscript mixes literal `σ`, `δ`, `ζ`, section-sign `§`, and TeX commands. This may compile under Xe/LuaLaTeX; pdfLaTeX needs an actual compile check.
- “sim-verified” is used for proof-critical arithmetic. For publication, include the selected `\tau` coordinates and cycle table in the paper or appendix.

**Top Three Fixes**
1. Fix the remaining `\Psi_t` overclaim: `1629-1633`, `1804`, `1939-1943`, `2069-2070`. It is a designed polynomial filter, not the cascade closure flow.
2. Make the main `\widehat\zeta` construction reproducible without trusting an opaque “script-selected” representative: give `\tau`, the 12 cycles, and the `K` table; weaken the cocycle normalisation claim at `750-756`.
3. Repair dependency statements: remove `\alpha,\beta,\gamma\in\mathbb Q` at `550`, and update companion Millennium bridge summaries at `2678-2683` and `2802-2837` to match the local papers’ actual hypothesis lists.
