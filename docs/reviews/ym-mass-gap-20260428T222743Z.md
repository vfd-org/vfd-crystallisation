**1. Claim Audit**
- “P8 … Der(O) ≅ g2 … stabilizer … su(3)” `54-65`: dimension counts are verified by the local script; the script proves `14` and `8`, not by itself the Lie-algebra identification `su(3)`. Classical references can supply that, but the proof text should separate “dimension verified” from “isomorphism known classically.”
- “P9 … P3 carries a cylindrical SU(3)-bundle” `66-75`: not established. P3 has cylindrical refinement spaces, but I found no local SU(3)/su(3) bundle theorem there.
- “H4 oscillator … theorem-grade … λ1=+0.00384 … PC2/PC1=0.9997 … MI=0.046” `80-96`: not locally verified. The local oscillator note derives a law and leaves coupling constants free; the cited `aria-chess` paths are absent.
- `H_OS-lift` `101-115`: honest as a hypothesis. But `149-153` then says weak compactness follows from F4 plus torus boundedness; that is not proved.
- Main proposition `216-244`: valid only as a restatement of `H_OS-lift`; calling it an “equivalence” to Clay `239-243` is stronger than shown.
- Discrete spectrum theorem `246-252` / lemma `864-886`: numerically correct; I ran the script and got the nine eigenvalues. The printed proof is incomplete: the character table is schematic, the irrep/multiplicity bookkeeping is confused, and Step 6 has “higher mixings” with no derivation `970-976`.
- F1 `358-361`: fixed point is correct; “contraction constant φ^{-2} on [1,2]” is false as stated.
- F2/F4/F5/F6/F7 `364-416`: mostly scoped correctly to Paper XXXVI, except F6 adds “spectral convergence” `401-402`, which I did not verify in Paper XXXVI.
- Finite-volume existence `491-503`: true if the integration is really over compact SU(3) link variables with product Haar measure. The proof instead invokes F4 and polynomial variables, which is not the right argument.
- Continuum measure existence `519-561`: not established. Tightness and uniqueness of subsequential limits are asserted, not proved.
- Uniform tightness `563-610`: false as written. It uses the deprecated F4 spectral claim `573-575`, uses a Gaussian moment formula for nonabelian YM `584-590`, then says convergence requires `s<-3` but applies `s=1/2` `592-597`.
- OS0 `623-639`: correctly demoted to conditional content.
- OS1 `643-711`: not proved. It assumes a reconstructed Hamiltonian, mass gap, OS Hilbert space, and field operators before OS reconstruction is available.
- OS2 `715-728`: not proved. Finite Weyl symmetry plus GH convergence to `S^3` does not give full `E(4)` invariance on `R^4`.
- OS3 `736-804`: not proved and circular. Reflection positivity is assumed when the OS Hilbert space/norm is defined. F5 does not give continuum `F[θA]=F[A]`.
- `σ=θ` lemma `806-832`: not established. The algebraic Galois involution is not shown to define Euclidean time reflection; the log branch and `(-1)^n` issue are hand-waved.
- OS4 `836-854`: not proved. It again uses the obsolete “F4 eigenvalues φ^{-k}” claim.
- Gap proposition `1006-1020`: acceptable only as a tautological consequence of `H_OS-lift`; `N_1` and units are undefined.
- No massless mode proposition `1068-1107`: overclaimed. The statement is unconditional, while the proof depends on the conditional gap and repeats the unjustified continuum spectral preservation `1091-1093`.
- Main proof `1113-1160`: does not prove the main proposition. It cites OS propositions that are either conditional or invalid.

**2. Internal Consistency**
- The paper alternates between `R^4`, `S^3 × R`, and `S'(R^4,su(3))` without a defined embedding or limit map `41`, `447-449`, `520-522`.
- `Definition~\ref{hyp:OS-lift}` is used for a hypothesis `218`, `1008`.
- `prop:OS0` labels a remark, but is later cited as a proposition `623`, `1117`.
- F4 is correctly described as a dimension count `373-383`, then later used as a spectral theorem `573-575`, `849-853`.
- Nearest-neighbour geometry conflicts: `2π/5` geodesic distance `862` versus squared chord distance for `π/5` `867`.
- Section summaries are stale: “Section 4 proves OS axioms” `272-273` and “Section 6 deduces the mass gap” `278-279` contradict the later conditional status.

**3. External Consistency**
- Paper XXXVI verifies F2 under H-MIN, F4 as a dimension count, F5 under H-SIG, F6 conditionally, and F7 scoped. But this paper cites `cascadeFoundations` instead of Paper XXXVI `418-419`; the bibliography lacks a Paper XXXVI item.
- The citation “cascadeFoundations lines 539--562” `391` is wrong: those lines in `cascade-foundations.md` are F7, not F5. In Paper XXXVI, lines 539--562 are the intended F5 theorem.
- Paper XXXVI explicitly says F5 is discrete finite-sum equality and continuum extension needs another hypothesis; this contradicts the use of F5 as continuum reflection invariance `753-755`.
- P3 does not verify the claimed SU(3) cylindrical bundle `66-75`.
- `cascade-schrodinger.md E9` exists, but its unitarity argument rests on old σ-invariance language; it is not reliable after the Paper XXXVI F5 demotion.
- The attractor spectrum/dynamics scripts do verify the `94/120`, `26/120`, `0.6636`, `0.6639`, and `1.0000` numbers `1178-1226`.
- The local H4 Laplacian script computes the nine-eigenvalue spectrum, but its final self-check still compares against the obsolete `{0,4,8,10,12}`.

**4. Tightness**
- `80`: replace “theorem-grade” with “externally reported empirical witness, not locally verified.”
- `149-151`: replace “follows from” with “would require a separate compactness theorem beyond the stated inputs.”
- `614`: replace “We now verify” with “We record the OS axioms as conditional requirements and note where the current argument falls short.”
- `1006`: replace “Cascade spectral gap” with “Conditional gap inheritance hypothesis.”
- `1266-1268`: replace “σ-invariance provides reflection positivity” with “reflection positivity would require an additional continuum OS-positivity theorem.”

**5. Surface Issues**
- Undefined/missing LaTeX: `\textbeta` `118`, `\bm` `1190`, and likely `\T_n` `647`.
- Missing bibliography entries: `billingsley` `532` and `conwayQ` `906`, `944`, `955`.
- Raw Unicode symbols/checkmark may break pdfLaTeX: `σ`, `θ`, `γ`, `✓`.
- `gamma` rationality is not fully removed: “positive rationals” `319-321` and “specific positive rational constant” `475-476` are still wrong.
- Bibliography paths use `Papers/...` while repo paths are lowercase `papers/...` `1322`, `1325`, `1337`.

**6. Top Three Fixes**
1. Demote or delete the continuum measure/OS proof chain `519-610`, `621-854`, `1113-1160`. It still claims to prove the open hypothesis.
2. Repair foundation imports: add a real Paper XXXVI citation, stop using F4 as spectral data, stop using F5 as continuum σ-invariance, and remove unverified F6 spectral convergence `391`, `418-422`, `573-575`, `753-755`, `849-853`, `1091-1093`.
3. Remove unsupported “theorem-grade” H4O numerics and P9 bundle claims unless local sources are added `66-96`, `1230-1236`.
