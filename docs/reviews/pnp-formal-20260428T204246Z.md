Line references are to `papers/millennium-pnp-formal/pnp-formal.tex`.

**1. Claim Audit**
- `“P_sigma^cas is strictly smaller”` at line 168 is not established. At most the algorithm class is syntactically restricted; language-class strictness is exactly what the paper later makes conditional.
- Proposition `Restricted-model separation` at lines 173-184 is conditionally valid only under both H$_{\sigma\text{-irr-NP}}$ and B$_{\sigma-P}$. The downstream corollary does not state both.
- Proposition `Conditional P vs NP` at lines 192-198 is not proved as stated. The proof at lines 501-525 also needs the hidden assertion at lines 512-515 that generic σ-irreducible SAT lacks rung-localization, φ-adic, and `2I` structures.
- CT equivalence at lines 253-256 is locally supported by `cascade-dynamics.md`, but only at proof-sketch level.
- Theorem `Generic σ-irreducibility` at lines 275-279 overclaims. It says `any α > 0`, but the proof only gives decay for `α > 1` at lines 301-302, and the per-clause `≤ 1/2` bound at lines 289-293 is asserted, not proved.
- Corollary `Generic 3-SAT is σ-irreducible` at lines 307-311 inherits the defective theorem.
- The claim `L_{σ-red} ∈ NP^cas` at lines 363-367 is not established. The definition quantifies over all σ-orbit-constant assignments at lines 266-268; no polynomial verifier for that universal condition is supplied.
- Consequently `L_{σ-irr} ∈ coNP^cas` at lines 368-369 is also not established.
- The “at least as hard as NP=coNP” claim at lines 380-388 has no reduction. It should be framed as a warning, not a complexity classification.
- Lemma `Conditional NP-containment` at lines 392-398 is tautological and correct.
- Corollary `Separation, conditional` at lines 402-405 is false as stated: it assumes only H$_{\sigma\text{-irr-NP}}$, but the proof invokes B$_{\sigma-P}$ at line 410.
- Structural completeness at lines 455-460 is acceptable as a conjecture, but the “Equivalently” sentence is not formal unless “exploits” is defined and linked to a property of the problem.
- `Conjecture ... equals (c1)+...+(c5)` at line 492 is not justified. The listed existence/examples statements do not imply a normal-form theorem.
- Barrier propositions at lines 531-589 prove only “not obviously the same as” BGS/RR/AW. They do not prove barrier evasion.
- Theorem `Cascade evades three barriers` at lines 593-601 remains theorem-styled despite the status section demoting it at lines 718-723. The demotion is correct; the theorem environment is not.
- The spectral count at lines 619-634 is locally supported, but the target cites a script whose eigenvalue recognition is numerical/heuristic; the exact closed-form support is clearer in `rh-formal.tex`.
- The finite-noise values at lines 648-667 are locally supported. However, the cited RH source explicitly says R3 is not a true control because the projector forces the result.
- The aria H$_4$O claim at lines 671-677 is not locally verifiable; the paper does mark it external.
- `If Conjecture~... can be proved, we have a proof of P != NP` at lines 774-775 is wrong. It still needs H$_{\sigma\text{-irr-NP}}$, B$_{\sigma-P}$, and the hidden non-structure bridge.

**2. Internal Consistency**
- `thm:sigmaPlim` is a `hypothesis`, but is cited as `Theorem` at lines 410, 508, and 541.
- `thm:separation` and `thm:conditional` are propositions but called theorems at lines 200, 433, 501, and 606.
- Line 433 says the separation follows under H$_{\sigma\text{-irr-NP}}$ and structural completeness; it should say H$_{\sigma\text{-irr-NP}}$ and B$_{\sigma-P}$.
- Definition uses a bipartition `A \sqcup B` at lines 264-266, but the complexity discussion invokes Bell-number all-partition growth at lines 355-356. These are different combinatorics.
- σ-orbits of variables are used at line 267, but σ is never defined as an action on variables/formulas.
- Line 512 says σ-irreducible instances have “NO σ-symmetry”; that does not follow from Definition 3.1.
- `Heuristic~\ref{thm:genirr}` at line 724 points to a theorem environment at line 275.

**3. External Consistency**
- `CascadeDynamics` supports the primitive/time/CTM/P/NP framework, but also contains an older claim that σ-IRREDUCIBLE-SAT is σ-NP-complete. That conflicts with this paper’s corrected open H$_{\sigma\text{-irr-NP}}$ posture.
- `ConvergenceWithSmart` supports only descriptive architectural similarity, not a formal meta-theorem.
- `CascadeRHformal` supports the bridge-pattern caution and says the 94/120 spectral count is the only unconditional part.
- `CascadeAttractorSpectrum` supports the 94/26 split as a finite diagnostic; exact theorem-grade support is better attributed through the RH formal source / Paper XXII chain.
- `CascadeAttractorDynamics` supports the printed values and also says R3 is not a real control.
- `ariaH4Olaw` cannot be verified in this repository; `aria-chess/...` is absent.

**4. Tightness**
- Line 168: replace “is strictly smaller” with “is a restricted subclass; strictness is conditional below.”
- Lines 275-279: change theorem to “Heuristic” or restrict to `α > 1` and supply the missing probability bound.
- Lines 402-435: state B$_{\sigma-P}$ everywhere the separation is claimed.
- Lines 531-597: replace “evades” with “is not shown here to fall under the standard barrier formulations.”
- Lines 774-775: replace with “If H$_{\sigma\text{-irr-NP}}$, B$_{\sigma-P}$, and structural completeness are proved, the argument would imply P != NP.”

**5. Surface Issues**
- `\bm` is used at lines 631 and 633 but no `bm` package is loaded.
- `\HH` is used at line 637 but is undefined.
- `\textgamma` at lines 76, 202, and 427 is likely undefined without an added package.
- Unicode σ inside math identifiers is engine-sensitive; use `\sigma` macros consistently.
- Bibliography entry `CascadeFoundations` is unused.
- All-caps proof prose such as `FIXED`, `NONE`, `NO`, `ARBITRARY` should be normalized.

**6. Top Three Fixes**
1. Rebuild σ-irreducibility formally: define σ on variables/formulas, resolve bipartition vs Bell partitions, and prove or withdraw the NP/coNP claims and generic-density theorem. See lines 262-303 and 350-389.
2. Repair the dependency graph outside the abstract: the corollary, separation paragraph, conditional P-vs-NP proof, and conclusion still omit B$_{\sigma-P}$ or add the wrong hypothesis. See lines 402-435, 501-525, and 774-775.
3. Demote the barrier-evasion and structural-completeness material to programme remarks unless a real normal-form/lower-bound theorem is supplied. See lines 455-497 and 531-601.
