**1. Claim Audit**

- `def:icosian`: “`V_600` is the set of 120 unit icosians…” at `bekenstein-incidence.tex:118`. Established for the implemented coordinate model: `verify.py` checks count, unit norm, closure.
- `lem:basics`: “120 elements; unit norm; closed under multiplication” at `:142`. Established by exhaustive exact checks over all `120^2` products.
- `def:Ttau`, `def:Kclass`, `def:bulk` at `:161`, `:170`, `:192`. Definitions are computable and implemented exactly.
- `lem:kmulti`: “`{72:1, 0:1, 52:5, 20:5}`” at `:181`. Established by exact shell ordering in `group.py` and verified.
- `lem:h_is_dic5`: “`H ≅ Dic_5` with presentation” at `:197`. Established: closure, generator pair, and generated-subgroup equality are checked.
- `lem:cosetkpair`: right cosets are whole `K=52/K=20` cycle pairs; left cosets have `2` vertices from each non-bulk cycle at `:217`. Established. The new finite check `g^{-1}τg ∉ H` for all `g∉H` is present in `verify.py:149` and passes. Minor exposition only: add one sentence that the induced action on the five non-bulk left cosets is therefore a 5-cycle, so a length-10 orbit hits each twice.
- `lem:nonnormal`: “`N_G(H)=H`” at `:252`. Established by exhaustive `gH != Hg` for all 100 outside elements.
- `def:galois`, `def:V24`, `lem:24cell` at `:273`, `:279`, `:287`. Established: rational-coordinate fixed set has size 24; `V_600 ∩ σ(V_600)=V_24` is checked.
- `thm:main`: every left/right coset has `4` fixed and `16` mobile at `:305`. Established by direct exact enumeration of all six left and six right cosets.
- `thm:taunoindep`: all 24 order-10 `τ` choices yield uniform incidence and 6 conjugate subgroups at `:343`. Established by verifier sweep.
- `def:SAcount`, `thm:onequarter` at `:394`, `:404`. Established as a direct corollary of `thm:main`; aggregate `20/80=1/4` also checked.

No substantive mathematical over-claim found in the derivation.

**2. WO Acceptance Audit**

- B1-B7 priority gaps: resolved. Normality corrected, Dic5 certificate added, exact shell comparator used, `V_24` proof/certificate present, incidence table printed, scope firewall present, verifier exits 0.
- D1: partially resolved literally. Content exists, but file is `bekenstein-incidence.tex`, not `paper.tex` as specified at `WO.md:53`.
- D2-D10: resolved.
- D11: resolved. `verify.py` asserts all required finite facts.
- D12: resolved. Exact command `PYTHONDONTWRITEBYTECODE=1 python3 papers/bekenstein-incidence/verify.py` exits 0. Shared tests also pass: `35 passed`.
- F1-F3 next builds: resolved in substance. Minor stale WO metadata remains: `WO.md:1` says `28 passed`, current suite is `35 passed`.

**3. Catalogue Audit**

Catalogue matches the LaTeX labels: D1-D7, L1-L6, T1-T3 are all present and backed. The requested `V_600 ∩ σ(V_600)` content is included under L6 and is certified by `verify.py`. No extra catalogue entry lacks derivation support. No derivation definition/lemma/theorem is missing from the catalogue.

**4. Attribution / External Consistency**

Repo-local attributions check out:

- `NARRATIVE.md:60,68` supports Paper 1 as group-theoretic `4/16=1/4`.
- `SCOPE.md:20-26` contains the intended spine.
- `NOTATION.tex:7-10,26,60` defines the symbols claimed.
- `icosian.py:1-9,112-157` supports exact `Q(sqrt(5))` arithmetic and explicit vertex construction.
- `test_24cell_incidence.py:25-63` and `test_dic5_subgroup.py:10-129` cover the cited facts.
- `cascade-algebraic-substrate.tex:918-928` supports the “inscription, not quotient” warning.
- `cascade-algebraic-substrate.tex:2247-2260` contains Coxeter/Conway-Sloane/Moody-Patera anchors.

**5. Sim Correctness**

`verify.py` is not a statistical sim; it is a deterministic exact finite certificate. That is the honest setup for these claims. No χ²/KL/null-hypothesis issue applies. It implements the derivation’s computable claims directly and passes, including the new `g^{-1}τg ∉ H` check.

**6. Tightness**

- `bekenstein-incidence.tex:235-243`: proof is correct but terse. Add the 5-cycle sentence noted above.
- `bekenstein-incidence.tex:71-75`: the Bekenstein-Hawking formula/proportionality wording is awkward. Use natural units or write the full dimensional coefficient cleanly.
- `WO.md:1`: update `28 passed` to `35 passed`.

**7. Top Three Fixes**

1. `bekenstein-incidence.tex:240-242`: add the explicit induced-5-cycle argument for `lem:cosetkpair`.
2. `bekenstein-incidence.tex:71-75`: clean the dimensional Bekenstein-Hawking formula wording.
3. `WO.md:1`: update stale test baseline to `35 passed`.

**8. Verdict**

Publication ready: yes.
