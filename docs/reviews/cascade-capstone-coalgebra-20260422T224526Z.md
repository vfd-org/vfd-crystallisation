**1. H-grad** (`cascade-capstone-coalgebra.tex` lines 2333-2340; imported dependency)
- Needed: prove a theorem of parenthesisation-independent character grading strong enough to justify `deg(s_1\cdots s_n)=\sum deg(s_i)` for all products involving the Observer rung, not just the commutative rungs. In the capstone this is the real upstream engine behind both P-A and the claimed derivation of H-FIC.
- Use: `papers/cascade-derivation/cascade-access-principle-theorem.md` §4.3 and §6.1, especially lines 164-178 and 225-233; `cascade-meta-layer-theorem.md` M1 for the `\widehat{\mathbb Z[\varphi]^6}` grading space; mainstream inputs are exactly the ones already named there: Bellissard-Kellendonk-Sadun on the commutative side, Baez plus the icosian/mod-2 identification on the octonion side.
- Work: days, not months. The note itself says the remaining sub-gap is the classical identification `\widehat{\mathbb Z[\varphi]^6}\to (\mathbb Z/2)^3`, with an explicit estimate of `1-2 weeks` (`cascade-access-principle-theorem.md` lines 231-233, 319-320). That is the shortest high-impact closure in the whole stack.
- Risk: the full `\widehat{\mathbb Z[\varphi]^6}`-grading may be stronger than the capstone actually needs. If the mod-2 lift is awkward, weaken to a theorem that only the character support, not the full vector, is parenthesisation-independent; that is enough for P-A and likely enough for H-FIC-O.
- Priority: first. It tightens two downstream hypotheses at once: H-FIC becomes nearly formal, and P-A stops being conditional in the only place the local note still calls non-trivial.

**2. H-FIC** (`cascade-capstone-coalgebra.tex` lines 834-880)
- Needed: replace Remark 3.6’s handwave with two explicit lemmas: one for `\mathcal G_O^{(S_\infty)}=\cap_n \mathcal G_O^{(S_n)}`, one for the analogous `E_8` generated-subgroupoid identity.
- Use: the capstone already tells you the route at lines 856-876. For the observer rung, combine H-grad with `Q_O\cong Meas(S^7,\sigma)` from `cascade-q-o-measurement-bridge.md` §3. For `E_8`, use the factor-through presentations in `cascade-query-algebra-presentations.md` §1 and the character-subset formalism in `cascade-access-principle-theorem.md` §1.3-§2.
- Work: 1-3 days once H-grad is in hand. This is a bookkeeping proof, not a new idea.
- Risk: H-FIC-`E_8` as stated may be too strong for arbitrary generated subgroupoids; intersection need not commute with “generated by restrictions” without a closure hypothesis. If that fails, weaken to the only sufficient form the capstone uses: commutation along descending chains with eventually constant rung-index and characterwise closed components.
- Priority: second. It removes a fake hypothesis from the existence/comonad half of the capstone.

**3. H-lift** (`cascade-capstone-coalgebra.tex` lines 639-679, 2370-2376)
- Needed: prove a region-surjectivity theorem for the bridge: every observer morphism `\gamma\in\mathcal G_O` is hit by the lift of some `Q_O` observable, or at minimum by a finite join of such lifts. That is exactly what the `S^7`-coverage lemma uses.
- Use: `cascade-q-o-measurement-bridge.md` §2-§4, especially the explicit bridge map and inverse (lines 74-102, 112-138); `cascade-query-algebra-presentations.md` §6 for the concrete `Q_O` generators. The natural proof route is to identify the observer direction attached to `\gamma` and use the corresponding projector/basis section.
- Work: 2-5 days if you phrase it correctly. This is an extension of an existing theorem, not a fresh construction.
- Risk: the current statement may be stronger than necessary. The capstone only needs terminal coverage at line 665. A weaker but sufficient replacement is: for every `\gamma\in\mathcal G_O`, there exists a finite set `{g_i}\subset Q_O` such that `\gamma` lies in `\bigvee_i \mathcal G_O^{(g_i)}`. That is much more plausible and still proves `\mathcal G_O^{(\mathbf 1)}=\mathcal G_O`.
- Priority: third. It closes the last non-formal step in the final-coalgebra existence theorem.

**4. H-rdr** (`cascade-capstone-coalgebra.tex` lines 1377-1399)
- Needed: formulate and prove the closure-data-aware Access Principle properly. As stated, H-rdr says “reachable subgroup strictly shrinks as `\Phi` shrinks.” That is too blunt. The actual theorem needed is: define refined character sets `C_{r,\Phi}` from the installed subgroupoids; then `Q_{(S,\Phi)}` is generated by those refined character sets, and `Q_{(\Rungs,\Phi)}=\Gamma(\mathcal G,\mathcal F)` only for `\Phi=\Phi_{\max}`.
- Use: extend `cascade-access-principle-theorem.md` §1.3, §1.5, and §5 by replacing bare rung-sets with closure-data-refined objects; use `cascade-query-algebra-presentations.md` §1.1-§1.3 for the factor-through model of `Q_r`.
- Work: 3-7 days after the statement is weakened. Most of the proof is already written in the bare-rung case.
- Risk: the strict monotonicity clause is probably false as written. Shrinking subgroupoids can remove redundant morphisms without shrinking the reachable character subgroup. The weaker uniqueness criterion above is safer and is all the capstone actually needs.
- Priority: fourth. Fix the statement before trying to prove it.

**5. H-loc** (`cascade-capstone-coalgebra.tex` lines 1313-1375, 2377-2384)
- Needed: not the current literal statement unless you enjoy pain. The capstone only needs a witness-separation theorem: if `\gamma\in\mathcal G_r\setminus \mathcal G_r^\Phi`, there exists some section in `Q_r` that is admitted by `\Phi_{\max}` and excluded by `\Phi`. Compact support inside `\mathcal G_r` is overkill.
- Use: `cascade-meta-layer-theorem.md` M3d (lines 175-190) gives local-to-global extension of finite-patch assignments; `cascade-query-algebra-presentations.md` §1 gives a factor-through description of rung observables. The obvious route is to build a local rung-`r` observable around `\gamma` and extend it globally via M3d.
- Work: 1-2 weeks if weakened to a witness theorem; longer and riskier if you insist on exact support containment outside all other rung morphisms.
- Risk: the current H-loc may simply be false. M3d gives extension of locally consistent finite patches, not arbitrary compactly-supported sections avoiding every outside morphism. Replace it with the weaker witness-separation theorem and the proof of Lemma 5.2 still goes through.
- Priority: fifth, but this is the hardest one conceptually. Do not attack it before H-rdr is reformulated, or you will prove more than you need.

**6. H-meas** (`cascade-capstone-coalgebra.tex` lines 2333-2346; imported dependency)
- Needed: theorem-grade closure of the parenthesisation selector, i.e. a clean statement that each measurement event picks a definite parenthesisation and hence a definite output in the relevant isotypic component.
- Use: `cascade-access-principle-theorem.md` lines 156-180 already treats this as established; `cascade-q-o-measurement-bridge.md` Theorem 3.1 does the heavy lifting; the only remaining hole is G6.4-a at lines 193-195, the explicit Fano-triad sign check.
- Work: hours to 2 days. This is the smallest patch in the stack.
- Risk: low. If the full multiplicative statement is awkward, a weaker “definite character component per measurement event” theorem already suffices for P-A. But given the existing script note, this should just be closed.
- Priority: sixth in conceptual difficulty, but it can be done immediately in parallel with H-grad as cleanup.

**Which to tackle first**
- First move: close H-grad.
- Immediate follow-up in parallel: close H-meas/G6.4-a.
- Then discharge H-FIC almost formally.
- Then prove a weakened H-lift.
- Then rewrite H-rdr into a defensible theorem and prove that.
- Leave H-loc for last, and weaken it before proving it.

**Hidden load-bearing assumptions not yet surfaced**
- `G6.1-L40` is still speculative (`cascade-query-algebra-presentations.md` lines 255-288, 332-367). Any theorem quantified over all seven rungs is quietly leaning on an unclosed life-rung observable model.
- The capstone uses support language for refined closure data, but the programme’s rigorous query-algebra model is now factor-through. The bridge between “support in `\mathcal G_r^\Phi`” and “lies in `Q_{(S,\Phi)}`” should be isolated as its own lemma/hypothesis.
- H-rdr currently hides a stronger monotonicity claim than the proof needs. Surface the weaker uniqueness criterion explicitly.
- H-lift currently hides a pointwise-surjectivity claim when the proof only needs terminal coverage. Surface the weaker finite-cover variant if that is all you can prove.
