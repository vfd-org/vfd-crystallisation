**Publication Ready?**

No. As a conditional reduction it is mostly honest, but it is not publication-ready until the attribution and scope problems below are fixed. The core RH implication is valid only because the added hypothesis is essentially RH in reformulated language.

**Claim Audit**

Round 16 must-fixes are substantively closed. The trivial-zero prose at `rh-formal.tex:1001-1008` and `rh-formal.tex:1240-1243` is now correct. The U3 titles at `rh-formal.tex:170` and `rh-formal.tex:823` are appropriately softened. The H4O wording at `rh-formal.tex:1704` is now only reported. `docs/convergence-with-smart.md` no longer claims formal equivalence.

- “The cascade zeta function equals the classical Riemann zeta function” (`rh-formal.tex:367-380`): established, but only by definition/compatibility. It is not an independent derivation of zeta.
- “The critical line is a joint fixed set” (`rh-formal.tex:382-397`): established for the anti-holomorphic involution. The zero set is invariant setwise, not pointwise; the text now respects this.
- “The Dedekind reduction for Q(sqrt 5)” (`rh-formal.tex:399-408`, `1312-1353`): established from the standard factorization into `zeta(s)L(s,chi_5)`.
- “Conditional RH” (`rh-formal.tex:414-420`, `1541-1553`, `1988-2000`): formally established, but the hypothesis `H_{\sigma-fix}` is equivalent to RH for non-trivial zeros. This is a conditional restatement, not progress toward RH.
- “F5 factor-2 equality” (`rh-formal.tex:506-527`): attribution to Paper XXXVI is verified locally; Paper XXXVI states the equality under explicit hypotheses.
- “Primes splitting by quadratic character mod 5” (`rh-formal.tex:592-620`): standard and proved.
- “Pentagonal torsion,” “shell-class theorem,” “K-multiset,” and “exact closed form” (`rh-formal.tex:642-765`): supported by the cited scripts/Paper XXII. The proof establishes the selected-clock version; it does not prove class-independence beyond the stated computation.
- “Mellin pole symmetry” (`rh-formal.tex:769-819`): established. Precision issue: the `K=0` case is a self-reciprocal double location, not a distinct reciprocal pair. Replace “four reciprocal pole-location pairs” around `rh-formal.tex:886-892` / `939-941` with “four K-values: three off-line reciprocal pairs and one self-reciprocal double location.”
- “Obstruction to one natural Hecke matching” (`rh-formal.tex:823-852`): now scoped correctly. It does not rule out all global Hecke interpretations.
- “Uniqueness of zeta” (`rh-formal.tex:989-1045`): proof works, but U1 alone plus analytic continuation carries most of the load. U2/U3 are not materially used. The corollary at `1042-1045` should say “meromorphic on C with at most a simple pole at 1,” not “on C\{1}” while also invoking the pole.
- “Sigma-attractor support” (`rh-formal.tex:1499-1539`, `1723-1782`, `2120-2124`): over-attributed. The cited AdaptiveClosureTransport source does not establish that non-sigma-fixed orbits are transient rather than attracting. It only motivates an open edge-space/equivariance selection programme.
- “Spectral attractor theorem” (`rh-formal.tex:1611-1658`): verified against Paper XXII and the local spectrum script.
- “H4O same 600-cell substrate” (`rh-formal.tex:1692-1720`, `1823-1830`): correctly presented as reported, not established.
- “Smart canonical overlap” (`rh-formal.tex:2130-2218`): partially verified. The abstract 12-fold/600-dimensional decomposition is in the Smart canonical framework, but the cited `SmartCanonicalFramework` file no longer contains the 120-cell Appendix M model.

**Internal Consistency**

The main definitions are now internally consistent. The sigma/tau distinction is mostly handled correctly.

Remaining substantive consistency issues:

- `\tau` is used both for the pentagonal clock element (`rh-formal.tex:642-646`) and for the anti-holomorphic critical-line involution (`rh-formal.tex:1914-1928`). Rename one, e.g. `\tau_{2I}` and `\tau_{\mathrm{crit}}`.
- U1/U2/U3 are used both for the zhat properties (`rh-formal.tex:148-184`) and for zeta uniqueness (`rh-formal.tex:993-1008`). This is not fatal, but it invites misreading.
- Several remarks are labelled as theorem-like objects: `thm:hecke_obs`, `thm:h4o_bridge`, `thm:B14`, `thm:dyn_attractor`. This is a presentation problem with mathematical consequences: it makes reported or heuristic material look load-bearing.

**External Consistency**

Verified locally:

- Paper XXXVI supports the F5 factor-2 hypothesis used at `rh-formal.tex:506-527`.
- Paper XXII supports the shell decomposition and the 94/26 sigma split used at `rh-formal.tex:666-673` and `1611-1658`.
- The B5/B6, B8, and B11 scripts support the pentagonal-clock, zhat, and Mellin-pole computations.
- Paper II in the Smart repository supports the ID* conditional RH framework.
- Smart Paper C supports the Bridge Axiom as an explicit bridge assumption, not a theorem of RH.

Not verified / must fix:

- `rh-formal.tex:1524-1532` and `2120-2124`: AdaptiveClosureTransport does not establish the claimed “non-sigma-fixed orbits would be transient rather than attracting” mechanism. This attribution is too strong.
- `rh-formal.tex:2148-2152`: the cited Smart canonical framework file does not contain the claimed Appendix M 120-cell model. The model is in a separate extracted/companion Appendix M file. Cite that file directly or weaken the statement.
- `rh-formal.tex:2227`: “Smart ID*” is present in Paper II, but I could not verify the locator “Def. 1.4” as written. Fix the locator.
- `rh-formal.tex:2228-2233` and `2282-2297`: the claim that the same Bridge Axiom shape appears across six Millennium reductions is not verified by the cited material in this pass. Either provide exact local theorem locators for all six or soften this to a programme-level observation.

**Tightness**

- `rh-formal.tex:316-322`: “we establish unconditionally” is defensible only for the definitional/classical consequences. Suggested edit: “We record unconditional identifications and conditional reductions.”
- `rh-formal.tex:1524-1532`: replace the AdaptiveClosureTransport sentence with: “AdaptiveClosureTransport motivates an edge-space equivariance programme; it does not prove a transient/non-attracting mechanism for non-sigma-fixed modes.”
- `rh-formal.tex:2208-2212`: replace “many of the same numerical constants” with “several analogous numerical constants, without a proved equivalence.”
- `rh-formal.tex:2282-2297`: “not six independent miracles” is too strong for a journal paper. Suggested edit: “The recurrence of similar bridge patterns is programme-level evidence, not a theorem.”

**Surface Issues**

No substantive undefined-macro or broken-LaTeX blocker found.

Surface issues that matter mathematically:

- Use “lines” rather than `\S` for citations such as `\S 539--562` and `\S 175--223`; those are line ranges, not sections.
- Replace “four reciprocal pole-location pairs” with the more precise K-count language noted above.
- The overloaded `\tau`, U1/U2/U3, and theorem-style labels on remarks should be cleaned before submission.

**Top Three Fixes**

1. Fix the AdaptiveClosureTransport attribution at `rh-formal.tex:1524-1532`, `1761-1782`, and `2120-2124`. The cited source does not prove or even clearly state the transient/non-attracting sigma-selection mechanism.

2. Fix the Smart citations at `rh-formal.tex:2148-2157` and `2226-2228`. The 120-cell Appendix M model is not in the cited canonical framework file, and the ID* locator is inaccurate.

3. Remove or explicitly support the six-Millennium “same Bridge Axiom shape” claim at `rh-formal.tex:2228-2233` and `2282-2297`. Without exact theorem locators across all six reductions, this is an unsupported meta-claim.
