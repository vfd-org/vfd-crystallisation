All line references are to `papers/millennium-ym/ym-mass-gap.tex` unless otherwise stated.

**1. Claim Audit**
| Claim | Verdict |
|---|---|
| “P8 … Der(O) has dim 14 … stabilizer … dim 8 exactly” (52-65) | Established. I ran `verify_der_O_su3_stabilizer.py`; it returns dim Der(O)=14 and dim stab(e1)=8. |
| “P9 … refinement graph … carries a cylindrical SU(3)-bundle” (66-75) | Not established. P3 explicitly excludes gauge correspondence; the local gauge source only gives a finite-level projection with modelling choices and open H2. |
| “H4 oscillator … theorem-grade … Lyapunov/MI/PC numerical claims” (80-96) | Overclaim. Local `cascade-h4-oscillator-law.md` derives a flow law but leaves coupling constants free; these numerical claims are not locally verified. |
| `H_OS-lift` (101-115) | Correctly stated as open. |
| “Step (ii) … follows from uniform F4 bound + H4 boundedness” (148-150) | Not established. F4 is a dimension count, not a compactness or Sobolev bound. |
| Main proposition (215-243) | Valid only as a restatement of `H_OS-lift`; the later proof does not independently establish it. |
| Discrete spectral theorem (245-251) | Numerical result is correct; proof is not rigorous as written. The character arithmetic/table in 947-975 is inconsistent. |
| Sigma orbit lemma (339-344) | Only citation-level, not proved locally. Acceptable if the Elkies/icosian claim is imported. |
| F1-F7 imports (357-415) | Mostly scoped correctly to Paper XXXVI, but the bibliography points to the older `cascade-foundations.md`, not `paper-xxxvi.tex`. |
| Finite-volume existence (490-492) | Plausible for compact finite link space; proof should use product Haar compactness, not F4. |
| Continuum measure existence theorem (518-521) | Not proved. It is exactly part of the open OS/continuum hypothesis. |
| Uniform tightness lemma (562-565) | False proof. It uses the deprecated “F4 spectral concentration” claim (572), a Gaussian moment formula for nonabelian YM (583-589), and an impossible convergence condition: it says convergence needs `s<-3` then uses `s=1/2` (591-594). |
| OS1, OS2, OS3, OS4 (642-842) | Not proved. OS1/OS3/OS4 are circular through the conditional mass gap/OS reconstruction; OS2 does not follow from finite Weyl symmetry. |
| `sigma = theta` lemma (805-810) | Not proved. P7 gives `tau=phi^{-n}`, not a Galois-to-time-reflection theorem; the logarithm/sign handling is formal. |
| H4 spectrum lemma (863-885) | Result verified by script; representation proof needs replacement. |
| Cascade spectral gap proposition (1005-1019) | Conditional only. The formula with `hbar * phi^{-N1}` is not derived from `H_OS-lift` and has undefined scale/units. |
| No massless gauge-invariant mode (1067-1071) | Not proved; it just repackages the open gap inheritance and adds unsupported spectral-subset claims. |
| Main theorem proof (1112-1158) | Does not establish the theorem because it depends on the failed OS/gap propositions. |
| 94/120 spectral count (1177-1192) | Verified locally by `test_sigma_attractor_spectrum.py`. |
| Dynamics diagnostic R1/R2/R3 (1206-1225) | Verified locally by `test_sigma_attractor_dynamics.py`; it supports only a finite diagnostic, not YM continuum transfer. |
| External aria H4O claim (1229-1235) | Properly caveated as not independently verified. |

**2. Internal Consistency**
F4 is repeatedly used as a spectral/compactness theorem despite being explicitly demoted to a dimension count at 372-381. Offending uses include 132-134, 148-150, 572-574, and 848-851.

The paper says `H_OS-lift` is the “single remaining open hypothesis” (123-126), but P9/gauge reconstruction, continuum measure existence, OS positivity, functional convergence, and the H4 oscillator coupling/numerical layer are still open or external.

F5 is scoped as finite-sum factor-2 equality under H-SIG (384-393), but OS3 uses it as full continuum action invariance `F[σA]=F[A]` (752-754). That is exactly the older formulation the paper says it no longer uses.

The target space oscillates between `S^3 x R` (446-448), `R^4` (41, 520-521), and Wightman QFT on `R^4` without a proved identification map.

Cross-references mostly resolve, but several resolve to the wrong logical kind: `Definition~\ref{hyp:OS-lift}` points to a hypothesis (217, 1007); `\ref{prop:OS0}` points to a remark (622); `thm:main` labels a proposition (215).

**3. External Consistency**
Paper XXXVI: verified in `papers/paper-xxxvi/paper-xxxvi.tex`. The scoped versions of F2/F4/F5/F6/F7 are broadly as stated, but this paper cites `cascadeFoundations` instead of Paper XXXVI. The line citation `cascadeFoundations` lines 539-562 does not verify the Paper XXXVI F5 claim.

P3: not verified. `cascade-refinement-spaces.tex` explicitly says P3 proves no “gauge … correspondence theorem” (180-189). Therefore P9 at 66-75 cannot be credited to P3.

Gauge correspondence: `papers/cascade-correspondence-foundations/foundations.tex` supports a finite-level `SU(3)` holonomy projection only with modelling choices M1-M3 (1975-2021), leading-order gauge equivariance (2142-2162), and open H2 for continuum reconstruction/OS positivity (2237-2257).

H4 oscillator: `cascade-h4-oscillator-law.md` supports the core law, but says coupling constants are free and `STUCK #3` remains (410-439). It does not verify the abstract’s Lyapunov/PC/MI claims.

RH analogue: `rh-formal.tex` supports the conditional-bridge comparison and exact 94/120 count, but explicitly says the H4O numerical artifact is external and not reverified (2159-2166).

`cascade-schrodinger.md` should not be used for OS1 support: it still states the stale five-value H4 spectrum `{0,4,8,10,12}` (131-132).

**4. Tightness**
Line 52: replace “Two foundational inputs are established” with “P8 is established; P9 is a finite-level/gauge-reconstruction input conditional on the gauge correspondence hypotheses.”

Line 80: replace “theorem-grade” with “core flow-law derivation; numerical diagnostics reported externally.”

Line 123: replace “single remaining open hypothesis” with “dominant open continuum hypothesis, after the finite gauge and reconstruction hypotheses are folded into its statement.”

Line 518: replace `theorem` with `hypothesis` or delete; it is not proved.

Line 613: replace “We now verify the OS axioms” with “We record the OS axioms required by `H_OS-lift`.”

Line 1067: remove the proposition or restate it as “conditional restatement of gap inheritance.”

**5. Surface Issues**
Broken LaTeX: `\emph{` opened at 1094 is never closed.

Undefined or missing-package macros: `\textbeta` (118), `\T_n` (646 etc.), `\bm` (1189, 1191).

Missing bibliography entries: `billingsley` (531) and `conwayQ` (905, 943, 954).

The accompanying spectrum script has stale text claiming `{0,4,8,10,12}` even though it computes the nine-value spectrum.

“positive rationals” for coefficients involving `π` is false (318-320, 475). Say “positive elements of `Q(π)`” or just “positive constants.”

The graph gap formula in physical units (1014) is dimensionally unsupported without a lattice spacing/energy-scale convention.

**6. Top Three Fixes**
1. Demote Sections 3-6 from proofs to hypotheses/requirements: continuum measure, tightness, OS axioms, reflection positivity, clustering, gap inheritance, and no-massless-mode are not proved (518-609, 642-853, 1005-1158).

2. Correct P9/gauge status: P9 is not established by P3 or P8. Cite the gauge-correspondence paper and state the modelling choices, leading-order nature, and open H2 explicitly (66-75, 215-220, 1120-1126).

3. Replace the H4 spectral proof with a correct character-table computation and clean the scripts/citations. The numerical spectrum is right, but the proof arithmetic and source support are not publication-grade (863-999).
