**Claim Audit**
- `cascade-12d-closure.md:39-45`: “I is the Z-subring ... generated by the 120 unit icosians” is consistent with `cascade-algebraic-substrate.tex:480-491`. The non-naive-basis correction is also consistent with `cascade-algebraic-substrate.tex:503-513`.
- `cascade-12d-closure.md:41-43`: rank-8 over `Z` and rejection of `{1,i,j,k}` as a `Z[φ]` basis are supported. Caveat: `cascade-algebraic-substrate.tex:480-514` states rank-8 over `Z`, not the rank-4-over-`Z[φ]` claim explicitly; that rank claim is consistent with the Task #10 spec, but should not be attributed to those lines alone.
- `cascade-observer-zeta.md:394-401`, `482-489`: O6 “RESOLVED” is justified for the stated basis/coordinatisation defect. No remaining positive implication that `I = Z[φ]{1,i,j,k}` was found in the scoped artefacts.
- `cascade-observer-zeta.md:431`, `437-449`: C7 is correctly still downgraded. The note separates the fixed O6 basis issue from the still-open simplified-decomposition / working-note issue.

**WO Acceptance Audit**
- Task #10 A: resolved. Level 2 no longer implies the naive basis and cites the canonical source. `cascade-12d-closure.md:39-45`
- Task #10 B: resolved with attribution caveat above. `Z`-rank 8 is canonical-source-backed; `Z[φ]`-rank 4 is preserved and non-conflicting. `cascade-12d-closure.md:41`
- Task #10 C: resolved. The Level 2 to Level 3 narrative still reads coherently. `cascade-12d-closure.md:46-54`
- Task #10 D: resolved. O6, C7, and catalogue O6/C7 are updated. `cascade-observer-zeta.md:394-401`, `431`, `482-489`; `cascade-observer-zeta-catalogue.md:35`, `60`, `69`
- Task #10 E: no naive-basis phrasing requiring correction found in `WO-H-GRAD-1.md:56-58`; `cascade-h-grad-1-closure.md:73-87` uses extra icosian generators, not the naive basis alone.
- Task #10 F/G: no conflicting canonical-source text or new “publication ready” declaration found in the scoped artefacts.

**Catalogue Audit**
- Catalogue O6 is backed by the derivation and corrected Level 2. `cascade-observer-zeta-catalogue.md:60`
- Catalogue P5 correctly lists O6 as resolved while retaining O1/O5 blockers. `cascade-observer-zeta-catalogue.md:35`
- Catalogue C7 correctly records that O6 is resolved but C7 remains downgraded for simplified-decomposition reasons. `cascade-observer-zeta-catalogue.md:69`
- No new Task #10 definition/theorem/numerical result was introduced that needs a new catalogue row.

**Attribution / External Consistency**
- Canonical source check passes for the actual coordinatisation fix: generated by 120 unit icosians, rank-8 over `Z`, not the naive `Z{1,i,j,k} ⊕ Zφ{1,i,j,k}` module. `cascade-algebraic-substrate.tex:480-514`
- C7 attribution remains weak as theorem support: `cascade-12d-closure.md:52` only gives the working-note physical/phason decomposition, while `dual_600cell_factor2.py:142-145` explicitly says the decomposition is simplified. The current note flags this, so it is not an over-claim.

**Sim Correctness**
No sim correctness audit was needed for Task #10. The only scoped script use is as a caveat source, and the cited caveat exists. `dual_600cell_factor2.py:142-145`

**Tightness**
- `cascade-12d-closure.md:41`: “The Z[φ]-basis is not...” would be tighter as “In particular, `{1,i,j,k}` is not a `Z[φ]`-basis...”
- `cascade-observer-zeta.md:394-401`: add that O1 still blocks P5’s domain; O5 is only the remaining icosian-bridge blocker.
- `cascade-observer-zeta.md:431`: acceptable, but “background σ-swap working-note claim” would be more precise than making `cascade-12d-closure.md:52` look like exact support for the shellwise free-swap statement.

**Top Three Fixes**
No must-fix in Task #10 scope. Optional tightenings are the three wording edits in **Tightness**.

**Verdict**
Publication ready: yes, for the Task #10 resolution scope only.
