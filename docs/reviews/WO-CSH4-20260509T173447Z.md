**Claim Audit**
- `papers/cascade-selection-h4/WO-CSH4.md:24` claims `(O1)` has “all four cascade-mechanism §3.4 sub-clauses (precondition, top-rung fixed-point, convergence, intertwining-via-flow).” This is false. In `papers/cascade-mechanism/cascade-mechanism.tex:594`, the four obligations are `(O0)`–`(O3)`: preconditions, top-rung fixed point/convergence, flow intertwining, residual monotonicity. `(O1)` alone is only top-rung fixed point plus convergence at `papers/cascade-mechanism/cascade-mechanism.tex:609`. This is the main residual scope drift.
- `papers/cascade-selection-h4/WO-CSH4.md:30` says the witness is conditional on choosing `ξ* ∈ (F^0)^{W(H_4)} \ {0}`. Mathematically viable as a family of witnesses, but not literally a single explicit witness unless the paper fixes one `ξ*` once and for all. Current wording oscillates between “single tuple” and “parametrised tuple.”
- `papers/cascade-selection-h4/WO-CSH4.md:51` defines `L_N^{block}` fibrewise from the scalar graph Laplacian. This is mathematically sound, assuming the scalar Laplacian acts componentwise on vector-valued functions.
- `papers/cascade-selection-h4/WO-CSH4.md:71` says P4’s vertex operator has kernel dimension `25|V| + 7`. This follows from P4’s `d_n^*d_n` acting only on the imaginary `V_8` direction (`papers/cascade-closure-dynamics/cascade-closure-dynamics.tex:419`), assuming the scalar graph is connected: ignored fibre dimensions are `4+4+16+1 = 25` per vertex, plus one constant imaginary direction per connected component. Acceptable, but the connectedness hypothesis should be stated.
- `papers/cascade-selection-h4/WO-CSH4.md:75` says the new operator’s kernel is exactly `F^0`-valued constants. Correct if `G_N^{H_4}` is connected; that hypothesis is later claimed at `papers/cascade-selection-h4/WO-CSH4.md:128`.
- `papers/cascade-selection-h4/WO-CSH4.md:84` says P4’s `A_n^{vertex}` is the `V_8`-imaginary projected restriction of `L_N^{block}`. Substantively correct but too compressed: it is better written as `A_n^{vertex} = ι_{\Im O} L_N^{cl} π_{\Im O}` on the vertex sector, not as a literal restriction of the full block operator.
- `papers/cascade-selection-h4/WO-CSH4.md:109` says P4’s `thm:flow-exists` applies to `L_N^{block}`. Over-attributed. P4’s theorem is stated for P4’s `L_n` on `X_n` (`papers/cascade-closure-dynamics/cascade-closure-dynamics.tex:651`), not arbitrary self-adjoint operators on `X_N^{0,H_4}`. The proof is generic finite-dimensional ODE/matrix exponential, so the result is true, but the citation should be “by the same finite-dimensional argument as P4 `thm:flow-exists`,” not “applied to.”
- `papers/cascade-selection-h4/WO-CSH4.md:125` self-adjoint/PSD/boundedness of `L_N^{block}` is established by the definition plus standard scalar Laplacian facts. Fine.
- `papers/cascade-selection-h4/WO-CSH4.md:128` connectivity of `G_N^{H_4}` is plausible from P3 refinement: new midpoints attach to old vertices and centroids attach to midpoints (`papers/cascade-refinement-spaces/cascade-refinement-spaces.tex:339`, `papers/cascade-refinement-spaces/cascade-refinement-spaces.tex:346`). The proof must explicitly use base connectivity and class-(a)/(b) edges. Class-(c) is not needed.
- `papers/cascade-selection-h4/WO-CSH4.md:133` convergence to `P_ker` with rate `λ₂(L_N^{block}) = λ₂(L_N^{cl}) > 0` is correct for finite connected graphs. Multiplicity caveat: `L_N^{block}` has the scalar spectrum repeated `32` times; the first positive eigenvalue is still the scalar `λ₂`.
- `papers/cascade-selection-h4/WO-CSH4.md:140` fixed-subspace dimension `28` is supported by P3’s branchwise action: standard on `V_H4`, trivial on `V_D4,V_16,V_8` (`papers/cascade-refinement-spaces/cascade-refinement-spaces.tex:1179`). The irreducibility/no-fixed-vector step needs Humphreys; locally P3 does not prove it. This is acceptable.
- `papers/cascade-selection-h4/WO-CSH4.md:155` claims the tuple discharges `(O1)`. Correct only for `(O1)` narrowly understood as top-rung fixed point and convergence. It does not discharge `(O0)`, `(O2)`, or `(O3)`, and must not be advertised as doing so.
- `papers/cascade-selection-h4/WO-CSH4.md:168` folds `(O0)` into the `(O1)` proof. Wrong taxonomy. `(O0)` is a separate consumer obligation at `papers/cascade-mechanism/cascade-mechanism.tex:595`.
- `papers/cascade-selection-h4/WO-CSH4.md:205` says no open analytic hypothesis is needed at finite `N`. True for the top-rung `L_N^{block}` flow. False if the narrative implies full cascade closure across rungs, because `(O2)` flow intertwining and `(O3)` monotonicity remain separate per `papers/cascade-mechanism/cascade-mechanism.tex:618` and `papers/cascade-mechanism/cascade-mechanism.tex:623`.
- `papers/cascade-selection-h4/WO-CSH4.md:271` asks for `λ₂` exact from `H_4` eigenstructure, while A8 only requires numerical `λ₂`. P2 already has the `600`-cell Laplacian spectrum with `λ₂ = 9 - 3√5` at `papers/cascade-algebraic-substrate/cascade-algebraic-substrate.tex:1154`; using it for worked numerics is fine, but do not conflate this with the fixed-subspace proof.

**WO Acceptance Audit**
- **A1** `papers/cascade-selection-h4/WO-CSH4.md:326`: Partially specified, not resolved. The theorem is drafted in the WO, but no `.tex` exists. Also clause (5) must be narrowed to `(O1)` only, not API-wide closure.
- **A2** `papers/cascade-selection-h4/WO-CSH4.md:329`: Not resolved. Section order is specified, but no `.tex`.
- **A3** `papers/cascade-selection-h4/WO-CSH4.md:330`: Partially resolved in the WO. Most labels exist, including `def:Atilde`, but the WO also cites P4 `thm:flow-exists` outside its exact stated scope.
- **A4** `papers/cascade-selection-h4/WO-CSH4.md:333`: Not resolved. The WO asks for a non-claims box in `.tex`; no `.tex`.
- **A5** `papers/cascade-selection-h4/WO-CSH4.md:334`: Resolved at WO-spec level. `H-uniqueness` and `H-physical-interpretation` are named at `papers/cascade-selection-h4/WO-CSH4.md:300`.
- **A6** `papers/cascade-selection-h4/WO-CSH4.md:338`: Resolved at WO-spec level. The operator is explicitly introduced at `papers/cascade-selection-h4/WO-CSH4.md:51` and distinguished at `papers/cascade-selection-h4/WO-CSH4.md:82`.
- **A7** `papers/cascade-selection-h4/WO-CSH4.md:342`: Resolved at WO-spec level. The proof route uses Humphreys rather than P2 character data at `papers/cascade-selection-h4/WO-CSH4.md:150`.
- **A8** `papers/cascade-selection-h4/WO-CSH4.md:345`: Not resolved. Worked numerics are requested but not present; sim is optional.
- **A9** `papers/cascade-selection-h4/WO-CSH4.md:349`: Not resolved. This review returns “no.”
- **H-uniqueness** `papers/cascade-selection-h4/WO-CSH4.md:302`: Properly left open. Do not let theorem wording imply it is solved.
- **H-physical-interpretation** `papers/cascade-selection-h4/WO-CSH4.md:315`: Properly left open.
- **R1** `papers/cascade-selection-h4/WO-CSH4.md:354`: Partially mitigated. The WO is honest that `L_N^{block}` is auxiliary/ad hoc.
- **R2** `papers/cascade-selection-h4/WO-CSH4.md:363`: Partially mitigated. The freedom in `ξ*` is acknowledged, but this also weakens the “single explicit tuple” claim.
- **R3** `papers/cascade-selection-h4/WO-CSH4.md:368`: Resolved as a non-claim.
- **R4** `papers/cascade-selection-h4/WO-CSH4.md:370`: Mitigation is adequate; class-(a)/(b) edges suffice for connectivity.
- **R5** `papers/cascade-selection-h4/WO-CSH4.md:375`: Not yet resolved; numerics are not supplied.

**Catalogue Audit**
- No math-catalogue artefact was supplied or present under `papers/cascade-selection-h4/`.
- Defect if a catalogue is required for this WO: the WO introduces at least one definition (`L_N^{block}`), one theorem, two named open hypotheses, and numerical requirements, none of which are catalogued in the supplied artefacts.

**Attribution / External Consistency**
- P3 `def:base-graphs` supports `V_0^{H_4}=V_{600}` and the base graph definition at `papers/cascade-refinement-spaces/cascade-refinement-spaces.tex:262`.
- P3 `def:refinement` supports class-(a), class-(b), class-(c) refinement edges at `papers/cascade-refinement-spaces/cascade-refinement-spaces.tex:320`.
- P3 `not:fibres` supports `F^0 = V_H4 ⊕ V_D4 ⊕ V_16 ⊕ V_8`, dimension `32`, at `papers/cascade-refinement-spaces/cascade-refinement-spaces.tex:548`.
- P3 `def:Xn-0` supports `X_n^{0,bullet} = ℓ²(V(G_n^bullet);F^0)` at `papers/cascade-refinement-spaces/cascade-refinement-spaces.tex:604`.
- P3 `def:coxeter` supports standard `H_4` action on `V_H4` and trivial action on `V_D4,V_16,V_8` at `papers/cascade-refinement-spaces/cascade-refinement-spaces.tex:1172`.
- P4 `def:coboundary` supports the correction that P4 projects to `V_8` then `Im(O)` at `papers/cascade-closure-dynamics/cascade-closure-dynamics.tex:313`.
- P4 `def:An` supports that P4’s vertex Laplacian acts classically only on `V_8`-imaginary and trivially on other vertex-fibre directions at `papers/cascade-closure-dynamics/cascade-closure-dynamics.tex:412`.
- P4 `prop:block-bounds` does not directly give a bound for the new full-fibre scalar block Laplacian; it bounds P4’s `d_n`, `A_n`, `B_n`, `C_n` at `papers/cascade-closure-dynamics/cascade-closure-dynamics.tex:458`. Use Chung or a fresh finite-graph estimate for `L_N^{block}`.
- P4 `thm:flow-exists` is not literally applicable to `L_N^{block}` as stated; see `papers/cascade-closure-dynamics/cascade-closure-dynamics.tex:651`.
- compat `def:Atilde` exists and says `\widetilde A_n := 2^n A_n^{vertex}` at `papers/cascade-refinement-compat/cascade-refinement-compat.tex:279`.
- cascade-mechanism §3.4 does not say `(O1)` contains preconditions/intertwining. It separates `(O0)`–`(O3)` at `papers/cascade-mechanism/cascade-mechanism.tex:594`.

**Sim Correctness**
- No sim was supplied. Not applicable.
- If a sim is added for A8, it should compute the scalar `600`-cell graph Laplacian spectrum at `N=0`; the expected first positive eigenvalue is `9 - 3√5 ≈ 2.2918` from P2’s table at `papers/cascade-algebraic-substrate/cascade-algebraic-substrate.tex:1154`.

**Tightness**
- Replace `papers/cascade-selection-h4/WO-CSH4.md:24`: “Convert the cascade-mechanism consumer-API obligation `(O1)` … with all four … sub-clauses” with “Discharge the top-rung `(O1)` obligation; `(O0)` is checked separately and `(O2)`/`(O3)` are not discharged here.”
- Replace `papers/cascade-selection-h4/WO-CSH4.md:109`: “equals the flow of P4’s `thm:flow-exists` applied to `L_N^{block}`” with “is justified by the same finite-dimensional matrix-exponential argument as P4 `thm:flow-exists`.”
- Replace `papers/cascade-selection-h4/WO-CSH4.md:14`: “single witnessed `(O1)`-tuple” with “for each fixed nonzero `ξ*`, a witnessed `(O1)`-tuple.”
- Replace `papers/cascade-selection-h4/WO-CSH4.md:227`: “four sub-clauses of `(O0)` and `(O1)`” with “the `(O0)` preconditions and the `(O1)` top-rung fixed-point/convergence clauses.”
- Replace `papers/cascade-selection-h4/WO-CSH4.md:392`: “cascade closure as selection mechanism” with “top-rung `(O1)` witness for a chosen `ξ*` on the `H_4` branch.”

**Top Three Fixes**
- Fix the API taxonomy at `papers/cascade-selection-h4/WO-CSH4.md:24`: `(O1)` is not the four-part consumer API; do not claim precondition/intertwining as `(O1)` content.
- Fix the P4 flow attribution at `papers/cascade-selection-h4/WO-CSH4.md:109`: P4’s theorem is not stated for the new operator; cite the generic finite-dimensional proof or restate it locally.
- Fix “single explicit tuple” wording at `papers/cascade-selection-h4/WO-CSH4.md:14`: either choose a concrete `ξ*` or consistently state a theorem “for every fixed nonzero `ξ*`.”

**Verdict**
Publication ready: no.
