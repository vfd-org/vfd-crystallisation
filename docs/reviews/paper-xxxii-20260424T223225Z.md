All line numbers below refer to `papers/paper-xxxii/paper-xxxii.tex`.

**1. Claim Audit**

- L136, Proposition `prop:phi`: proof establishes `r = phi` from the geometric-mean condition. Hidden hypotheses should be stated in the proposition: `d_1^2 > 0`, `Delta > 0`. The proof now explicitly states shell normalisation `d_2 = 1` at L164. That part is fixed.
- L188-L196, angular scale: the `pi/5` conclusion is valid only after shell-normalisation `d_2 = 1` plus unit chordal `S^3` convention. L195-L196 still says “forced by the geometric-mean permeability axiom” too strongly. It is forced by geometric mean + shell normalisation + chordal embedding.
- L176-L183, Hurwitz remark: corrected to “infinitely many rationals” and labelled motivational. Acceptable.
- L228, Theorem `thm:600cell`: proof establishes uniqueness of `2I` only within H1-H4. That is now honestly stated. No overclaim here.
- L273, “Classification note”: demotion from corollary to remark is appropriate. However the label remains `cor:phi_determined`, which is misleading.
- L349, Theorem `thm:F`: not fully proved as stated. Faddeev uniqueness gives Shannon entropy up to scale/sign under additional normalisation/positivity conventions; the proof silently chooses the negative-entropy sign needed for log-sum-exp. Also the derivation from A3 to `R(p tensor q)=R(p)+R(q)` is only sketched. This needs either a tighter theorem statement or a real variational proof.
- L400, `prop:properties` now a remark: demotion is good. The item “uniform Hessian at vertices” is plausible but not shown unless “uniform” only means vertex-independent by symmetry. Vertex minimality/barrier height are correctly only sketch-level.
- L424, Proposition `prop:Z2`: algebraic factorisation is proved.
- L445, Theorem `thm:Fint`: follows from definitions plus `Z_2=Z_1Z_2-D`; acceptable, though the theorem should explicitly require `0 <= eta < 1`.
- L457, Proposition `prop:Fint_props`: repulsion is established. Cluster decomposition is proved only for states near basin centres (`Z(psi_k) >= 1`, L495), not for arbitrary states as stated. The “maximum at `psi_1=psi_2`” hard-core claim is not proved globally.
- L538-L543, spectral decomposition: now explicitly heuristic and open. Good retreat.
- L557-L582, mode-sector definition/additivity: status is honest; additivity across different operator spaces is explicitly assumed at L582.
- L584-L589, “remaining modes negligible”: W/Z suppression is plausible; the irrational-sector decoupling is overstated unless restricted to the shell-adjacency algebra/polynomial Hamiltonians of Paper XXII.
- L643, Observation `obs:fibre_flat`: tautologically proved from the trivial lift.
- L666, Proposition `prop:pion`: still not Pass-A honest. It says vertex exclusion “ensures” two basins localise on disjoint vertex sets (L673-L676). But the open-items list correctly says vertex exclusion does not by itself force disjoint `P_3` support (L1066). This contradiction must be fixed in the proposition itself.
- L683-L688 repeats the same problem: “disjoint-support property of the vertex-exclusion partition function” is not established.
- L729, Proposition `prop:photon`: zero-mode statement is proved by graph theory; identification with photon exchange and alpha is correctly marked identified.
- L804, Proposition `thm:factor`: now marked conditional, but the statement omits two assumptions used in proof: existence of a pion-sector binding minimum at `r_* = 6 pi lbar_p` and positive curvature `K_pi > 0` (L831-L834). These are later listed as open (L1067), but they belong in the proposition hypotheses.
- L933, Proposition `prop:deuteron`: correctly demoted to conditional computation. Arithmetic checks out: `r_d = 2.12800 fm`, `0.02 sigma` vs 2018, `0.83 sigma` vs 2022.
- L977, prediction: properly labelled assumption/extrapolation.
- L1178, Appendix proposition `prop:triple`: proof establishes the algebraic identity given R1-R5/minimality assumptions.

**2. Internal Consistency**

- Major inconsistency: `prop:pion` claims disjoint support follows from vertex exclusion (L673-L676), while the open-items list says the opposite (L1066). This is the main remaining honesty failure.
- Table chain L1015 marks “Six flat phase-coherence channels on `P_3`” as `blacktriangleright`, while L1038 says it is conditional on disjoint-support. The table should say “derived conditional” or use `blacktriangleright*`.
- L916 says “Compton wavelengths and fine-structure constant are taken … from CODATA,” but then uses VFD `alpha^{-1}=137+pi/87`, explicitly not CODATA. Rewrite: “Compton wavelengths from CODATA; alpha from the VFD identification.”
- Labels `cor:phi_determined`, `prop:properties`, and `thm:factor` no longer match their environments/status. Not mathematically fatal, but sloppy.
- Cross-references appear to resolve; I found no duplicate labels.

**3. External Consistency**

- Paper V: XXXII’s `0.014%` mass claim (L83-L84) is locally supported by Paper V, but Paper V states it as conditional/numerical correspondence, not a derivation. XXXII’s wording “produced” is acceptable.
- Paper XXII: alpha arithmetic and `0.81 ppm` are locally verified. Paper XXII also explicitly says this is a numerical/structural correspondence, not a loop derivation. XXXII now mostly respects that.
- Paper XXII decoupling: local theorem verifies invariant integer/irrational sectors only for shell-adjacency matrices and polynomials in them. XXXII’s L588-L589 should inherit that restriction.
- Paper XIII: verified. It defines four ansatz components with unspecified weights: proximity, alignment, compatibility, binding.
- SmartRadius / `papers/proton-radius/proton-radius.tex`: verifies `r_pi = pi lbar_p`, proton boundary resolvent, neutron formula, and old LO deuteron formula. The prior source itself overclaims “derived” in places, but XXXII now treats the imported uses mostly as identified/conditional.
- Paper IV: no direct `papers/paper-iv` source exists. The Paper-IV-like local source `papers/pemr/pemr.tex` supports the heat-kernel cumulant material, but XXXII’s bibliography does not make that traceable.

**4. Tightness**

- L195-L196: replace “forced by the geometric-mean permeability axiom” with “obtained from the geometric-mean ratio after imposing `d_2=1` shell normalisation and the chordal unit-`S^3` convention.”
- L673-L676: replace with “assuming the two `P_3` support probes are disjoint...”
- L684-L686: replace “the disjoint-support property of the vertex-exclusion partition function” with “the additional disjoint-support hypothesis.”
- L584-L589: replace “exactly decoupled” with “decoupled for shell-adjacency-algebra operators, per Paper XXII.”
- L1088-L1095: “Starting from one physical axiom” is still too grand; the derivation also needs geometric mean, compact `S^3`, finite subgroup closure, maximality, and entropy axioms. Say “Starting from the permeability axiom plus the stated structural axioms...”

**5. Surface Issues**

- L61 abstract is overlong and hard to parse; split the sentence.
- L273 label `cor:phi_determined` should be renamed to `rem:classification_note`.
- L400 label `prop:properties` should be renamed to `rem:properties`.
- L804 label `thm:factor` should be renamed to `prop:factor`.
- L916 CODATA/VFD alpha wording is wrong as noted above.
- L1034 uses `Eq.~\ref{eq:angle}`; use `Eq.~\eqref{eq:angle}` for consistency.
- No obvious undefined macros from source inspection.

**6. Top Three Fixes**

1. Fix `prop:pion` at L666-L681 and the explanatory remark at L683-L688. It still asserts the disjoint-support result that the open-items list denies.
2. Fix `prop:factor` at L804-L817 by adding the positive-curvature pion-minimum assumption to the proposition hypotheses, not only to the open-items list.
3. Tighten Theorem `thm:F` at L349-L386: specify the entropy sign/normalisation and give the missing step from A3/A4 to the log-sum-exp variational form.

Verdict: not quite Pass-A honest-retreat exit. The major retreats are present, but the disjoint-support contradiction is still inside a named proposition, and the factor theorem still uses an unstated binding-minimum hypothesis.
