Publication ready: no.

**1. Claim Audit**
- Lines 176-187: the central claim is not established. You write that if every path passes through
  “`\mathrm{MM}_{ij} := {(\Phi_S,\Phi_A): \Phi_S \notin U_i \text{ or } \Phi_A \notin V_i} \cup {(\Phi_S,\Phi_A): \Phi_S \notin U_j \text{ or } \Phi_A \notin V_j}`”
  and if (iii) holds, then “`\Delta\Fcal_{i\to j}` inherits this lower bound directly” (eq. `\ref{eq:barrier}`).
  This fails as written. Because `U_i,U_j` and `V_i,V_j` are disjoint, the starting minimum `\Phi^{(i)*}` already lies in the second half of `\mathrm{MM}_{ij}`, and `\Phi^{(j)*}` lies in the first. So:
  1. hypothesis (ii) is vacuous: every path “passes through” `\mathrm{MM}_{ij}` at `t=0` and `t=1`;
  2. hypothesis (iii) is internally incompatible with a positive mismatch lower bound, since
     `\inf_{\Phi\in\mathrm{MM}_{ij}} \Fcal_{\mathrm{total}}(\Phi) - \Fcal_{\mathrm{total}}(\Phi^{(i)*}) \le 0`
     by taking `\Phi=\Phi^{(i)*)}`.
  The current `\mathrm{MM}_{ij}` definition therefore destroys the barrier argument instead of formalising it.
- Lines 396-397: the conclusion overclaims when it says the deformed landscape “would carry `N` apparatus-correlated basins whose directional barriers `\Delta\Fcal_{i\to j}` satisfy the pairwise lower bound~`\eqref{eq:barrier}`.” Given the defect above, the paper has not even stated a coherent sufficient condition for that bound.

**2. Internal Consistency**
- Lines 178-180 conflict with their own prose. The prose says `U_k,V_k` are chosen so that the interaction is small on `U_k\times V_k` and large on the cross-mismatched sets `U_i\times V_j`, `U_j\times V_i`. But the displayed definition of `\mathrm{MM}_{ij}` is not the cross-mismatch region; it is the union of complements of the matched neighbourhoods. Those are different objects, and only the latter appears in the formal hypothesis. This is not notation sloppiness; it changes the logic.

**3. External Consistency**
- No must-fix repository-attribution failure found in the checked dependencies relevant to this draft. The citations to Paper XIV for stationary measure and static-landscape Kramers asymptotics are supported at [papers/paper-xiv/paper-xiv.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/paper-xiv/paper-xiv.tex:174) and [papers/paper-xiv/paper-xiv.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/paper-xiv/paper-xiv.tex:239). The admissibility-set citation to Paper XXIX is supported at [papers/paper-xxix/paper-xxix.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/paper-xxix/paper-xxix.tex:164). Paper XXX does indeed state the five Route C assumptions at [papers/paper-xxx/paper-xxx.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/papers/paper-xxx/paper-xxx.tex:323).

**4. Tightness**
- Line 193 is too strong. Replace
  “measurement-type coupling is intended as a heuristic route to `N` separated basins with the barrier bound~`\eqref{eq:barrier}`”
  with
  “measurement-type coupling is intended as a heuristic route to `N` separated basins; a barrier bound requires a separately correct definition of the cross-mismatch region and an additional lower-bound hypothesis.”
- Lines 396-397 should be weakened similarly. Replace
  “whose directional barriers ... satisfy the pairwise lower bound”
  with
  “for which one would still need a correct cross-mismatch lemma to obtain any pairwise lower bound on directional barriers.”

**5. Surface Issues**
- No separate surface-only items rise to must-fix level. The barrier-set defect is the substantive issue.

**6. Top Three Fixes**
1. Lines 176-183: redefine `\mathrm{MM}_{ij}` so it excludes the matched minima and actually captures the intended cross-mismatch region. As written, (ii) is vacuous and (iii) is impossible.
2. Lines 184-187 and 396-397: withdraw the claimed lower bound `\eqref{eq:barrier}` until a correct lemma is stated and proved from coherent hypotheses.
3. After fixing `\mathrm{MM}_{ij}`, re-audit every sentence that says the round-8 revision “now” establishes the pairwise directional barrier bound. At present it does not.
