Line numbers refer to [rh-formal.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/millennium-rh-formal/rh-formal.tex>).

**1. Claim Audit**

- L400 “Cascade zeta identity”: established only because `\zetacas` is defined to be the Euler product plus classical continuation. Valid, but tautological; not a cascade derivation.
- L415 “Critical line as fixed set”: established for `\tau_{\mathrm{crit}}=s\mapsto 1-\bar s`. Valid. It uses classical conjugation, not cascade σ alone.
- L432 Dedekind reduction: established by definition of the prime-ideal Euler product and standard quadratic factorization. Valid.
- L465 Conditional critical-line theorem: established, but only because Hσ-fix is RH in different words.
- L644 prime splitting: standard and correct.
- L701 pentagonal torsion: proof plus local script support the claim.
- L725 shell-class bijection: supported by Paper XXII.
- L762 K-multiset: supported for the script-selected `\tau`; independence across order-10 conjugacy classes is still not proved, as stated.
- L798 exact closed form: established assuming L762. The Lucas factorization proves the all-degree integer closed form; degree-30 script verification is only a check.
- L836 Mellin pole symmetry: palindromy and pole real parts are established. Wording should say K=0 is a double self-location, not a genuine pair.
- L890 Hecke obstruction: too vague to count as a theorem. It rules out, at most, a unitary/norm-one local prescription under a specified normalization. It does not rule out “Hecke matching” in any classified sense.
- L1045 zeta uniqueness: correct but over-specified; U1 plus meromorphic continuation already forces equality.
- L1136 eta convergence: the geometric-series computation is correct, but `x^{s-1/2}` is not in `L^2(R_+,dx/x)`. Treat as a Mellin character/distribution, not a Hilbert-space vector.
- L1212, L1242, L1285: Euler product, functional equation, analytic continuation are classical/definitional imports. Valid, no new content.
- L1374, L1390, L1410: Dedekind zeta identification, factorization, and RH/GRH product equivalence are valid.
- L1501 Galois spectrum lemma: valid under H-Mat. It proves spectral Galois stability, not operator equivariance.
- L1704 spectral decomposition: supported by Paper XXII and the local spectrum script; valid finite claim.
- L1835 σ-paired suppression: true for the explicitly constructed polynomial flow `\Psi_t`. It does not prove anything about the original closure functional.
- L1906 corollary H_attr: over-claims unless restricted to that artificial `\Psi_t`.
- L1951 trace identity: not a usable mathematical hypothesis yet. `K`, `K^*`, `m^K_K`, `\lambda_K^\pm`, `h_K^{pair}`, and `R(h)` are not defined well enough.
- L2185 σ-Mellin convention: valid as a convention. It is not a Galois action on arbitrary positive reals.
- L2258 fixed set and L2267 zero-set invariance: correct.
- L2333 conditional RH proof: valid but tautological, because Hσ-fix is exactly RH.

**2. Internal Consistency**

- The original closure functional `F[\Phi]` at L540 is reused as a new polynomial functional `F(x)` at L1804. This is a serious notation collision.
- L1927 says the trace bridge would concern `\widehat\zeta` poles, but L1936 says “Mellin shadow zeros.” That is a substantive contradiction.
- L1955 uses `K` both as the number field notation elsewhere and as the set `{0,20,52,72}`. Use `\mathcal K`.
- L2186 says σ-Mellin is defined as a convention, but L2380 says “σ induces” the involution. That wording reintroduces the over-claim.
- All `\ref` and `\cite` keys in the file resolve.

**3. External Consistency**

- Paper XXXVI lines 539–562 support the narrowed F5 factor-2 statement; the draft’s caveat is correct.
- Paper XXII lines 204–223 support shell/class bijection; lines 177–193 support the spectrum, with expressions equivalent to the draft’s `\varphi` form.
- `cascade-foundations.md` explicitly says F4/F5/F7 are superseded by Paper XXXVI; the draft correctly treats it as non-authoritative for F5.
- `AdaptiveClosureTransport` lines 327–328 and 377–382 support the “edge-space lift/projector remains open” caveat.
- `TransportLaw` supports only programme-target motivation, not the missing selection theorem.
- `rh-arithmetic-analytic-seam.md` is not a safe formal source: it still says σ-invariance “forces” zeros to the line. The draft correctly refuses to use it as proof.
- `docs/convergence-with-smart.md` is stale relative to this draft: it names “Prime Detector L canonicity” as the RH bridge and contains stronger “isomorphic across problems” language. Update it or cite it only as an older motivational note.

**4. Tightness**

- L398: replace “unconditional results” with “unconditional definitions/imports and one conditional RH reformulation.”
- L401: replace “cascade Mellin spectral zeta” with “the function `\zetacas` defined below.”
- L1818: replace “canonical for the cascade” with “spectrum-defined for this finite testbed.”
- L1907: replace “establishes H_attr” with “establishes an H_attr analogue for the explicitly defined flow.”
- L1936: replace “Mellin shadow zeros” with “Mellin shadow pole data.”
- L2380: replace “σ induces” with “under our Mellin convention, σ is represented by.”

**5. Surface Issues**

- L926 uses literal `★`; likely fragile under pdfLaTeX. Use `$\star$`.
- L1951 labels a hypothesis as `thm:H_trace`; use `hyp:H_trace`.
- L467 `\textup{H$_{\sigma\text{-fix}}$}` is ugly LaTeX; use `\(\mathrm{H}_{\sigma\text{-fix}}\)`.
- Literal Greek letters appear throughout; despite the preamble note, pdfLaTeX may still be brittle. XeLaTeX/LuaLaTeX is safer.
- The finite-noise diagnostic script did not return during review, so I did not independently reproduce L2070–2076.

**6. Top Three Fixes**

1. Fix the trace/zero/pole bridge block: L1916–1938 and L1951–1984. Define the trace identity rigorously or remove the formula; do not say “zeros” where the construction gives `\widehat\zeta` poles.
2. Reframe the closure-flow theorem: L1786–1914. Rename the new `F`, stop calling it the cascade closure functional, and state plainly that suppression is by construction for `p_fix(L)^2`.
3. Reconcile the σ/convergence layer: L2185–2223, L2380–2385, L2484–2574. Keep σ-Mellin as a convention, and update or quarantine the stale convergence note.
