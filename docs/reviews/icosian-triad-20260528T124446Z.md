Target read: [icosian-triad.tex](/mnt/c/Users/nexus/OneDrive/Documents/My%20Projects/vfd-crystalisation-paper/release-bundles/icosian-triad-v600/papers/01-icosian-triad/icosian-triad.tex:1).

**Claim Audit**

- L53-L65, “complete geometry-first derivation” and “P-polynomial”: not established. The local output `p_polynomial_results.json` reports `A_2` has degree 8, not degree 2, and `tridiagonal_L1_results.json` has `spectra_match: false`. Add an exact association-scheme proof or weaken to “`A_1` generates the Bose-Mesner algebra numerically.”
- L68-L76, norm surjective onto all totally positive primes and formula for every odd prime: not proved. The proofs later only enumerate bounded examples. Needs Eichler/Brandt/local-density theorem with exact order specified.
- L88-L101, L-function identity: not proved. It depends on unsupported global representation counts, unsupported prime-power coefficients, and an unspecified `C_2(s)`.
- L104-L106, “Every claim is verified by exact-arithmetic simulation”: false as written. Several checks are floating point or truncated analytic comparisons; one DRG/P-polynomial diagnostic explicitly fails.
- L253-L261, `V_{600}` is closed under quaternion multiplication: asserted, not proved in text. Add exact multiplication/inversion certificate or cite a standard icosian construction.
- L274-L286, association scheme theorem: finite claim may be checkable, but the proof cites a script and mixed exact/floating tolerance. The theorem claims exact `P,Q` relations and Krein nonnegativity; the paper should print or archive exact algebraic matrices/intersection numbers.
- L297-L319, spectrum theorem: spectral table is plausible, but the “degree exactly `i`” P-polynomial claim is contradicted by the local output. Standard P-polynomial terminology would imply a tridiagonal recurrence; the paper’s own diagnostics reject that.
- L354-L365, closure spectrum: the commutation/eigenvalue part follows immediately. But `\mu_j^\sigma := (12+\varphi^2)-\lambda_j` is wrong for non-fixed eigenvalues; Galois conjugation sends `\lambda_j` to `\sigma(\lambda_j)`, i.e. the partner eigenspace.
- L390-L396, universal representation: statement says `\NQ(\pi) <= 50`, but the listed inert primes include norms `13^2,17^2,...,47^2`. The proof establishes only 20 witnesses above rational primes `p <= 50`, not universality.
- L438-L457, sigma-pairing: eigenvalue pairing follows from the spectrum. The “94 sigma-fixed dimensions” is only a sum of eigenspaces with rational eigenvalues unless an actual semilinear/linear `\sigma` action on `\mathbb C[V_{600}]` is constructed.
- L475-L479, antipodal pairing: proposition has no proof. Add the calculation via the `A_8` eigenvalues from the `P` matrix.
- L483-L501, triad isomorphism: algebraic norm equivariance is fine only after proving `\widehat\sigma(\II)=\II`. The inert/split/ramified “consequently” bullets do not follow for all primes from checking 20 witnesses.
- L518-L527, closed-form `r(\pi)`: proof checks 19 odd examples. It does not prove “for every odd totally positive prime.”
- L538-L545, multiplicativity: six examples do not prove multiplicativity on all coprime elements. State as a bounded verification unless a Brandt/Eichler proof is added.
- L562-L571, L-function definition: summing over `0 != alpha in Z[phi]` is not well-defined because of units and signs. Define it over nonzero integral ideals, or over totally positive generators modulo totally positive units.
- L576-L585, L-function theorem: not established. Prime values plus coprime multiplicativity do not determine prime powers, so the Euler factors of `\zeta_K(s)\zeta_K(s-1)` are not derived.
- L609-L617, Euler bridge corollary: formal only if the prior theorem is fixed. Also `\zeta(s)` is a global factor, not a local “Euler factor.”
- L629-L640, zero-set claim: incomplete. `C_2(s)` may introduce zeros/poles; trivial zeros and poles of the zeta factors are ignored.

**Internal Consistency**

- The paper calls `\II` a maximal icosian order with `V_{600}` as unit group (L166-L168), but later treats `\II` as `(Z[\varphi])^4` with Lipschitz coordinates (L381-L385). These are not the same order; `V_{600}` contains half-coordinates.
- The P-polynomial claim (L318-L319) conflicts with the non-DRG discussion (L329-L337) and with the failed tridiagonal diagnostic.
- `C_2(s)` is omitted in the abstract equality (L94-L98) but included in Theorem L576-L585 and Corollary L609-L614.
- The paper says coordinate-wise `\sigma` does not preserve the displayed `V_{600}` (L424-L433), but later uses spectral counting as if it identified the geometric `\tau_{\mathrm{ico}}` fixed space (L438-L457, L645-L649). The basis bridge is missing.
- In-file `\ref` targets appear defined. The semantic issue is not broken labels, but references claiming sections “prove” results that are only sampled numerically.

**External Consistency**

- L213-L214 cites `rhFormalSmart,trilogyArchive` for the earlier “cascade Euler bridge.” Locally, `papers/millennium-rh-formal/rh-formal.tex` explicitly says it does not identify `\widehat\zeta` with a global Hecke L-function and leaves that open. The trilogy audit says Paper III’s bridge is conditional and unsupported as VFD content.
- The local trilogy source does contain a conditional Euler bridge theorem: `papers/euler-bridge-closure-irreducibles/...tex` L407-L418. It is conditional on H-NaturalIrreducibles and H-EulerBridge.
- `papers/primes-as-closure-irreducibles/...tex` L528-L536 explicitly marks H-EulerBridge open. The target paper should cite this as motivation, not support.
- L645-L650 cites `Existence as Closure` for `tau_ico`/`tau_spec`. Verified: the source distinguishes `94` and `116`, but also flags the icosian-to-spectral basis-change as a residual gap. The present paper does not close that gap.

**Tightness**

- L53: replace “complete geometry-first derivation” with “finite geometry-first verification, supplemented by classical arithmetic inputs.”
- L104: replace “Every claim is verified” with “Finite spectral and witness claims are reproducible; global arithmetic claims require the cited theorems.”
- L318: replace “P-polynomial” with “`A_1` generates the Bose-Mesner algebra,” unless the tridiagonal recurrence is proved.
- L405-L407: replace “We re-derive it here” with “We give bounded explicit witnesses; full universality is imported from ...”
- L518: replace “For every odd” with “The expected formula is verified for the tested odd primes; a proof requires ...”
- L576: add the explicit `C_2(s)` before claiming an L-function identity.
- L615: replace “Euler factor” with “Dedekind/Euler-product factor.”
- L631: replace “zero set coincides” with a statement accounting for `C_2`, poles, and trivial zeros.

**Surface Issues**

- L168: “an Euclidean” should be “a Euclidean.”
- L253-L258: use `\cup`, not leading `+`, for the vertex-set union.
- L359: `Z[\varphi][\varphi^{-2}]` is just `Z[\varphi]`; `\varphi^{-2}=2-\varphi`.
- L391: “rational norm `<=50`” should be “rational primes `p <= 50`” if that is intended.
- L443: write `$A_1$-eigenvectors`, not `A1-eigenvectors`.
- L570-L571: replace element-sum notation by ideal-sum notation.
- L577: Dedekind zeta Euler product should run over prime ideals, not prime elements `\pi`.
- L582: “explicit local-2 correction” is not explicit anywhere.
- L685-L687: “All thirteen sims pass” conflicts with the failed tridiagonal diagnostic.

**Top Three Fixes**

1. Fix the association-scheme terminology at L318-L337. Either prove the standard P-polynomial/tridiagonal recurrence exactly, or stop using “P-polynomial.” This is the easiest hostile-referee attack.

2. Replace the global arithmetic claims at L518-L585 with a real theorem: specify the exact icosian order, cite/prove the Eichler/Brandt representation formula including prime powers and the 2-adic factor, and write `C_2(s)` explicitly.

3. Construct the actual Galois/`\tau_{\mathrm{ico}}` action on the icosian order and its relation to the displayed `V_{600}` (L424-L457, L645-L649). Spectral conjugacy alone does not prove a 94-dimensional fixed subspace.
