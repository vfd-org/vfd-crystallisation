**1. Claim Audit**
- Setup imports, `V_600=2I`, `V_24`, `T_tau` cycles, K-multiset, `H=Dic_5`: supported as imports from Paper 1 at `tau-sigma-construction.tex:110-128`; locally backed by `bekenstein-incidence.tex:119-146`, `:162-187`, `:193-216`, `:284-306`. OK.
- “Right-coset carrier”: `tau-sigma-construction.tex:131-144`. The conclusion is locally backed by Paper 1 at `bekenstein-incidence.tex:218-255` and by `verify.py:91-126`. Defect: `tau-sigma-construction.tex:134-136` says “Equivalently” as if `T_tau(Hg)=Hg` proves the `K=52+K=20` pairing. It only proves whole-cycle preservation; the K-pairing is finite enumeration/import.
- Trace form: `tau-sigma-construction.tex:148-161`. Definition matches `verify.py:56-65`. OK, except the “puts `V_600` inside `E_8`” sentence is literature/background, not proved here.
- Reflection obstruction remark: `tau-sigma-construction.tex:169-180`. Not adequately supported by the cited local scripts. `block3c_e8_reflection_obstruction.py:1-26` gives an argument/diagnostic, not an enumeration of “the standard generating set of `E_8` Weyl reflections fixing H pointwise.” Keep it as motivation, but rewrite the attribution.
- Phase-`j` swap: `tau-sigma-construction.tex:201-210`. Correct once cycle basepoints are fixed. Hidden convention: `w_0` is never explicitly chosen; the theorem’s phase labels depend on both cycle basepoints.
- Symmetric maximin score: `tau-sigma-construction.tex:218-239`. Matches code at `verify.py:147-162`. OK.
- Phase selection theorem: `tau-sigma-construction.tex:241-260`. Verified exactly by `verify.py`; I ran it and it passes with all five cosets having `[0,-5/4,...,0,...]`. OK as a computational theorem.
- Canonical lift: `tau-sigma-construction.tex:265-279`. The all-zero lift is rebuilt and matched at `verify.py:179-190`. OK, but “canonical” is convention-dependent, as the paper later admits.
- Worked example: `tau-sigma-construction.tex:283-294`. Matches verifier output: coset 1 uses cycles 2 and 3. OK.
- Main theorem P1-P4: `tau-sigma-construction.tex:298-337`. P1-P4 follow from the construction; antipodal compatibility is true and verified. The prose proof of antipodal compatibility at `:331-335` is muddier than needed; all phase-`j` swaps commute with antipode because both cycle indices shift by `+5`.
- `Z_2^5` ambiguity: `tau-sigma-construction.tex:341-360`. Count is verified at `verify.py:211-232`. OK for count and P1-P4. “Group of phase choices” is informal but harmless.

**2. WO Acceptance Audit**
- B1 right-coset carrier: resolved, with the “Equivalently” caveat above.
- B2 trace normalization: resolved in paper and `verify.py`.
- B3 literal per-vertex projection theorem: not resolved; deliberately avoided by switching to Route K. That is acceptable only if Route Q is no longer required.
- B4 Route K phase filter: resolved.
- B5 verification rebuild: resolved; `verify.py` rebuilds and compares to canonical data.
- Paper criterion 1: resolved. Pure math/no physics is stated at `tau-sigma-construction.tex:39`, `:95-96`.
- Paper criterion 2: resolved. Paper 1 is cited/imported for setup and coset distinction.
- Paper criterion 3: partially resolved. §3 has a softened computational remark, but not a real W(H4)/E8 failure proof and not the exact locally supported attribution.
- Paper criterion 4: partially resolved. Construction and worked example exist; pseudocode is absent.
- Paper criterion 5: resolved for P1-P4 and antipodal compatibility; right-coset preservation is checked post-build.
- Paper criterion 6: resolved.
- `verify.py` criteria 1-5: resolved. The script passed.

**3. Catalogue Audit**
- D1-D4, L1, R1-R2, T1-T2, N1-N3 all correspond to objects in the derivation.
- Defect: T2b says right-coset preservation “follows from (P3) plus Lemma 2.1” at `CATALOGUE.md:85-88`. False. P1-P4 plus K-swap do not force preservation of each right coset; one can pair K52 cycles with different K20 cycles and still satisfy P1-P4. It follows from Definition 4.3/the build, and is separately checked.
- Same false implication appears in the appendix at `tau-sigma-construction.tex:397-399`.

**4. Attribution / External Consistency**
- Paper 1 setup/coset claims are locally consistent: `bekenstein-incidence.tex:119-146`, `:182-187`, `:193-227`, `:257-268`, `:284-306`.
- Shared code left/right cosets agrees: `group.py:107-167`; non-normal check at `group.py:170-172`.
- Static τ infrastructure claim is consistent: `tau_sigma.py:1-8`, P1-P4 verifier at `tau_sigma.py:40-76`, tests at `test_tau_sigma.py:7-17`.
- Reflection attribution is not consistent. The local scripts do not say what the derivation claims they say at `tau-sigma-construction.tex:170-176`.

**5. Sim Correctness**
- `verify.py` implements the symmetric maximin definition exactly at `verify.py:147-162`, asserts the full table at `:163-177`, rebuilds at `:179-190`, and verifies P1-P4/antipodal/right-cosets/count at `:192-232`.
- Bug in documentation, not computation: the docstring score at `verify.py:13-16` shows only the forward score, while the code uses forward/backward symmetric min.
- No statistical tests are present; none are needed. This is exact enumeration, not χ²/KL inference.

**6. Tightness**
- `tau-sigma-construction.tex:134-136`: replace “Equivalently” with “In particular”.
- `tau-sigma-construction.tex:173-176`: replace with “the auxiliary scripts record tested reflection/parity-flip failure modes; no exhaustive Weyl-group enumeration is claimed.”
- `tau-sigma-construction.tex:397-399` and `CATALOGUE.md:85-88`: replace “follows from (P3) plus Lemma” with “follows from the per-right-coset construction and is checked separately.”
- `verify.py:60-62`: “integer-valued” is too strong for the raw returned scores, which include `-5/4`.

**7. Top Three Fixes**
1. Fix the false right-coset-preservation implication: `tau-sigma-construction.tex:397-399`, `CATALOGUE.md:85-88`.
2. Fix the reflection-obstruction attribution: `tau-sigma-construction.tex:169-180`.
3. Clarify phase conventions: explicitly choose both `v_0` and `w_0` in `tau-sigma-construction.tex:192-199`, and say `{0,5}` is label-dependent.

**8. Verdict**
Publication ready: no.

The construction/verifier are basically sound, but the current artefacts still contain two substantive overclaims: the unsupported reflection-script attribution and the false statement that right-coset preservation follows from P1-P3 plus Lemma 2.1.
