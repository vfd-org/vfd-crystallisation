Publication ready: no.

Reviewed [paper-xxii.tex](</mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxii/paper-xxii.tex:1>). Line references below are to that file unless otherwise stated.

**1. Claim Audit**
- `Definition 1` (`80-93`): parts `(i)` and `(ii)` are established. Part `(iii)` is fine as stated. Part `(iv)` is not established in the paper, but it is explicitly marked script-reported; that is acceptable.
- `Unit matching` (`99-103`): acceptable. This is correctly framed as a consistency identification, not a derivation.
- `"d_2^2-d_1^2=1/\varphi"` (`137-146`): proved.
- `"the nontrivial integer eigenvalues are {9,12,14,15}"` and the irrep table (`44`, `164-188`): not proved in the paper. What is established is weaker: numerical diagonalisation agrees with imported irrep labels. That is fine if kept as script-reported/imported. The current phrasing still drifts toward theorem-tone in the abstract.
- `Proposition 2 / isotypic decomposition` (`193-207`): not established as written. The proof assumes, but does not prove, the crucial step that every shell-adjacency matrix is preserved by the left-regular `2I` action. “Identify vertices with group elements” is not enough. You need either a proof that the shell relations are left-translation invariant under the chosen identification, or you must downgrade the proposition to a conditional statement throughout.
- The abstract claim of an `exact 2I-isotypic decomposition ... invariant under every shell-adjacency matrix` (`46`) therefore over-claims relative to the paper’s actual proof.
- `"any Hamiltonian built out of shell-adjacency matrices ... leaves W_int and W_irr invariant"` (`209-210`) inherits the same gap. This is only as good as Proposition 2.
- `"the 240-to-120 map is two-to-one"` with Galois-conjugate preimages (`217-221`): not reproved here, but properly attributed; the local source does support it.
- `Table 2` NNN data (`223-237`): correctly demoted to script-reported and correctly attributed. This round-14 fix is in place.
- `Proposition 3 / arithmetic identity 137` (`255-264`): proved.
- `"\alpha^{-1}=137+\pi/87" at 0.81 ppm` (`280-284`): the arithmetic and ppm comparison are fine; the paper now correctly refuses to call this a derivation.
- `Weinberg-angle paragraph` (`290-292`): acceptable as a structural/numerical match. It does not over-claim derivation.
- `Generation decomposition 16=8⊕4⊕4 and three-level spectrum` (`302-309`): not proved, but correctly marked script-reported/exploratory.
- `Mass-formula reuse from Papers I/IV` (`316-324`): acceptable as inherited ansatz, not derivation.
- `"restriction ... to W_int is an invariant reduction, not a truncation approximation"` (`330-331`): again depends on Proposition 2, so currently not established.

**2. Internal Consistency**
- The status taxonomy declared at `66-72` is not fully propagated. Later you use extra labels: `verified computationally` (`168`) and `script-checked` (`209`, `217`). Those are not your declared categories. Pick one taxonomy and enforce it.
- The representation-theoretic section is internally soft at `164` (“empirical / imported identification”) but hard again at `193-210` and `330-331`. Those passages are not on the same epistemic footing.
- Cross-references look textually consistent on manual inspection: labels cited at `sec:discussion`, `sec:constants`, `tab:spectrum`, `prop:gap`, `thm:decoupling` etc. all exist. I did not compile, so this is a source-level check, not a PDF-level check.

**3. External Consistency**
- `Paper V reports 13 numerical mass correspondences ... 0.014% ... R_D(4)=15 ... ten reverse-identified chain ratios` (`57`): verified in `papers/paper-v/paper-v.tex:59-67,177-178,426,489-500`.
- `Papers XVIII-XXI derive Schrödinger-type evolution at equilibrium / leading order` (`59, 99-103`): verified, but only in the qualified sense you now state. See `paper-xviii.tex:45-47,71` and `paper-xxi.tex:251-258,475-476`.
- `Paper V uses the four nontrivial integer eigenvalues {9,12,14,15}` (`187-188`): verified in `paper-v.tex:235-240,251`.
- `CascadeAlgSubstrate gives the local derivation of the E8→H4 two-to-one projection` (`217, 221, 387`): verified in `cascade-algebraic-substrate.tex:556-621,659-666`.
- `Paper II conditional winding operator with 0.5% and 3.7% matches` (`310`): verified in `lepton-generations.tex:42-48,200-204,298-299,347-351`.
- `Paper III gives ΔC=15 correspondence` (`322`): verified in `spectral-dimension.tex:87-95,120,250`.
- `Papers IX-X treat gravity as structural/programmatic, not derived` (`332, 367`): verified in `paper-ix.tex:281-316` and `paper-x.tex:39-43,63,223,270-274`.
- `Papers XIX-XX: residual is phase-invariant, norm-preserving, vanishes at equilibrium` (`358`): verified in `paper-xix.tex:43,83-87,172-176,223-224`; `paper-xx.tex` is consistent with that placement.
- Bibliographic traceability is not clean. In `references.bib`, `SmartVI` points to `papers/electroweak-structure/` (`15-20`), which does not match the actual source path `papers/paper-vi/paper-vi.tex`. `SmartXI` points to `papers/mechanism/` (`22-27`), not `papers/paper-xi/paper-xi.tex`. `SmartXIV` points to `papers/` (`29-34`), which is useless. `SmartXVII` gives only a directory (`36-40`). This round-14 item is not complete.

**4. Tightness**
- `46`: “an exact 2I-isotypic decomposition ... invariant under every shell-adjacency matrix” is too strong given `193-207`. Edit to: `under the standard 2I identification assumed below, this yields a candidate exact isotypic decomposition...`
- `168`: “verified computationally” is too vague and outside your taxonomy. Edit to: `numerical spectrum script-reported from ... ; irrep labels imported from the standard 2I character table`.
- `209`: “Script-checked for all eight shells...” is weaker than the surrounding theorem and muddles proof with verification. Either remove it or rephrase the whole paragraph as conditional.
- `330-331`: “is an invariant reduction, not a truncation approximation” is too strong until Proposition 2 is fixed. Edit to: `is an invariant reduction if the shell-adjacency algebra is indeed 2I-equivariant under the chosen identification`.

**5. Surface Issues**
- Status-language drift: `verified computationally` (`168`) and `script-checked` (`209`, `217`) violate the announced four-level taxonomy.
- Bibliography local-source notes are still defective: wrong or vague paths for `SmartVI`, `SmartXI`, `SmartXIV`, `SmartXVII` in `references.bib:15-40`.
- `references.bib:19` appears to cite a nonexistent path `papers/electroweak-structure/`.
- Minor spacing typo: double space before “carries” at `338`.
- No obvious undefined macros in the manuscript source. No obvious broken `\ref` targets at source level.

**6. Top Three Fixes**
1. Fix Proposition 2 and every downstream sentence that depends on it (`46`, `193-210`, `330-331`). As written, the proof does not establish shell-adjacency equivariance under left-regular `2I`.
2. Repair `references.bib` traceability (`15-40`). The “Local source” notes are still wrong/vague for four key internal citations, so attribution is not clean.
3. Enforce the declared status taxonomy across the manuscript (`66-72`, `168`, `209`, `217`). Right now the paper mixes “computed/proved”, “script-reported”, “verified computationally”, and “script-checked” without discipline.

Round-14 check:
- `(i)` line-221 rephrase: in place.
- `(ii)` Table-2 NNN attribution: in place.
- `(iii)` line-209 no longer says “verified computationally”: in place, but replaced by a non-taxonomy label.
- `(iv)` `references.bib` propagation: incomplete and in places incorrect.
