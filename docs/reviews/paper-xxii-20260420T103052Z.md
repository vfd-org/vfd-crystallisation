Publication ready: no.

All Round 9 wording fixes you named are in place in [paper-xxii.tex](/mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxii/paper-xxii.tex): the Section 13.2 heading is now “What is proved, computed, or script-reported” (line 380), the crystallisation-operator passage is softened and no longer identifies it with `\Fcal` (line 347), the abstract now says “numerical spectrum computed, irrep labels imported” rather than “character-theoretic identification” (line 44), Table 1 uses “Count” and the revised caption (lines 116, 120), and the Coxeter-order sentence now says “standard result; script-checked” (line 216). The remaining must-fixes are elsewhere.

**1. Claim audit**
- “An explicit `H_4`-invariant smooth log-sum-exp functional on the 600-cell is constructed at the level of a definition” (line 44). Established. Definition 1 gives the object, and items (i) and (ii) prove invariance and smoothness (lines 79-92).
- “The nontrivial integer eigenvalues are `{9,12,14,15}`” (line 44). Only partly established in-manuscript. The table reports them (lines 173-181), but the paper itself says the spectrum is numerical and the irrep labels are imported (line 163). That is fine as a computed result, not as a theorem-grade derivation.
- “An exact `2I`-isotypic decomposition in which a 94-dimensional integer-eigenvalue subspace and a 26-dimensional irrational-eigenvalue subspace are invariant under every shell-adjacency matrix” (line 46; Proposition at lines 192-202). Mostly established, but with a hidden setup assumption: the proof uses “the action of `2I` on its vertex set (given by the Cayley-graph embedding)” (line 205), yet that embedding/identification is not proved in this paper. As written, the proposition is conditional on that standard identification.
- “Permeability gap” / `d_2^2-d_1^2=1/\varphi` (lines 136-145). Established.
- “Arithmetic identity … = 137” (lines 254-263). Established.
- “Paper V’s … eigenvalues `{9,12,14,15}` match the nontrivial integer Laplacian eigenvalues” (lines 186-187, 383). Established as an observed coincidence, not more.
- “The Coxeter projection … sends these 240 vectors two-to-one onto 120 vectors identifiable with the 600-cell vertex set” (line 216). Not proved here, but adequately signposted as imported from cited sources; local verification exists in the repository source `CascadeAlgSubstrate`.
- “The accompanying script … gives the numerical ratio `9:0:4:9`” (line 295; repeated line 389). Script-reported only. That status is honest.
- “A computation … gives the `\mathbb Z_3` decomposition `16=8⊕4⊕4`” (lines 301-305; repeated line 388). Script-reported only. Honest.
- “For the … restricted Hamiltonian … the spectrum has three distinct eigenvalues …” (line 307). Script-reported exploratory output only. Honest.
- “The nonlinear closure residual … is phase-invariant, norm-preserving, and vanishes at equilibrium” (line 357). Externally supported by Paper XIX. The present paper correctly treats the physical reading as conditional.
- Over-claim still present by omission: line 57 cites Paper V’s “13 numerical mass correspondences … under explicit structural assignments (including `R_D(4)=15`)” without mentioning that Paper V itself says this assignment is still unconsolidated and the current verification script uses `R_D(4)=10`. That caveat matters.

**2. Internal consistency**
- The status taxonomy is not internally consistent. At lines 66-71 the paper declares only three classes: `Computed/proved`, `Structural`, `Open`. But lines 380-390 then elevate a fourth class, `script-reported`. That is a real taxonomy drift, and it matters because several claims are only script-defined, not manuscript-defined.
- The heading fix at line 380 is good, but the body text still conflicts with the taxonomy announced earlier. This is the main remaining Round 9 failure.
- Line 68 says computed/proved results are “Verified by accompanying scripts.” That is too coarse given line 378, which admits some scripts are exploratory or incomplete, and given lines 388-389, which treat some outputs as merely script-reported.
- Line 88 says the functional is “translation-equivariant only under `H_4` orbits.” That is mathematically sloppy: `H_4` acts linearly, not by translations. This is wording drift, not a theorem error, but it should be cleaned.
- Cross-references appear internally consistent. Every `\ref`/`\eqref` target used in the manuscript is defined, and the surrounding prose matches the referenced object.

**3. External consistency**
- Line 57, Paper V: verified locally. Paper V does report 13 non-reference correspondences at `0.014%` average relative error, with a non-unique `(S,w)` scheme, ten reverse-identified chain ratios, and `R_D(4)=15` as a structural assignment. But Paper V also says that `R_D(4)=15` is pending consolidation and its verification script currently uses `10`. This manuscript should carry that caveat when summarising Paper V.
- Line 98 / line 59, Papers XVIII-XXI: verified locally. Papers XIX and XXI support the claim that Schrödinger evolution appears at equilibrium / to leading order near equilibrium with Witten Hamiltonian `H_W`, and that physical identification of `\sigma^2` with `\hbar/m` is not derived.
- Line 187 / line 321, Paper III: verified locally via `spectral-dimension.tex`. It does state the exact computed correspondence `\lambda=15=\Delta C`, and it retracts the earlier `d_eff≈\varphi` claim. This manuscript is consistent with that.
- Line 309, Paper II: verified locally. The winding operator `f(w)=\varphi^5(w-1)^{1/\varphi}` and the `0.5%` / `3.7%` muon/tau figures are stated there as conditional on the conjectural spectral identification.
- Line 331 / line 366, Papers IX-X: verified locally. Those papers frame gravity as structural analogy / programme target, not a derivation.
- Line 357, Papers XIX-XX: verified locally. Phase invariance, exact equilibrium vanishing, and norm conservation are in XIX; XXI reiterates the limited scope. The present manuscript’s caveat is appropriate.
- Line 216 / line 220 / line 386, `CascadeAlgSubstrate`: verified locally. That source does contain a theorem-grade `E_8→H_4` projection statement with the 240-to-120 pairing via Galois-conjugate icosians.
- Line 244, “Papers VI-XI provide the broader structural correspondences to Standard Model structure.” Broadly plausible from the local corpus, but not audited claim-by-claim here. If the author wants this sentence to bear weight, cite the specific papers for the specific correspondences invoked.

**4. Tightness**
- Line 57 is too strong by omission.
  Suggested edit: “Paper V reports 13 numerical mass correspondences … under explicit structural assignments, including the presently unconsolidated assignment `R_D(4)=15`.”
- Line 68 is too strong.
  Suggested edit: “Computed/proved: exact results established in the manuscript or by finite computation; some are script-verified.”
- Line 208 is slightly too strong.
  Suggested edit: “Verified computationally for the shell matrices and polynomial powers tested in the accompanying scripts.”
- Line 287 is stronger than the paper needs.
  Suggested edit: “one-loop Standard Model running can place `\alpha^{-1}` in the vicinity of 137 at low scale for GUT-scale inputs in the standard ballpark.”
- Line 424 is weaker than the paper now supports in one respect: the caveat about no dynamical identification is already good, but it should also say the picture depends on the imported `E_8→H_4` projection.
  Suggested edit: “... offered as a candidate interpretation contingent on the imported `E_8→H_4` projection structure ...”

**5. Surface issues**
- Line 88: “translation-equivariant only under `H_4` orbits” is bad wording.
- Line 291: “NN-angle / NNN-angle” appears without prior definition of that abbreviation pair in the paper.
- Line 224: table caption “The conjugate pair within `E_8`” is vague; the table is about an interpretive split, not an established pair of `E_8` sectors.
- Unused macros: `\Sadm` is defined (line 21) and never used.
- No undefined `\ref`/`\eqref` targets found in the manuscript text.
- I did not compile the LaTeX, so this is a source audit, not a build audit.

**6. Top three fixes**
1. Fix the status taxonomy inconsistency. The paper currently advertises a three-level scheme at lines 66-71, then uses a fourth category, “script-reported,” at line 380 onward. This is the main organisational failure because it obscures which claims are theorem-grade, finite computations, or merely script outputs.
2. Add the omitted Paper V caveat when citing its mass results at line 57. Paper V itself says `R_D(4)=15` is structurally assigned and not yet fully consolidated; failing to mention that here is attributionally misleading.
3. Make Proposition 2’s hypothesis explicit at lines 192-205. As written, the proof leans on the “standard identification” / Cayley-graph embedding of the 600-cell vertices with `2I` without stating it as a hypothesis. Either prove that setup or state the proposition conditionally on it.

On the narrow substantive-math/attribution criterion: not publication-ready until those three points are fixed.
