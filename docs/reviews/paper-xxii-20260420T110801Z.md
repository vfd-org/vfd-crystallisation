Publication ready: no.

Round 17 check: (a) fixed at line 59; (b) fixed at line 209; (c) fixed at line 358; (e) fixed at line 288. Item (d) is only partly clean: line 46 still repeats the same conditional qualifier twice.

All line numbers below refer to `papers/paper-xxii/paper-xxii.tex`.

**1. Claim audit**

- Lines 80-92, Definition `\ref{def:F600}`: the stated properties (i) invariance, (ii) smoothness, (iii) vertex-constant value are established at the level claimed. Property (iv) is explicitly script-reported, so the status is honest.
- Lines 137-146, Proposition `\ref{prop:gap}`: established. The algebra is elementary and sufficient.
- Lines 164-165 and 193-206, Proposition `\ref{thm:decoupling}`: not established as stated. You prove only that each shell adjacency matrix commutes with the left-regular `2I` action, hence preserves isotypic components. You do not prove that `\mathbf A_1` acts by a scalar on each isotypic block. Without that, the phrase “the corresponding eigenvalue of `\mathbf A_1`” at lines 194-197 is not justified. This is the main mathematical defect in the paper.
- Lines 187-188, Remark `\ref{rem:integer}`: acceptable as an observational statement. It does not overclaim a selection principle.
- Lines 217-221: the `E_8 -> H_4` two-to-one pairing claim is acceptable if imported from the cited source; this paper itself does not prove it, but it says so.
- Lines 255-264, Proposition `\ref{prop:137}`: established.
- Lines 280-288: the `137 + \pi/87` statement is presented as a numerical correspondence, not a derivation. That status is consistent with the text.
- Lines 292-293: the `9/15 = 3/5` to `\sin^2\theta_W = 3/8` discussion is correctly framed as a structural/numerical match, not a derivation.
- Lines 302-310: the `16 = 8 \oplus 4 \oplus 4` decomposition and the three energy levels are script-reported only. Status is correctly limited.
- Lines 316-324: the mass formula is explicitly an ansatz inherited from earlier papers. No overclaim here.
- Lines 330-332: this paragraph inherits the defect of Proposition `\ref{thm:decoupling}`. Until the scalar-action issue is fixed, “not a truncation approximation” is too strong.
- Lines 358-360: now properly limited. The falsifiability paragraph no longer overclaims physical testability.

**2. Internal consistency**

- The paper is internally inconsistent on the role of centrality/scalar action. Line 164 explicitly says the character-theoretic scalar formula needs an extra assumption not proved here. Lines 193-202 then proceed as if each isotypic block already comes with a single eigenvalue of `\mathbf A_1`. That is not the same statement.
- The conditional status is mostly consistent elsewhere: lines 46, 164, 209, 330 all try to mark the `2I` story as imported/conditional. But line 193 is stronger than the rest.
- Cross-references are mechanically fine. I found no missing `\ref`/`\eqref`.
- Line 46 still has duplicated conditioning: “under the standard `2I` identification...” and then again “under the imported identification...”.

**3. External consistency**

- Line 57, Paper V mass correspondences: verified. `papers/paper-v/paper-v.tex` states 13 non-reference mass correspondences at `0.014%` average relative error under the structural assignment `R_D(4)=15` (lines 59, 349, 489 there).
- Line 59 and line 99, Papers XVIII/XXI Schrödinger recovery: verified. `paper-xviii.tex` gives exact equilibrium recovery and formal leading-order near-equilibrium recovery (lines 319-356, 568-576). `paper-xxi.tex` preserves the same restricted status (lines 50, 244-258).
- Lines 188 and 384, Paper V use of `{9,12,14,15}`: verified. `paper-v.tex` uses those four integer eigenvalues in the `\alpha` section and in its programme summary (lines 235-240, 489).
- Lines 188 and 322, Paper III `\lambda=15=\Delta C`: verified. `papers/spectral-dimension/spectral-dimension.tex` explicitly says this survives the spectral-dimension retraction (lines 119-123, 199-210).
- Line 310, Paper II conditional winding operator and lepton-ratio status: partially verified. `papers/lepton-generations/lepton-generations.tex` does present the winding programme as conditional on the spectral identification and treats the F4 support as exploratory (lines 235-257, 309-311). I did not do a full direct source check of the exact `0.5%` and `3.7%` claims in context, though `rg` locates them in that file.
- Lines 217, 221, 387, `CascadeAlgSubstrate`: verified locally. `papers/cascade-algebraic-substrate/cascade-algebraic-substrate.tex` gives the `240` unit icosians pairing into `120` conjugate pairs whose projection yields the `120`-element `H_4` set (lines 614-620).
- Line 245, Papers VI/XI broader Standard Model correspondences: broadly consistent. `paper-vi.tex` and `paper-xi.tex` both describe structural correspondences and disclaim full derivation (VI lines 38-42, XI lines 39-43).
- Line 332, Papers IX/X gravity as separate programme: verified. `paper-ix.tex` and `paper-x.tex` both explicitly disclaim GR derivation and frame the result as structural/geometric only (IX lines 41-45, 121-125; X lines 39-43, 63).
- Line 322, Papers I/IV as “spectral-moment / cumulant ansatz”: not directly verified from the cited source papers. I did not locate dedicated `paper-i` / `paper-iv` source files under those names in the repo. I found indirect support in `paper-v.tex` and `master-mass/master-mass.tex`, but that is not the same as direct source verification.

**4. Tightness**

- Line 194: too strong. Suggested edit: “Consequently each `\mathbf A_d` preserves the isotypic summands; if `\mathbf A_1` acts scalarly on each isotypic block, these summands may be grouped into integer- and irrational-eigenvalue sectors.”
- Line 221: “double cover” carries unnecessary topological freight. Suggested edit: “The `240 -> 120` map is a two-to-one pairing at the root-set level.”
- Line 330: too strong until Proposition `\ref{thm:decoupling}` is repaired. Suggested edit: “...is a conditional invariant reduction under the same imported `2I`/scalar-action hypotheses, rather than a numerical truncation.”
- Line 358: wording is slightly sloppy. Suggested edit: “The nonlinear closure residual ... is invariant under global phase rotation `\Psi \to e^{i\theta}\Psi`...”

**5. Surface issues**

- Line 46: duplicated parenthetical qualification.
- Lines 164, 193, 209: the paper alternates among “standard identification,” “imported identification,” and “empirical / imported identification” without pinning down exactly what is assumed where.
- Line 358: “phase-invariant (covariant under ...)” mixes two different notions.
- No broken internal cross-references found.
- No obvious undefined macro jumped out from the source.

**6. Top three fixes**

1. Repair Proposition `\ref{thm:decoupling}` at lines 193-206, and all downstream uses at lines 209 and 330. Either prove the shell adjacency operators are central/class sums under the chosen `2I` identification, or weaken the statement to preservation of manually defined sums of isotypic components.
2. Align the paper’s conditional-status language around the `2I` decomposition, especially lines 164-165 versus 193-202. Right now the manuscript admits the missing hypothesis and then immediately uses it.
3. Clean the attribution chain at line 322. Either verify the Papers I/IV claim directly from the cited source files and cite them cleanly, or rewrite the sentence as an indirect programme summary via Paper V.

On the narrow substantive-math/attribution criterion: not ready, because of item 1 and the weaker attribution issue in item 3.
