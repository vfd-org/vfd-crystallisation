**1. Claim Audit**

- “Right-coset carrier; Paper 1 Lemma 3.4” (`tau-sigma-construction.tex:130`): mathematically supported, but the ordinal citation is suspect. In local Paper 1, the carrier statement is `lem:cosetkpair` at `papers/bekenstein-incidence/bekenstein-incidence.tex:218-227`; the following lemma at `257-260` is self-normalizing/non-normality. Cite the label/title, not “Lemma 3.4”.

- “non-bulk left cosets … are not whole-cycle carriers” (`tau-sigma-construction.tex:140-143`): established. Paper 1 states it at `218-227`, and shared tests verify it at `test_dic5_subgroup.py:87-102`.

- “natural integer-valued bilinear form” (`tau-sigma-construction.tex:147-159`): over-strong. The actual σ-projection scores are fractional under the paper’s own trace normalization; losing maximin scores are `-5/4`. Say “rational on these cross-Galois pairs” unless a rescaled lattice normalization is introduced.

- “Reflection obstruction” (`tau-sigma-construction.tex:163-180`): not established by the written proof. The step “contains all of H” ⇒ “4-dimensional span” (`173-175`) is asserted, not proved; “closed under inversion” does not imply a 4D subalgebra span. The cited Blocks 3a–3c do not supply the exact proposition as stated.

- “Phase-j swap … well-defined involution … Tτ-equivariant” (`tau-sigma-construction.tex:199-208`): established, assuming the two cycle indexings are fixed in the stated Tτ orientation.

- “Φ(Hg) = {0,5}, with s(0)=s(5)=0 and s(j)=-5/2” (`tau-sigma-construction.tex:229-233`): false as written. Running the supplied exact code gives losing scores `-5/4` for every nonwinning phase, not `-5/2`. The verifier only asserts the winning set, not the claimed losing value.

- “phase labels {0,5}” (`tau-sigma-construction.tex:229-233`): convention-dependent. Rotating the base point of either cycle relabels phases. The intrinsic claim is “two winning phases separated by 5”; `{0,5}` only holds under the deterministic cycle/basepoint ordering later discussed at `330-337`.

- “canonical τσ is phase-0” (`tau-sigma-construction.tex:251-260`): established as a convention-backed representative, not an intrinsic unique object.

- Worked example cycles 2 and 3 (`tau-sigma-construction.tex:265-276`): correct. Local `build_state()` returns exactly those two cycles with K-classes `52` and `20`.

- Main theorem P1–P4 (`tau-sigma-construction.tex:280-290`): established by construction and verifier. The proof is adequate for P1–P4.

- “Additionally, τσ commutes with antipodal map” (`tau-sigma-construction.tex:291-318`): true by verifier, but the prose proof is loose. A direct index proof would be cleaner: `τ_j(-v_k)=w_{j+k+5}=-w_{j+k}`.

- “Z₂⁵ = 32 admissible involutions” (`tau-sigma-construction.tex:323-342`): established for P1–P4 by enumeration, subject to the phase-label convention above.

**2. WO Acceptance Audit**

Paper criteria:

- E1 pure math only (`WO.md:61-62`): resolved.
- E2 cite Paper 1 setup/right-left distinction (`WO.md:63`): mostly resolved, but the right-coset lemma ordinal is wrong/suspect.
- E3 W(H4)/E8/reflection failure (`WO.md:64`): partial. Present proof/citation is not strong enough.
- E4 exact construction, pseudocode, worked example (`WO.md:65`): partial. Construction and example exist; pseudocode is effectively delegated to `verify.py`, and phase conventions are underspecified before the theorem.
- E5 P1–P4 plus antipodal (`WO.md:66`): resolved by verifier; proof text should be tightened.
- E6 Z₂⁵ phase ambiguity (`WO.md:67`): partial because `{0,5}` is convention-dependent and the theorem’s losing score is wrong.

Verifier criteria:

- V1 builds from `vfd_v600` (`WO.md:70`): resolved.
- V2 rebuilds τ from selected σ-projection algorithm (`WO.md:71`): resolved for Route K maximin.
- V3 asserts equality to canonical data (`WO.md:72`): resolved.
- V4 verifies P1–P4, right-coset preservation, antipodal, 32 count (`WO.md:73`): partial. P1–P4, antipodal, and count are checked; right-coset preservation is by construction but not asserted as a separate postcondition.
- V5 left-coset regression (`WO.md:74`): resolved.

Open items:

- B1 right-coset carrier (`WO.md:18-22`): resolved.
- B2 trace metric normalization (`WO.md:24-28`): partial; normalization is implemented, but paper/catalogue state the wrong losing score and overstate integer-valuedness.
- B3 literal per-vertex projection (`WO.md:30-34`): not resolved; bypassed by Route K.
- B4 Route K cycle-phase filter (`WO.md:36-40`): partial/resolved modulo the wrong `-5/2` value and phase-convention wording.
- B5 verification rebuild (`WO.md:42-46`): mostly resolved.

**3. Catalogue Audit**

- Missing catalogue entry: trace inner product definition at `tau-sigma-construction.tex:145-159`.
- Catalogue T1 repeats the false value `s(j)=-5/2` (`CATALOGUE.md:40-43`); exact current value is `-5/4`.
- Catalogue L1 says Paper 1 Lemma 3.4 (`CATALOGUE.md:26-29`), but local right-coset carrier source is `bekenstein-incidence.tex:218-227`; use the label/title.
- Catalogue global claim “Every entry below is established by verify.py” (`CATALOGUE.md:3-7`) is false for reflection obstruction P1.
- Catalogue N3 says worked example is reproducible from `verify.py` output (`CATALOGUE.md:59-61`); the script output does not print the vertex lists.
- No uncatalogued lemma/theorem besides the trace-form definition. No conjectures found.

**4. Attribution / External Consistency**

- Paper 1 supports `V600=2I`, closure, and 120 vertices at `bekenstein-incidence.tex:119-146`.
- Paper 1 supports `Tτ` cycles at `162-168`, K-multiset at `182-190`, `H=Dic5` at `193-216`, right/left coset carrier distinction at `218-254`, self-normalizing/non-normal at `257-268`, and `V24` at `284-297`.
- Shared code agrees: `right_cosets` is `H*g` at `group.py:140-167`; left cosets are `gH` at `107-137`; tests distinguish them at `test_dic5_subgroup.py:64-102`.
- Reflection obstruction attribution to Blocks 3a–3c is weak. Block 3c explicitly discusses embedding caveats at `block3c_e8_reflection_obstruction.py:61-72` and does not prove the K-linear proposition in `tau-sigma-construction.tex:163-167`.

**5. Sim Correctness**

`verify.py` is useful and exact for the core Route K construction. It correctly rebuilds phase-0, matches `tau_sigma_canonical.txt`, verifies P1–P4, antipodal compatibility, and enumerates 32 P1–P4 lifts.

Defects:

- It does not assert the full score table claimed in Theorem 4.1. That allowed the false `-5/2` value to survive.
- It implements one-direction K52→K20 maximin scoring, despite the paper calling the criterion “symmetric-maximin”.
- It does not separately assert post-build right-coset preservation.
- No χ²/KL/statistical null issue applies; this is exact finite arithmetic.

**6. Tightness**

- `tau-sigma-construction.tex:148`: replace “integer-valued bilinear form” with “rational trace bilinear form on the cross-Galois pairs used here”.
- `tau-sigma-construction.tex:229-233`: replace `{0,5}` with “two phases separated by 5; in the canonical cycle ordering these are `{0,5}`”.
- `tau-sigma-construction.tex:237-239`: “Direct exact-arithmetic computation” is acceptable only after the exact losing values are asserted in the verifier.
- `tau-sigma-construction.tex:177-179`: do not cite Blocks 3a–3c as full confirmation of Proposition 3.1.

**7. Top Three Fixes**

1. Fix the false phase-score value in `tau-sigma-construction.tex:231-232`, `CATALOGUE.md:40-43`, and add a full-table assertion in `verify.py:155-161`. Current exact value is `-5/4`, not `-5/2`.

2. Make phase labels explicitly conventional before Theorem 4.1: `tau-sigma-construction.tex:190-197`, `216-233`, `330-337`. Intrinsic statement: two winning phases separated by antipodal shift.

3. Replace the reflection obstruction proof/citation at `tau-sigma-construction.tex:163-180`. Prove exact K-rank/span of `H` or remove the proposition from theorem load-bearing status.

**8. Verdict**

Publication ready: no.

The τσ construction is close, and the right-coset correction appears real. But the paper is still dismissable on a false numerical theorem value, an underspecified phase convention, and an unproved reflection-obstruction proposition with weak local attribution.
