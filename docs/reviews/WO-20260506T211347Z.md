**1. Claim Audit**
- Setup imports (`tau-sigma-construction.tex:118`): established as imported, not reproved. Paper 1 supports `V_600=2I`, `T_tau` cycles, K-multiset, `H=Dic_5`, and `V_24` at `bekenstein-incidence.tex:119`, `:162`, `:182`, `:193`, `:284`.
- Right-coset carrier lemma (`tau-sigma-construction.tex:139`): supported. The `tau in H` step is now explicit, and Paper 1 proves the right/left distinction at `bekenstein-incidence.tex:218`; `verify.py:93` rechecks it.
- Trace inner product (`tau-sigma-construction.tex:160`): definition is correct and `verify.py:57` implements exactly `Tr_{K/Q} Re(v conj(w))`. No issue.
- Reflection obstruction remark (`tau-sigma-construction.tex:181`): current wording is appropriately weak. It only claims two restricted computational observations, and says no exhaustive Weyl enumeration. Supported by `block3b_parity_flip_tau_sigma.py:240` and `block3c_e8_reflection_obstruction.py:1`.
- Phase swap definition (`tau-sigma-construction.tex:225`): establishes involution and `T_tau`-equivariance by construction, conditional on the right-coset carrier lemma. OK.
- Symmetric maximin definition (`tau-sigma-construction.tex:242`): matches `verify.py:151`. OK.
- Phase selection theorem (`tau-sigma-construction.tex:265`): established by exact finite enumeration. I ran the verifier; it passed and printed the claimed score table for all five cosets. Minor tightening: make the opening sentence convention-dependent, not just the later qualification.
- Canonical `tau_sigma` (`tau-sigma-construction.tex:325`): established as the all-zero representative, not as a unique intrinsic choice. The convention remark at `tau-sigma-construction.tex:409` prevents overclaim.
- Worked example (`tau-sigma-construction.tex:339`): verified; it matches the canonical data file entries.
- Main theorem (`tau-sigma-construction.tex:354`): proof establishes P1-P4 and antipodal compatibility. Minor hidden lemma: lines `:387` and `:394` use `tau^5=-1`; add a one-line justification or cite the standard unit-quaternion fact.
- `Z_2^5` ambiguity (`tau-sigma-construction.tex:400`): established from independent two-phase choices over five cosets; `verify.py:213` and `:219` confirm 32 valid P1-P4 lifts.

**2. WO Acceptance Audit**
- Paper AC1 pure math: resolved (`tau-sigma-construction.tex:39`, `:100`).
- AC2 cites Paper 1 setup: resolved (`tau-sigma-construction.tex:118`, `:139`).
- AC3 W(H4)/E8 failure: resolved only under the revised Round-6 scope. The paper gives restricted failure observations and explicitly avoids exhaustive W(H4)/W(E8) claims (`tau-sigma-construction.tex:103`, `:196`). If the old WO line `WO.md:64` is read literally, it is only partial.
- AC4 exact construction, pseudocode, worked example: resolved (`tau-sigma-construction.tex:225`, `:293`, `:339`).
- AC5 theorem P1-P4 plus antipodal: resolved (`tau-sigma-construction.tex:354`).
- AC6 `Z_2^5`: resolved (`tau-sigma-construction.tex:400`).
- Verifier AC1-5: resolved. `verify.py` builds state (`:74`), rebuilds map (`:181`), compares static data (`:190`), checks P1-P4/antipodal/right cosets/32 lifts (`:194`, `:201`, `:207`, `:213`), and checks left coset regression (`:115`).
- Open B1/B2/B4/B5/F1-F3: resolved.
- Open B3 literal per-vertex argmax: not resolved; bypassed by Route K. This is acceptable because the derivation no longer uses Route Q.

**3. Catalogue Audit**
Catalogue coverage is good. D1-D4, L1, R1-R2, T1-T2/T2b, and N1-N3 all correspond to derivation objects or explicit certificate claims.

No uncatalogued theorem/lemma/conjecture found. No catalogue theorem is pretending to be proved when the derivation only gives a remark. T2b is correctly marked as post-build certificate, not part of Theorem 5.1.

**4. Attribution / External Consistency**
- Paper 1 right-coset claim: supported at `bekenstein-incidence.tex:218`.
- Paper 1 left-coset warning: supported at `bekenstein-incidence.tex:223`.
- Self-normalizing/non-normal: supported at `bekenstein-incidence.tex:257` and `group.py:170`.
- Shared code right/left cosets: supports the convention at `group.py:108` and `group.py:140`.
- `tau_sigma.py` static helper only checks P1-P4, as WO says (`tau_sigma.py:40`).
- `SCOPE.md` right-coset rationale is corrected (`SCOPE.md:17`).
- `TAU_SIGMA_RESULT.md` flags the legacy left-coset bug (`TAU_SIGMA_RESULT.md:5`, `:33`), but still contains old strong W(H4)/E8 wording at `:61`. Since the current paper does not rely on it, this is not a paper-blocker, but it is still a programme-consistency defect.

**5. Sim Correctness**
`verify.py` implements the derivation’s Route K claim correctly. No χ²/KL/statistical null issue: this is exact finite enumeration.

Ran:
`PYTHONPATH=papers/v600-programme/lib python3 papers/tau-sigma-construction/verify.py`

Result: all assertions passed.

Also ran shared tests with capture disabled because the sandbox has no writable temp directory:
`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=papers/v600-programme/lib pytest -q -s papers/v600-programme/tests`

Result: `39 passed`.

One code-label nit: `verify_tau_sigma`’s P3 helper says “in each non-bulk coset” but only checks K-opposition (`tau_sigma.py:62`). `verify.py` separately checks right-coset preservation at `verify.py:207`, so the certificate is not wrong.

**6. Tightness**
- `tau-sigma-construction.tex:265`: change “For every non-bulk right coset, Phi(Hg) = {0,5}” to “Under the base-point convention of §4.1, for every...”
- `tau-sigma-construction.tex:430`: “canonical” is acceptable only because §6 explains the convention. Do not strengthen it to uniqueness.
- `TAU_SIGMA_RESULT.md:61`: legacy overclaim should be softened or explicitly marked superseded in that paragraph.

**7. Top Three Fixes**
1. `tau-sigma-construction.tex:387`: add one sentence proving/citing `tau^5=-1`.
2. `tau-sigma-construction.tex:265`: make `{0,5}` explicitly convention-labelled in the theorem’s first sentence.
3. `TAU_SIGMA_RESULT.md:61`: remove or supersede the W(H4)/E8 exhaustion wording.

**8. Verdict**
Publication ready: yes.

No substantive math, attribution, or sim-correctness blocker remains in the current tau-sigma artefacts. The remaining fixes are tightness and programme-ledger hygiene.
