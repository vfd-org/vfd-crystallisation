Publication ready: no.

**1. Claim Audit**
1. Lines 137-146, Proposition `prop:gap`: established. The proof is elementary and sufficient.
2. Lines 259-268, Proposition `prop:137`: established. This is just arithmetic and the proof matches the claim.
3. Lines 164-188 and Table 1: the nearest-neighbour spectrum as a numerical computation is fine as a script-reported claim. The manuscript does not prove the irrep labels internally; it explicitly imports them from the standard `2I` character table, which is acceptable if kept labelled as imported/computational.
4. Lines 193-210, Theorem `thm:decoupling`: not established as stated.
   Quote: “an unconditional `2I`-isotypic decomposition ... in which a 94-dimensional integer-eigenvalue subspace and a 26-dimensional irrational-eigenvalue subspace are invariant under every shell-adjacency matrix” (lines 46-47), and “Isotypic decomposition of the shell-adjacency algebra, unconditional” (line 193).
   Problems:
   - The proof relies on “direct enumeration” in `run_icosian_verification.py` (lines 194, 210), but that script is floating-point and tolerance-based (`np.allclose`, approximate vertex lookup), not exact arithmetic. That is computer evidence, not a rigorous proof of subgroup closure or conjugacy-class decomposition.
   - The proof enumerates BFS shells of the vertex graph, namely sizes `1,12,32,42,32,1` (lines 194-197), but the theorem concludes a statement about shell-adjacency matrices `A_d` indexed `d=1,...,8` (lines 198, 204), i.e. the eight Euclidean distance shells used earlier in Table `tab:shells` (lines 113-130). That implication does not follow. A union-of-conjugacy-classes statement for BFS shells does not prove centrality for all eight Euclidean shell sums.
   - Therefore the step “Consequently the shell-adjacency generating sum at each distance-shell is a central element” (line 198) is over-claimed unless “distance-shell” is redefined to mean BFS shells only.
5. Lines 334-336: the statement “restriction of any such Hamiltonian to `W_int` is an invariant reduction, not a truncation approximation” is only as good as Theorem `thm:decoupling`. As written, it inherits the same gap.
6. Lines 284-288: the numerical correspondence `alpha^{-1} ≈ 137 + pi/87` is correctly presented as a numerical correspondence, not a derivation. No issue beyond attribution/status.
7. Lines 296-296: the `9/15 = 3/5` / `SU(5)` discussion is correctly labelled as a structural match, not a derivation.
8. Lines 306-314: the `Z_3` decomposition and three-level restricted spectrum are properly labelled script-reported. No overclaim in the manuscript text.
9. Lines 362-362: the residual-properties sentence is broadly supported by the cited papers, with one caveat under external consistency below.

**2. Internal Consistency**
1. The paper conflates two different notions of shell.
   - Euclidean distance shells: eight shells, counts `12,20,12,30,12,20,12,1` (lines 113-130).
   - BFS graph shells from the identity: six shells including `S_0` and `S_5`, counts `1,12,32,42,32,1` (lines 194-197).
   Theorem `thm:decoupling` slides between them without saying so.
2. Lines 194-198 are internally inconsistent:
   - `A_d` is justified by Euclidean-distance preservation.
   - Centrality is justified by BFS-shell conjugacy decomposition.
   Those are not the same shell partition.
3. Lines 198 and 204 use `A_1,...,A_8`, but the only shell decomposition actually written out is `S_0` through `S_5` (lines 195-197). Either the theorem is about the 5 nontrivial BFS shells, or the proof needs the 8 Euclidean shells. At present it is neither cleanly.
4. Equation on lines 203-205 uses `P_int` and `P_irr` without defining them.
5. Line 210 says “each `A_d` acts scalarly on each isotypic `V_rho`.” That is sloppy notation. `V_rho` is the irreducible module; the isotypic component is `(\dim rho)·V_rho`.
6. Cross-references themselves appear to resolve. I do not see a broken `\ref`/`\eqref` target in the LaTeX source.

**3. External Consistency**
1. Paper V mass-sector eigenvalues, lines 187-188: verified locally. `papers/paper-v/paper-v.tex` explicitly says the 13-particle construction uses the integer 600-cell eigenvalues and reports the `alpha` correspondence.
2. Paper III `lambda = 15 = Delta C`, line 188 and lines 324-326: verified locally in `papers/spectral-dimension/spectral-dimension.tex`, especially lines 87-91.
3. Paper II winding operator and its conditional status, line 314: verified locally in `papers/lepton-generations/lepton-generations.tex`, lines 42-46 and surrounding status language.
4. Papers XVIII-XXI on Schrödinger recovery, lines 99-103 and 427-429: verified locally, but only with the manuscript’s caveat. Paper XVIII states exact recovery at equilibrium and formal leading-order recovery near equilibrium, without rigorous remainder bounds. Paper XXI repeats that status. So the present paper’s cautious phrasing is consistent.
5. Papers XIX-XX on the residual, line 362: mostly verified locally. Phase invariance, equilibrium vanishing, and exact `L^2` norm conservation are in Paper XIX. Paper XXI explicitly notes that amplitude homogeneity belongs to Paper XX, not XIX. The present paper does not overstate that point.
6. Local `CascadeAlgSubstrate` citation, lines 221, 225, 391: verified locally. `papers/cascade-algebraic-substrate/cascade-algebraic-substrate.tex`, lines 556-624, does establish that the `E_8` minimal shell projects two-to-one onto the 120-element `H_4` root set.
7. Lines 249 and 336 invoke Papers VI-XI and IX-X in broad programme language. Those statements are too vague to count as precise verifiable imports, but they are not obviously false. They are not doing mathematical work in this paper.
8. I do not find any local source that upgrades the new theorem to a rigorous unconditional theorem. The only new support I can verify is a floating-point script.

**4. Tightness**
1. Line 193:
   Replace “Isotypic decomposition of the shell-adjacency algebra, unconditional”
   with “Computer-assisted isotypic decomposition for the BFS-shell algebra”.
2. Lines 46-47:
   Replace “are invariant under every shell-adjacency matrix”
   with “are invariant under the shell operators actually covered by the conjugacy-class computation”.
3. Line 198:
   Replace “Consequently the shell-adjacency generating sum at each distance-shell is a central element”
   with “Consequently the generating sum for each verified conjugacy-class-union shell is central”.
4. Line 334:
   Replace “not a truncation approximation”
   with “not a truncation approximation within the algebra covered by Theorem `thm:decoupling`”.

**5. Surface Issues**
1. Lines 203-205: `P_int` and `P_irr` are undefined.
2. Lines 194-206: `A_d` is never formally defined.
3. Lines 194-198: “distance-shell” is ambiguous between Euclidean shells and BFS shells.
4. Line 210: “isotypic `V_rho`” is incorrect notation.
5. The internal script commentary still contains false language about “all eight BFS distance shells”; the manuscript says the claims should be read against the manuscript, not script commentary, but if you are relying on the scripts for theorem support, that looseness matters.

**6. Top Three Fixes**
1. Fix the theorem statement/proof mismatch at lines 193-206.
   Decide whether the theorem is about:
   - the 8 Euclidean shell-adjacency operators, in which case you must prove the Euclidean shells are conjugacy-class unions; or
   - the BFS-shell algebra, in which case reindex the operators accordingly and stop writing `A_1,...,A_8`.
2. Stop calling the theorem unconditional unless the computational part is made exact.
   Lines 194, 210, 213 presently rest on floating-point enumeration. That is not enough for “rigorous and unconditional” in a referee-facing manuscript.
3. Clean the formal statement.
   Define `A_d`, define `P_int` and `P_irr`, and fix the representation-theoretic notation at lines 198-205 and 210.

On the narrow substantive-math/attribution criterion: the attribution work is mostly in order. The new theorem is not yet rigorous and unconditional as stated. The main gap is not the group-classification step; it is the shell notion mismatch plus the fact that the “verification” is numerical rather than exact.
