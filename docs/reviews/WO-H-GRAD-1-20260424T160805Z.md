**1. Claim Audit**

`verify_h_grad_1_b6.py` PASS confirmed: 270 total maximal singular 4-spaces, 240 not through `p`, `|GL(3,2)|=168`, `|Adm_red|=40320`, and type-equivalence 2/2 PASS.

B6.3 is basically sound despite `p=[1]` being isotropic: since maximal totally singular `L` satisfies `L=L^\perp`, `p∉L` implies `p` pairs nontrivially with `L`, so all `(L,p)` have the same Witt type. But `cascade-h-grad-1-closure.md:495` wrongly says “external-anisotropic-vector”; edit to “external isotropic vector with nonzero pairing against L”.

Overclaim: `cascade-h-grad-1-closure.md:549-553` says the sim constructs an explicit `g∈G_p`; the script only checks counts/type invariants, not a map.

**2. WO Acceptance Audit**

A1-A4: resolved in current artefacts, but the derivation still contains stale contradictions at `cascade-h-grad-1-closure.md:324-327`, `389-390`.

O1: partially resolved. `G_p` canonicality is proof-sketched; `G_2` pullback is not executed.  
O2/O3: resolved, but stale text remains.  
O4/O5: resolved per WO table.

**3. Catalogue Audit**

Defect: `cascade-h-grad-1-catalogue.md:24` labels B6 as “G₂-orbit canonicality theorem” while its own status only supports `G_p` canonicality.  
Defect: `cascade-h-grad-1-catalogue.md:105-108` says publication-readiness awaits either `κ` or explicit `G_p` sufficiency, but WO/derivation already make that declaration.

**4. Attribution / External Consistency**

`cascade-algebraic-substrate.tex:480-499`, `cascade-phase-m3-closure.md:49-69`, `cascade-q-o-measurement-bridge.md:112-138`, and `insight.md:670-679`, `878-892` support the uses claimed.  
Not verified locally: the assertion that full P-A only needs `G_p`-canonicality. Existing P-A files still phrase full closure as conditional on H-grad-1, not on `G_p`-equivalence specifically (`cascade-access-principle-theorem.md:231-233`).

**5. Sim Correctness**

Counts/type PASS. Null is honest as a sanity check. The explicit-Witt-witness wording is false.

**6. Tightness**

Replace “G₂-canonicality theorem” with “`G_p`-canonicality proof sketch; `G_2` pullback open.”

**7. Top Three Fixes**

1. `cascade-h-grad-1-closure.md:324-327`, `389-390`, `742-758`: remove stale “BUILD REQUIRED/open” text.  
2. `cascade-h-grad-1-closure.md:495`, `549-553`: fix isotropic wording and sim overclaim.  
3. `cascade-h-grad-1-catalogue.md:24`: relabel B6 as `G_p`, not `G₂`.

**8. Verdict**

Publication ready: no. B6 is plausible at `G_p` level, but the artefacts still contain substantive stale status contradictions and an unbacked downstream claim that `G_p` canonicality suffices for full P-A.
