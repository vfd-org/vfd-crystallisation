**1. Claim Audit**

- `F1` at line 156 is established. The algebra and attraction argument prove the stated positive fixed point and global convergence.

- `F2 Proposition` at lines 219-226 is still not established. The added sentence at line 226 acknowledges that `R[\Phi]` is a total-divergence representative, but the proposition still claims a three-dimensional space “modulo total divergences” with `R` as a basis element. In that quotient, `R` is zero on flat backgrounds. The “physical content” combination `R - \Box tr\Phi` is also a linearised total divergence unless the quotient or boundary data are redefined. This remains the main mathematical failure.

- `F2 Theorem` at lines 265-270 inherits the same defect. The proof at line 274 depends entirely on the failed uniqueness statement of Proposition F2.

- `H_4` corollary at lines 328-329 overclaims “exactly one.” The proof shows at most that any non-crystallographic rung must be conjugate to the icosian `H_4`; it does not prove that a chain cannot contain repeated/conjugate `H_4`-type rungs unless rungs are identified modulo `Aut(E_8)` by definition.

- `F3` at lines 349-354 is mostly fixed: it now says “exhibited” and explicitly disclaims maximal uniqueness. However, the abstract still says the sequence is “selected under H-NC” at line 61, and the structure paragraph still says “subsequent six steps” at line 148; after the `E_8 -> H_4` passage there are five subsequent steps.

- `F4` at lines 430-439 is established only as a definition-driven count. That is now honestly stated; the proof proves the arithmetic from Definition `V`.

- `F5 Lemma` at lines 516-520 is established under H-SIG and the assumed `sigma`-compatibility of the discrete invariants.

- `F5 Theorem` at lines 539-544 proves the finite-sum factor-2 statement under H-SIG. The continuum carryover at line 544 is not proved by F6 alone; GH convergence of vertex sets is not continuity of the discrete closure functional.

- `F6` at lines 591-596 is a conditional reduction, not a theorem in the ordinary sense. The proof reduces BI1/BI2 to H-DENS, H-BI2, H-HR. The numerical `0.7621 -> 0.3805` check at line 604 is reported, but the script is a sampling/numerical check, not a closed proof.

- `F7` at lines 706-708 establishes only a candidate rank-2 localisation at the discrete representation level. The text is mostly honest about H-FP/H-RLA. The word “unique” still depends on the programme’s rung inventory, not on an independent classification theorem.

- `F8` at lines 790-798 is coefficient matching, not derivation. The later scope remarks correctly say this; the theorem title and “cascade-determined” language remain stronger than the proof.

- `Zero-Parameter Corollary` at lines 804-806 is valid only in the weak sense “no further continuous fitted parameters after three imported couplings and thirteen structural hypotheses.” The abstract/intro sentence at line 101 is false as written: Paper V/XXII/XXXII observables are not all “theorems of F1-F8.”

- `Cascade Lambda Theorem` at lines 875-880 follows algebraically only after H-K, H-PROD, H-COPY-ADD, H-RLA, F4, and the candidate F7 localisation are accepted. The numerical value is fine; the mathematical content is hypothesis-stacked rather than derived.

- `Cascade Theorem` at lines 930-944 is a summary and inherits all qualifications above.

**2. Internal Consistency**

- H-COPY-ADD is now present in the main stack lists at lines 57, 68, 105, 130, 876, 931, and 1013. It is still missing from the explicit “Hypothesis stack of the Lambda theorem” list at lines 904-916. That list includes H-K, H-PROD, and H-RLA but omits H-COPY-ADD, even though line 890 uses it.

- Line 148 still says “subsequent six steps”; line 354 correctly says “subsequent five steps.”

- Line 462 calls the Galois twist a `\Zphi`-linear bijection, but line 468 correctly says it is not `\Zphi`-linear. Change the subsection title to “Galois-semilinear” or “Z-linear.”

- Line 591 makes H-HR part of F6’s convergence theorem, while line 630 says GH convergence does not require H-HR, only the explicit rate does. Separate the convergence and rate statements.

- Static label/citation check: no unresolved `\ref`, `\eqref`, or bibliography keys found.

**3. External Consistency**

- Paper V: local text verifies the `13` mass correspondences and `0.014%` average error, but repeatedly labels them numerical correspondences under model conventions, not theorem-level derivations. Lines 85 and 321 of this paper should use that status.

- Paper XXII: local text verifies `alpha^{-1}=137+\pi/87` at `0.81 ppm` and `sin^2 theta_W=3/8`, but explicitly calls them structural/numerical correspondences. Lines 66 and 101 still say “cascade-derived” too strongly.

- Paper XXXII: local text supports proton `0.04%` and deuteron `0.0006%`; the “six radii” claim is stated there but some support is routed through `SmartRadius`. Do not present all six as fully derived in Paper XXXII alone.

- Paper XXXV: supports the `H_4`, `40`, `D_4` projection story, Hopf cell-fibration `600=15*40`, and Schläfli `5*24` decomposition. It also labels its earlier Lambda formula as pattern-matched, so XXXVI should not lean on XXXV for Lambda rigor.

- Paper XXXVII: supports the strong-CP/H-CP claim, but it cites older “F5 sigma-intertwining” language. Since XXXVI has changed F5 to finite-sum equality plus H-COPY-ADD for residue additivity, the downstream strong-CP wording needs synchronization.

- Paper XXX and Papers XL/XLI/XLII: the current descriptions are broadly verifiable and appropriately conditional/target-level.

**4. Tightness**

- Line 60: “unique form” -> “canonical three-coefficient form under H-MIN, after the stated gauge quotient is made precise.”
- Line 61: “is selected under H-NC” -> “is exhibited under H-NC and the programme’s refinement inventory.”
- Line 66: “cascade-derived in Paper XXII” -> “cascade-placed in Paper XXII at structural-correspondence grade.”
- Line 101: “Every cascade observable ... is a theorem” -> “Every downstream observable inherits explicit F1-F8 dependencies and its source paper’s epistemic status.”
- Line 226: “does not affect the downstream programme” -> delete; the point is exactly load-bearing for F2/F8.
- Line 548: “no new conceptual gaps” in Paper XXXV-style GR language is not in this paper’s main proof, but avoid importing that tone here.

**5. Surface Issues**

- Line 890: Unicode sigma is fixed; it now reads `$\\sigma$-related`.
- Line 890: use `\widehat{K}` instead of `\widehat K`.
- Line 972: `D$_4$` should be `$D_4$`.
- Line 148: “subsequent six steps” should be “subsequent five steps.”
- Line 462: title says `\Zphi`-linear but body says not `\Zphi`-linear.
- Line 602 vs 622: script path is inconsistent (`papers/cascade-derivation/scripts/...` vs `scripts/...`).
- No undefined macros or broken references detected by static scan.

**6. Top Three Fixes**

1. Fix F2, lines 219-226 and 250-274. Either stop quotienting by total divergences, replace `R` by a nontrivial quadratic Fierz-Pauli/EH kinetic representative, or define H-MIN/boundary data precisely enough that `R` is not zero in the claimed quotient.

2. Fix the Lambda dependency stack, lines 876 and 904-916. Add H-COPY-ADD to the line 904 stack list; separate Lambda-used hypotheses from full-programme hypotheses, since H-MX/H-YM are included at line 876 but line 835 says they do not feed the Lambda theorem.

3. Finish the F3 downgrade consistently, lines 61, 148, 328-335, 354, 376. Use “exhibited” everywhere, fix “six” to “five” after the H4 passage, and weaken “exactly one non-crystallographic rung” unless quotient-by-conjugacy/no-repeat is made an explicit chain convention.
