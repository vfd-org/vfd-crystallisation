Publication ready: no

**1. Claim Audit**
- **Proposition 1 (“Permeability gap”), lines 137–145.**  
  Claim: `d_2^2 - d_1^2 = 1/\varphi`.  
  Verdict: established. The proof is a one-line algebraic identity and is sufficient.

- **Table 1 / spectral map, lines 164–188.**  
  Claim: the nearest-neighbour spectrum is as listed, and the irrep labels match the standard `2I` character table.  
  Verdict: only script-supported, not proved in the paper. That is acceptable because the table is labeled “numerical spectrum script-reported” and “irrep labels imported.” The surrounding prose is mostly honest. The one thing you do not prove here is the irrep assignment from first principles.

- **Theorem 2 (“Isotypic decomposition of the shell-adjacency algebra, unconditional”), lines 193–206.**  
  Claim: all shell-adjacency matrices preserve the `2I`-isotypic splitting, hence `\mathbb C^{120}=W_int\oplus W_irr` with dimensions `94` and `26`, and all polynomials in the shell-adjacency matrices preserve this split.  
  Verdict: the proof establishes the general mechanism, but **not the theorem as stated**.  
  Problems:
  - At line 194 the theorem defines `W_int` using the `\mathbf A_1`-eigenvalue. That is wrong. Your proof at line 206 uses `\mathbf A_{1/\varphi}`. Those are different operators. As written, the theorem’s statement is false or at least undefined relative to Table 1.
  - Line 200 writes `p(\mathbf A_1,\ldots,\mathbf A_8)`, but the matrices were indexed by distances `d`, not by shell numbers. Again: wrong notation, wrong objects.
  - The proof is also only “unconditional” if the computational certification is itself rigorous. The manuscript gives no exact-arithmetic certificate, no appendix, and no statement about tolerance control. Since the proof rests on “direct enumeration” of shell/class coincidence, the theorem is at best **computer-assisted** as presented, not unconditional in the ordinary journal sense.

- **Remark 3 / Paper V alignment, lines 187–188.**  
  Claim: Paper V uses the nontrivial integer eigenvalues `{9,12,14,15}` and not the irrational ones.  
  Verdict: locally verifiable and accurately hedged. This is fine.

- **Proposition 3 (“Arithmetic identity”), lines 255–263.**  
  Claim: the displayed expression equals `137`.  
  Verdict: established. Pure arithmetic.

- **Fine-structure correspondence, lines 280–284.**  
  Claim: `87 + 50 + \pi/87 = 137.03611`, agreeing with CODATA 2018 at about `0.81 ppm`, as a numerical correspondence rather than derivation.  
  Verdict: established as arithmetic plus comparison. The paper is careful enough here.

- **Weinberg-angle passage, lines 290–292.**  
  Claim: `9/15=3/5` matches the `SU(5)` hypercharge-normalisation factor and hence yields `\sin^2\theta_W=3/8` at the GUT scale.  
  Verdict: acceptable only as a **numerical/structural correspondence**. The text mostly says that. Still, “structural match” is doing a lot of work; nothing in the paper ties the 600-cell ratio to the `SU(5)` normalisation beyond coincidence.

- **Generation-structure claims, lines 302–310.**  
  Claim: the `16 = 8 \oplus 4 \oplus 4` restricted `\mathbb Z_3` decomposition and the three-level spectrum on the 8-dimensional weight space.  
  Verdict: not proved, only script-reported. The manuscript says so. That is acceptable if you keep it quarantined as exploratory. It is not publication-grade mathematics.

- **Invariant reduction statement, lines 330–332.**  
  Claim: within the shell-adjacency algebra, restriction to `W_int` is an invariant reduction, not a truncation approximation.  
  Verdict: this follows once Theorem 2 is repaired. At present it inherits the theorem’s notation defect.

**2. Internal Consistency**
- **The central notation bug is still present.**  
  Lines 194 and 200 use `\mathbf A_1` / `(\mathbf A_1,\ldots,\mathbf A_8)`, while line 194 earlier defines shell matrices by Euclidean distance and explicitly says the nearest-neighbour matrix is `\mathbf A_{1/\varphi}`. The proof at line 206 also uses `\mathbf A_{1/\varphi}`. This is a direct contradiction inside the theorem.

- **Indexing scheme conflicts.**  
  In the same theorem, shell matrices are distance-indexed (`\mathbf A_d`) but later treated as if they were shell-number-indexed (`\mathbf A_1,\dots,\mathbf A_8`). Pick one convention and keep it.

- **Cross-references appear conceptually aligned.**  
  I do not see a surrounding-text mismatch in the use of `\ref`/`\eqref`. The references to `thm:decoupling`, `prop:gap`, `prop:137`, `sec:discussion`, `sec:predictions`, etc. point where the prose says they should. The problem is not broken references; it is wrong notation inside the referenced theorem.

- **Terminology drifts between “nearest-neighbour”, “shell 1”, `S_1`, and `A_{1/\varphi}`.**  
  That can be managed, but at present it contributes to the theorem-level confusion. You need one explicit sentence fixing the convention.

**3. External Consistency**
Verified against local sources in the repository:
- **Paper V summary, lines 57–58 and 187–188.**  
  Verified in `papers/paper-v/paper-v.tex`: the `0.014%` average-error claim, non-unique `(S,w)` scheme, reverse-identified chain ratios, and use of integer eigenvalues are all stated there.

- **Papers XVIII/XXI on Schrödinger recovery, lines 59, 99–103, 423.**  
  Verified in `papers/paper-xviii/paper-xviii.tex` and `papers/paper-xxi/paper-xxi.tex`: equilibrium and near-equilibrium Schrödinger-type recovery are stated; physical identification of `\sigma^2` with `\hbar/m` is explicitly not derived.

- **Paper III claim `\lambda=15=\Delta C`, lines 188 and 322.**  
  Verified in `papers/spectral-dimension/spectral-dimension.tex`.

- **Papers I/IV claim `1265/81 = 15 + 2/3 - 4/81`, line 322.**  
  Verified in `papers/master-mass/master-mass.tex` and `papers/pemr/pemr.tex`.

- **Paper II conditional winding-operator claim, line 310.**  
  Verified in `papers/lepton-generations/lepton-generations.tex` as conditional.

- **Papers IX/X gravity caveat, line 332 and 367.**  
  Verified in `papers/paper-ix/paper-ix.tex` and `papers/paper-x/paper-x.tex`: structural/geometric analogy only, no Einstein equations.

- **Papers XIX/XX residual properties, line 358.**  
  Verified in `papers/paper-xix/paper-xix.tex` and `papers/paper-xx/paper-xx.tex`: phase invariance, norm preservation, equilibrium vanishing, and caution about physical interpretation are all there.

- **Repository-local `CascadeAlgSubstrate` claim, lines 217, 221, 387.**  
  Verified in `papers/cascade-algebraic-substrate/cascade-algebraic-substrate.tex`: the `E_8` minimal shell projects two-to-one onto the `120`-element `H_4` root set, with Galois-conjugate pairing.

Not verified from local source in this pass:
- None of the repository-local attributions above looks fabricated. The weak point is not mis-citation; it is overstatement of what the current paper itself proves.

**4. Tightness**
- **Line 193:** “unconditional”  
  Better: “computer-assisted, with exhaustive finite verification”.  
  The present wording oversells the proof.

- **Line 46:** “an unconditional `2I`-isotypic decomposition … invariant under every shell-adjacency matrix”  
  Better: “a computer-assisted `2I`-isotypic decomposition …”.  
  Same issue.

- **Line 330:** “not a truncation approximation”  
  Better: “an exact invariant reduction within the shell-adjacency algebra”.  
  Stronger and cleaner.

- **Line 292:** “matches the numerical factor”  
  Better: “coincides numerically with the factor”.  
  Less suggestive of mechanism.

- **Line 308:** “has three distinct eigenvalues”  
  Better: “the script reports three distinct eigenvalues”.  
  You do say “script-reported” elsewhere; keep it in the lead clause.

**5. Surface Issues**
- **Major theorem notation error:** `\mathbf A_1` should be `\mathbf A_{1/\varphi}` at line 194, and line 200 must be rewritten consistently.
- **Indexing abuse:** `\{\mathbf A_d\}_{d=1}^{8}` is wrong if `d` denotes distance values rather than shell labels.
- **No remaining `\mathbbm{1}` issue.** The token is gone from the LaTeX source.
- **The bibliography/source mapping is sane.** No obvious undefined local citation keys surfaced from reading.
- **Not in the manuscript, but relevant:** the supporting scripts still use floating-point tolerances and rounded coordinates. That is tolerable for exploratory computation; it is not what “rigorous proof” usually means.

**6. Top Three Fixes**
1. **Repair the statement of Theorem `thm:decoupling`** at lines 194–200.  
   Replace every occurrence of `\mathbf A_1` that is meant to denote nearest-neighbour adjacency by `\mathbf A_{1/\varphi}`, and stop switching between distance indexing and shell-number indexing in the same theorem.

2. **Stop calling Theorem `thm:decoupling` “unconditional” unless you actually provide a rigorous computational certificate** at lines 193–206.  
   As written, the proof relies on script enumeration without exact-arithmetic documentation in the manuscript. Either:
   - downgrade the claim to “computer-assisted”, or
   - add an appendix giving the exact finite verification method in a referee-checkable form.

3. **Quarantine the script-only generation and coupling material more aggressively** at lines 296 and 302–310.  
   The current prose is better than earlier rounds, but this is still not publication-grade mathematics. Move it more explicitly into an “Exploratory computations” register or trim it.

As it stands, the paper is not publication-ready. The central theorem is internally inconsistent on its own page, and the advertised rigor of that theorem is stronger than the manuscript currently supports.
