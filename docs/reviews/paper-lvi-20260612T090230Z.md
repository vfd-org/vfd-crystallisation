Line numbers below refer to `papers/paper-lvi/paper-lvi.tex`.

**1. Claim Audit**

- L32, “`verify_residual_closure.py` ... suite 23/23”: false in this checkout. I ran it: `24 PASS, 0 FAIL`. `verify_crystal_forcing.py` is correctly `17 PASS, 0 FAIL`.
- L49, A1 “leaving \(S^3\) and \(S^7\)” and “sedenions have zero divisors”: the zero divisor is verified by SM6. The exclusion of \(S^0,S^1\) is an imported rendering requirement, not proved here. “Tower stops” only stops this division-algebra mechanism.
- L50, A2 frame split: locally supported by Paper LIII/T14 for the actual 600-cell/finite \(2I\) action. The text states the full \(SU(2)_L\times SU(2)_R\) arena version; that needs either a short analytic argument or explicit “continuum arena input” wording.
- L51, A3 clock axis: order-10 quaternionic clock is verified. “SM5 verifies ... frame-triviality” is misleading; SM5 checks order, while frame-triviality comes from LIII/T14. The octonionic clock-unit necessity is asserted as inherited, not proved.
- L61, “dimension exactly 14”: the script verifies nullity 14 numerically by floating SVD. That is evidence, not an exact rank certificate. The \( \mathfrak g_2\) name is correctly imported from Baez, not computed.
- L65-L70, “stabiliser is \(\mathfrak{su}(3)\)”: the equality-by-dimension route is the right correction. But the inclusion proof needs to state the missing ingredients: define \(W=e_1^\perp\cap\operatorname{Im}\mathbb O\), show \(D(W)\subset W\), use alternativity for \(J^2=-1\), and use norm preservation for skew-Hermiticity. Also this proves Lie algebra equality, not the global group \(SU(3)\) without a connectedness/global-form argument.
- L68, compact/simple/rank checks: correctly labelled corroboration. The “random-element ideal closure” is not a proof of simplicity.
- L79-L81, internal \(SU(2)\) and clock circle: the clock element is non-central, so the unique circle subgroup claim is mathematically fine. But \(q_0=\varphi/2\) is only a real part, not a unique vertex/axis.
- L83-L89, inventory theorem: the corrected structure \(SU(3)\times SU(2)\) with \(U(1)_{\rm clock}\subset SU(2)\) is substantially fixed. The remaining overclaim is “the mechanism produces nothing else”: this follows only after adding the extraction rule “one factor from the octonion clock stabiliser, one internal quaternionic factor, one marked circle.” Division algebras stopping does not by itself exclude all subgroups, quotients, discrete remnants, or other arena-derived structures.
- L98-L103, Lie-algebra forcing proposition: YM1/YM2 establish exactly two finite-grid checks. The general forcing theorem is cited, not proved, and the text mostly says that. The index convention “\(f^{abc}\) antisymmetric in \((a,b)\)” is nonstandard for the displayed Yang-Mills formulas and should be cleaned up.
- L118, “Z at shell 82, offset \(-0.049\)”: locally supported by `cascade-masses.md` and `claim-status-ledger.md`; correctly stated as a placement, not a derivation.

**2. Internal Consistency**

- Cross-references resolve: `sec:honest`, `thm:su3`, `thm:inventory`, and `thm:lie` are defined. No `\eqref`s are present.
- L32 and L129 conflict: residual suite is called `23/23` in both places, but current script is `24/24`; crystal suite is `17/17` at L32 but stale `14/14` at L129.
- L61 and L129 say nothing about \(G_2\) or \(\mathfrak{su}(3)\) is assumed, but L61 uses the standard identification \(\operatorname{Der}(\mathbb O)\cong\mathfrak g_2\), and L68 uses \(\dim\mathfrak{su}(3)=8\). Say “not supplied to the computation,” not “not assumed anywhere.”
- L79 says the relevant isometry is \(SU(2)_L\times SU(2)_R\). As a group action on \(S^3\), the isometry group is really \((SU(2)_L\times SU(2)_R)/\{\pm1\}\). If global gauge groups matter, this quotient issue cannot be ignored.
- L88 and L115 are consistent only because of “factor types.” The paper must keep saying “factor types,” because it has not derived an independent \(U(1)_Y\).
- L123, “the shape of the bundle is derived,” is stronger than the theorem. The theorem derives an inventory with a marked subgroup, not the SM gauge bundle shape.

**3. External Consistency**

- L42, Paper LIII/LVII derive GR “from one mechanism”: broadly verifiable, but overcompressed. Paper LIII states effective-field-level derivation with named residuals; Paper LVII upgrades several residuals but leaves open items. Add “at effective-field level, with named residuals.”
- L50, frame split: verified in Paper LIII at its tensoriality proposition and in `verify_gr_closure.py` T14a-b. Scope is finite \(2I\)/actual 600-cell unless extended analytically.
- L95, “Maxwell forced; Paper LIII sim T13”: verified in Paper LIII’s Maxwell proposition.
- L118, Paper LVII conditional Newton anchor: verified. Paper LVII states H-shell-96, \(G=\hbar c/(m_\mu\varphi^{96})^2\), deviation \(1.9\times10^{-4}\), and explicitly leaves shell-96 derivation open.
- Repository inconsistency: Paper LIII’s second-pass update still says the SM inventory is \(SU(3)\times SU(2)\times U(1)\), not the corrected “\(SU(3)\times SU(2)\) with clock circle” version. That stale statement should be repaired.

**4. Tightness**

- L44: replace “with its choice removed” with “with the choice supplied by A3.”
- L61: replace “has dimension exactly 14” with “the verification computes nullity 14; an exact rank certificate would make this theorem-grade.”
- L68: replace “simple (random-element ideal closure)” with “random ideal-generation test; not used as proof.”
- L86: replace “the mechanism produces nothing else” with “under the stated arena-to-factor extraction rule, it produces no further independent continuous factors.”
- L103: rewrite the index convention using standard \(f^a{}_{bc}\) antisymmetric in \(b,c\), or explain the metric-lowered convention.
- L123: replace “the shape of the bundle is derived” with “the available internal-symmetry inventory is derived; the SM bundle wiring is not.”

**5. Surface Issues**

- L67 uses \(W\) before defining it.
- L81: “clock vertex \((q_0=\varphi/2)\)” is under-specified; many vertices share that real part.
- L129: `14/14` is stale; current crystal-forcing suite is `17/17`.
- Existing log shows hyperref PDF-string warnings from math in section titles L55 and L76; use `\texorpdfstring`.
- Existing log shows overfull boxes at L32, L68-L69, L85-L87, L129-L130.
- BibTeX warns `SmartLIII` has empty journal. Not fatal, but sloppy.

**6. Top Three Fixes**

1. Fix L86: make the no-others theorem explicitly mechanism-scoped by adding the extraction rule and excluding only further independent continuous arena factors, not all possible internal symmetry.
2. Strengthen L61-L70: provide exact linear-algebra certificates or soften “exactly,” define \(W\), state the composition-algebra facts used, and distinguish Lie algebra \(\mathfrak{su}(3)\) from group \(SU(3)\).
3. Repair provenance/counts at L32, L51, L81, L129: update `24/24` and `17/17`, separate SM5 from LIII/T14, and cite the local source for the octonionic clock-axis selection.
