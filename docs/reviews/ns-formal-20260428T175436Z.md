**1. Claim Audit**
- Main theorem, lines 142-149: not established. It assumes only `u_0 \in H^s`, `s >= 1`, but the proof uses “smooth divergence-free initial data” at lines 568-572. Kato local smooth theory does not give a `C^\infty` solution from arbitrary `H^1` data.
- `\Pi_{\mathrm{hyd}}` lemma, lines 152-185: not proved. The scale sum is stated over `k \in \mathbb Z` at lines 153 and 179, but `\sum_k \varphi^{-2k}` diverges over all integers. The value `1+\varphi` only matches `k >= 0`.
- F2/F4/F5, lines 305-319: overclaimed. Current Paper XXXVI makes F2 conditional on H-MIN, F4 a dimension count `dim V = 583`, and F5 a finite-sum factor-2 statement under H-SIG, not the unconditional spectrum/σ-invariance claims here.
- Cascade-NS correspondence, lines 340-357: not established. The proof is a heuristic asymptotic sketch, not a theorem. It also conflicts internally over which term yields viscosity/convection: compare lines 351-353 with 371-376.
- Viscosity numerical claim, lines 390-395: unsupported. No computation derives `10^{-6} m²/s`; no units are tracked from “shell spacing.”
- Rank preservation lemma, lines 402-408: does not prove the claim. Absence of rank-3 invariants does not bound the rank-2 vortex stretching term; the proof itself admits vortex stretching exists at lines 431-433.
- Bounded macroscopic operators lemma, lines 438-447: false as proved. Item 3 is essentially global regularity in Sobolev norms; the proof only says “bootstrap” and invokes the later vorticity hypothesis.
- BKM theorem, lines 534-541: acceptable as a continuation criterion only with proper NS regularity hypotheses and citation. The cited BKM paper is for Euler.
- Global extension corollary, lines 545-563: conditionally valid only if Hypothesis B_NS is granted and the local solution is smooth with adequate initial regularity.
- “Cascade provides an alternative proof” for 2D NS, lines 748-751: no such proof is given.

**2. Internal Consistency**
- Main theorem hypotheses conflict with scope text: lines 142-147 require H1/H_NS/H_P6/P7, while lines 225-228 say the theorem is conditional only on the rigorous `\Pi_{\mathrm{hyd}}` lemma.
- Spectrum conflict: lines 230-235 list `7 ± sqrt(5)`, while lines 613-615 list `12-4\varphi` and `8+4\varphi`. These are not equal.
- F4 conflict: lines 311-315 say the closure operator spectrum is concentrated at `\varphi^{-1}` with multiplicity 583, while later sections use the 600-cell Laplacian spectrum with 120 total multiplicity.
- `B_NS_3` is mentioned at lines 227 and 253 but no corresponding labelled hypothesis/lemma exists.
- Cross-references mostly resolve, but `\label{thm:vorticity}` labels a hypothesis, not a theorem.

**3. External Consistency**
- Paper XXXVI does not support the imports as written. Its F2 is under H-MIN; F4 is a definition-driven dimension count, not a spectral theorem; F5 is H-SIG-conditional and discrete.
- P6 does not provide theorem-grade spectral convergence. Its own abstract says spectral convergence is a conditional program and “not yet Clay-grade theorem-level output.”
- P7 explicitly says it makes no unconditional NS regularity, uniqueness, smoothness, or blow-up claim. Its NS limit is conditional on `H_hyd` plus P6 Hypothesis 6.4.
- `verify_pi_hyd_one_scale.py` supports only a one-scale numerical Laplacian check and itself says the continuum `B_NS_3` bridge remains open.
- `aria-chess/...` artifacts cited at lines 662-668 were not present locally. The local `cascade-h4-oscillator-law.md` supports a formal H4O flow-law discussion, not the cited Lyapunov/torus artifact claims.

**4. Tightness**
- Line 53: replace “None of the three is established” with “The theorem is therefore a conditional reduction, not a regularity proof.”
- Line 98: replace “standard Sobolev analysis” with “a substantial unresolved sampling/interpolation and PDE-compatibility problem.”
- Line 387: replace “IS Navier-Stokes” with “is intended to approximate Navier--Stokes under the stated projection hypotheses.”
- Line 477: replace “This is the key theorem” with “This is the key open hypothesis.”
- Lines 741-743: replace “rules out INFINITELY high vorticity” with “would rule out finite-time blow-up through BKM if the hypothesis were proved.”

**5. Surface Issues**
- Undefined macros/packages: `\textbeta` line 59, `\Vcal` line 507, `\bm` line 621.
- Unicode likely breaks pdfLaTeX without setup: literal `σ`, `m²` line 394, `H₄` line 756.
- Inconsistent typography: `Navier-Stokes` vs `Navier--Stokes`; all-caps “IS,” “BOUNDED,” “INFINITELY.”
- Bibliography item `KatoUniqueness` is incomplete.
- The title and abstract correctly say “conditional,” but theorem environments still present unproved bridge claims as theorem-grade.

**6. Top Three Fixes**
1. Fix the main theorem statement at lines 142-149: require smooth or sufficiently high Sobolev data, and include all actual bridge hypotheses, including P5/P6/P7 imports and H1/H1′/H_hyd-type assumptions.
2. Replace F2/F4/F5 imports at lines 305-319 with the current Paper XXXVI scoped versions. The present F4 statement is simply wrong.
3. Rewrite the `\Pi_{\mathrm{hyd}}` lemma at lines 152-185 as an open hypothesis or precise target. The current proof has a divergent scale sum, no valid sampling theorem, no divergence-free compatibility, and no NS-continuum argument.
