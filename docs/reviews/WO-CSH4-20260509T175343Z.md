**Claim Audit**
Artefact inventory: `papers/cascade-selection-h4/` contains only `WO-CSH4.md`. No `.tex` derivation, catalogue, or sim is supplied.

- “labelled-multiset combinatorial scalar graph Laplacian” `papers/cascade-selection-h4/WO-CSH4.md:62`: established. This now matches P3’s labelled-edge multiset convention at `papers/cascade-refinement-spaces/cascade-refinement-spaces.tex:328`.
- “kernel is exactly the `F^0`-valued constants” `WO-CSH4.md:110`: established, assuming the proved/argued connectivity of `G_N^{H_4}`.
- “P4’s `A_n^{vertex}` … differs by replacing three of the four fibre blocks with zero” `WO-CSH4.md:116`: supported by P4 `def:coboundary` and `def:An`; P4 projects to `Im(O)` and acts trivially on `V_{H_4}, V_{D_4}, V_{16}` plus real octonion direction.
- “compat’s `\widetilde A_n` inherits the same fibre structure” `WO-CSH4.md:122`: not cleanly supported. Compat’s `def:abstract-model` is explicitly scalar/simple/pure-midpoint (`cascade-refinement-compat.tex:175-203`), and `def:Atilde` defines a scalar `2^n A_n^{vertex}` (`:278-283`). Replace with “scalar analogue / scaled abstract-model operator.”
- The theorem setup for `R_N`, `e^{-tL_N^{block}}`, and constant `\phi_N^\star` `WO-CSH4.md:139-159`: established.
- Clause 1, self-adjoint/PSD/bounded with `2Δ` bound `WO-CSH4.md:163-169`: established for the stated multigraph convention.
- Clause 2, connectivity and `dim ker = 32` `WO-CSH4.md:170-180`: established. P3 refinement attaches midpoints to endpoints and centroids to midpoints (`cascade-refinement-spaces.tex:346-354`); base graph is the 600-cell edge graph.
- Clause 3, convergence with `\lambda_+` `WO-CSH4.md:181-202`: theorem statement is now correct. But the drafting plan still says “`\lambda_2 > 0`” and “rate `\lambda_2`” at `WO-CSH4.md:360-361`. That is a substantive spec defect because the block kernel is 32-dimensional.
- Clause 4, `dim_R (F^0)^{W(H_4)} = 28` `WO-CSH4.md:203-217`: established from P3 branchwise action (`cascade-refinement-spaces.tex:1172-1188`) plus standard irreducibility/no fixed vector for the nontrivial `H_4` reflection representation.
- Clause 5, witnessed `(O1)` discharge `WO-CSH4.md:218-233`: established narrowly for top-rung residual-zero plus convergence. It does not discharge `(O2)`, `(O3)`, inverse-limit propagation, or unique selection; the WO mostly states that honestly.
- `(O0)` verification `WO-CSH4.md:235-255`: now adequate for the stated finite/top-rung use. Mechanism requires finite/smooth space, `C^1` residual, well-posed flow, and `C^1` pushforwards (`cascade-mechanism.tex:594-608`); P3 `def:p0`/`thm:bonding` support bounded linear vertex bonding (`cascade-refinement-spaces.tex:687-735`).
- Worked `N=0` numerics `WO-CSH4.md:374-386`: supported. P2 gives degree 12 and `L=12I-A_1` (`cascade-algebraic-substrate.tex:1116-1123`) and the spectrum including `9-3√5`, `9+3√5` (`:1148-1161`).

**WO Acceptance Audit**
- A1: partially resolved at WO-spec level. The theorem is coherent, except the stale `lambda_2` drafting instruction conflicts with clause 3.
- A2: partially resolved. Section order is specified, but §6.5 contains the stale false `lambda_2` instruction.
- A3: resolved at WO-spec level. Labels checked; no live nonexistent-label citation found. The `def:scaled-curvature` mention is only a negative example.
- A4: resolved at WO-spec level.
- A5: resolved.
- A6: mostly resolved; fix the compat “same fibre structure” wording.
- A7: resolved.
- A8: resolved as a drafting requirement; no sim required.
- A9: resolved.
- A10: not met for this WO review because of the stale `lambda_2` spec defect.

Open/risk items:
- `(H-uniqueness)` `WO-CSH4.md:420`: correctly open.
- `(H-physical-interpretation)` `WO-CSH4.md:433`: correctly open.
- R1-R5 `WO-CSH4.md:479-499`: adequately identified; R5 sim remains optional.

**Catalogue Audit**
No catalogue is supplied. If a catalogue is required later, it must include at least: block Laplacian definition, witnessed `(O1)` theorem, `(H-uniqueness)`, `(H-physical-interpretation)`, and the `N=0` numerical result.

**Attribution / External Consistency**
P3, P4, cascade-mechanism, compat, and P2 citations are locally verified. The only attribution/wording problem is `WO-CSH4.md:122-125`: compat is scalar pure-midpoint, not a full-fibre operator inheriting P4’s fibre structure.

**Sim Correctness**
No sim supplied. Not applicable.

**Tightness**
- `WO-CSH4.md:360-361`: replace “`\lambda_2 > 0` … rate `\lambda_2`” with “`\lambda_+(L_N^{block}) > 0` … rate `\lambda_+`.”
- `WO-CSH4.md:122-125`: replace “inherits the same fibre structure” with “is the scaled scalar abstract-model analogue.”
- `WO-CSH4.md:522` and `WO-CSH4.md:537`: update stale `v3` text to `v4`.

**Top Three Fixes**
1. `WO-CSH4.md:360-361`: remove the remaining false `lambda_2` drafting instruction.
2. `WO-CSH4.md:122-125`: correct the compat attribution; compat is scalar pure-midpoint.
3. `WO-CSH4.md:522` and `WO-CSH4.md:537`: fix stale `v3` metadata before drafting.

**Verdict**
Publication ready: no.
