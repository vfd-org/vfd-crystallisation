Publication ready: no.

Round 8 fix status: (1) only partially resolved, (2) resolved in `prop:87` but not fully propagated, (3) resolved.

**1. Claim audit**
- “We record the fine-structure constant identity `\alpha^{-1} = 137 + \pi/87`...” ([paper-xxxiv.tex](/mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxxiv/paper-xxxiv.tex:44)) is not established by the paper. The body correctly downgrades the result to a conditional correspondence with an unproved irrational term, but the abstract still states an identity with `=`. Same problem in the conclusion ([...:266]) and the numerical table row ([...:218]). The stated proof does not establish a physical identity for `\alpha^{-1}`.
- “`E_8 = \mathcal I \oplus \sigma(\mathcal I)`” in the chain and status summaries ([...:63], [...:75], [...:239]) is not what the cited classical result says. Your own Lemma says the correct object is `{(x,\sigma(x)):x\in\mathcal I} \subset \R^4\oplus\R^4` ([...:97]). As written, `\mathcal I \oplus \sigma(\mathcal I)` is a direct sum statement, not the diagonal embedding, and is mathematically wrong/sloppy.
- Lemma “Elser--Sloane realisation of `E_8`” ([...:96-102]) is acceptable as an imported classical statement. The proof establishes only that it is classical and cited, which is enough for this paper’s level.
- Proposition “Phason slot count, as a counting convention” ([...:151-161]) now says exactly what the proof establishes: an assigned count under Hypothesis B plus the gauge-reduction convention. This Round 8 fix is substantively resolved.
- Proposition “Rational-irrep eigenvalue sum, from P2” ([...:165-174]) is established, conditional on accepting the imported P2 computation. The proof matches the claim.
- Proposition “Integer part of the structural identity, conditional” ([...:178-191]) proves the arithmetic `137 = 87 + 50`. It does not quite justify the gloss “where `87` is the Kostant-gauge-fixed phason slot count” ([...:183]), because `prop:87` explicitly demotes that label to an adopted counting convention. The arithmetic is proved; the ontological label is not.
- Remark on `\pi/87` ([...:193-195]) is honest. It does not overclaim.
- The `0.81` ppm numerical comparison ([...:217-223]) is arithmetically fine, but it should not be wrapped in equality language implying derivation.

**2. Internal consistency**
- The manuscript is internally inconsistent on whether the main formula is a correspondence or an identity. Title/opening display are softened ([...:29-30], [...:52-55]), but the abstract reverts to “identity” with `=` ([...:44]), the table uses `=` ([...:218]), and the conclusion does the same ([...:266]). Round 8 fix (1) is incomplete.
- The manuscript is internally inconsistent on the `E_8`/icosian statement. Lemma `\ref{lem:ES}` gives the diagonal embedding ([...:97]); the introduction and status table rewrite it as a direct sum ([...:63], [...:75], [...:239]).
- The manuscript is internally inconsistent about what C3 depends on. The Phase B status remark says C3 is argued “conditional on C1 plus classical `\Zphi` structure” ([...:125]), while the status table says “Conditional on C1+C2” ([...:242]). Pick one.
- The manuscript is internally inconsistent about B’s strength. `prop:87` says the phason-slot reading is an adopted convention ([...:156]); the status table hardens this to “Upper exponents `{17,19,23,29}` are phason” ([...:244]). That is stronger than the body supports.
- Static inspection shows no obvious broken `\ref`/`\eqref`; the local labels referenced in the file exist.

**3. External consistency**
- `cascade-12d-closure.tex` is cited for the notation reconciliation ([...:128-130]). Verified. The source says the two readings are “the same underlying algebraic object up to relabeling” and that no mathematical content depends on the choice of reading ([cascade-12d-closure.tex](/mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/cascade-12d-closure/cascade-12d-closure.tex:963)). Round 8 fix (3) is resolved.
- P2 is cited for the `600`-cell Laplacian spectrum and the `94+26` split ([...:165-173], [...:194]). Verified. The source contains the imported spectrum and the `94/26` decomposition ([cascade-algebraic-substrate.tex](/mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/cascade-algebraic-substrate/cascade-algebraic-substrate.tex:1116), [...:1166]).
- Paper XXII is cited for the same `94+26` decomposition ([...:173]). Verified. The theorem is there ([paper-xxii.tex](/mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-xxii/paper-xxii.tex:200)).
- Paper V / Paper XXII are cited for prior reporting of the `\alpha^{-1} \approx 137 + \pi/87` correspondence ([...:60]). Verified. Paper V presents it explicitly as a numerical correspondence, not a derivation ([paper-v.tex](/mnt/c/Users/nexus/OneDrive/Documents/My Projects/vfd-crystalisation-paper/papers/paper-v/paper-v.tex:303)).
- Phase B is cited as giving a bespoke dimensional-counting argument for C2 and only a minimality-conditional closure for C1 ([...:76], [...:124-126]). Verified. The source labels itself a proof attempt and says exactly that.
- `cascade-assumptions-a-b-closure.md` and `cascade-identity-c-consolidated.md` are cited in the irrational-term discussion ([...:194]). After inspection, I do not find a closed proof there. Those files assert theorem-level closure, but the actual text is still heuristic bullet-point reasoning, especially for the `\pi/87` phase story. The present paper is right to distrust them.

**4. Tightness**
- [Line 44] Replace “identity `\alpha^{-1} = 137 + \pi/87`” with “correspondence `\alpha^{-1} \approx 137 + \pi/87`”.
- [Lines 63, 75, 239] Replace “`E_8 = \mathcal I \oplus \sigma(\mathcal I)`” with “`E_8` is realised as `{(x,\sigma(x)):x\in\mathcal I} \subset \mathcal I \oplus \sigma(\mathcal I)`”.
- [Line 183] Replace “`87` is the Kostant-gauge-fixed phason slot count” with “`87` is the quantity counted as the phason-sector slot total under Hypothesis B and the gauge convention”.
- [Line 244] Replace “Upper exponents ... are phason” with “Upper exponents ... are identified with the phason sector under B”.

**5. Surface issues**
- No serious LaTeX breakage is apparent from static inspection.
- The main surface-level problems are really mathematical wording problems: misuse of `=` for an admitted correspondence, and misuse of `\oplus` for the icosian realisation.

**6. Top three fixes**
1. Fix the false/sloppy icosian statement at [lines 63, 75, 239], and in the abstract where the same shorthand is used ([...:43-44]). This is the worst defect because it misstates a standard classical result.
2. Finish Round 8 fix (1) everywhere: abstract ([...:44]), table ([...:218]), and conclusion ([...:266]) must stop calling `\alpha^{-1} = 137 + \pi/87` an identity. Right now the paper says two incompatible things.
3. Propagate the Round 8 fix (2) beyond `prop:87`: line [...:183] and status row [...:244] still reify “phason slot count” and “upper exponents are phason” more strongly than the proposition allows. Also resolve the C3 dependency contradiction at [...:125] vs [...:242].

Verdict: no. The draft is closer, but it still contains one outright mathematical misstatement (`E_8 = \mathcal I \oplus \sigma(\mathcal I)`) and two unresolved overclaim/consistency problems.
