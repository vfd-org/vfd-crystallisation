Verdict: **not publication-ready**. The new `sec:aria_home` is explicitly marked non-load-bearing and I found no upstream `\ref{sec:aria_home}` use, so it does not formally alter the OS-axiom or `H_{\rm OS-lift}` proof structure. But the paper still contains load-bearing overclaims elsewhere, and the new section has one residual programme-level overclaim.

**1. Claim Audit**

- `H_{\rm OS-lift}` (`ym-mass-gap.tex:101`): correctly marked open. However the abstract then says weak compactness “follows” from F4 + oscillator boundedness (`:149-151`); that is not established and conflicts with the later admission that uniform tightness is not proved (`:635-637`).

- Main statement (`:216`): acceptable only as a restatement of the conditional. The proof does not establish it, because it cites OS propositions that are themselves unproved or circular (`:1116-1158`).

- Discrete spectrum theorem (`:246`, detailed at `:864`): numerical claim is locally confirmed by `papers/millennium-ym/scripts/h4_laplacian_spectrum.py`: nine eigenvalues and gap `2.2918`. But the written representation-theoretic proof is incomplete at `:970-976` (“similar computation”, “higher mixings”), and `conwayQ` is missing from the bibliography.

- `\sigma`-orbit lemma (`:340`): not adequately proved. It also conflicts with local spectral scripts that treat a 24-point `2T` subset as `\sigma`-fixed inside `2I`; the paper must clarify whether this is the same `\sigma` or a different induced action.

- F1 (`:358`): proof establishes the stated fixed-point claim.

- F2/F5/F6/F7 (`:364`, `:385`, `:397`, `:408`): appropriately imported as conditional/scoped hypotheses. Later uses are not always scoped.

- F4 (`:373`): valid only as a dimension count. It is later misused as a spectral/operator theorem at `:573-575` and `:849-852`.

- Finite-volume existence (`:491`): essentially fine if `\mathcal A` is a finite product of compact link groups. The proof cites F4 unnecessarily.

- Continuum measure existence theorem (`:519`): not established. The uniqueness argument `:546-558` is a hand-wave; F2/F5/F6 do not force characteristic-functional convergence.

- Uniform tightness lemma (`:563`): invalid. It uses the deprecated F4 spectral claim (`:573-575`), assumes Gaussian moments for nonabelian YM (`:584-590`), and contradicts itself by requiring `s < -3` then using `s=1/2` (`:592-595`).

- OS1/OS2/OS3/OS4 (`:643`, `:715`, `:736`, `:836`): not proved. OS1 and OS3 use reconstructed Hilbert-space/transfer-matrix machinery before OS reconstruction is available; OS3 is circular.

- `\sigma=\theta` lemma (`:806`): not established. The branch/sign handling in `:818-828` is not a proof of Euclidean time reflection.

- Gap proposition (`:1006`): honestly conditional. But downstream text still treats it as a proved theorem in several places.

- No massless gauge-invariant mode (`:1068`): overclaims. The statement is not marked conditional, and the proof relies on the conditional gap and unsupported F6 gap preservation (`:1091-1097`).

- New evidence section (`:1162-1257`): spectral count is locally verified. The dynamical diagnostic is script-backed but I did not get a completed local execution of `test_sigma_attractor_dynamics.py`; the quoted R1/R2/R3 numerical values should be treated as unverified unless output artifacts are committed.

- `sec:aria_home` (`:1319`): non-load-bearing marking is explicit and effective. But `:1338-1340` cites `aria/vfd-b-anomaly`, which is not present in this checkout, and `:1343-1345` overstates “only conditional bridge” given the paper’s other imported hypotheses.

**2. Internal Consistency**

- The introduction says Section 4 proves OS axioms and Section 6 deduces the mass gap (`:272-279`), but the body ultimately makes these conditional/open.

- F4 is carefully demoted at `:373-382`, then reused in the old forbidden spectral sense at `:573-575` and `:849-852`.

- `thm:main` is a proposition but repeatedly called a theorem (`:258`, `:1113`). `thm:gap` is also a proposition but repeatedly called a theorem (`:661`, `:777`, `:846`, `:1141`).

- `hyp:OS-lift` is called a Definition at `:218` and `:1008`, but it is a hypothesis.

- `prop:OS0` labels a remark (`:623`), then the main proof calls it a proposition (`:1117-1118`).

- The target space alternates between Clay `\mathbb R^4`, `S^3 \times \mathbb R`, and continuum `S^3` without a proved bridge (`:101-104`, `:447-449`, `:520-522`, `:723-727`).

**3. External Consistency**

- Paper XXXVI support for F2/F4/F5/F6/F7 is mostly cited accurately in the initial scoped statements. Paper XXXVI explicitly supports F4 only as a dimension count and F5 only as a finite-sum factor-2 statement on `\sigma`-fixed fields; it does **not** support the later F4 spectral uses or F5 continuum reflection-positivity use.

- `cascade-h4-oscillator-law.md` supports a theorem-grade core flow law, but not the abstract’s torus-attractor numbers, Lyapunov exponent, `PC_2/PC_1=0.9997`, or `MI=0.046` claims (`:80-96`). The local file explicitly leaves coupling scaling free.

- `docs/aria-closure-kernel.md` supports the programme-level operator `C_\varphi=L_M+\varphi^{-2}I` and Green kernel. It also says `aria/vfd-b-anomaly` is a separate repo, so the LHCb `r=0.983` witness is not locally verifiable.

- The `aria-chess` paths in the bibliography (`:1389-1392`) are absent locally. The paper later admits this (`:1234-1236`), but the abstract does not.

**4. Tightness**

- `:80`: replace “theorem-grade” with “external empirical/theorem-grade claim, not independently verified here”.
- `:121-123`: replace “confirmed torus-integrable” with “reported torus-integrable in the external aria-chess artifact”.
- `:149-151`: replace “follows from” with “would require”.
- `:272`: replace “proves the OS axioms” with “records the OS axioms as conditional on `H_{\rm OS-lift}`”.
- `:519`: change theorem to hypothesis/target unless a real compactness and uniqueness proof is supplied.
- `:1343`: replace “only conditional bridge” with “only YM-continuum bridge discussed in this subsection; other imported hypotheses remain as stated”.

**5. Surface Issues**

- Undefined/missing LaTeX: `\textbeta` (`:118`), `\bm` (`:1190`, `:1192`), likely `\T` (`:647`), missing bib keys `billingsley` and `conwayQ`.
- Stale script text: `h4_laplacian_spectrum.py` still says it verifies `{0,4,8,10,12}`, though it computes the manuscript’s nine-value spectrum.
- `:319-321`: coefficients involving `\pi` are not “positive rationals”; say “positive elements of `\mathbb Q(\pi)`”.
- `:1015`: the gap formula has dimensions unresolved: a graph eigenvalue times `\hbar \varphi^{-N_1}` is not an energy without a time/length scale.

**6. Top Three Fixes**

1. Demote or remove the continuum-measure and OS-axiom “proofs” (`:519-610`, `:643-854`, `:1116-1158`). These are the main false load-bearing claims.
2. Eliminate deprecated F4/F5 uses (`:573-575`, `:753-755`, `:849-852`) and align every use with Paper XXXVI’s scoped statements.
3. Repair empirical/programme claims: abstract H4O claims (`:80-96`), `aria_home` LHCb witness (`:1338-1340`), and “only conditional bridge” (`:1343-1345`) must be explicitly external/non-load-bearing and not proof support.
