Line numbers refer to `papers/millennium-hodge-formal/hodge-formal.tex`.

**1. Claim Audit**

- L187-196, “Main conditional reduction”: not proved here. It follows only if `H_{4a}^{alg}` is read as essentially the missing Hodge-algebraicity assertion. The proposition has no proof at its statement site, and the later proof at L671-680 uses a different version of the axiom.
- L323-325, “F5”: unsupported theorem. No proof or precise citation is supplied.
- L332-340, “σ action on cohomology”: coefficientwise action is fine over `Q(φ)`, but the proof uses undefined `H^{p,q}(X,Z[φ])` and needs a clean tensor-product formulation.
- L359-361, “σ-fixed classes are rational”: true over `Q`; the integral proof assumes a free basis and ignores possible integral torsion.
- L382-383, “σ-fixed = rational”: established over `Q`, modulo changing `Q[φ]` to the consistently used `Q(φ)`.
- L390-392, “CP^n admits a Kähler embedding into a cascade substrate rung”: not established. The proof’s `CP^n`-as-projective-bundle step is false for `n >= 3`, and the cascade target is not defined as a Kähler/algebraic target.
- L416-419, `H^*(CP^n,Z)=Z[h]/...`: standard and sufficient.
- L427-428, “Hodge for CP^n”: proof establishes the claim.
- L463-469, Bruhat decomposition: standard after adding hypotheses on `G` and `P`; current examples use compact `U(n)` notation inconsistent with complex algebraic `G/P`.
- L475-476, Schubert basis: standard, but degree/codimension indexing is muddled at L472-473 and L492.
- L519-526, Danilov theorem: overclaimed. `rank = number of p-dimensional cones` is false in general; `CP^2` already contradicts it. Generation by invariant cycle classes is the usable part.
- L532-533, “Hodge for toric”: plausible for smooth projective toric varieties, but the statement says smooth complete, and the citation key is broken.
- L563-566, Deligne-Langlands for CM: Deligne gives absolute Hodge status; this does not imply algebraicity.
- L574-575, “CM abelian; absolute Hodge”: the stated theorem is weak, but the proof at L582-585 falsely claims CDK proves algebraicity of absolute Hodge classes on CM abelian varieties. It does not.
- L608-614, Lefschetz hyperplane theorem: standard.
- L619-620, “selected complete intersections”: the statement is vague; the proof then claims all codimension `<=2`, which is false. Middle primitive Hodge classes are not disposed of by a generic Noether-Lefschetz reference.
- L661-668, `prop:embedhodge`: demotion to proposition happened, but the proof is still not valid. It treats `H_{4a}^{alg}` as a statement about `Ω` at L674-676, while Definition L161-179 states it on `X`.
- L690-691, universality conjecture: conjectural, no proof expected.
- L696-700, Langlands-cascade reduction: not proved. “Every motive is automorphic” is itself conjectural and does not yield an embedding into the cascade substrate.
- L769-785, 600-cell spectrum and `94/120 = 78.3%`: verified by the local spectrum script. This is only a finite spectral count.
- L798-819, dynamics diagnostic: locally stated in the script/source; I did not get a complete run in the sandbox. In any case, R2/R3 equal 1 by explicit projection, as the text admits.
- L823-831, aria H4O witness: correctly labelled external and not independently verified.

**2. Internal Consistency**

- L101-102 still says the fixed image is algebraic under `H_{4a}`. That contradicts the new separation at L161-184. It must say `H_{4a}^{alg}`.
- L187 is a `proposition`, but L48, L215, and elsewhere call it a theorem. Either restore theorem status or rename references.
- Definition L161-179 defines `H_{4a}^{alg}` on `X`; proof L674-676 uses it on `Ω`. This is the main formal inconsistency.
- L57-65 and L234-260 say all five classes have classical Hodge proofs. L730-734 and L741-743 admit CM abelian and complete-intersection cases are weaker. These cannot both stand.
- L569-570 says absolute Hodge implies Hodge for the specific classes; L575 says neither Deligne nor CDK implies Hodge in general. The former is wrong.
- All `\ref{...}` targets appear to exist, but semantic references are off because `thm:main-cond` labels a proposition.
- The toric section uses “smooth complete” while the paper’s Hodge scope is smooth projective.

**3. External Consistency**

- `CascadeFoundations`: locally found in `papers/cascade-correspondence-foundations/foundations.tex`. It verifies fixed-part rationality and functoriality, and states `H_{4a}` as open embeddability/compatibility. It does not verify algebraicity or regular-morphism pullback.
- `CascadeEmbeddings`: local `papers/cascade-derivation/cascade-embeddings.md` is a first analytic pass on `E8/H4/D4`; I did not find the claimed projective-variety embeddings used at L390-411.
- `CascadeRHformal`: verified the “strengthened conditional” discussion and the `94/120` spectral count. The Hodge paper’s use as conditional analogy is fair.
- `ConvergenceWithSmart`: verified; it explicitly says architectural similarity, not a meta-theorem. Hodge’s descriptive usage is acceptable.
- `CascadeAttractorSpectrum`: script completed and reproduced `94/120`, `26/120`.
- `CascadeAttractorDynamics`: claim found in local source/RH paper, but my execution did not return in the sandbox.
- `ariaH4Olaw`: cited `aria-chess/...` paths are not present in this repo. A similarly named local file exists, but I did not find the quoted artifact there.

**4. Tightness**

- L57-65: replace “where Hodge is already known classically” with “where classical status is available; only (C1)-(C3) are used here as complete Hodge examples.”
- L101-106: replace “algebraic under ... `H_{4a}`” with “algebraic only under the additional bridge axiom `H_{4a}^{alg}`.”
- L252-254: replace “Hodge is a theorem of Pohlmann/Deligne” with “Deligne proves absolute Hodge status; this is not a general Hodge proof.”
- L619-648: replace the proof with “outside the middle degree follows by weak Lefschetz; middle-degree primitive cases are excluded unless separately cited.”
- L696: rename “Theorem” to “Conjectural programme statement.”
- L877-878: “would complete the proof” is still too strong; embeddability alone would not prove algebraicity.

**5. Surface Issues**

- Undefined citations: `Hodge1941`, `BorelHirzebruch`, `Danilov`, `CDK`, `Voisin2002`.
- Existing bib keys are `Danilov1978` and `CDK1995`; use them consistently.
- L399 contains literal `H₄`; use `$H_4$` for pdfLaTeX safety.
- L437: “This IS” should be “This is”.
- L383 uses `\Q[\pphi]`; most of the paper uses `\Q(\pphi)`.
- `hypothesis` and `bm` repairs are present. `\HH` is defined. `inputenc` is present.
- I did not run a LaTeX build because the sandbox is read-only; the static citation scan already shows bibliography breakage.

**6. Top Three Fixes**

1. Fix the false classical-status claims for CM abelian varieties and low-codimension complete intersections: L57-65, L234-260, L569-585, L619-648, L721-743, L857-860.
2. Make `H_{4a}^{alg}` formally coherent: choose whether it is an axiom on `X` or on `Ω`, then rewrite L187-196 and L661-680 accordingly. Right now the proof does not match the definition.
3. Repair the classical stubs and bibliography: remove the false toric rank claim at L523-524, fix Schubert indexing at L472-493, restrict toric to the correct projective setting, and add/fix missing bibitems.
